# 五階段工作流計畫書

> 2026-08-05 立案。user 提出以**交付物**為核心的五階段工作流：
> `Research → Design & Spec → Execute → Review → Fix & Deploy`，標題是**「關鍵在交接」**。
> W-1～W-6 已於 2026-08-06 定案並落地（§6／§11）。2026-08-25 對齊「全平台同一條」見 **§14**。
> 檔頭舊句「只做大綱、未討論前不動工」已作廢，勿再當現況。

---

## §0 為什麼要改（現況三階段的缺口）

現行工作流在全域 `~/.claude/CLAUDE.md` §3，是三階段：**規劃 → 執行 → Review**。

三個缺口，都是今天實際發生過的：

| 缺口 | 今天的實例 |
|---|---|
| **沒有 Research 階段** | 我改角色目錄時只 grep 了一種寫法就動手，漏掉三支產生器的路徑常數 —— 那是「研究沒做完就進執行」 |
| **沒有 Fix & Deploy 階段** | Review 找到問題後，「修完了沒、上線了沒」沒有獨立的完成判準，容易停在「已知道要修」 |
| **★ 每階段沒有定義交付物** | 現行規則只講「做什麼」（先寫計畫書、先派查詢員），沒講「**產出什麼給下一棒**」。於是交接靠對話裡的上下文，換一個 session 或換一個角色就斷了 |

**第三點是這次的核心。** 圖上每一格底下都掛著一個交付物，那不是裝飾——
**沒有交付物的階段無法驗收，也無法交給別人接手。**

---

## §1 五階段定義（大綱）

每階段四件事：**目的／交付物／完成判準／誰做**。

### 1. Research（研究）→ 交付物：**研究資料包**

- **目的**：把「我以為」變成「我查過」。回答「現況是什麼、有哪些副本、上次為什麼這樣做」。
- **交付物**：一份可貼給別人的清單 —— 命中位置（`檔案:行號`）、已排除的可能性、**沒找到什麼**。
- **完成判準**：下一棒不必重查就能開始設計；「沒找到的」欄位非空（空白＝沒窮盡）。
- **誰做**：`locator`（唯讀查詢員）、`Explore`；跨專案文件漂移用 `project-auditor`。

### 2. Design & Spec（設計與規格）→ 交付物：**工作規格**

- **目的**：把要做的事寫成可逐項討論的東西，而不是邊做邊決定。
- **交付物**：計畫書（現況 → 目標 → 做法 → 驗證 → 狀態）＋ **待決分岔表**（選項＋傾向＋理由）。
- **完成判準**：每個分岔都有 user 的決定；**驗證方式在動工前就寫好**（不是做完才想怎麼驗）。
- **誰做**：主 session；架構層面可派 `Plan`。

### 3. Execute（執行）→ 交付物：**可檢查的成品**

- **目的**：照規格做，不擴張範圍。
- **交付物**：改好的檔案 ＋ **改了什麼／為什麼是這個值** 的對照。
- **完成判準**：規格上每一項都有對應的改動或明確的「沒做＋理由」。
- **誰做**：主 session；視覺與版面派 `visual-designer`。

### 4. Review（覆核）→ 交付物：**清單 ＋ 證據**

- **目的**：找出「看起來完成、實際沒有」的部分。
- **交付物**：逐項判定表，每項附**證據**（實際跑的指令／量到的數字／截圖），不接受「應該沒問題」。
- **完成判準**：測試全綠**且**變異測試證明它會紅；未驗項已落檔（四欄齊全）。
- **誰做**：`sync-checker`（雙份同步）、`harness-auditor`／`project-auditor`（文件漂移）、
  `Plan`（對抗式覆核）；判準本體在 `/verify-rules`。

### 5. Fix & Deploy（修完上線）→ 交付物：**修完上線**

- **目的**：Review 的發現要收斂到「已修且已生效」，不是「已知道」。
- **交付物**：修復對照（哪一條發現 → 哪個改動 → 怎麼驗的）＋ 上線後的驗證輸出。
- **完成判準**：Review 清單每一條都標 fixed／skipped（附理由）；部署有 served 版本的實測。
- **誰做**：主 session；部署走 `/deploy-prod`；資料遷移走 `/dry-run-migrate`。

---

## §2 交接契約（本次的核心）

**「關鍵在交接」的操作定義：下一棒不必回頭問上一棒，就能開始工作。**

| 交接點 | 上一棒必須給 | 下一棒才能假設 |
|---|---|---|
| Research → Design | 命中位置、已排除項、**沒找到的** | 不必重查；範圍已知 |
| Design → Execute | 定案的分岔、**驗證方式** | 不必再問「這樣對嗎」 |
| Execute → Review | 改動對照、沒做的部分 | 知道該檢查什麼、什麼是刻意不做 |
| Review → Fix | 逐項判定＋證據 | 知道哪一條要修、依據是什麼 |

**三條交接硬規則（草案）**：

1. **交付物要能脫離對話存在** —— 貼進一個新 session 也讀得懂。這是「換人接手」的最低標準。
2. **「沒做的／沒找到的」是交付物的一部分**，不是可選欄位。空白會被下一棒讀成「已窮盡」。
3. **跨階段不繼承推測**。上一棒的推測要標成推測；下一棒**不得把推測當事實往下傳**
   （今天的實例：「Edge headless 一直可用」被當事實，直到實測才發現它壞了）。

---

## §3 四個維度的現況對照（後續逐項套用的清單）

### 人員（角色 5＋3）

| 階段 | 現有角色 | 缺口 |
|---|---|---|
| Research | `locator`／`Explore`／`project-auditor` | 角色正文**沒有規定交付物格式**（現在的輸出格式是各寫各的） |
| Design | `Plan` | 內建角色，**不載入 CLAUDE.md**，拿不到工作流規則 |
| Execute | `visual-designer` | 只涵蓋視覺；一般開發沒有專屬角色（主 session 自己做） |
| Review | `sync-checker`／兩個 auditor | ✅ 這一階段的角色最完整 |
| Fix & Deploy | **無** | 沒有部署角色（走 skill 不走角色） |

### 技能（14 支）

| 階段 | 現有 skill |
|---|---|
| Research | `/asset-data-rules`・`/platform-resource-rules`・`/license-rules`（參考型，供查規則本體） |
| Design | `/design-spec`（**2026-08-06 補上**，見 §6；在它之前這一格是空的） |
| Execute | `/deploy-prod`・`/dry-run-migrate`（其實偏 Fix&Deploy） |
| Review | `/adversarial-review`・`/verify-rules`・`/audit`・`/verify-skill`・`/diagnose-bug`・`/data-incident`・`/codebase-health` |
| Fix & Deploy | `/deploy-prod`・`/shougong` |

**觀察：Review 有 7 支、Design 有 0 支。** 規則寫最多的階段不是最容易出錯的階段。

### 規範

| 層 | 現況 |
|---|---|
| 全域 `~/.claude/CLAUDE.md` | 今天剛搬過去：溝通／路由／三階段工作流／模型／交付 |
| 專案 `<repo>/CLAUDE.md` | 專案特有（§8 硬規則速查、§9 部署環境等） |
| `.claude/rules/*.md` | 5 支 path-scoped（碰到對應檔案自動載入） |

**缺口**：工作流只到三階段，且**沒有任何一條規則講交付物格式**。

### 工具（harness）

| 類型 | 現況 | 對工作流的支援 |
|---|---|---|
| hook 閘門 | 8 條規則（6 enforce／2 shadow） | 擋的是**動作**（部署、編碼、選擇題），不擋**流程**（例如「沒有研究資料包就進入執行」） |
| 觀測 | event log ＋ 平台 subagent 紀錄 | 看得到誰被派、用了什麼工具；**看不到現在在哪個階段** |
| 看板 | 8 分頁 | 沒有工作流視圖 |
| 成本 | 按模型／按日 | **無法按階段歸因** ← user 圖上的 💰 填不了就是這個原因 |

---

## §4 差距清單（要做什麼，尚未排序）

1. **定義交付物格式**（五份範本）——這是所有其他事情的前提。
2. **工作流規則進全域 `CLAUDE.md`**：三階段 → 五階段 ＋ 交接契約三條。
3. **角色正文加交付物規定**：每個角色的「輸出格式」要對齊它所屬階段的交付物。
4. **補 Design 階段的執行器**（目前 0 支 skill）。
5. **階段可觀測**：讓 event log 記得下「現在在哪個階段」，成本才能按階段歸因。
6. **考慮流程閘門**：例如「Execute 前沒有研究資料包 → WARN」。⚠ 這條風險高，容易變成摩擦。

---

## §5 待決分岔（逐項討論，同意才做）

| # | 分岔 | 選項 | 傾向 |
|---|---|---|---|
| W-1 | 五階段是**硬性流程**還是**檢查清單** | (a) 硬性：每階段都要有交付物才能往下 (b) 清單：提醒但不強制 (c) 依任務規模分級 | **(c)**：小改動硬套五階段會變成儀式；但「規模」的判準要寫死，否則每次都會自我豁免 |
| W-2 | 交付物要不要**落成檔案** | (a) 落檔（可跨 session） (b) 留在對話 (c) 只有大型任務落檔 | **(a) 或 (c)**：§2 第一條硬規則就是「能脫離對話存在」，留在對話等於沒交接 |
| W-3 | 要不要為 Execute／Fix&Deploy **建專屬角色** | (a) 建 (b) 主 session 自己做 | **(b) 先不建**：那兩階段需要完整寫入權，角色化的收益低、風險高 |
| W-4 | 階段觀測怎麼做 | (a) 自我宣告時多帶一個階段欄位 (b) hook 自動推斷 (c) 不做 | **(a)**：現行已經有「每次任務開始自我宣告」，加一欄成本最低 |
| W-5 | 先做哪一項 | (a) 交付物格式 (b) 規則進全域 (c) 階段觀測 | **(a)**：格式沒定，其餘三項都沒有可對齊的對象 |

---

## §5.1 定案（2026-08-06 逐項討論）

| # | 決定 | 影響 |
|---|---|---|
| **W-1** | ✅ **(c) 依任務規模分級** | 判準寫死成可檢查的事實 → §7。**這個選項的唯一風險是自我豁免**，三個機制防它 |
| **W-2** | ✅ **(c) 只有大型任務落檔** | 落檔位置＝**該任務的計畫書**，不新增檔案類型 → §8 |
| **W-3** | ✅ **建**（與原傾向 (b) 相反） | 要解權限邊界問題 → §9。⚠ 設計時發現一個新分岔（§9 待決） |
| **W-4** | ✅ 已由 G2 階段成本歸因做掉（8/6） | 自我宣告「階段」欄 ＋ 成本面板離線歸因，本檔此前未同步 |
| **W-5** | ✅ 順序 ①交付物格式 → ②規則進全域 → ③角色正文 | 格式沒定，其餘兩項沒有可對齊的對象 |

---

## §7 規模判準（W-1 定案）

三級，**命中任一即升級（OR 不是 AND）**，且每條都是**動工前就能回答的事實** ——
不含「複雜不複雜」「風險高不高」這類要判斷的詞。**那種判準一定會被自我豁免**，
因為判斷的人就是想省事的人。

| 級 | 命中條件（任一即成立） | 五階段怎麼走 | 交付物 |
|---|---|---|---|
| **M 大型** | 資料遷移／DELETE ・新建或修改角色·規則·hook ・跨 repo ・user 明說「計畫書／大型」・預估跨 session | 五階段全走 ＋ **計畫書先行** | **落檔**（§8） |
| **S 標準** | 改 **≥3 個檔** ・碰硬規則區（專案 §8／§9、harness 全域層與核心層）・改有多處副本的共用邏輯 ・要部署 ・要派 subagent | 五階段全走 | 留在對話，**欄位齊全**（§8） |
| **L 輕量** | 以上都不命中 | Research 壓成一次 grep；Design 可省 | 留在對話 |

**防自我豁免的三個機制**：

1. **級別由既有宣告欄自動可查，不新增欄位** —— 自我宣告本來就要列「修改檔案」，
   檔案數 ≥3 就是 S。**事後對帳看宣告行即可，不必相信我當時的自評。**
2. **判定 L 而實際改了 3 個檔以上 → 那一行宣告自己就是證據**。跟「空白＝沒驗過」同型：
   沒寫理由不代表沒問題，代表沒檢查。
3. **只有 M 級要明說**（它有額外動作＝落檔）。L／S 不必標，降低宣告成本。

---

## §8 交付物格式（五份範本 · W-2 定案）

**不新增檔案類型。** M 級的交付物就是**該任務計畫書的五個節**，S／L 級是對話裡的同一組欄位。
這是刻意的：多一種「交接檔」就多一個要維護、要清理、會過期的東西，而計畫書
（現況 → 目標 → 做法 → 驗證 → 狀態）本來就長成這五個節的形狀。

| 階段 | 交付物 | 必填欄位 | M 級落在計畫書哪一節 |
|---|---|---|---|
| Research | 研究資料包 | 命中（`檔:行`）／已排除／**沒找到的** | §現況 |
| Design | 工作規格 | 做法／待決分岔（選項＋傾向＋理由）／**驗證方式** | §目標 ＋ §做法 |
| Execute | 改動對照 | 改了什麼／為什麼是這個值／**沒做的＋理由** | §狀態 |
| Review | 判定＋證據 | 逐項判定／每項證據（實跑的指令·量到的數字·截圖）／**未驗項四欄** | §驗證 |
| Fix | 修復對照 | 發現 → 改動 → 怎麼驗／每條標 fixed 或 skipped＋理由 | §狀態（續寫） |

**粗體那一欄是必填的「空缺欄」** —— `沒找到的`／`沒做的`／`skipped 理由`／`未驗項`。
這是 §2 硬規則第 2 條的落地：空白會被下一棒讀成「已窮盡」，所以**寫「無」也比留空好** ——
「無」是一個判定，留空是一個未知。

---

## §9 Execute／Fix 角色設計（W-3 定案「建」）

### 權限邊界：為什麼不能給 Bash

`hooks/agent_readonly_gate.py` 的實測結論寫得很清楚：**`tools: Bash(git diff:*)` 的括號限定
只對 `Agent` 工具生效**，其他工具靜默拿到整支 shell。所以「要嘛完全不給 Bash，
要嘛給了之後用 hook 真的擋」。

而對 Execute 角色，hook **擋不住**：它要跑測試就得允許 `python xxx.py`，
而那等於允許任意程式碼執行 —— 一支 python 腳本裡面可以 `git push`。
白名單制在這裡失效，因為要放行的東西本身就是萬能的。

**結論：Execute／Fix 角色不給 Bash**，只給 `Read / Grep / Glob / Edit / Write`。

| | 為什麼可接受 |
|---|---|
| 不可能部署、不可能 commit | 沒有執行類工具，連繞的路都沒有（不是靠白名單擋，是能力不存在） |
| 邊界清晰、零新閘門成本 | 沿用 `locator` 的做法（完全不給），不必寫第三種閘門形態 |
| 代價 | 它跑不了 `node --check`／測試 → **驗證交回主 session 或 Review 角色**。這剛好符合五階段：Execute 的交付物是「可檢查的成品」，**檢查是下一棒的事** |

⚠ 順帶記一筆：`visual-designer` 目前有 `Edit + Bash` 且**沒掛任何角色閘門**
（只受全域 dispatch 管）。那是既有狀態，本次不動，但它與上面這條原則不一致 ——
留在這裡免得下次有人以為「有 Edit 的角色都被擋過」。

### 待決分岔（設計時發現的新問題）

不給 Bash 之後，`executor` 與 `fixer` 的**能力完全相同**，只差職責描述與輸入
（規格 vs Review 清單）。這會讓「建兩個角色」變成「建兩個長得一樣的角色」。

| # | 分岔 | 選項 | 傾向 |
|---|---|---|---|
| **W-6** | Execute／Fix 要幾個角色 | (a) 一個 `executor`（施作員），Execute 與 Fix 都派它，差別在輸入 (b) 兩個角色，能力相同、正文不同 (c) 兩個角色且 fixer 額外掛部署白名單 | **(a)**：Fix 本質是「照 Review 清單改檔」，跟 Execute 是同一種工作。兩個能力相同的角色會讓派工時多一次沒有實益的選擇，且看板上會出現兩個難以區分的人。(c) 與全域安全閥衝突 —— 角色收不到 user 的「明確說」 |

---

## §10 遵循度量測（2026-08-06 立·user：「只看得出規則，不知道有沒有照規範走」）

### 為什麼要做

看板的工作流程頁把**規則本文抄了一遍**，四張表全是靜態文字。於是規則有沒有被遵守、
改動有沒有效，**都無法回答** —— 沒有量測就沒有優化的依據，這是 user 卡住的實際原因。

**先實測資料撈不撈得到，再設計**（2026-08-05 起樣本，`probe_compliance.py`）：

| 訊號 | 實測結果 |
|---|---|
| 宣告行 | 11 筆全抓到 |
| 五欄逐欄解析 | 模式 11/11・分類 11/11・階段 11/11・**檔案 10/11**・摘要 11/11 |
| 階段序列 | 可重建（`Research → Design → Research`／`Execute → Fix → Research×4 → Fix → Design`） |
| 宣告 vs 實際改檔 | **可對帳**，第一次跑就抓到 4 段不符 |
| 派工歸階段 | ⚠ 25 次全歸不到 —— 派工在 8/05、階段規則 8/06 上線，**分母沒同齡** |

**第一次跑抓到的三件事**（含我自己的違規，照實記）：

1. Design 段宣告「修改檔案 `WORKFLOW_5STAGE_PLAN.md`」，實際寫入 **12 個檔**。
2. **規則與量測直接衝突**：§2 原本允許「換階段可只帶階段欄」，而只帶階段欄的宣告
   **沒有「修改檔案」欄** → 對帳只能把後續改動全歸給上一次完整宣告，數字必然失真。
   → **已改規則**（全域 §2）：重宣告至少帶「階段 ＋ 修改檔案」兩欄。
3. `修改檔案 無` 卻實際改了檔（scratchpad 暫存）。→ **口徑：scratchpad 與 `/tmp` 不算專案改動**。

### 四個量測項（user 定案：四項全做 · 強度＝**先只量測**，不做閘門不落便箋）

| # | 量測 | 回答什麼問題 | 判定怎麼下 |
|---|---|---|---|
| 1 | **宣告對帳表** | 宣告的「修改檔案」跟實際寫入的檔對得上嗎 | 每段一列，旗標：`宣告無但實際有改`／`實際 ≥3 檔（S 級門檻）`／`缺修改檔案欄` |
| 2 | **階段軌跡** | 有沒有跳階段（Design 沒走就 Execute） | 每 session 一條序列，標出 Research 缺席與 Execute→缺 Review |
| 3 | **五階段覆蓋率** | 五個階段各佔多少、有多少任務停在 Execute 沒 Review | 各階段宣告次數與佔比；分母＝規則生效後的宣告數 |
| 4 | **交接契約遵循** | 角色回報有沒有填「沒找到的」／「沒做的」 | ⚠ **樣本可能為 0**（角色正文今天才改）→ 零樣本顯示「尚無樣本」，**不畫空表** |

**強度為什麼是「先只量測」**：`COST_OBSERVABILITY_PLAN` §0 已經學過一次——
**只比比例就發警報＝假警報製造機，三次之後會被無視**。那裡的結論是「讓偏離可見且可解釋，
不是叫」。遵循度同理：先跑一陣抽出真實違規率，再決定要不要擋。
`UNIVERSAL_HARNESS_PLAN` §4 也把流程閘門列為高風險（容易變成摩擦）。

### 做法

**第六支產生器 `dashboard/gen_workflow_compliance.py`**，marker `WORKFLOW_COMPLIANCE_START/END`
放在工作流程頁新子分頁「遵循度」內（**排第一個並預設選中**——打開這一頁該先看到結果，不是規則）。

沿用既有紀律，每條都有出處：

- **離線批次，不進 `refresh_dashboard.SOURCES` 的內容雜湊**（`dashboard-generators.md`：
  上游每回合都在長，接進去會讓 Stop hook 的 109ms 秒退失效）。跟 `gen_cost_panel` 同類。
- **分母跟規則同齡**：`SINCE = 2026-08-06`（宣告階段欄的生效日）。規則上線前的紀錄
  沒有機會照規則走，算進來會讓違規率永遠 100%。
- **按 message id 去重**（實測 45% 是重複 block）、**只認 assistant 的 text block**
  （user 轉述的「階段 X」不算宣告）、**不用字面 `"階段" in line` 前置過濾**
  （同一份 transcript 兩種編碼都有，字面比對會漏掉 `\uXXXX` 逃逸那半）。
- **排除 scratchpad／tmp 的寫入**：那不是專案改動。
- **零目標拒跑**：解析不到宣告 → `SystemExit`，不出空表。

### 驗證方式（動工前先寫好 · Design 完成判準）

| # | 驗什麼 | 怎麼驗 |
|---|---|---|
| V1 | 數字對得上 | 產生器 `--print` 的數字 vs `probe_compliance.py`（兩支獨立實作）逐項一致 |
| V2 | 零目標拒跑 | `SINCE` 設成未來日期 → 必須 `SystemExit` 且說清楚缺什麼，**不得出空表** |
| V3 | marker 缺失拒跑 | 暫時改掉 marker 名 → 必須拒跑不猜插入位置 |
| V4 | 冪等 | **在快照上驗**（不可驗「連跑兩次雜湊相同」——第二次輸入真的變了） |
| V5 | 變異會紅 | 拿掉「排除 scratchpad」→ 檔數應變多；拿掉去重 → 段數應變多。兩者測試都要抓到 |
| V6 | 結構與徽章 | `test_dashboard_structure` 綠、子分頁數 4→5、nav 徽章跟著改 |
| V7 | 視覺 | headless Chrome 淺色＋深色各一張，量「頁面對了才會有」的特徵（不只看 png 存在） |

### 已知限制（先寫出來，免得被當成已窮盡）

- **量測項 4 的樣本數現在是 0**。角色正文今天才加必填欄位，且本輪沒派過角色。
- **派工歸階段要等新資料**：8/05 那 25 次派工永遠歸不到階段（分母同齡的必然代價）。
- **量測不擋任何事**。宣告造假、事後補宣告都測不出來——這是「先只量測」的已知邊界。

### 已完成（2026-08-06）· 第六支產生器上線

`dashboard/gen_workflow_compliance.py`（**核心層**）＋看板工作流程頁新子分頁「遵循度」
（排第一、預設選中）＋`tests/test_workflow_compliance.py` **32 條**接進回歸網
（348 → **380/380**）＋`/shougong` 步驟 3.5 加跑這支（離線批次）。

**第一次跑出來的基線**：20 段任務**對得上 35%**。五階段覆蓋率
Research 35%／Execute 25%／Design 15%／Review 15%／Fix 10%。交接契約尚無樣本。

**寫的過程中被自己的量測抓到四件事**（都已修，記下來因為它們是同一類錯）：

| # | 問題 | 為什麼會發生 |
|---|---|---|
| 1 | 去重寫成「按 message id 跳過整筆紀錄」→ **假拒跑** | 同一則 API 訊息拆成多筆、每筆帶**不同** block（thinking 常排 text 前），跳過整筆會把宣告連同 tool_use 一起吃掉。`gen_cost_panel` 去重是為了 usage 計費，那個理由在這裡不存在——**照抄別人的紀律但沒照抄它的理由** |
| 2 | 正則要求宣告行有「模式」→ **`Review` 覆蓋率顯示 0%** | 而那一輪確實走過 Review。規則允許（且要求）重宣告只帶「階段 ＋ 修改檔案」，那種宣告沒有「模式」欄。**量測把「規則允許的簡短形式」讀成「沒有宣告」** |
| 3 | 只帶階段欄的宣告被整筆丟棄 | 丟棄是錯的判法：那一段確實走過那個階段，只是宣告不合格。**「不合格」與「沒發生」是兩件完全不同的事**，畫成同一件會讓真實軌跡消失 |
| 4 | chip 裡放檔名 → 畫成 `MEMORY.MD` | `.chip` 帶 `text-transform:uppercase`，檔名讀起來像另一個檔。截圖才發現——這正是「改完要重截一張」的價值 |

**驗證結果**（對照上表的 V1–V7）：

| # | 結果 |
|---|---|
| V1 | ⚠ **降級並改法**：原訂「與 `probe_compliance.py` 兩支獨立實作比對」。**兩支都是我寫的，共享同一個誤解時會一起錯**（第 1、2 項就是這種誤解）。改成 32 條判定層單元測試 ＋ 變異證明，比互相比對強 |
| V2 | ✅ `SINCE` 設 2099 → 拒跑且指名「零目標」；transcript 目錄不存在 → 拒跑 |
| V3 | ✅ marker 改名 → 拒跑，不猜插入位置 |
| V4 | ✅ **在快照上驗**（餵固定 data 進 `build_html` 兩次逐字相同）。⚠ 第一次是用「連跑兩次比對檔案雜湊」——**那次通過純屬僥倖**，因為那兩分鐘內剛好沒有新宣告 |
| V5 | ✅ 植入變異（`_decl_file_set` 缺欄回空集合＝把「缺欄」與「宣告無」混為一談）→ **25/27 且 exit 1**，指名該紅的兩條。⚠ 順手咬到：`python … \| tail` 之後的 `$?` 取到的是 **tail** 的碼（顯示 0），要不接管線才看得到真正的 1 |
| V6 | ✅ 子分頁 4→5、nav 徽章跟著改、`test_dashboard_structure` 綠 |
| V7 | ✅ 淺色＋深色各截一張，四節都渲染正確 |

### 追加：對帳表限高捲動 ＋ 判定篩選（2026-08-06 · user 要求）

原本 21 列全攤開，**後面三節（階段軌跡／覆蓋率／交接契約）永遠在摺線以下** ——
而那三節才是總結。改成：

- **`.twrap.wfc-scroll` 限高 620px（約 10 列）＋ `thead` sticky**。捲到第 20 列時
  還要看得出哪一欄是「宣告」哪一欄是「實際」。
- **右上判定篩選**（全部／有問題／相符），沿用既有 `.cv-switch` 樣式**不另造一套**
  （硬規則 7）。JS 走**全域委派**：按鈕是產生器產出的，每次重生都是新節點，
  綁在 `document` 上才不會在重生後靜靜失效。

三個實作決定，都是為了「畫面不許自己跟自己不一致」：

1. **判定分類寫在每列的 `data-wfc`，client 不重算判準**。重算＝第二份真相，
   兩邊漂開時徽章說 13、按下去跳出 8 列。
2. **篩選鈕的計數用「表列的那幾段」算，不是全部段數**。表格截到最近 25 段，
   用全部段數會讓數字與畫面不符。
3. **`tr[hidden]` 要明確宣告 `display:none`** —— `display:table-row` 會蓋掉瀏覽器
   對 `hidden` 的預設樣式，漏了這行篩選會靜靜沒反應（列還在畫面上）。
   回饋動畫做在 `tbody` 整塊淡入，**不做逐列動畫**：被隱藏的列是 `display:none`，
   transition 根本不會跑。

**驗法升級**：這次把量測值**畫進截圖**（頁面頂端的探針條），不再靠 `--dump-dom`
抓 `document.title`（那條路實測抓不到）。一張圖同時驗版面與數字：
`表格高=620px(上限620) 可捲動=true 列數 篩選前=21 後=14 pressed=[有問題14]`。
**按鈕是真的 `.click()`**，不是用 CSS 硬顯示。

### 追加：多專案（2026-08-06 · user 要求「辨識是哪個專案的聊天室窗」）

**原本只掃 `d--IT-department` 一個目錄，於是漏掉整個 AI-Projects。**
規則寫在**全域** `CLAUDE.md`，所以它對每個專案都生效 —— 只看一個專案等於
把別的專案的遵循度藏起來。實測：`d--AI-Projects` 有 **12 筆宣告**
（Execute／Research／Review），佔總量三分之一。段數 21 → **36**、聊天室窗 2 → **5**。

**白名單而不是黑名單**：`~/.claude/projects` 底下有 10 個目錄，其中 **8 個是 harness
自己的測試探針**（`tests-post-probe`／`stop-warn-probe`／scratchpad…）。黑名單要維護
排除 pattern，漏一條就把測試資料算成真實工作；白名單則是**對不上專案清單的目錄
自然不存在**，日後新增探針目錄不必改這支。專案清單的單一真相是
`gen_layers.survey_projects()`（它已經在算 `dispatchWired`），自己再數一份會漂移。

**專案徽章帶閘門狀態**：● 接了 harness dispatch（規則有 hook 閘門）／○ 沒接。
AI-Projects 只有 `settings.local.json`、沒有 `settings.json` → **規則對它生效但沒有閘門攔**。
這個區別本身就是徽章要講的話 —— 它解釋了為什麼某個專案的宣告紀律更差。

三個實作細節：

- **目錄名正向產生、不反向解析**。`:`／`\`／`.` 全部被編成 `-`，反解會猜錯
  （`D---ai-harness` 同時來自三種字元）。比對一律 casefold：目錄是 `d--IT-department`
  而 `Path` 給 `D:\IT-department`，直接比會抓到 0 個、看起來像「沒有任何 transcript」。
- **軌跡的 key 帶專案**：不同專案的 session hash 前 8 字有機會撞，撞了會把兩個工作區
  的階段序列接成**一條假軌跡**。
- **別的專案用「未定」「尚無」表達還沒決定** —— 不在原本的空值集合裡。只認自己習慣的
  詞會把它們當成「一個叫『未定』的檔案」，那一段若有改檔就會被誤標成「未宣告就改」。

回歸網 **399/399**（遵循度那支 51 條）。截圖探針量到 `bar到表格間距=-1px`（真的貼著）。

**再一輪收斂（user 看畫面後）**：表格上方那列專案徽章**移除** —— 每一列的來源欄
就標著專案，兩者資訊重複。但**閘門狀態搬進 `(!)` 浮窗不是刪掉**：「哪個專案沒接閘門」
正好解釋了它為什麼「缺修改檔案欄」的比例更高，那是這張表最有解釋力的一段。
列內專案名改 `--accent-ink` 藍色粗體，跟灰色的 session hash 分開（兩者是不同東西：
工作區 vs 聊天室窗，同一個灰階會讀成一串亂碼）。**所有專案同一個藍**——既有 token
只有狀態色，借去當分類色會讓「AI-Projects」看起來像出事了。

⚠ 移除徽章列時咬到一個 flex 陷阱：bar 只剩篩選鈕一個子元素，`justify-content:space-between`
在**單一子元素時等同 flex-start**，按鈕會靜靜跑到左邊。改 `flex-end`。

### 追加：專案分類色（2026-08-06 · user 要求「不同專案不同顏色、自動配色」）

**我上一輪判斷「不分色」，user 否決。照做，但用 `dataviz` 規範選色而不是憑感覺挑。**

第一組候選（blue＋magenta＋orange）**被 validator 擋下**：magenta↔orange 的
normal-vision ΔE **12.9**（<15 是 hard FAIL——全色覺的人都分不出來），
且 magenta 對白底對比只有 **2.69**。這證明了「憑感覺挑三個看起來不一樣的顏色」不可靠。

**根本限制有兩個**：

1. **狀態色是保留色**，而看板已經用掉五個色相：accent 青綠／pass 綠／block 紅／
   warn 琥珀／shadow 紫。分類色借用任何一個，都會讓「某個專案」看起來像出事了。
2. **這是 11px 文字，不是圖表 mark**。色盤的亮度帶（L 0.43–0.77）是為 mark 設計的，
   對白底文字太淺 —— 判準要換成 **WCAG 4.5**。validator 自己也提示
   「for a lone status/text color check WCAG text contrast」。

**最終色盤（兩個 slot，全部跑過 validator）**：

| slot | 淺色 | 對比 | 深色 | 對比 |
|---|---|---|---|---|
| 1 藍 | `#256abf` | 5.39 | `#3987e5` | 4.54 |
| 2 洋紅 | `#c4407a` | 4.81 | `#dd6293` | 4.91 |

兩模式全 PASS（亮度帶／chroma／CVD 分離／normal-vision ΔE／對比）。深色那組是
第三次迭代才同時滿足亮度帶與 4.5 對比 —— 前兩組各差一項。

**只有兩個 slot 不是偷懶，是色相預算就剩這麼多。** 第三個以上的專案用**中性灰**，
靠文字本身辨識 —— 分類色的硬規則是「固定順序、不循環、**不生成新色**」，
而生成的色相不可能保證與五個既有狀態色都分得開。

**分配按名稱排序，不按 hash**：只有 2 個 slot 時 hash 撞色率 50%，撞了就得有一個退位，
反而更不穩。代價是「新增專案可能讓既有專案換色」，而分類色通則正是
「顏色跟著實體、不跟著排名」—— **這裡刻意接受那個代價**：專案集合不隨畫面互動變化，
那條通則防的是「篩選一下顏色就全洗牌」，不是這個情境。理由寫在 `proj_classes()` 裡。

**浮窗的專案清單就是圖例**：用與表格內完全相同的 class，顏色一定對得上。
分類色不能只靠顏色承載身分，圖例把「哪個顏色是哪個專案」講明白（●／○ 同時帶閘門狀態）。

回歸網 **404/404**（遵循度那支 56 條）。

---

## §10.1 派工實測：`executor` 過關，但量測項 4 露出資料源缺陷（2026-08-06）

授權派 agent 後一次驗三項未驗。**兩個結果各自都比預期有價值。**

### `executor` 通過核心失敗模式測試

規格**刻意缺第 3 項**（「還要納入哪些角色回報的小節名」只寫成問句）。它做完 1、2，
**對第 3 項停下來回報「規格沒寫，我停在這裡，沒有自己補」** —— 那正是這個角色唯一的
高風險失敗模式（自己補一個看起來很合理、但沒人審過的決定），而它做對了。

它的回報還超出規格三處，每一處都是判斷不是機械執行：

1. **刻意不寫 `沒.*找到的`** —— 那會命中「沒有問題，找到的都列在下面」這種正面陳述。
   **反方向的假命中會讓表格永遠 100%，比低估更難發現。** 只放行「有」一個字的插入。
2. **`gap_hits()` 回 canonical label 而不是 match 到的原文** —— 否則同一欄在不同回報
   會顯示成「沒有找到的項目」vs「沒找到的」＝同欄異名。
3. **把判定收斂成單一真相**：原本 `collect()` 內嵌的 comprehension 抽成函式，
   否則測試只能複製一份同樣的邏輯來驗＝第二份真相（比對規則改了測試仍綠）。

它也盤出第 3 項的**證據**：六支角色檔的空缺欄小節名全掃過，
`沒找到的`／`沒做的`／`我查不到的`／`該補的檢查項` 對六支角色是 **6/6 全覆蓋**。
→ **採它的建議 A：一個都不加。** 加名稱只會增加假命中面。

⚠ 順帶釐清一個容易搞混的地方：計畫書 §8 講的「必填空缺欄」是**交付物層**的概念
（含 `skipped 理由`、`未驗項`），而 `GAP_HEADS` 比對的是**角色回報的小節名**。
兩者不是同一份清單 —— `skipped` 在 `executor` 正文裡是對照表的一欄不是小節，
命中它不等於填了空缺欄。**不要為了「對齊 §8」把那兩個加進去。**

驗證：`test_workflow_compliance.py` **67/67**（新增 11 條，含變異證明「退回精確比對
確實漏掉同義寫法」＋對照組「變異只鬆綁能力、沒把函式弄壞」）、整合網 **415/415**。

### 量測項 4 的資料源缺陷（**待修**·規格已寫好）

派完之後 `--check` 顯示「樣本 2 份、兩份都空缺欄=無」——
**但 executor 的回報明明有 `## 沒做的`。** 查 transcript 得到真因：

| | 實際形狀 |
|---|---|
| 量測掃的 | `message.content[].tool_result`，用 `Agent` 的 `tool_use_id` 配對 |
| 背景 agent 的那個 tool_result | 只有 **「Async agent launched successfully」啟動 metadata** |
| 真正的回報 | 以 **task-notification（`type=user` 的訊息文字）** 注入主 session |

所以現在那 2 份是**假樣本＋假判定**，而且是**假的低估** ——
畫面上看起來像「角色都沒填空缺欄」，正好是 executor 警告過的那種錯誤的鏡像。

**修法規格**（動工前寫好，`/design-spec` 步驟 4）：

1. **「Async agent launched」的 tool_result 不得算樣本** —— 那是啟動 metadata。
   零樣本要顯示「尚無樣本」，不是「空缺欄=無」。
2. 背景派工的回報從 notification 的 `<result>` 區塊抓，用 agent 名稱與 `<task-id>` 配對。
3. **同步派工（`run_in_background: false`）的回報仍在 tool_result** —— 兩條路徑都要認。
4. 驗證方式：修完後 `executor` 那筆應顯示**空缺欄=沒做的**（它的回報確實有那一節）；
   變異證明＝把 notification 那條路徑拿掉 → 樣本數應掉回 0 且顯示「尚無樣本」。

**本輪沒改**，因為 `harness-auditor` 還在跑、不與它搶同一個檔。已落 `PENDING_VERIFY.md`。

---

## §11 §4 差距清單的收尾（2026-08-06）

§4 立案時列了六項，逐項結案：

| # | 項目 | 結果 |
|---|---|---|
| 1 | 定義交付物格式 | ✅ §8 五份範本 |
| 2 | 工作流規則進全域 `CLAUDE.md` | ✅ §3 三階段 → 五階段 ＋ 交接契約三條 |
| 3 | 角色正文加交付物規定 | ✅ 六個角色各加「階段＋交付物＋必填空缺欄」 |
| 4 | **補 Design 階段的執行器** | ✅ **`/design-spec`**（見下） |
| 5 | 階段可觀測 | ✅ G2 階段成本歸因 ＋ 遵循度四個量測項 |
| 6 | 流程閘門 | ⏸ **範圍已定案、尚未實作**（見下） |

### 已完成：`/design-spec`（第 4 項）

**Review 階段有 7 支 skill、Design 有 0 支** —— 規則寫最多的階段不是最容易出錯的階段。
user 定案「薄編排」，四步各有完成判準：

1. **判規模** → L／S 到此結束（不落檔），只有 M 繼續。
2. **落檔位置** → 疊代既有計畫書，新建要說得出「這主題在既有計畫書裡沒有歸屬」。**不准兩份都寫。**
3. **列分岔並逐項用 `AskUserQuestion` 問完** → 禁陳述句把決定丟回去（AWC-1 擋的形狀）；
   **討論中長出來的新分岔要補進表裡**（W-6 就是設計到一半才長出來的）。
4. **驗證方式守門** → 每項要答得出**「怎麼證明它會紅」**；
   **兩支自己寫的實作互相比對不算獨立驗證**（V1 就是這樣事後降級的）；驗不到的當下落 `PENDING_VERIFY.md`。

**它其實屬於通用層**（不含任何本專案語意），但先放專案層 `.claude/skills/`——
搬到全域要先建 skills 的分發機制（junction ＋ bootstrap ＋ 看板掃描路徑），
範圍超出「薄編排一支 skill」，而 U-5 說沒有第二個部門真的要用之前不做抽象化重構。

驗證：`eval/run_all.py` **L1–L4 全 PASS**，design-spec 被歸為「流程」型、
契約 3 項 OK 0 缺、1251 tok。⚠ **L4 的「實跑」未做**（需人在場判斷），
eval 標它「未驗收」——這是誠實狀態不是漏做。

### 待做：流程閘門（第 6 項）·範圍已定案

user 定案：**只擋「缺修改檔案欄」，WARN 級不擋動作**。理由是它是量測到的主要違規類型
（37 段裡 23 段不符，多數是這一條），而且判準客觀（宣告行有沒有那一欄）→ 誤擋空間小。
走現有的 Stop 便箋 → `UserPromptSubmit` 投遞路徑（跟 AWC-1／BUDGET-1 同一條）。

⚠ **那條投遞路徑本身還在待驗清單上**（`PENDING_VERIFY.md`「AWC-1／BUDGET-1 尚未被真實違規觸發過」）
——實作這條閘門前先確認它真的送得到，否則會做一個永遠沒人看到的警告。

---

## §6 狀態

- 2026-08-05 立案，**本檔只有大綱**。§3 四維度現況已實際盤點（角色 5＋3、skill 14 支、
  規範三層、harness 工具四類）。
- 2026-08-06 **W-1～W-6 全部定案**（§5.1、§9），並補 §7 規模判準、§8 交付物格式、§9 角色設計。

### 已完成：①②③ 全做完（2026-08-06）

| 項 | 做了什麼 | 驗證 |
|---|---|---|
| ① 交付物格式 | 本檔 §7 規模判準（三級＋防自我豁免三機制）、§8 五份範本 | —— |
| ② 規則進全域 | 全域 `CLAUDE.md` §3 三階段 → **五階段**：規模分級 ＋ 交付物表 ＋ 五個階段各自硬規則 ＋ **交接契約三條**。原有的踩坑規則（查詢員疊代三條／改前 grep／對抗式覆核／驗證紀律）全部保留，各歸其階段 | 全域檔非 `check_bloat` 管轄範圍（那支只管專案 §8），靠自律精簡 |
| ③ 角色正文 | 六個角色各加一段「**五階段位置＋交付物＋必填的空缺欄**」；`sync-checker` 原本沒有「我查不到的」欄，補上 | `check_layers` 39/39（全域層 6）·回歸網 **348/348** |
| W-3 新角色 | 建 `executor` 施作員（`Read/Grep/Glob/Edit/Write`，**無 Bash**·W-6 定案一個不是兩個） | 看板重生後結構測試綠 |
| 看板同步 | 工作流程頁撤「草案」→「已定案」，新增子分頁**規模與交付物**（3→4 子分頁），五階段表加「誰做」欄 | `run_hook_tests` 348/348（含頁籤↔面板配對） |

**W-6 定案：一個 `executor`，Execute 與 Fix 都派它。** 不給 Bash 之後兩者能力完全相同，
建兩個只會讓派工多一次沒有實益的選擇，且看板上會出現兩個難以區分的人。

**先讓它紅再改到綠的實例**：新增角色檔後 `test_dashboard_structure` 立刻 FAIL
（「看板角色數 5 vs 實際 6」），重生看板才綠 —— 這條測試守得住「新增角色但忘了重生看板」。

**未驗項**：看板 HTML 已改但**尚未截圖視覺驗證**、**尚未重新發布 artifact**（見下）。

---

## §12 宣告欄位「為什麼是這個形狀」（2026-08-14 從全域 `CLAUDE.md` §2 搬入）

搬入理由：全域 `CLAUDE.md` 是 always-loaded、每則對話都付；這兩欄的**規則本身**只需要一句話，
但**當初為什麼加**是兩段實測踩雷紀錄——留在常駐層等於每輪重付一次事故報告。
規則精髓留在全域 §2，理由在這裡。（`check_bloat` 2026-08-14 報這兩條 214 字／168 字超標。）

### 12.1 階段欄：為什麼「換階段要重宣告，且至少帶兩欄」

- **寫法固定**「`階段 Execute`」這種形狀——成本面板是**離線從 transcript 用字串撈**這一欄的，
  形狀漂掉就撈不到。
- **換階段時重宣告一行，至少帶「階段 ＋ 修改檔案」兩欄**。
  ⚠ **只帶階段欄會讓對帳把後續改動全歸給上一次完整宣告，數字必然失真**——2026-08-06 實測咬到。
- 成本面板據此做**階段成本歸因**；**漏標的一律算「未標記」**，
  **未標記佔比本身就是宣告紀律的量測**（見 `MODEL_ROUTING_PLAN.md` M-2／G2）。

### 12.2 規模欄：為什麼 2026-08-07 才補上

§7 規模判準寫著「判成 L 而實際改了 ≥3 檔，**那行宣告自己就是證據**」——
但**沒有規模欄就沒有分母**。8/07 盤點看板時才發現這句話當時在技術上做不到：
只量得到「實際改了幾檔」那一半，對帳的另一半根本不存在。

⇒ 判準是「宣告的規模欄 vs 實際動的檔數，兩欄一比」，**不靠自評**。
判斷不出來就寫「待定」，**別猜一個**——猜出來的規模欄會讓分母變成假的，比沒有更糟。

### 12.3 任務欄：為什麼「靜靜改名」是個問題（2026-08-25 從全域 §2 搬入）

`check_bloat` 2026-08-25 報這條 191 字超標。規則精髓（繁體中文、≤12 字、開場自己命名、
轉向寫一次 `任務 舊名→新名`）留在全域 §2，代價說明放這裡。

- **「≤12 字」的例外**：程式識別字可原樣留，不必硬翻成中文
  （例：`任務 看板篩選列收成一列`）。字數判準是給人讀的，不是給識別字加負擔。

- **名稱即歸因 key**：成本面板從 transcript 撈「任務」欄的字串做歸因。
  同一段工作**靜靜換個名字**，會把它的成本拆成兩筆對不起來的紀錄——
  兩筆都不完整，而且**事後無法合併**（沒有任何欄位記得它們原本是同一件事）。
- 所以轉向時要**明寫一次** `任務 舊名→新名` 再沿用新名：那一行就是接合點。
- 同一段工作跨 session 沿用同一個名字，理由同上。
- 對照：`階段` 欄漏標會讓歸因錯位（§12.1），`任務` 欄改名會讓歸因**斷裂**。
  前者算得出「未標記佔比」，後者連自己被拆過都看不出來。

## §13 規劃層改制（2026-08-22／23）—— 指標

明確 **M 級**的規劃層已從「計畫書（`/design-spec`）」改成 **wayfinder 規劃圖**
（`.scratch/<effort>/map.md` ＋ `issues/NN-*.md` 決策票）。判準、模板、審查握手、
回寫慣例全部在 `SKILL_IMPORT_WAYFINDER_PLAN.md`，本檔不重述（重述就會漂）。

與本檔的關係：五階段沒有變，變的是 **Design 階段的載體** ——
規模待定或判不出來的仍走 `/design-spec`（安全預設），L／S 不變。

---

## §14 全平台同一條工作流（2026-08-25）

> 狀態：執行中（疊代本檔，不另開計畫書）。舊檔不回頭標「待審核」（design-spec 步驟 5）。
> 稽核：[harness-auditor](30e7b06a-093c-40cb-a512-b00a8ce5a6fa)／[project-auditor](c1d67962-532d-461d-8fed-4595b364eb70)／[explore](98b9d91e-c85e-4474-8ea7-5dfe740c5689)。

### 14.1 現況（2026-08-25 探測）

使用者要的流程＝評估派工（自動查詢＋規劃）→ 寫作法確認 → 施作 → 檢查評估到完成 → 交付；作法錯回第二步。

| 層 | 已有 | 缺口 |
|---|---|---|
| 全域 `CLAUDE.md` §3 | 五階段＋規模＋交付物＋查詢員前置＋同意才執行 | 無「開場必派規劃」；無「作法錯回 Design」 |
| 技能 | `/design-spec` 只在 IT-department；`/wayfinder` slash-only | 無階段編排執行器；Cursor 自動清單看不到 design-spec（專案層才有） |
| 角色 | 六支有「輸出」空缺欄 | Cursor Task 沒有 `locator`／`Plan` 型別 |
| hook | DECL-1 缺修改檔案欄 WARN 已 enforce（18 次） | **沒有**「沒 Research 就 Execute」閘門（本輪維持不建） |
| 量測 | 遵循度 244/577（42%）；Design 宣告 9%；Execute 前無 Research 4 條 | 只量不擋（本輪維持） |
| 專案 `CLAUDE.md` §2 | 轉述全域 | 仍寫「三階段」；查詢員誤指 `/verify-rules` |
| 計畫書本檔 | §6／§11 已完成 ①–⑤、DECL-1 | 檔頭／§3 仍寫大綱、Design 0 支（過期） |

### 14.2 目標

換專案、換 Claude／Cursor，**開場都走同一條**：評估 → 自動查詢規劃 → 寫作法等人點頭 → 施作 → 檢查（錯回寫作法、沒好回施作）→ 交付。規則一份在全域 §3；步驟在 `/session-workflow`；規格在全域 `/design-spec`。

### 14.3 分岔定案（2026-08-25 AskQuestion）

| # | 分岔 | 決定 |
|---|---|---|
| **W-7** | 編排器 | ✅ **新建薄 skill `/session-workflow`**（組合既有零件，不重寫規則） |
| **W-8** | Cursor 強制力 | ✅ **全域常駐＋adapter 最短路由**；wayfinder／to-tickets 維持 slash-only |
| **W-9** | 階段順序閘門 | ✅ **這次不做**（先量測；Design 宣告 9%，現在擋誤擋大） |
| **W-10** | `/design-spec` 位置 | ✅ **搬進 `D:\Patrick-AI\.ai-harness\skills`**，專案層刪副本（兩份會漂） |

### 14.4 做法（一次補齊清單）

1. 全域 `CLAUDE.md` §3 各加一句（≤120 字）：開場必派查詢＋規劃；Review 作法錯回 Design、沒好回 Execute。
2. 新建 `skills/session-workflow/SKILL.md`（**不要** `disable-model-invocation`）。Claude 派 `locator`＋`Plan`；Cursor 派 `explore`／主則規劃（Task 沒有 locator／Plan 型別——寫進 skill，不准假裝有）。
3. 移動 `/design-spec`：harness 為真相；選擇題寫「Claude＝`AskUserQuestion`／Cursor＝`AskQuestion`」。
4. 專案 `CLAUDE.md` §2：三階段→五階段指標；查詢員不再指向 verify-rules；路由補 `/session-workflow`。
5. `cursor-adapter.mdc` 加一條工作流路由（不拷 skill）。
6. `skills/_meta/PROVENANCE.md` 本地表加兩列。DECL-1／階段順序閘門本輪不動。

### 14.5 驗證（怎麼證明會紅）

| 驗什麼 | 怎麼驗 | 會紅的條件 |
|---|---|---|
| provenance | `py -3 D:\Patrick-AI\.ai-harness\dashboard\capability_checks.py` 含 `_p_skill_provenance` | 新 skill 目錄沒進 PROVENANCE 表 |
| eval 結構 | `py -3 D:\Patrick-AI\.ai-harness\eval\run_all.py` | 新 skill 缺 frontmatter／完成判準；design-spec 雙份撞名 |
| 專案不再有副本 | `Test-Path D:\IT-department\.claude\skills\design-spec` | 仍存在＝兩份會漂 |
| 常駐句子 | grep 全域 §3「開場必派」「回 Design」 | 零命中＝沒寫進去 |
| adapter | grep `session-workflow` in `.cursor/rules/cursor-adapter.mdc` | 零命中＝Cursor 仍無路由 |
| 不誤做閘門 | grep hooks/rules 無新 RULE_ID 擋階段順序 | 多出來＝違反 W-9 |
| wayfinder 仍 slash-only | `wayfinder/SKILL.md` 仍有 `disable-model-invocation: true` | 被拿掉＝違反 W-8 |

**兩支自己寫的實作互相比對不算獨立驗證。** 遵循度百分比本輪不當完成判準（分母每則對話都在長）。

### 14.6 刻意不做

- 「沒 Research 就 Execute」WARN／BLOCK（W-9）。
- 拿掉 wayfinder／to-tickets 的 `disable-model-invocation`（W-8）。
- `.cursor/skills/` 複本。
- 把本檔整份標「待審核」（舊計畫不回頭補標）。

### 14.7 狀態

2026-08-25 **已落地**（W-7～W-10）。驗證：`eval/run_all.py` L1–L4 PASS（0 FAIL）；`_p_skill_provenance`＝`14 支全有來歷登記：外部 6／自建 8`；專案 `.claude/skills/design-spec` 已刪；`wayfinder` 仍 `disable-model-invocation: true`。L4 實跑與 Cursor 自動清單見 IT-department `PENDING_VERIFY.md` 8/25 兩列。

## §15 §3 與 `/session-workflow` 去重（2026-08-25）

> 狀態：規格已定、**尚未動手**（等 Cursor 端勘查回報，見 §15.4）。
> 起因：`/session-workflow` 2026-08-25 建立後，全域 `CLAUDE.md` §3 有幾段與它逐字重複，
> 而 §3 是 always-loaded（每則對話都付），skill 是 on-demand。
> 分岔定案（user 選擇題）：**只做工作流這一條**／**只搬步驟細節、判準留常駐**／
> **Cursor 端先勘查再決定**。

### 15.1 量測基準（2026-08-25 動手前）

| 檔 | bytes | 條目 | 超標條目 |
|---|---|---|---|
| 全域 `CLAUDE.md` | 10,970 | 40 | 1（191 字「任務欄」條） |
| 全域 `CLAUDE.md` §3 單節 | 2,120（76 行） | — | 散文塊 4 個／598 字 |
| IT-department `CLAUDE.md` | 22,234 | 67 | 1（125 字 `/session-workflow` 條） |
| IT-department `MEMORY.md` | 16,515 | 92 | 0 |
| **每則都付合計** | **49,719** | | |

`session-workflow/SKILL.md` = 3,346 bytes（65 行）。

### 15.2 §3 逐段判定（搬／留）

**搬進 skill（純步驟操作，SKILL.md 已有等價內容）——合計候選 1,298 bytes**

| §3 位置 | bytes | 內容 | SKILL.md 的對應 |
|---|---|---|---|
| L78–80 | 383 | 「開場必派」平台對照（Claude＝`locator`＋`Plan`／Cursor＝`explore`）＋「查詢員前置」重複句 | 步驟 1 的平台表格（更完整） |
| L92–96 | 364 | 「逐項用選擇題討論」「不要邊做邊問」的操作細節 | 步驟 2（另有 `AskUserQuestion`／`AskQuestion` 對照） |
| L98–100 | 120 | Execute 段操作重述 | 步驟 3 完成判準 |
| L103–104 | 235 | 「回退：作法錯→回 Design／沒做完→回 Execute」 | 步驟 4 |
| L112–115 | 196 | Fix 段操作重述 | 步驟 5 完成判準 |
| L62–63 | ~200 | 規模欄沿革括號（「2026-08-07 才加進 §2…」） | 搬本檔 §12（沿革本來就收在那） |

**留常駐不動（判準與硬規則，skill 沒被叫到也不能消失）**

- 規模分級 L／S／M 的三條判準（＝ `session-workflow` 的 SKILL.md 邊界節明文不重寫）
- 「每階段交出什麼」交付物表格（同上）
- 交接契約三條硬規則
- 查詢三層疊代方法論（因果→結構→環境／「都正常」是查錯方向的訊號／帶入已排除項）
- 「改前先 grep 找齊全部 copy」
- 對抗式覆核的判準（錯誤成本高不高，不是複雜不複雜）
- 驗證紀律（語法檢查≠執行期／先證明它會紅）

**淨效果**：§3 從 2,120 → 約 1,100–1,300 bytes（被搬的位置各留一句指標）。
**不是**把 §3 壓成一行——那是 W-2 已否決的做法（遵循度只有 42%，skill 沒被叫到就等於規則消失）。

### 15.3 SKILL.md 要同步改的聲明

現行開頭寫「規則本體在全域 `CLAUDE.md` §3 與本檔。**不一致時以那兩處為準**」，
邊界節寫「不重寫規模判準、交付物格式、派工授權」。搬完之後這句會變成互指：
§3 說平台對照看 skill、skill 說一切以 §3 為準。

改成兩層分工寫死：
- **判準與硬規則**（規模分級／交付物格式／交接契約）本體在 §3，**本 skill** 不得重寫 —— 維持現狀
- **步驟操作與平台對照**（派誰、走哪一步、哪個工具名）本體在本 skill，§3 只留指標句

### 15.4 動手前的閘門（**為什麼還沒動**）

Cursor 端**沒有裝這支 skill**（`~\.cursor\skills-cursor\` 只有內建 25 支），
只有 `cursor-adapter.mdc:13` 一行文字路由。現在搬，Cursor 可能兩頭落空。

勘查清單：`D:\IT-department\.scratch\research\cursor-skill-sharing-probe.md`（13 題／A–E 五組）。
**A（發現路徑能不能 junction）／B（現在看不看得到）／C（常駐通道與 §3 對 Cursor 有無效力）
三組回報之前不動手。** D 組（Cursor 有無 hook）決定搬走之後那邊誰來守。

### 15.5 驗證（怎麼證明會紅）

| 驗什麼 | 怎麼驗 | 會紅的條件 |
|---|---|---|
| **內容沒失聯**（最重要） | §3 刪掉的每一句，逐句 grep `session-workflow/SKILL.md` | 任何一句在 skill 裡找不到＝內容消失，不是搬家 |
| 常駐層真的降了 | `check_bloat.py` 比對本節 15.1 基準 | 全域 bytes 沒降、或超標條目增加 |
| 散文塊降了 | `check_prose_blocks.py` | 全域 4 塊／598 字沒下降 |
| 兩份不互指 | grep SKILL.md 不得再有「以那兩處為準」的全稱句 | 仍在＝ §15.3 沒做 |
| skill 契約完整 | `eval/run_all.py` L1–L4 | 缺 frontmatter／完成判準 |
| provenance | `capability_checks.py` 的 `_p_skill_provenance` | 新目錄沒登記 |
| Cursor 沒被搞壞 | 依 §15.4 回報結果補驗 | 回報是「完全接不到」卻仍動手 |

**基準快照要在動手前後各跑一次**（`--write-snapshot` 只在確認壓下去之後才重建）。
兩支自己寫的實作互相比對不算獨立驗證。

### 15.6 刻意不做

- 不把 §3 壓成一行指標（W-2 已否決，理由見 15.2 末）
- 不碰 IT-department `CLAUDE.md` §8 速查表與 `MEMORY.md` 索引（那是 `/context-health` 的全面瘦身題，本輪範圍外）
- 不拷 skill 進 `.cursor\skills\`（W-10 剛因為「兩份會漂」刪掉 design-spec 副本）
- 不建階段順序閘門（W-9 維持）

### 15.7 Cursor 端勘查回報（2026-08-25，閘門解除）

問卷：`D:\IT-department\.scratchesearch\cursor-skill-sharing-probe.md`。Cursor 端回報結論
**可共用**，本地已複驗。要點：

| 題 | 回報 | 對本計畫的影響 |
|---|---|---|
| A-1/A-2 | Cursor 官方掃描路徑含 `~\.claude\skills\`（相容層）；`skills-cursor` 是內建保留區、官方明文禁止放使用者 skill | **不必再建 junction**，現有那條就是共用管道 |
| A-3 | 兩邊 `SKILL.md` SHA-256 相同 = 同一份檔 | 不會漂 |
| B-4/B-6 | `session-workflow` **已在** Cursor 的自動清單；`cursor-adapter.mdc:13` 那句路由**有實際效力**（它照著去 Read 了） | 路由不是空文字 |
| **C-7/C-8** | **全域 `CLAUDE.md` 對 Cursor 不是 always-loaded**；Cursor 自動載入的只有專案 `CLAUDE.md` ＋ `cursor-adapter.mdc`。`~\.cursorules\*.mdc` 確認沒注入 | **搬步驟進 skill 對 Cursor 是加強不是削弱**——它本來就沒在付 §3 的 token |
| E-13 | `disable-model-invocation` Cursor 同樣支援，有旗標的（`wayfinder`／`to-tickets`）都不在自動清單 | ⇒ **本支禁加該旗標**，已寫進 SKILL.md 邊界節 |
| E-12 | `ui-rules` 現在沒有 `paths:`，**無法重現** 8/25「只會藏、開了檔也不加回」那次實測 | 該結論**降級為待重驗**，不再當既定事實 |

**Cursor 提出、本輪未做的一項**：L／S／M 規模判準只在全域 `CLAUDE.md`，Cursor 現在就沒有。
要它也常駐得放專案 `CLAUDE.md` 或 adapter。**本輪刻意不做**（範圍是去重，不是補規則），登記在 §15.9。

### 15.8 Execute 實測結果（2026-08-25）

**⚠ 估計與實際差很多，這裡照實記。** §15.2 估「候選 1,298 bytes」是照**段落邊界**算的；
真正逐句判定之後，**那些段落裡絕大多數是判準與硬規則，不是步驟操作**：

| §3 段落 | 段落 bytes | 真正搬走的 | 留下的是什麼 |
|---|---|---|---|
| Research 開場必派 | 383 | 平台對照＋角色型別＋「查詢員前置」重複句 | 「查得準＋查不到要疊代」是方法論 |
| Design 計畫先行 | 364 | 「逐項用選擇題」的操作 | M 級先寫計畫書／同意才執行／不要邊做邊問／驗證方式先寫 **全是硬規則** |
| Execute | 120 | 「每項有對應改動或沒做＋理由」（交付物表已有） | **不擴張範圍**是硬規則 |
| Review 回退 | 235 | 「作法錯→回 Design；沒做完→回 Execute」 | **禁把作法錯誤當 Fix 硬改**是硬規則 |
| Fix | 196 | 「每條標 fixed／skipped」（交付物表已有） | 已修且已生效／served 實測是硬規則 |

⇒ **教訓：段落邊界不是判準邊界。** 下次估瘦身空間要逐句分，不要按段落 bytes 相加。

**量測（動手前 → 動手後）**

| 指標 | 前 | 後 |
|---|---|---|
| 全域 `CLAUDE.md` | 10,970 bytes・超標 1 | **10,479・超標 0**（−491） |
| 全域散文塊 | 4 塊／598 字 | **3 塊／396 字** |
| IT-department `CLAUDE.md` | 22,234・超標 1 | **22,215・超標 0** |
| `session-workflow/SKILL.md` | 3,346 | 4,069（吸收步驟細節＋兩層分工聲明） |
| `eval/run_all.py` | — | **L1-self／L1／L2-self／L2／L3／L4 全 PASS** |

−491 裡有 273 是今天新增的兩條超標條目（理由搬 §12.3），真正 §3 去重是 **−218**。

**失聯檢查（§15.5 第一條）**：搬走的 9 個關鍵字逐一 grep `SKILL.md` 全部命中
（`locator`／`Plan`／`explore`／`沒有 locator`／`選擇題`／`沒做＋理由`／`回步驟 2`／`回步驟 3`／`fixed 或 skipped`）。
過程中抓到一次真失聯（「程式識別字可原樣留」刪了沒搬），已補進 §12.3。

**中途修正**：第一版在 §3 各段加了行內指標（`→ /session-workflow 步驟 N`），
反而讓兩個散文塊變長（L89 133→157 字）。§3 開頭已有「編排走 `/session-workflow`」一句，
行內再指一次是重複——已改成「見 skill 步驟 N」的短形。

### 15.9 未做 / 待決

| 項目 | 為什麼沒做 | 下一步 |
|---|---|---|
| L／S／M 判準對 Cursor 不常駐 | 本輪範圍是去重，補規則屬擴張 | 決定放專案 `CLAUDE.md` 還是 `cursor-adapter.mdc`（都會增加 Cursor 的 always-loaded） |
| `check_bloat` 基準快照未重建 | §15.1 是動手前基準，現在 `--write-snapshot` 會洗掉對照組 | 確認數字穩定後 `--write-snapshot --project __global__` |
| E-12「`paths:` 只會藏」待重驗 | 現在沒有任何 skill 帶 `paths:`，無對照組 | 造一支帶 `paths:` 的測試 skill，開／不開對應檔各看一次自動清單 |
| **Cursor 端 13 條 hook 從未生效** | 本輪範圍外的**獨立問題**，已登記 `TODOS.md`（閘門・高） | 見 TODOS 那一列的四步 |

### 15.10 狀態

2026-08-25 **W-11 已落地**：§3 去重完成、SKILL.md 改為兩層分工、Cursor 端確認可共用。
eval L1–L4 PASS、超標條目歸零、失聯檢查全數命中。未做項見 §15.9。
