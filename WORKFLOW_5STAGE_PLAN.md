# 五階段工作流計畫書（大綱）

> 2026-08-05 立案。user 提出以**交付物**為核心的五階段工作流：
> `Research → Design & Spec → Execute → Review → Fix & Deploy`，標題是**「關鍵在交接」**。
> **本檔只做大綱**，四個維度（工具／規範／技能／人員）的逐項套用另開輪次，未討論前不動工。

---

## §0 為什麼要改（現況三階段的缺口）

現行工作流在全域 `~/.claude/CLAUDE.md` §3，是三階段：**規劃 → 執行 → Review**。

三個缺口，都是今天實際發生過的：

| 缺口 | 今天的實例 |
|---|---|
| **沒有 Research 階段** | 我改角色目錄時只 grep 了一種寫法就動手，漏掉三支產生器的路徑常數 —— 那是「研究沒做完就進執行」 |
| **沒有 Fix & Deploy 階段** | Review 找到問題後，「修完了沒、上線了沒」沒有獨立的完成判準，容易停在「已知道要修」 |
| **★ 每階段沒有定義交付物** | 現行規則只講「做什麼」（先寫計畫書、先派查詢員），沒講「**產出什麼給下一棒**」。於是交接靠對話裡的上下文，換一個 session 或換一個角色就斷了 |

**第三點是這次的核心。** 圖上每一格底下都掛著一個交付物，那不是裝飾——
**沒有交付物的階段無法驗收，也無法交給別人接手。**

---

## §1 五階段定義（大綱）

每階段四件事：**目的／交付物／完成判準／誰做**。

### 1. Research（研究）→ 交付物：**研究資料包**

- **目的**：把「我以為」變成「我查過」。回答「現況是什麼、有哪些副本、上次為什麼這樣做」。
- **交付物**：一份可貼給別人的清單 —— 命中位置（`檔案:行號`）、已排除的可能性、**沒找到什麼**。
- **完成判準**：下一棒不必重查就能開始設計；「沒找到的」欄位非空（空白＝沒窮盡）。
- **誰做**：`locator`（唯讀查詢員）、`Explore`；跨專案文件漂移用 `project-auditor`。

### 2. Design & Spec（設計與規格）→ 交付物：**工作規格**

- **目的**：把要做的事寫成可逐項討論的東西，而不是邊做邊決定。
- **交付物**：計畫書（現況 → 目標 → 做法 → 驗證 → 狀態）＋ **待決分岔表**（選項＋傾向＋理由）。
- **完成判準**：每個分岔都有 user 的決定；**驗證方式在動工前就寫好**（不是做完才想怎麼驗）。
- **誰做**：主 session；架構層面可派 `Plan`。

### 3. Execute（執行）→ 交付物：**可檢查的成品**

- **目的**：照規格做，不擴張範圍。
- **交付物**：改好的檔案 ＋ **改了什麼／為什麼是這個值** 的對照。
- **完成判準**：規格上每一項都有對應的改動或明確的「沒做＋理由」。
- **誰做**：主 session；視覺與版面派 `visual-designer`。

### 4. Review（覆核）→ 交付物：**清單 ＋ 證據**

- **目的**：找出「看起來完成、實際沒有」的部分。
- **交付物**：逐項判定表，每項附**證據**（實際跑的指令／量到的數字／截圖），不接受「應該沒問題」。
- **完成判準**：測試全綠**且**變異測試證明它會紅；未驗項已落檔（四欄齊全）。
- **誰做**：`sync-checker`（雙份同步）、`harness-auditor`／`project-auditor`（文件漂移）、
  `Plan`（對抗式覆核）；判準本體在 `/verify-rules`。

### 5. Fix & Deploy（修完上線）→ 交付物：**修完上線**

- **目的**：Review 的發現要收斂到「已修且已生效」，不是「已知道」。
- **交付物**：修復對照（哪一條發現 → 哪個改動 → 怎麼驗的）＋ 上線後的驗證輸出。
- **完成判準**：Review 清單每一條都標 fixed／skipped（附理由）；部署有 served 版本的實測。
- **誰做**：主 session；部署走 `/deploy-prod`；資料遷移走 `/dry-run-migrate`。

---

## §2 交接契約（本次的核心）

**「關鍵在交接」的操作定義：下一棒不必回頭問上一棒，就能開始工作。**

| 交接點 | 上一棒必須給 | 下一棒才能假設 |
|---|---|---|
| Research → Design | 命中位置、已排除項、**沒找到的** | 不必重查；範圍已知 |
| Design → Execute | 定案的分岔、**驗證方式** | 不必再問「這樣對嗎」 |
| Execute → Review | 改動對照、沒做的部分 | 知道該檢查什麼、什麼是刻意不做 |
| Review → Fix | 逐項判定＋證據 | 知道哪一條要修、依據是什麼 |

**三條交接硬規則（草案）**：

1. **交付物要能脫離對話存在** —— 貼進一個新 session 也讀得懂。這是「換人接手」的最低標準。
2. **「沒做的／沒找到的」是交付物的一部分**，不是可選欄位。空白會被下一棒讀成「已窮盡」。
3. **跨階段不繼承推測**。上一棒的推測要標成推測；下一棒**不得把推測當事實往下傳**
   （今天的實例：「Edge headless 一直可用」被當事實，直到實測才發現它壞了）。

---

## §3 四個維度的現況對照（後續逐項套用的清單）

### 人員（角色 5＋3）

| 階段 | 現有角色 | 缺口 |
|---|---|---|
| Research | `locator`／`Explore`／`project-auditor` | 角色正文**沒有規定交付物格式**（現在的輸出格式是各寫各的） |
| Design | `Plan` | 內建角色，**不載入 CLAUDE.md**，拿不到工作流規則 |
| Execute | `visual-designer` | 只涵蓋視覺；一般開發沒有專屬角色（主 session 自己做） |
| Review | `sync-checker`／兩個 auditor | ✅ 這一階段的角色最完整 |
| Fix & Deploy | **無** | 沒有部署角色（走 skill 不走角色） |

### 技能（14 支）

| 階段 | 現有 skill |
|---|---|
| Research | `/asset-data-rules`・`/platform-resource-rules`・`/license-rules`（參考型，供查規則本體） |
| Design | **無**（計畫書格式只寫在 CLAUDE.md，沒有執行器） |
| Execute | `/deploy-prod`・`/dry-run-migrate`（其實偏 Fix&Deploy） |
| Review | `/adversarial-review`・`/verify-rules`・`/audit`・`/verify-skill`・`/diagnose-bug`・`/data-incident`・`/codebase-health` |
| Fix & Deploy | `/deploy-prod`・`/shougong` |

**觀察：Review 有 7 支、Design 有 0 支。** 規則寫最多的階段不是最容易出錯的階段。

### 規範

| 層 | 現況 |
|---|---|
| 全域 `~/.claude/CLAUDE.md` | 今天剛搬過去：溝通／路由／三階段工作流／模型／交付 |
| 專案 `<repo>/CLAUDE.md` | 專案特有（§8 硬規則速查、§9 部署環境等） |
| `.claude/rules/*.md` | 5 支 path-scoped（碰到對應檔案自動載入） |

**缺口**：工作流只到三階段，且**沒有任何一條規則講交付物格式**。

### 工具（harness）

| 類型 | 現況 | 對工作流的支援 |
|---|---|---|
| hook 閘門 | 8 條規則（6 enforce／2 shadow） | 擋的是**動作**（部署、編碼、選擇題），不擋**流程**（例如「沒有研究資料包就進入執行」） |
| 觀測 | event log ＋ 平台 subagent 紀錄 | 看得到誰被派、用了什麼工具；**看不到現在在哪個階段** |
| 看板 | 8 分頁 | 沒有工作流視圖 |
| 成本 | 按模型／按日 | **無法按階段歸因** ← user 圖上的 💰 填不了就是這個原因 |

---

## §4 差距清單（要做什麼，尚未排序）

1. **定義交付物格式**（五份範本）——這是所有其他事情的前提。
2. **工作流規則進全域 `CLAUDE.md`**：三階段 → 五階段 ＋ 交接契約三條。
3. **角色正文加交付物規定**：每個角色的「輸出格式」要對齊它所屬階段的交付物。
4. **補 Design 階段的執行器**（目前 0 支 skill）。
5. **階段可觀測**：讓 event log 記得下「現在在哪個階段」，成本才能按階段歸因。
6. **考慮流程閘門**：例如「Execute 前沒有研究資料包 → WARN」。⚠ 這條風險高，容易變成摩擦。

---

## §5 待決分岔（逐項討論，同意才做）

| # | 分岔 | 選項 | 傾向 |
|---|---|---|---|
| W-1 | 五階段是**硬性流程**還是**檢查清單** | (a) 硬性：每階段都要有交付物才能往下 (b) 清單：提醒但不強制 (c) 依任務規模分級 | **(c)**：小改動硬套五階段會變成儀式；但「規模」的判準要寫死，否則每次都會自我豁免 |
| W-2 | 交付物要不要**落成檔案** | (a) 落檔（可跨 session） (b) 留在對話 (c) 只有大型任務落檔 | **(a) 或 (c)**：§2 第一條硬規則就是「能脫離對話存在」，留在對話等於沒交接 |
| W-3 | 要不要為 Execute／Fix&Deploy **建專屬角色** | (a) 建 (b) 主 session 自己做 | **(b) 先不建**：那兩階段需要完整寫入權，角色化的收益低、風險高 |
| W-4 | 階段觀測怎麼做 | (a) 自我宣告時多帶一個階段欄位 (b) hook 自動推斷 (c) 不做 | **(a)**：現行已經有「每次任務開始自我宣告」，加一欄成本最低 |
| W-5 | 先做哪一項 | (a) 交付物格式 (b) 規則進全域 (c) 階段觀測 | **(a)**：格式沒定，其餘三項都沒有可對齊的對象 |

---

## §6 狀態

- 2026-08-05 立案，**本檔只有大綱**。§3 四維度現況已實際盤點（角色 5＋3、skill 14 支、
  規範三層、harness 工具四類）。
- §5 五項待決，**未逐項討論前不動工**。
