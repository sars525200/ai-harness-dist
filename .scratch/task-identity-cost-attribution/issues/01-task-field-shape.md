# 01 「任務」欄的字面形狀與解析錨點

**Type:** grilling
**Blocked by:** 07
**Status:** resolved（2026-08-23 · user 裁決 ＋ 已施作）

## Question

宣告要多一欄 `任務 <名稱>`（D1）。**這一欄長什麼樣、由誰解析、缺了怎麼辦？**

⚠ **2026-08-23 覆核 R1 大幅改寫**：原版有三個錯誤假設 ——
以為問題出在右界欄位清單（實際是分隔符字元集，已分出票 07）、
以為宣告是一行（實際有圍欄式多行）、以為解析點有三處（實際四處）。

### 1. 字首碰撞 —— 這是最容易靜默的一條（R1-F1）

`任務` 是 `任務分類` 的**字首**。任何 `任務\s*[:：]?\s*(...)` 形狀的正則，
套在沒有任務欄的舊宣告上會命中 `任務分類`、捕捉到「分類」。實測：

- 8,684 個 assistant text block 掃出 379 命中，前幾名捕捉值：
  `分類`×166（＝任務分類）、`中心`×7、`工單`×6（＝**產品端的任務中心／任務工單，
  它們真的在語料裡**）
- 501 段既有宣告有 203 段填了 `任務分類` ⇒ 這 203 段不會落進未標記桶，
  而是變成一個叫「分類 [devops]」的**幽靈任務**
- 連鎖後果：「未標記佔比＝宣告紀律」這個量測會**方向性地偏低**，看起來像紀律很好

要定：正則怎麼寫才擋得住字首碰撞（negative lookahead？要求分隔符？限定值域？）。

### 2. 位置與長度上限（R1-F11）

`DECL_LINE = ^.{0,40}(?:模式|階段)[^\n]{0,400}$`，兩個上限都是硬的：

- **前綴 40 字**：既有宣告前綴中位數 0、最大 22 ⇒ 只剩約 18 字餘裕。
  若把 `任務` 放最前面（對身分欄很自然），`任務 task-identity-cost-attribution｜`
  就是 34 字，加上實測存在的 `🔧 **` 之類前綴＝38，**再長一點整行匹配失敗**
- **尾巴 400 字**：實測最長的一段**正好 400**，貼著上限
- **失效方向最壞**：整行失配 ⇒ 這一段同時從閘門與遵循度分母消失 ⇒
  少一段違規 ⇒ **數字變好看**，沒有任何訊號

### 3. 宣告不一定是一行（R1-F13）

現場存在「圍欄式多行宣告」（2%，9/420），而**三個消費端對它三種答案**：
`decl1` 先剝掉整段圍欄（看不到）／`gen_workflow_compliance` 不剝但行錨定（只看到第一行）／
`gen_cost_panel` 整段 search（照收）。任務欄放第一行 DECL-1 看不到，放後面幾行遵循度看不到。

### 4. 四處解析點，而 parity 測試只綁三條（R1-F12）

`decl1_stage_files.py`（子集）／`gen_workflow_compliance.FIELD`／`gen_cost_panel._STAGE_RE`／
**`gen_task_flow.py:349` 抄了一份 120 字啟發式**。
`tests/test_decl1.py:227-232` 的逐字相同只綁 `DECL_LINE`／`stage`／`files` 三條 ——
**新增 `FIELD["task"]` 不在保護範圍內，只改一份、測試照樣綠。**
⇒ 這一項**不是選擇題**：不擴 parity 測試等於沒有保護，票裡直接寫死。

### 5. DECL-1 缺欄位的處置

缺「修改檔案」現在會 WARN。缺「任務」要不要也 WARN？要的話第一天所有既有 session
都會被叫一次 —— 那正是 WARN 疲勞的起手式。

### 6. 未標記桶的語意

任務的未標記要不要跟階段的未標記分開算（兩者可以獨立缺席）。

### 7. 捕捉到的字串怎麼變成 key（R4-M6·第七個沒有票的決定）

D1 說「**名稱即 key**，不另設 ID」。但任務名是模型每一輪重打的自由字串，
而 hook 回餵只保證「有抄」不保證「**一字不差**」（票 02 的 fixture ② 問的是
「模型真的抄進宣告行了嗎」，沒問「抄得逐字相同嗎」）。

`任務 成本歸因`／`任務 **成本歸因**`／`任務 成本歸因 `／全形半形空白／結尾標點
——**各是一個 key** ⇒ 同一個任務裂成好幾列，每列一部分錢。
表能跑、看起來對、而 Destination 那句「這個任務花了多少錢」答錯。

⚠ **四個到達點條件會全綠**：Σ==Σ 成立、「非未標記桶的任務數 > 0」成立、
票數關完、更新路徑有人。**沒有任何一條抓得到裂開的 key。**

前例就在隔壁：`任務分類` 是**封閉五值**，203 段有值卻長出 **30 種寫法**
（`[UI|邏輯]` 與 `[UI｜邏輯]`、`devops` 與 `[devops]` 各自成格，見票 04）。
封閉值域都這樣，開放字串只會更糟。

要定：大小寫／全半形／前後空白／markdown 粗體／結尾標點／同義合併
——**由誰做、在哪一層做**（解析當下？寫狀態檔時？聚合時？）。

**驗收**：同一個任務用三種寫法宣告，任務表**只能有一列**。

## 驗收錨點

四種 fixture 缺一不可：舊宣告（不得產生幽靈任務）／三種分隔符各一份／圍欄多行／
貼著 40 與 400 上限。**只用 `／` 的單行 fixture 一定綠**，那是這批缺陷至今沒被發現的原因。

## Answer

**決定（user 2026-08-23）**

| # | 問題 | 決定 |
|---|---|---|
| §2 | 欄位位置 | **接在「模式」後面當第二欄**。不吃前綴 40 字預算——那個上限是用來擋「談論規則的句子」的，不該為了新欄位放寬 |
| §5 | DECL-1 缺「任務」要不要 WARN | **第一版不 WARN，只統計**。第一天所有既有 session 都會被叫一次，那是 WARN 疲勞的起手式（AWC-1 的前例：該率曾被刻意壓到 14.4%） |
| §7 | 正規化在哪一層 | **解析當下就做**。下游所有消費端拿到的已經是乾淨值；各自正規化就是下一個不一致來源 |

### 字首碰撞的守門不是 `(?!分類)`，是**要求分隔符**

票 §1 的藥方是 negative lookahead。實作後對真實語料一跑，**還是漏了一筆**：
`任務動線` 被當成 `任務 動線…`。票自己也記過同類的 `中心`×7、`工單`×6
——**那兩個是產品端真實名詞**（任務中心／任務工單）。

⇒ **黑名單擋不住還沒出現的詞。** 真正的守門是：宣告寫 `任務 X`（帶分隔符），
而複合詞 `任務動線` 不帶。正則因此要求 `任務` 後面必須跟空白或冒號：

```
r"任務(?!分類)(?:\s*[:：]|\s)\s*\**\s*(.+?)"
r"(?=\s*[／/｜]\s*\**\s*(?:(?:修改)?摘要|模式|階段|規模|任務分類|分類|修改檔案)|$)"
```

`(?!分類)` 保留為額外保險。實測真實語料**捕捉 0 筆**（欄位今天才上線，正確）、
**幽靈任務 0 筆**（205 段有任務分類、無任務欄的宣告全數不命中）。

### 正規化 `_norm_task()`

去 markdown 粗體與行內碼、全形空白→半形、內部連續空白收成一個、去頭尾、
去結尾標點。實測六種寫法（`成本歸因`／`**成本歸因**`／`任務：成本歸因`／
`` `成本歸因` ``／尾隨空白／尾隨句點）**正規化成同一個 key**。

### 票 §4「不是選擇題」那一項的實況更新

票寫「四處解析點，parity 測試只綁三條」。實查後**四處只有兩處是真的複本**：

- `gen_task_flow.py:349` **是 `import wfc` 重用同一份正則**（`wfc.DECL_LINE`／
  `wfc.FIELD["stage"]`），票說的「抄了一份 120 字」指的是那條 `len(raw) > 120`
  啟發式，不是正則 ⇒ **不需要 parity**，改一份自動同步。
- `gen_cost_panel._STAGE_RE` 確實是複本，但**只管階段、沒有任務**。
  第一版 `FIELD["task"]` 只存在於 `gen_workflow_compliance` ⇒ 目前無對象可 parity。
  等票 03 讓 `gen_cost_panel` 也要讀任務名時，**那時才是擴 parity 的時機**（記入票 03）。

### 驗證方式（票的驗收錨點：四種 fixture 缺一不可）

`tests/test_workflow_compliance.py` 新增一組（回歸網 1075 → **1102**）：

1. **舊宣告不得產生幽靈任務** ＋ **複合詞不得被當成任務欄**（最高風險：205 段）
2. **三種分隔符各一份**（`／`／`/`／`｜`）
3. **正規化**：六種寫法只能是同一個 key
4. **貼著兩個硬上限**：前綴 39 字仍匹配、**41 字確實失配**（證明前一條不是白撿的）、
   尾巴接近 400 仍匹配
5. **圍欄式多行**：任務欄放第一行取得到

**變異證明（實跑，兩次）：**

- 變異①「拿掉字首守門，退回票裡設想的天真寫法」⇒ 第 1 組兩條**立刻紅**。
- 變異②「拿掉正規化接線（`_norm_task(got["task"])` → `got["task"]`）」⇒
  **1101/1101 全綠，沒抓到。** 原因：第 3 組直接呼叫 `m._norm_task()` **驗的是函式不是接線**。
  ⇒ 補第 6 條**接線斷言**（`inspect.getsource(m.collect)` 必須含 `"task": _norm_task(`），
  再變異一次確認紅。**這是本票最有價值的一條，而票裡沒有。**

⚠ 第 6 條刻意做在原始碼層而不是資料層：`collect()` 現在回傳 **0 個帶任務名的 segment**
（欄位今天才上線），資料層斷言會 **vacuously 通過** —— 那正是「零目標須拒跑」要擋的假綠燈。
語料裡有真實任務名之後應換成「每個 segment 都滿足 `task == _norm_task(task)`」。

### 沒做的

- **§3 圍欄多行的三個消費端不一致**（`decl1` 剝圍欄看不到／遵循度行錨定只看第一行／
  `gen_cost_panel` 整段 search）：第一版 DECL-1 不檢查任務欄，這個不一致**暫時無害**。
  票 03 讓 `gen_cost_panel` 讀任務名時必須一起處理，已記入該票的前置。
- **§6 未標記桶語意**（任務的未標記要不要跟階段分開算）：那是看板呈現的決定，
  留給票 05。
- **DECL-1 側不加任何 `任務` 正則**（決策二：不 WARN）⇒ `decl1_stage_files.py` 零改動。
