# 09 規模 × 派工強度：第一版要存哪些欄位

狀態：**未修**　派工強度那軸沒進歸因結構，兩軸覆蓋率與缺漏筆數的斷言都不存在。前置票 03 未修
（2026-09-03 盤點：三個唯讀稽核員逐張比對現行碼與 git 歷史，不只看 commit 訊息。判不出來的一律留「待確認」，不猜。）


**Type:** grilling
**Blocked by:** 03
**Status:** ready-for-human

## Question

D10 說「象限＝規模 × 派工強度，**第一版先把欄位存齊**」，而 Destination 第一段也
明文承諾「具備日後判斷派工準不準所需的欄位」。**但八張票沒有一張碰它**
（R2-N8 點名的第五個「必須落定但沒有票」的決定）。

畫圖是第二版，**存欄位是第一版承諾** —— 沒存齊的話第二版要回頭重跑歸因。

1. **「該用多少馬力」怎麼取**：規模欄（L／S／M）是唯一候選嗎？
   它實測缺漏 65/343，而且是自評。缺漏的那些在象限上要落哪裡？
2. **「實際用了多少」怎麼取**：主 session 那側是 model；subagent 那側是
   `agents\*.md` frontmatter 的 `model`／`effort`。兩者要合成一個「貴不貴」的量嗎？
   `gen_task_flow` 已經有 `_quad_of(model, effort)` 與 `HIGH_EFFORT = {"xhigh","max"}`
   —— 沿用還是另定？（沿用的話門檻就綁在一起，改一邊會動到另一張圖）
3. **存在哪**：併進票 03 的歸因結構，還是另一份？
4. ~~subagent 的錢算誰的~~ ⚠ **R3-F2 訂正並移出本票**：這個問法假設錢已經在表裡、
   只是掛錯人。**它根本不在表裡** —— `gen_cost_panel` 是非遞迴 glob，
   `subagents/*.jsonl` 那 327 個檔從來沒被讀過（post-cutoff 佔全部訊息 24.1%、≈US$430）。
   「要不要納入語料」是**第一版**的決定，已移到**票 10**。本票只在語料確定之後
   決定「兩軸的值怎麼存」。

## 驗收錨點

⚠ **R3-F5 訂正**：上一版寫「第二版要畫圖時不必回頭重跑歸因」——那在第一版
**無法證偽**，而到達點條件 1 要求每張票的 Answer 寫出自己的驗證方式 ⇒
等於無條件關票。

改成第一版就能跑的：對 `collect()`／`stage_attribution()` 的**真實輸出**，
兩軸欄位的非空覆蓋率 ≥ 一個寫死的門檻，**且缺漏筆數本身是一個被斷言的數字**（不是「大部分都有」）。
⚠ **R4 提醒**：規模欄缺漏在活語料上四天內從 65/343 → 87/367 ⇒ **別把常數焊進斷言**，
照本圖 R2-N12 的原則「記算法不記數字」：斷言演算法 ＋ 斷言那個數字有被印出來。
