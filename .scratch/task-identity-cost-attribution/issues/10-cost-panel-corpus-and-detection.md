# 10 cost_panel 的語料範圍與宣告偵測口徑

狀態：**已修** 8cc74c8（2026-08-24）　token 語料納入 subagent、session 集合隔離、階段偵測收斂成單一真相，18 條全綠
（2026-09-03 盤點：三個唯讀稽核員逐張比對現行碼與 git 歷史，不只看 commit 訊息。判不出來的一律留「待確認」，不猜。）


**Type:** grilling
**Status:** resolved（2026-08-23 · user 裁決 ＋ 已施作）

## Question

⚠ **2026-08-23 覆核 R3-F2／F4 開的票。** 三個地方點名 `gen_cost_panel` 的偵測口徑
（票 01 §4、R2-N6、票 03），**零張票負責它** —— 而票 03 非改那支函式不可。

### 1. subagent 的錢整批不在語料裡

`gen_cost_panel.py:199` 是 `PROJECT_DIR.glob("*.jsonl")`，**非遞迴**。實測：

⚠ **R4-M2 訂正路徑**：佈局是**兩層** ——
`~\.claude\projects\d--IT-department\<session-uuid>\subagents\agent-<id>.jsonl`，共 **328** 個。
**`subagents/*.jsonl` 這個樣式今天匹配 0 個檔** ⇒ 照字面改會「跑得動、數字紋風不動、沒有紅燈」。
正確樣式是 `*/subagents/*.jsonl`，而且**單一真相已經存在**：
`dashboard\subagent_stats.py:12`（佈局文件化）與 `:119`（`project_dir.glob("*/subagents/*.meta.json")`），
`gen_cost_panel` 甚至已經 import 了那支模組。

- top-level jsonl（歸因看得到）**148** 個
- post-cutoff 唯一訊息：主語料 18,751／subagent **6,065（24.3%）**，與主語料 id **零重疊**
- 按各家族實際單價 ≈ **US$385**（R4-M2 訂正：原估 $430 是把 opus 單價套到全部，偏高約 12%）

⚠ 票 09 §4 原本問「subagent 的錢**算誰的**」，那個措辭假設錢已經在表裡、只是掛錯人。
**它根本不在表裡。** 這與 R2-N1 推翻的錯誤前提是同一種形狀。

**三個連帶效應，每一個都會靜默**（R4-M2 實測）：

1. **污染 §7 的模型 mix**：`gen_cost_panel.py:130` 的 `aggregate_tokens()` 用**同一個**非遞迴 glob。
   ⚠ **R5 訂正口徑**：`gen_cost_panel.py:631` 的 mix 是 **output token**
   （`:855` 明文「mix 用 output token，則數列為輔助」）。納入 subagent 後實測——
   **output token（＝§7 的 mix）：Opus 91.3%→89.6%、Sonnet 7.2%→8.8%**；
   則數（輔助列）：Opus 91.3%→83.9%、Sonnet 7.8%→13.7%。
   上一版引的 83.8／13.7 是**則數**那一列，掛到 mix 名下會把幅度放大約 4 倍
   （1.7pt 寫成 7.4pt），照票去對面板會對不上。方向不變：兩種口徑 Opus 佔比都下降。——
   而「Opus:Sonnet 目標 4:6」那條比例正是那一頁存在的理由。
2. **打斷金額交集**：subagent 檔名是 `agent-<hex>` 不是 session UUID，而 `:473-495` 是拿
   `fp.stem` 與 ccusage 的 session UUID 取交集 ⇒ 只會多出 328 個對不上的 key，
   `project_total` 原地不動。
3. **灌爆未標記桶**：實測 6,098 筆 post-cutoff subagent 訊息裡，帶「階段 X」字樣的只有 **1** 筆。
   直接納入 ⇒ 24% 的訊息、≈$385 一次全掉進未標記，而「未標記佔比＝宣告紀律」是
   `gen_cost_panel.py:189-190` 明訂的既有量測 ⇒ **紀律沒變、數字崩壞**。

**可行解（R4 找到的，不必重新發明）**：每個 `agent-*.jsonl` 旁邊有 `agent-*.meta.json`，
內容含 `toolUseId`；而 `gen_workflow_compliance.collect()` 已經以**同一個 tool_use id**
記 `agent_calls[...]`。「一次派工的錢記在派它的那一段底下」有現成的鍵。

要定：納不納入？納入的話走 `toolUseId` 掛回父段，還是另立一桶？

### 2. `_STAGE_RE` 與 `FIELD["stage"]` 已經漂了

`gen_cost_panel.py:95` 少了 `\**\s*`，`階段 **Review**` 只有遵循度那側看得到。
實測 384 vs 418、對稱差 44（雙向 10.4%）。要不要對齊？對齊之後
**既有階段成本數字會當場變動**，變動幅度要量出來、要不要先告知。

### 3. 任務名的值域怎麼封閉

`gen_cost_panel.py:235` 是**裸的 `.search()`** —— 沒有 `DECL_LINE` 行錨、沒有 40 字前綴限、
沒有 400 字尾限、沒有 120 字散文閘（遵循度那側三道全有）。
**它至今安全只有一個理由：階段是封閉五值。** 任務名是開放字串，同一機制下
每一句「這個任務…」都會移動游標並長出一列新任務。

⚠ 票 01 §1 的解藥（negative lookahead／要求分隔符／限定值域）是寫給 **wfc 的 `FIELD`** 的，
cost_panel 那側連 `FIELD` 介面都沒有。

### 4. 任務名到底由哪一側產出

- 走 **join**（wfc 出名字、cost_panel 出錢）→ ⚠ **R5 訂正：不是「沒有共用鍵」**（上一版講太死）。
  建段時 `mid` 就在手上（`gen_workflow_compliance.py:533-543`，去重用的 `seen_msg.add(mid)`
  就在建 `cur` 的前一行，只是沒放進去），**存起來是一行**。
  **真正的障礙是語料／cutoff／去重範圍不同** —— 這改變本節的選項集合：**join 不是死路**。
  （原本的理由：wfc segment 不存 message id，）
  兩邊 cutoff／專案範圍／去重範圍全都不同。
- 走 **cost_panel 自己解析**（票 03 假設的）→ 要先解掉上面第 2、3 點。

**這一題不定，票 03 動不了。**

## 驗收錨點

①對齊 `_STAGE_RE` 之後，既有階段成本的變動幅度是一個**被斷言的數字**，不是「有變」。
②**口徑對齊要雙向量**（R4-M3）：新增偵測 N 筆、**失去偵測 M 筆**，M 的每一筆都要人看過 —— 那 5 筆「只有 cost_panel 看得到」的樣本裡**有 2 筆是真宣告**（前綴 84 字被 40 限擋、前綴 10 字被 120 字散文閘擋），只往「散文不得移動游標」單向收緊，會讓真宣告從此不移動金錢游標、整段 Execute 的錢靜靜掛到上一個任務。
③任務名值域的擋散文能力：拿一則「討論宣告」的真實訊息當 fixture，
**不得**讓它移動游標（實測現行 cost_panel 對這種訊息會照收，已有樣本）。

## Answer

**決定（user 2026-08-23）**

| # | 問題 | 決定 |
|---|---|---|
| 1 | subagent 的錢納不納入 | **納入，走 `toolUseId` 掛回父段**。接不上的另立「無法歸屬」並顯示，不默默吐掉也不灌進未標記 |
| 2 | `_STAGE_RE` 要不要對齊 | **改成單一真相**：`gen_cost_panel` 不再自己解析，改吃 `gen_workflow_compliance` 的完整閘門鏈 |
| 3 | 對齊造成的數字變動 | **直接變 ＋ 斷言幅度**（票 §驗收①：幅度要是一個被斷言的數字，不是「有變」） |

### 施作與實測

**① token 語料納入 subagent**（`_token_corpus()`）

- 實測 **334 個** subagent 檔（票寫 328，還在長）、meta.json 也 334 個
- **模型 mix 修正**：Sonnet **7.2% → 8.6%**、Opus 91.3% → **89.8%**
  （票預測 89.6/8.8，語料又長了故略有差；**方向與量級都對得上**）
- ⚠ **只有這一個 glob 納入。** 另外兩處刻意維持非遞迴，理由不同：
  - `stage_attribution()`：6,098 筆 subagent 訊息裡帶「階段 X」的只有 **1** 筆 ⇒
    直接納入＝ 24% 訊息一次掉進未標記，**紀律沒變、數字崩壞**
  - ccusage 交集：檔名是 `agent-<hex>` 不是 session UUID

**② `sessions` 集合排除 subagent stem** —— **這是施作時才發現的坑，票沒寫。**
`aggregate_tokens()` 同一個迴圈裡有 `sessions.add(fp.stem)`，而那個集合正是餵給
ccusage 交集的。換了語料而沒動它 ⇒ 集合從 149 變 **483**，多出 334 個永遠對不上的 key。
`project_total` 原地不動，但「對不上的 session 數」暴增 —— 看起來像對帳突然壞掉。
**token 統計要 subagent、金額交集不要：兩件事，兩個集合。**

**③ 階段偵測改吃遵循度那側**（`_wfc_stage_of()`）

雙向實測：兩側都偵測到 404、只有遵循度側 39、只有 cost_panel 側 5。
那 5 筆**逐筆看過**（票 §驗收②要求 M 的每一筆都要人看過）：

| 前綴 | 判定 | 處置 |
|---|---|---|
| 533／42／1288 字 | 散文與**引述** | 正確擋掉。⚠ 42 字那筆票記成「真宣告」，實為**引述**（原文在講「那行是 `**階段 Review…**`，於是被整筆丟棄」）—— 所以真宣告是 **2 筆不是 3 筆** |
| 10 字 | **真宣告** `模式 VERIFY／階段 Research。…` | **已救回**：散文閘的訊號集加入 `got["mode"]` |
| 84 字 | **真宣告**（前面是 markdown 連結） | **已知漏掉、刻意不修**（見下） |

**為什麼不放寬前綴上限**（40 → 120 曾被評估）：實測多收 3 段，其中 **2 段是表格列與引述**
（其中一段還是覆核當下自己的輸出），只有 1 段是真宣告。**修 1 筆的代價是 2 筆噪音，淨值為負。**
相對地「模式欄有值」加進訊號集只多收 **1 段，就是那筆真宣告，零噪音** —— 兩個候選用同一把尺量，
勝負是數字分出來的，不是偏好。

### 變動幅度（票 §驗收①：被斷言的數字）

| 項目 | 改前 | 改後 | 差 |
|---|---|---|---|
| 宣告數 | 412 | **446** | **+34** |
| 未標記 | $365.57 | **$67.82** | **−$297.75** |
| Design | $476.35 | $630.03 | +$153.68 |
| Execute | $2193.66 | $2290.64 | +$96.98 |
| Review | $537.72 | $580.33 | +$42.61 |
| Fix | $379.97 | $391.12 | +$11.15 |
| **合計** | $5600.98 | $5603.05 | **+$2.07** |

**未標記佔比 6.5% → 1.2%。** 合計幾乎不動（+0.04%）＝錢沒有憑空出現或消失，只是換了桶。

### 驗證方式

`tests/test_cost_panel.py` 新增 2 個 case（回歸網 1113 → **1115**），**三個變異各自被抓到**：

| 變異 | 變紅的訊息 |
|---|---|
| `_token_corpus()` 退回非遞迴 | 「token 語料裡沒有任何 subagent 檔 —— glob 層數可能寫錯（少一層會靜默 0 命中）」 |
| 拿掉 session 集合守門 | 「session 集合混進 **334** 個 subagent stem —— ccusage 交集會被污染」 |
| 階段偵測退回裸 search | 「前綴超過 40 字的討論段落不得被當成宣告移動金錢游標」 |

⚠ **散文 fixture 第一版是不忠實的**：只寫了一個 90 字的短句，實測**直接綠燈**——
短行本來就該放行，真正擋掉真實樣本的是**前綴限**不是散文閘。改成兩個 fixture、
各對應一道閘才驗得到。**fixture 不忠實的症狀跟「程式沒問題」一模一樣。**

### 沒做的

- **subagent → 父段的歸因尚未接上**。鍵已證明可用（`meta.json` 的 `toolUseId` 與
  `gen_workflow_compliance.agent_calls` 交集 **225 個 ＝ agent_calls 側 95%**），
  但兩條游標的資料結構是**票 03** 的本體，在這裡做等於把 03 提前寫一半。
  ⚠ **票沒寫的兩個缺口**：①**30/334 個 meta.json 沒有 `toolUseId`**
  ②交集只涵蓋 meta 側 74%（79 個對不上，多半在 cutoff 之外）。
  ⇒ 票 03 必須有「無法歸屬」桶，**不能假設每一筆都掛得回去**。
- **前綴 84 字那筆真宣告**：已知漏掉，理由如上（修它的代價是 2 筆噪音）。
- **合計 +$2.07（0.04%）尚未解釋**。金額守恆大致成立但不是精確為零，
  推測與 `decl_of` 認領更多 message id 之後去重集合的交互有關 —— **是推測不是結論**。
- **`fail-open 量測` 那條測試會間歇性紅**（實測連跑兩次：1182 vs 1183）：
  它把持續在長的 event log 讀兩次比對。與本票無關，但撞到了就記著。
