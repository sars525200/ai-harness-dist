---
description: 對話名稱跟著任務走；開場與轉向呼叫 rename_chat
alwaysApply: true
---

# 對話名稱跟著任務走（Cursor）

字面約束見 `global/CLAUDE.md` §2「任務欄」：繁體中文、任務名 ≤12 字、程式識別字可原樣留。本檔只講 Cursor 怎麼執行。

## 用法

- **開場**：任務一確定就呼叫 `cursor-app-control` 的 `rename_chat`（參數 `title`）。不要留平台自動標題。
- **轉向**：再叫一次，自我宣告寫 `任務 舊名→新名`。
- **user 給了名字就用他的**，不要改寫。
- 工具 schema 寫「only when user explicitly asks」是通用說明；本工作區以本檔為準，不必等人開口。

## 格式

整串 ≤48 字；任務名本身 ≤12 字。分類標記固定在最前面（側欄會截尾巴）。

| 分類 | 格式 | 判定（來自自我宣告） |
|---|---|---|
| 【收尾】 | `【收尾】原任務名｜收尾` | 任務名含 收工／封存／交接／收尾／handoff |
| 【討論】 | `【討論】主題` | 模式 ASK／VERIFY **且** 修改檔案為「無」或「待定」 |
| 【任務】 | `【任務】名稱｜階段｜進度%` | 其餘 |
| （無標記） | `專案名｜等待任務｜上一個任務` | **還沒有自我宣告的新視窗**；沒有上一個任務就省略第三段 |

**判定順序不能換。** 收尾那輪通常也是 DEV、也會改檔，先判模式會誤成【任務】。VERIFY 但列了要動的檔＝在做事，算【任務】。

收尾用「原本那件事」的名字，不是宣告裡的「收工封存」。抽不到原名才退回宣告字面。

進度來自宣告的 `進度 60%` 欄，是整件事完成度，不從階段推。判斷不出來就不寫、不補空欄。

新視窗那一列**刻意不加分類標記**：那三種標記說的是「這則在做什麼」，等待中的視窗還沒有那件事，最該一眼看到的是**哪個專案**。專案名取工作目錄的資料夾名，不查中文對照表（核心層不得寫死專案知識）。

## 注意

- 平台自動標題達不到上面約束，必須主動改。
- 「上一個任務」查不到就寫兩段（`專案名｜等待任務`），**不要補空的｜或「無」**。Claude 側由 hook 自己記（`state\lasttask.<專案>.txt`，只記有分類標記的任務名）；Cursor 側沒有那份紀錄就省略。
- 不要搬 Claude 的 hook 實作（`hooks/session_title.py`）。字面約束共用，改名管道不同。
