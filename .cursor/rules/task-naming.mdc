---
description: 對話名稱要跟著任務走；繁體中文、不超過 12 字
alwaysApply: true
---

# 對話名稱跟著任務走（Cursor）

**字面約束的單一真相在 `global/CLAUDE.md` §2「任務欄」那條**，本檔不複製它的本文，
只講 Cursor 這一側怎麼執行。約束就三句：**繁體中文（不寫簡體、不寫英文句子）、
≤12 字、程式識別字可原樣留**。

## 做什麼

- **開場**：任務一確定就把這則對話**改名成任務名稱**，不要留平台自動產的標題。
- **轉向**：工作換成另一件事時**跟著改名**，並在該輪的自我宣告寫一次
  `任務 舊名→新名`。名稱是成本歸因的 key，靜靜換個名字會把同一段工作拆成
  對不起來的兩筆。
- **user 給了名字就用 user 的**，不要自己改寫他給的字。

## 為什麼要明文規定（實測，不是預防性條文）

平台自動產的標題**達不到這個要求**。2026-08-25 量了 102 個有標題的 session：

| 量到的 | 數字 |
|---|---|
| 純英文標題 | **73 / 102** |
| 對話中變過標題 | 8（而且只是同一主題換句話說，不跟任務走） |
| 出現簡體 | 有（`Steam安裝下載速度慢的問題` → `Steam安装下载速度缓慢问题`） |

所以「讓平台自己產」不是一個選項 —— 要人／模型主動改。

## 改名工具（實測 2026-08-26）

Cursor 第一方：`cursor-app-control` 的 `rename_chat`，參數 `title`。
對話一確定就叫，不要等 user 開口——平台自動標題達不到上面三句約束。
工具 schema 寫「only when user explicitly asks」是通用說明，本工作區以本檔為準。

## Claude 端怎麼做（2026-08-26 改寫·原本寫「模型碰不到」是錯的）

**模型仍然沒有改標題的介面**：`/rename` 是 CLI 互動指令，SlashCommand 工具碰不到內建指令，
`claude --help` 也沒有對應的非互動子指令。但**標題的真相是檔案**，而檔案寫得進去：

    <transcript>.jsonl 最後一筆 {"type":"custom-title","sessionId":…,"customTitle":…}
    （沒有這一筆才退回平台自產的 ai-title）

所以 Claude 這側改由 Stop hook `D:\.ai-harness\hooks\session_title.py` 自動寫：
抓本輪自我宣告的「任務」欄 → append 一筆 `custom-title`。模型不必記得做這件事。

**生效時機是下次 Reload Window，不是即時**（實測：寫檔 → 切走再切回，名稱不變；
寫檔 → `Developer: Reload Window`，名稱變了）。extension 對 session 檔沒有掛任何 watcher，
`ensureSessionLoaded` 還有一層記憶體快取。要當下就改名，只能自己打 `/rename <名字>`
（不帶參數它會依對話內容自己生一個）。

**兩個會讓它靜默失效的點**（都寫進 hook 註解與 `tests/test_session_title.py` 了）：

- 平台只掃 head 前 64KB ＋ tail 後 64KB。長對話會把寫過的 `custom-title` 推出窗口，
  名稱悄悄退回英文 ai-title —— 所以同名也要在被推遠時重寫。
- 自我宣告必須在該輪回覆的**開頭 200 字內**（hook 只在那個範圍找，且要求宣告欄位
  至少出現三個），否則抓不到，名稱就停在上一次。

字面約束（繁中／≤12 字／識別字原樣）與 Cursor 側共用，單一真相仍在 `global/CLAUDE.md` §2。

## 名稱有兩個真相：本機檔案與雲端 session（2026-08-26 實測）

開了 Remote Control（`claude.ai/code` 或手機看得到這則對話）時，同一則對話有兩份名字：

| 要改的東西 | 管道 | 生效時機 |
|---|---|---|
| 雲端（網頁／手機側欄） | `/rename` | **即時** |
| 本機 `<sessionId>.jsonl` | `/rename` 或 Stop hook 自動 | 即時 |
| 本機 VSCode 側邊欄 | 上面任一條 | **要 Reload Window** |

`/rename` 的實作是「寫本機 `custom-title` → 再 `PUT {BASE_API_URL}/v1/code/sessions/{cse_…}`
body `{"title": …}`」（舊路徑走 `PATCH /v1/sessions/{session_…}` 加 `anthropic-beta: ccr-byoc-2025-07-29`）。
**送出的唯一條件是當下 bridge 活著**；bridge 沒連就整段跳過、零請求。

**寫檔到不了雲端**：同步發生在 `/rename` 的函式呼叫鏈裡，沒有任何「監看 jsonl → 推雲端」的機制。
所以 Stop hook 自己補上這一段 —— 寫完本機檔之後直接呼叫同一組 v2 介面
（2026-08-26 實測 `PUT` 回 200、讀回驗證相符）。四道安全設計：

- **只在名字真的變了才發**（開場與轉向），同名重寫是本機 64KB 窗口的問題，雲端沒這回事
- **`CLAUDE_SESSION_TITLE_NO_CLOUD=1` 一個環境變數就能關**，出事不必改程式
- **失敗寫 `state/session_title.log`**，hook 照樣 exit 0 —— 這是未公開 API，
  版本一改就靜默失效，不留痕等於永遠不會發現
- **token 過期就跳過，絕不 refresh、絕不寫 `.credentials.json`**（寫壞會讓整個 CLI 登出）

## 命名格式：三種分類（2026-08-26 user 定）

**只有明確的執行計畫才算任務目標**，其餘另外命名，讓側欄一眼看得出哪些是真的在做事：

| 分類 | 格式 | 判定條件（全部來自自我宣告的欄位） |
|---|---|---|
| 【任務】 | `【任務】名稱｜階段｜進度%` | 其餘情形 |
| 【討論】 | `【討論】主題` | 模式 ASK／VERIFY **且** 修改檔案為「無」或「待定」 |
| 【收尾】 | `【收尾】**原任務名**｜收尾` | 任務名含 收工／封存／交接／收尾／handoff |

**判定順序不能換**：收尾那一輪通常也是 DEV 模式、也會改檔（補規範、commit），
先判模式會把它歸成【任務】。同理「模式 VERIFY 但列了要動的檔」＝其實在做事，算【任務】——
所以討論的判定要模式與檔案**兩個都成立**。

**收尾要沿用「原本那件事」的名字，不是宣告裡的「收工封存」**（2026-08-26 user 指正）：
`【收尾】收工封存｜收尾` 只說了「有件事收尾了」，**看不出是哪一件** —— 側欄上一排收尾長得一模一樣。
實作是從記憶檔（`state/title.<session>.txt`）存的上一個標題抽出任務名；抽不到才退回宣告的字面。

**進度來自宣告的 `進度 60%` 欄**，不從階段推算：階段不等於完成度，
一個 Execute 可能佔整件事 20% 也可能 80%。沒寫就不顯示（不補空欄位）。

**分類標記固定在最前面**：本機側欄會截尾巴，最該一眼看到的東西必須放前面。
長度上限放寬到 48 字（原本 12 字是為了側欄不截斷，改用完整格式後那個限制沒有意義了）——
任務名本身仍維持 ≤12 字。

## 為什麼 hook 要掛三個事件（2026-08-26 實測，每一個都是踩出來的）

**CLI 是本機 transcript 的主人**：每個新 prompt 進來時，它會把**記憶體裡**的 title
寫回 transcript（那筆夾在 `last-prompt` / `agent-name` / `bridge-session` 之間）。
CLI 記憶體裡的值只有 `/rename` 改得動 —— hook 改檔案它完全不知道。
更麻煩的是**那個回寫排在 `UserPromptSubmit` 之後**（log 實證：該事件讀到的還是
hook 自己寫的新名），所以那個時機補不到。

| 事件 | 做什麼 | 為什麼非它不可 |
|---|---|---|
| `Stop` | 寫本機 ＋ 推雲端 ＋ 記住這次的決定 | 名字剛定下來就同步；雲端 CLI 不會覆蓋 |
| `UserPromptSubmit` | 寫本機 | 上一輪的宣告此時已 flush，是最可靠的解析時機 |
| `PreToolUse` | 被 CLI 蓋掉就補回去 | CLI 的回寫排在 UserPromptSubmit 之後，**只有這裡補得到** |

`PreToolUse` 每次工具呼叫都跑，所以那條路徑**刻意不解析宣告**（那要讀 2MB
transcript 逐行 parse）：只比對 `state/title.<session>.txt` 記的「上次決定」與檔尾，
不同才補。沒有記錄就不猜。

實測到齊後的一輪長這樣：

    decided=會話自動改名                     ← Stop
    decided=None (同名不重寫)                ← UserPromptSubmit，冪等
    restored=會話自動改名 (was 雲端會話同步)  ← PreToolUse 把 CLI 蓋掉的補回來

⚠ 曾經把 CLI 的回寫誤判成「同一次 Stop 重入呼叫兩次」，還為此加了競態守門。
**決策 log（`state/session_title.log`）一開就看出來 hook 只跑了一次** ——
沒有 log 之前只能從檔案 offset 反推，於是推錯方向兩次。

⚠ `PreToolUse` 掛載會被 auto mode 分類器擋下（它看得到所有工具呼叫、能擋下它們）。
那一擋是對的，所以註冊改由人執行：`tools/register_session_title_hook.py`。

⚠ 這件事有一個性質要記住：**hook 不受 auto mode 分類器管**。同樣一段「讀 token 對外發請求」
的程式碼，在對話裡執行會被擋下，放進 hook 就照跑。這是刻意接受的取捨，不是沒看到。

反向是通的：在網頁或手機改名會經 bridge 鏡像回本機（`[bridge:title] remote rename for …`）。
VSCode extension 那側則完全沒有雲端改名程式碼（`bridgeSessionId` 在 extension.js 裡 0 命中），
所以側邊欄的 `⋮ → Rename` **永遠只改本機**，這是設計如此。

