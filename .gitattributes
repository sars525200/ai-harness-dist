# 行尾策略 —— 建 repo 時即定（預防式，不等出事再補救）。
#
# 教訓來源：IT-department repo 未先定策略，被並行寫入把 CRLF 翻成 LF、
# 造成巨量假 diff，2026-07-28 補救花了一次 87,128 行的 renormalize commit。
#
# 本 repo 選 LF：hook 程式碼未來可能搬到 VM（Linux）執行（見 D4），
# 且不依賴 core.autocrlf——那是 per-machine 設定，換機器就失效。

*.py    text eol=lf
*.md    text eol=lf
*.json  text eol=lf
*.txt   text eol=lf
# 2026-08-06 補：`.html` 原本漏了，於是看板落回 per-machine 的 core.autocrlf=true，
# **每次 git checkout 都被翻成 CRLF**，而 test_dashboard_structure 明文要求純 LF。
# 「不依賴 core.autocrlf」這條策略上面寫得很清楚，只是清單漏了一種副檔名 ——
# 漏的當下沒有徵兆：檔案照樣打得開，只有還原過再跑測試才會撞到。
*.html  text eol=lf
