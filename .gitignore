# session 執行期狀態：動作足跡、hook 錯誤 log、spike 捕捉的 payload
# （含其他 session 的內容，絕不進版控）
state/

__pycache__/
*.pyc

# 探針的「hook 有沒有跑過」分母證據：每次跑都重建，不是紀錄
tests/*/fired.log
# 探針執行時讓模型建出來的樣本檔
tests/*/sample.*

# 看板重生的執行期狀態：來源快照每次重生都變（進版控只會製造 commit 雜訊），
# 鎖檔更是純執行期。兩者都不跨機器有意義。
dashboard/sources_state.json
dashboard/.refresh.lock
