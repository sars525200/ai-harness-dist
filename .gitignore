# session 執行期狀態：動作足跡、hook 錯誤 log、spike 捕捉的 payload
# （含其他 session 的內容，絕不進版控）
state/

# 「正在改什麼」宣告檔（Claude ↔ Cursor 協作通道）。同 state/ 的理由：
# 它是**這一刻誰在碰哪些檔**的執行期狀態，跨機器、跨時間都沒有意義，
# 而且兩邊會同時寫 —— 進版控就變成新的同檔污染源，正好是它要防的那件事。
COLLAB_NOW.md

# 換則交接等 session 暫存（skills/chat-handoff 寫 .scratch/handoff/，不進版控）
.scratch/

# harness 層設定（2026-08-13·CONTEXT_HEALTH_PLAN C-11／覆核 F-8）：
# 內容是**這台機器的專案路徑**，跨機器沒有意義；進版控等於把 `D:\IT-department`
# 當成別的部門的預設值出貨 —— 而 `discover_projects()` 對 currentProject 是無條件插入，
# 那個錯會偽裝成「報告多一列、CLAUDE.md 印無」而不是報錯。
# 新機器跑 `py -3 dashboard/gen_layers.py --init` 產範本。
harness.config.json
# 備份與自癒救援檔（`swapped_config()` 會產 `.<tag>-rescued`；
#   測試被硬殺會留 `.<tag>`）——性質同上，都是本機暫存。
harness.config.json.*

__pycache__/
*.pyc

# V-14 變異測試的暫時備份（`test_context_health_skill.py` 會把**版控中**的腳本
# 改名成 `.v14bak` 來驗「拿掉零件會不會被抓到」，跑完立刻還原）。
# 中途被殺（Ctrl+C／timeout）就會留下來——不 ignore 的話它會出現在 `git status`，
# 而它長得像一個該被 commit 的新檔案。測試開跑時會先收拾殘留並印警告。
*.v14bak

# 探針的「hook 有沒有跑過」分母證據：每次跑都重建，不是紀錄
tests/*/fired.log
# 探針執行時讓模型建出來的樣本檔
tests/*/sample.*

# 看板重生的執行期狀態：來源快照每次重生都變（進版控只會製造 commit 雜訊），
# 鎖檔更是純執行期。兩者都不跨機器有意義。
dashboard/sources_state.json
# 金額快取：可由 --with-cost 重新產生，且每台機器的 transcript 不同 → 跨機無意義
dashboard/cost_state.json
dashboard/.refresh.lock

# 填滿的看板是產物（8099／refresh 從殼複製再填）。進 git 的是
# dashboard/harness-dashboard.shell.html。
dashboard/harness-dashboard.html

# user 提供的參考圖（Model routing 等）：計畫書已把內容轉成文字，
# 圖檔本身 8MB 且非程式資產，不進版控（要看原圖去本機這個目錄）
參考/

# /clear 時自動封存的 transcript 副本（hooks/session_archive.py）。
# 同 state/ 的理由：內容是**其他 session 的完整對話**，動輒數 MB 到數十 MB，
# 跨機器沒有意義，而且正是為了躲平台的 cleanupPeriodDays 才複製出來的。
session-archive/

# backup_global_config.py 覆寫 repo 側副本前留的還原點（<檔>.bak.<時間戳>）。
# repo 這一側本來就有 git 歷史，備份是給「還沒 commit 就被蓋掉」那個空窗用的，
# 不該再進版控。live 側（~/.claude）的同名備份不在這個 repo 裡，不受這條影響。
global/*.bak.*
global/output-styles/*.bak.*

# setup_new_pc_gui.py 打包成 exe 的產物與暫存輸入（tools/setup_new_pc_gui.py 交接檔
# .scratch/handoff/20260907-new-pc-installer.md）。build/、dist/ 是 PyInstaller 產物，
# .spec 是它重跑時自動產生的規格檔；tools/settings.json 是打包前從個人機器
# ~/.claude/settings.json 複製過來給 --add-data 用的暫存輸入，帶 116 條權限規則與
# 12 個工作目錄 = 公司專案結構，絕不進版控，打包完就該刪掉。
tools/setup_new_pc_gui.spec
tools/settings.json
build/
dist/
*.exe
