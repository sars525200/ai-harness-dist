# 常駐層健檢計畫書（CONTEXT_HEALTH）

> 狀態：待審核
>
> 主題：**always-loaded 檔案的跨專案健檢機制**——每次收工自動量、產出該專案專屬的
> 瘦身建議、並且**下一次收工比對上次壓縮的效果**，讓它自己疊代。
>
> **與既有計畫書的分工**（新建的理由）：`IT-DEPARTMENT_CLAUDEMD_PLAN.md` 管「IT-department
> 那一份檔的內容該放哪一層」，是**一次性的內容決策記錄**（多數項目已 ✅ 執行完）；
> `UNIVERSAL_HARNESS_PLAN.md` §3 是**準則表**、§4 是**分發策略**，不收實作規格。
> 本檔管的是「一個會反覆跑的機制」，兩者都沒有它的位置。本檔是 `UNIVERSAL_HARNESS_PLAN`
> **D-3（先拆路徑）的第一個實例**。

---

## 1. 現況（2026-08-13 實測，先量再談）

### 1.1 always-loaded 的實際重量（v2 修正：實體位置與 CRLF）

**先釐清「什麼才是 always-loaded」**——判準不是「重要」，是**它出不出現在 system prompt
的 `claudeMd` 區塊裡**。實測本 session 的該區塊有三份：全域 `CLAUDE.md`、專案
`CLAUDE.md`、`…\projects\d--<專案>\memory\MEMORY.md`（Claude Code 的 auto-memory）。
**`.claude\PROJECT_CONTEXT.md` 不在裡面**——它是角色開工時才 `Read` 的（五支角色檔都這樣寫）。

| 檔案 | 磁碟大小 | 每則都付？ | 實體位置（v2 修正） |
|---|---|---|---|
| 全域 `~\.claude\CLAUDE.md` | 9.8 KB | ✔ 每個專案 | — |
| `D:\IT-department\CLAUDE.md` | 21.2 KB（純 LF） | ✔ | — |
| **`D:\AI-Projects\CLAUDE.md`** | **44,719 B**（CRLF·314 個 CR） | ✔ | — |
| MEMORY.md（IT-department） | 25,481 B | ✔ | **`D:\IT-department\.aimemory\`**；junction 是 `~\.claude\projects\d--IT-department\memory` → 專案，**方向由外指向內** |
| MEMORY.md（AI-Projects） | **5,496 B** | ✔ | **`C:\Users\…\.claude\projects\d--AI-Projects\memory\`** —— **實體不在專案磁碟區**，AI-Projects 底下連 `.aimemory` 都沒有 |
| `.claude\PROJECT_CONTEXT.md` | 6,784 B | ✘ **開工讀一次** | 只有 IT-department 有 |

單一 session 實際載入：IT-department ≈ **56.5 KB**／AI-Projects ≈ **59.0 KB**（v1 少算了
AI-Projects 的 MEMORY.md 5.4 KB，且用 text mode 少算 314 B 的 CR）。

⚠ **兩份 MEMORY.md 的實體位置規則不同**：一份靠 junction 落在專案 repo（要進版控），
一份直接住在使用者設定檔底下。**任何以「專案根目錄」為基準的 glob 都會漏掉 AI-Projects
那一份**，而漏掉的症狀是「報告少一列」，跟「那個檔很乾淨」長得一模一樣。
正確做法是從 `~\.claude\projects\d--<mangled>\memory\` 這個 Claude Code 慣例推導
（`capability_checks._p_on_demand` 已經是這樣寫的，不必發明新的）。

### 1.2 AI-Projects 爆到 43.7 KB 的組成

| 節 | 字數 | 佔比 | 判斷 |
|---|---|---|---|
| §4 關鍵規範速查 | **12,051** | 55% | 規範沒分流到 topic 檔 |
| §6 現況與待辦 | **6,355** | 29% | **待辦不該在 always-loaded 層**（看板有 `gen_todos.py`） |
| §3 環境與遠端存取 | 2,453 | 11% | 多數可搬 topic 檔 |

**這是 IT-department 兩個月前做過的同一次瘦身**（28.5 KB → 21.6 KB，靠把規則搬進參考型
skill）。同一個病，只是沒有機制幫別的專案做。

### 1.3 現有機制為什麼沒擋住

`D:\.ai-harness\rulefile\check_bloat.py` 已經存在且設計得不錯（比對快照、只報「這次新增的
膨脹」、拒絕自動壓縮），但有三個結構性限制：

| # | 限制 | 證據 |
|---|---|---|
| B-1 | **寫死單一專案路徑** | `CLAUDE_MD = Path(r"D:\IT-department\CLAUDE.md")`（第 41 行）——違反 `UNIVERSAL_HARNESS_PLAN` U-1 |
| B-2 | **寫死單一專案的章節結構** | `_SECTION_START = "## 8. 關鍵硬規則速查"`；AI-Projects 的 §4 才是對應節，套不上 |
| B-3 | **只有單一快照，沒有歷史** | `bloat_snapshot.json` 是覆蓋式的 → **答不出「上次壓的有沒有效、有沒有反彈」** |

B-1／B-2 讓 AI-Projects 從來不在雷達上；B-3 讓「疊代」這件事在資料上不可能。

---

## 2. 目標

1. **每次收工自動檢測**，涵蓋**所有**有 CLAUDE.md 的專案，不是只有 IT-department。
2. 產出**該專案專屬**的健檢結果（依它自己的章節結構與技能組合，不套別人的模板）。
3. **下次收工比對上次**：壓下去了多少、有沒有反彈、哪幾條是新長出來的。

**不做的**（沿用 `check_bloat.py` 已經定調的判斷，不推翻）：
**壓縮不自動化。** 刪錯的後果不是規則變短，是**規則失去觸發力**——模型認不出情境就
永遠不會去開那份 topic 檔，等於規則消失。任何機械規則（刪括號、刪檔名、刪範例）都會
系統性砍掉資訊密度最高的部分，因為那正好也是最長的部分。工具只出**候選＋去處建議**。

---

## 3. 待決分岔（逐項討論，同意才做）

**2026-08-13 四項全部拍板（user 逐項選推薦）。**

| # | 分岔 | 選項 | 傾向（附理由） | 決定 |
|---|---|---|---|---|
| C-1 | 「專案專用 CLAUDE.md」是什麼 | (a) 出健檢報告＋骨架建議，人決定 (b) 自動生成／改寫 CLAUDE.md (c) 只出「這專案缺什麼」清單 | **(a)＋(c)**：§2 那條「壓縮不自動化」是踩過的判斷，不因為要全域化就推翻 | ✅ **(a)＋(c)** |
| C-2 | 檢測的強制力 | (a) 只在收工報告 (b) 收工自動跑＋超標落便箋 (c) 超標擋收工 | **(b)**：user 要「每次收工自動」；(c) 是流程閘門，`UNIVERSAL_HARNESS_PLAN` §4 列為高風險，且會重演 D5 的 WARN 疲勞 | ✅ **(b)** |
| C-3 | 納管哪些檔 | (a) 只 CLAUDE.md (b) ＋MEMORY.md (c) ＋PROJECT_CONTEXT.md 等全部 always-loaded | **(c)**：token 是一起付的，只看 CLAUDE.md 會讓膨脹搬家到 MEMORY.md 而數字變好看 | ✅ **(c)** |
| C-4 | AI-Projects 現在瘦不瘦 | (a) 機制做完跑一次，依報告當場瘦 (b) 只做機制，瘦身另案 | **(a)**：不然 user 的痛點沒解決，而且它是驗證機制真的有用的第一個實例 | ✅ **(a)** |

**Round 2 之後長出來的三項（2026-08-13 同日拍板）**——覆核揭露的分岔一律補進本表，
不留在對話裡（`/design-spec` 步驟 3）：

| # | 分岔 | 選項 | 傾向（附理由） | 決定 |
|---|---|---|---|---|
| C-5 | 覆核抓出 33 個發現、效益僅約 $24/月，範圍要不要縮 | (a) 縮成唯讀量測＋報告 (b) 照 v2 全做逐項修 15 項 (c) 只做去專案化 | **(a)**：四個致命項有三個來自「從唯讀變成寫入者」這一個決定；砍掉它三個當場消失，而 user 要的三件事全部保留 | ✅ **(a)** |
| C-6 | AI-Projects §6 實測看板一條都接不住 | (a) 先修表格形狀再搬 (b) 只壓 §4、§6 不動 (c) 整節搬 topic 檔 | **(a)**：直接刪＝6,355 字蒸發無人接住；(c) 則待辦不會出現在看板上 | ✅ **(a)** |
| C-7 | `gen_layers.py` 寫死三個專案路徑，本輪修不修 | (a) 不修但登記成債 (b) 本輪一併修 | **(a)**：看板六支產生器都依賴它，且 D-2（設定檔格式）未拍板；但**債要寫在正文不是附註**，否則 V-1 的綠燈會被讀成 U-1 達成 | ✅ **(a)** |

**v4 三項（2026-08-13·計畫書覆閱時我自己指出的缺口）**：

| # | 分岔 | 選項 | 傾向（附理由） | 決定 |
|---|---|---|---|---|
| C-8 | P-4（專案專屬建議）**沒有任何驗證項**，而它是 user 三個需求之一 | (a) 補 V-9，用 git 舊版當 fixture (b) 明寫「驗不了」靠人看 | **(a)**：IT-department 已經有多條規則搬進 `/asset-data-rules` 等 skill，**取搬遷前的舊版當 fixture 就有 ground truth**——答案已知，驗得出對錯 | ✅ **(a)** |
| C-9 | MEMORY.md 的條目層監控被 P-3b 排除，而它 25 KB | (a) 另訂索引型判準 (b) 沿用 120 字 | ⚠ **user 選 (a)，但查證後改成 (b)** —— 見下方「事實推翻傾向」 | ✅ **(b)·事實修正** |
| C-10 | 這輪瘦身對象要不要加 IT-department 的 MEMORY.md | (a) 先只做 AI-Projects (b) 兩個一起做 | 我原本傾向 (a)（先用一個專案證明機制有用）；**user 選 (b)** | ✅ **(b)** |

> **C-9 的事實推翻**：`MEMORY.md` 的檔頭自己就寫著「**新增 entry 一行 ≤~120 字**；
> 補規範分流見 CLAUDE.md §4」——**跟 CLAUDE.md §8 是同一個數字**。所以 Round 2 審查者
> 說「那些索引行本來就設計成一行帶完整摘要，120 字對它們沒有意義」是**錯的**，
> P-3b 據此把 MEMORY.md 排除也是錯的。實測超標情況：
> **IT-dept 87 條中 34 條超標（39%，最長 245 字）／AI-Projects 14 條中 10 條超標
> （71%，最長 620 字＝規則的 5 倍）**。那些是真違規，不是假警報。
> **不必另訂判準，沿用既有的 120 字就對了**——這也符合「規則已經存在就不要發明新的」。

**v5 三項（2026-08-13 晚·user 要求「做成工具＋包成 skill＋通用專案」後新增）**：

| # | 分岔 | 選項 | 傾向（附理由） | 決定 |
|---|---|---|---|---|
| C-11 | `gen_layers` 去專案化要用什麼設定形狀（D-2 的實例化） | (a) **兩層**：harness 層 `harness.config.json` 只放「有哪些專案」，專案層沿用既有 `.claude/PROJECT_CONTEXT.md` (b) 單層：一個設定檔放全部專案的全部資訊 (c) 不用設定檔，`SCAN_ROOT` 改由 CLI／環境變數帶 | **(a)**：`UNIVERSAL_HARNESS_PLAN` D-2 的 (a) 寫「放**專案根**」，但 `discover_projects()` 要回答的是「**有哪些專案**」——**那份清單不可能放在還沒被發現的專案裡**（雞生蛋）。所以 D-2 的 (a) 必須拆成兩層才成立。專案層不新發明：`PROJECT_CONTEXT.md` 已是「新部門導入唯一必填檔」 | ✅ **(a) 兩層** |
| C-12 | 跨檔重複偵測的判準（多相似算重複） | (a) 正規化後**完全相同的句子**（≥N 個非空白字元） (b) n-gram／shingle 相似度 ≥ 閾值 (c) **兩者都跑、分兩欄報**（逐字相同＝「確定」，相似＝「疑似」） | **(a)**：這支工具的輸出會被拿去**刪常駐層的字**，誤報的代價是規則消失。(b) 的閾值沒有非任意的訂法，而且「相似但不同」正是最危險的一類（同一條規則的兩個版本，刪掉新的留下舊的）。(a) 可證偽、往零誤報的方向偏 | ✅ **(c) 兩欄都報**·user 決定 |

> ⚠ **本列的選項欄 2026-08-13 晚訂正過（覆核 F-13）**：原本 (c) 寫成「只比對檔名＋關鍵詞共現」，
> **與實際問 user 的第三個選項不是同一件事**（實際問的就是「兩者都跑、分兩欄報」）。
> 決定欄的文字一直是對的，錯的是選項欄。**照原本的字母執行會做成最容易誤報索引句的那一種。**
> 這類錯位的危險在於：決定欄與選項欄各自看都合理，只有並排讀才發現對不上。
>
> ⚠ **且 C-12 目前是孤兒決定**（覆核 F-13 後半）：P-8 作廢後它唯一的實作是
> `find_duplicates.py`，而本計畫書同時規定「不得拿它的輸出當搬移依據」。
> **已拍板、無合法消費者、無回歸網。** 保留紀錄供回溯，但不得據此開工。
| C-13 | skill 的邊界：它可以自己改檔嗎 | (a) 只編排「量測→出候選→**人確認**→逐項搬→驗證」，每次搬移要 user 點頭 (b) 重複偵測 100% 確定的那批可自動搬 | **(a)**：C-1 定的是「**工具**不自動改」，而 skill 是模型在執行，繞過那條的方式就是「反正不是工具在改」。今天實測也支持 (a)——6 段散文全部命中重複偵測，**但其中一段（`SOP` repo 零副本）另有一句是獨有的**，機械判定會連它一起刪 | ⚠ **(b)→(a)·2026-08-13 晚改判** |

> **C-13 的裁決歷程（兩次·完整留著供回溯）**
> 1. 我原本傾向 **(a)**，理由是「C-1 禁的是**工具**不自動改，而 skill 是模型在執行，
>    繞過那條的方式就是『反正不是工具在改』」。
> 2. user 拍 **(b)**（100% 確定可自動搬）。我加了「句子級粒度」當安全支點後照做。
> 3. **覆核 F-6 查證後 user 改判 (a)**。改判的理由不是原則而是**實測**：
>    IT-dept CLAUDE.md 的 8 個結構塊裡**有 5 塊就是 §8 的規則索引句**
>    （`**設備／進出庫資料一致性（32 條）**：…先讀 /asset-data-rules` 這種形狀）。
>    **結構判定確實 100% 機械可判，但它判出來的東西恰好是最不該自動搬的。**
>    這與 🔴 P-8 框的結論② 是同一個坑——**換了一個偵測器，坑還在原地。**
>
> **定案：skill 一律「出候選 → 人確認 → 才動檔」。** `SKILL.md` 步驟 3 寫的就是這個
> （出貨時實際實作的也是這個，只是當時沒記錄這個回退——F-6 抓到的正是那個落差）。
> ⚠ **C-12 的「兩欄都報」連帶失去最後一個用途**：那兩欄的分界本來就是為了
> 「哪一欄可以自動搬」而存在的。兩個孤兒決定都保留紀錄，但**不得據此開工**。

> **⚠ C-9 的前提在同一天稍晚失效（2026-08-13 晚·第三次「實際去量推翻推理」）**：
> C-9 寫「IT-dept MEMORY.md **87 條中 34 條超標**」並據此立了 P-7b。**現在實測是 88 條中 0 條超標。**
> 不是資料變了——是 `check_bloat.py` 同日改了判準：**索引列的長度只算 hook 句、扣掉 markdown link 前綴**
> （`parse_entries()` L264-278 的註解寫得很清楚：`- [feedback-windows-deploy-script-traps](feedback-windows-deploy-script-traps.md)`
> 光前綴就 76 字＝上限的 63%，於是「檔名長的條目」不論 hook 多精簡都必定超標。
> **量錯東西的判準會把人逼去改不該改的地方**——去縮檔名？那會斷連結）。
> **所以 P-7b 的 IT-dept 那一半，工作量大部分不存在**。真正該處理的是 §7 記的那類**散文塊**
> （非索引列、不受 `- [` 判準管、check_bloat 完全沒在看）——實測 6 段共 5,978 字，已於同日處理掉 4,721 字。
> **這是本檔第三次因為「去讀那個檔／去量那個數」而推翻推理結論**（前兩次見 §7 v4 末段）。

---

## 4. 做法

改的是 `rulefile/check_bloat.py`（**疊代既有，不新建一支**——它的快照比對與「拒絕自動
壓縮」兩個設計是對的，壞的只有寫死那三處）。

> **執行順序（v3 新增·R2-13）**：`P-0 → P-1 → P-5 → P-2 → P-3/P-3b → P-4 → P-6 →
> 【跑一次留基準】→ P-7`。**這不是建議是硬相依**，三對顛倒就出事：
> ①P-0 沒先補錨 → 首次跑條目層全滅（見 P-0）②P-5 的三元組 key 沒先換就擴大納管 →
> 四個檔的條目擠進同一個扁平 dict，碰撞按檔案數放大 ③**最陰險的是最後一對**：
> 沒先跑一次留基準就壓 AI-Projects → 歷史第一筆就是**壓縮後**的值，
> 「44 KB → ?」這個改善在序列上不存在 —— **C-4 選 (a) 的理由是「它是驗證機制真的
> 有用的第一個實例」，順序顛倒的話這個旗艦實例永遠證明不了目標 3，而且沒有紅燈。**

- **P-0 先補規則節錨（v3 新增·R2-3·必須最先做）**：實測三份 CLAUDE.md
  `grep -c "rules-section"` **全部是 0**。P-3b 若照「找不到錨就不做條目層」執行，
  首次跑會讓 120 字判準對**所有檔**失效 → 永遠 exit 0。而現行版本實跑會 exit 1
  （抓到一條 230 字的新增超標）—— **這不是「新功能還沒開」，是既有能力倒退**，
  而 `_p_anti_bloat()` 只看檔案存在，看板仍顯示 ✔（F-1 的反向版本：能力真的沒了、probe 是綠的）。
  **定案**：①動 P-1 之前先在三份 CLAUDE.md 的規則節標題後加錨
  ②`check_bloat` 找不到任何錨時 **exit 2**（形同快照壞掉），不是靜默當首次跑。

- **P-1 去專案化（U-1）**：刪掉 `CLAUDE_MD` 那個模組層常數。掃描對象一律來自
  `gen_layers.survey_projects()` —— **看板已經在用的單一真相**，自己再數一份會漂
  （`gen_workflow_compliance` 為同一件事付過代價）。核心層
  `grep -c "IT-department\|AI-Projects"` 必須為 0。
  ⚠ ~~**刻意不新建 `harness.config.json`**：那是 `UNIVERSAL_HARNESS_PLAN` D-2 的待決項，
  **未拍板不動工**。`survey_projects()` 已經夠用，不必為此提前發明一個設定檔格式。~~
  **↑ 這段已被 P-12 推翻（2026-08-13），保留原文供回溯。** 覆核 F-7 指出：
  P-1 說「刻意不建」、P-12 說「建好了」，**兩段一度並存在同一份計畫書裡**。
  現況＝設定檔已建、D-2 已在 `UNIVERSAL_HARNESS_PLAN` §4 補上決定欄（(a)·拆兩層）。
  ⚠ **拍板順序是反的**（實作早於決定紀錄），那筆帳記在該檔 D-2 定案段與 §6。
- **P-2 納管範圍（C-3·v2 大改）**：報告分**兩欄**，合計只算前者：
  - **「每則都付」**＝全域 `CLAUDE.md` ＋ 專案 `CLAUDE.md` ＋ `MEMORY.md`
    （判準是它出現在 system prompt 的 `claudeMd` 區塊，見 §1.1）
  - **「開工讀一次」**＝`.claude\PROJECT_CONTEXT.md`（**只列不建議瘦身**——砍它的內容
    等於砍掉五支角色的作用對象設定，而那個失效沒有任何人會收到通知）
  - MEMORY.md 的位置一律從 `~\.claude\projects\d--<mangled>\memory\` 推導，
    **不以專案根為基準**（§1.1：AI-Projects 那份根本不在專案磁碟區）。
  - `bytes` 一律 `os.path.getsize()`／`read_bytes()`。**text mode 會把 CRLF 收斂成 LF**，
    對 `*.md text eol=crlf` 的專案系統性少算（AI-Projects 實測少 314 B）——
    而這支工具的賣點正是跨專案比較與時序，系統性偏差會直接餵進「反彈」判定。
- **P-3 章節結構不寫死（B-2·v2 補退路）**：解析**所有** `^##+\s` 標題、算每節非空白
  字元數、排序。**但 `##` 數為 0 或 1 時要退回條目層排名並明講「本檔無章節結構」**
  ——實測 AI-Projects 的 MEMORY.md **一個 `##` 都沒有**（純 bullet 索引），
  IT-department 的只有 2 個且其中一節幾乎是整個檔。不設退路的話，
  納管範圍裡最大的那批檔會產出空表，而空表跟「很乾淨」長得一樣。
- **P-3b 條目層的掃描範圍要明確宣告，不由「最大節」推導（v2 新增·審查者 C-2）**：
  現行 `parse_entries()` 第一件事是 `if _SECTION_START not in md_text: return []`，
  拿掉章節錨點後範圍是未定義的。兩種直覺補法各有一個靜默失效，實測過：
  - **全檔掃** → 首次跑噴 93 條「新增超標」（AI-Projects 全檔 100 條/42 超標、
    MEMORY.md 87 條/34 超標），其中絕大多數是 MEMORY.md 的索引 hook 行——
    那些**本來就設計成一行帶完整摘要**，120 字上限對它們沒有意義。
    第一次跑就重現 D5 的 WARN 疲勞。
  - **只掃最大節** → AI-Projects 現在 §4＝12,073 字、§6＝6,364。P-7 一旦把 §4 壓到
    6,364 以下，**最大節翻轉成 §6**，剛壓過的規則節從此退出監控——正好是目標 3 要防的事。
  - **定案**：規則節由檔案自己宣告（該節標題行後放 `<!-- rules-section -->` 錨），
    找不到錨就只做章節層不做條目層並明講。
  - ⚠ **v4 修正：MEMORY.md 不排除，照樣套 120 字**（C-9）。v2／v3 沿用審查者的判斷
    「索引行本來就設計成一行帶完整摘要，120 字對它們沒有意義」——**那是錯的**：
    `MEMORY.md` 檔頭自己寫著「新增 entry 一行 ≤~120 字」，**與 CLAUDE.md §8 同一個數字**。
    實測 IT-dept 87 條中 34 條超標（最長 245 字）、AI-Projects 14 條中 10 條超標
    （**最長 620 字＝自己規則的 5 倍**）。那些是真違規。
    **規則已經存在就不要發明新的**——索引檔的錨改成認 `- [` 開頭的索引列即可。
- **P-4 專案專屬建議（C-1）**：報告四塊 ①各節大小排名 ②超標條目 ③**該搬去哪**
  ④**缺什麼檔**。③ 的判準是掃該專案的 `.claude/skills/*/SKILL.md` 與
  `.claude/rules/*.md`，若某節的關鍵詞已經有對應的 skill／rule，就建議
  「搬過去、原地只留一句帶症狀端觸發詞的索引」—— **IT-department 的 28.5→21.6 KB
  就是這樣做到的**，工具要認得這個既有模式而不是發明新的。
- **P-5 歷史序列（B-3·v2 大改）**：`rulefile/bloat_history.jsonl` append
  `{ts, project, file, bytes, visible, sections[], over_limit[]}`。
  - **快照 key 改成 `(project, file, 前24字)` 三元組**：現行 flat schema 實測
    369 條裡已有 **1 組跨檔碰撞**（開頭 24 字皆為「…見 PENDING_VERIFY.md」），
    dict 推導會**靜默後蓋前**，該條的成長從此永遠報不出來。寫快照時遇重複 key
    要報錯，不是覆蓋。
  - **`--write-snapshot` 必須帶 `--project <name>`，缺參數拒跑**：現行是全域覆蓋，
    多專案化後「接受 A 專案的現況」會順手把 B 專案未處理的成長寫成新基準。
  - **反彈改成與「歷史最低點」比，不與相鄰筆比**：相鄰筆比會漏掉
    `44KB→30KB(降)→30KB(沒動)→43KB` 這個最常見的形狀（判定時「上次」沒降，
    反彈條件不成立）。門檻用**可見字數**（不受 CRLF 影響）並明訂百分比。
  - append 以 `(date, project, file)` 去重——收工流程本來就可能重跑
    （exit 1 → 壓完再跑一次確認）。
  - **⚠ 保留 `bloat_snapshot.json` 這個檔名**：`capability_checks._p_anti_bloat()`
    綁死 `snapshot.exists()`，改成 per-project 檔名會讓看板的「防膨脹能力」靜默翻 False
    （那支函式的 docstring 自己就記過同型的字面值漂移，7/30 一天咬兩次）。
  - **schema 遷移要顯式（v3 新增·R2-5）**：現行快照是**扁平單專案**
    （`total_bytes`／`entry_count`／`over_limit_count`／`entries` 共 63 條）。
    它是**合法 JSON**，所以走不到 `load_snapshot()` 的 `JSONDecodeError → exit 2`；
    新版拿三元組 key 去查舊的扁平 dict 會全部 miss → **63 條連同既有 2 條超標
    全被報成「新增超標」**，而升級後第一次收工正是最不該噴假警報的那一次。
    **定案**：快照加 `"schema": 2`，讀到缺欄位或版本不符時 **exit 2 並印出遷移指令**。
    ⚠ 這個 bug 會被 P-0 沒做時的「條目層全滅」遮蔽——**兩個 bug 互相遮蔽，症狀是一片安靜**。
  - **全域 `CLAUDE.md` 走 `project = "__global__"` 單獨一條線（v3 新增·R2-10）**：
    它是所有專案**共用同一個實體**，用 `(project, file)` 當 key 會為它開 N 條線
    →「歷史最低點」各自算、合計被灌 N 次、`--write-snapshot` 要跑 N 次才接受得完，
    漏跑一個就永遠掛著一筆假的成長。在各專案報告裡只當「共同成本」列出，不進該專案合計。
- **P-6 掛載點（C-2·v3 定案＝唯讀）**：v1 掛步驟 3.5＋pending_warn 便箋，**兩處都錯**；
  v2 改寫 `PENDING_VERIFY.md`，**又引入四個新問題**（R2-6/7/8/15）。v3 收斂成：
  - **掛步驟 3**（防膨脹量測，無條件執行）。步驟 3.5 的標題是
    「**只有動過 `D:\.ai-harness` 才跑**」——而「今天只改了專案的 CLAUDE.md、沒碰
    harness」正是它膨脹最典型的那一天，掛 3.5 等於在最該跑的日子不跑。
  - **🔒 這支工具唯一會寫的檔是它自己的兩個**（`bloat_snapshot.*.json`／
    `bloat_history.jsonl`）。**不寫 `PENDING_VERIFY.md`、不寫任何別人的檔。**
    這條是 v3 的核心邊界，一次解掉四個發現：便箋投不到（A-1）、寫哪張表會被
    `_NOT_TODO_SECTION` 靜默丟掉（R2-6）、結果落誰家（R2-7）、每次 append 變成
    看板疲勞（R2-8）；並讓 R2-15 的並發風險只剩自己的檔
    （`gen_todos.line_sha` 的 docstring 逐字寫「多 session 並行是這個環境的常態」，
    而看板的「完成」鈕正靠行號＋`line_sha` 寫回 `PENDING_VERIFY.md`——
    兩個寫入者搶同一個檔，換來的只是一則本來就會印在收工報告裡的訊息）。
  - **訊息怎麼到人眼前＝收工報告印出來**，由 shougong 步驟 3 的完成判準要求
    **貼出當次數字**（現行判準已經要求貼 exit code 與數字，沿用不新增機制）。
  - **exit code 的閘門語意收窄**：SKILL.md 現行完成判準「exit 1 時已壓回或已取得
    user 同意並更新快照」實質就是 C-2 否決掉的 (c) 擋收工，多專案化後會從後門進來
    （在 IT-department 收工卻被別的專案的膨脹卡住）。**定案**：exit 1 只對
    **cwd 所屬專案**成立；其他專案一律走報告不走 exit code。
  - **`bloat_history.jsonl` 要有保留上限**：`_entry_probe` 那筆待驗項的原文是
    「會無上限長（463 筆／20KB，每 session +11KB），要決定移除或加上限」——
    **複製形狀就要複製教訓**。上限＝每個 `(project, file)` 保留最近 60 筆。
- **P-6b 觸發點的已知邊界（v2 新增·審查者 A-3）**：`/shougong` 與 dispatch hook
  **都只存在於 IT-department**（全域 `~\.claude\skills\` 只有 `visual-check` 一支；
  `D:\AI-Projects\.claude\` 只有 `settings.local.json`、`hooks` 為 None、
  `survey_projects()` 回報 `dispatchWired: False`）。所以本機制的**觸發點**是
  「在 IT-department 收工」，其他專案是**被動被掃到的對象**。
  這是刻意接受的限制（健檢是主動去讀別人的檔，不需要對方裝東西），
  但**必須寫在畫面上**——否則會被讀成「每個專案收工都會自己檢查」。
- **P-7 用它瘦 AI-Projects（C-4·v2 加前置條件）**：
  ⚠ **§6「現況與待辦」6,355 字不可直接刪**。v1 的理由是「看板 `gen_todos.py` 已經在掃」，
  但 gen_todos 的來源表是從該專案的 `.claude\PROJECT_CONTEXT.md`「待辦來源」節讀的，
  docstring 明寫「**沒填就是沒有**」——而 **AI-Projects 根本沒有這個檔**，
  看板對它的待辦命中數是 **0**。照 v1 執行＝6,355 字直接蒸發，沒有任何地方接住。
  **前置條件**：先幫 AI-Projects 建 `.claude\PROJECT_CONTEXT.md` 填好待辦來源表、
  跑一次確認看板真的抓得到，**才動 §6**。
- **P-7b 兩份 MEMORY.md 一起瘦（v4 新增·C-10·user 決定）**：
  IT-dept **25,481 B／87 條／34 條超標**、AI-Projects **5,496 B／14 條／10 條超標**。
  ⚠ **瘦 MEMORY.md 的方法與瘦 CLAUDE.md 不同，不可套用同一招**：
  - CLAUDE.md 是「把細節搬進 topic 檔／skill，原地留索引句」；
  - MEMORY.md **本身就是索引**，沒有更下層可以搬。**刪掉一行＝那個 topic 檔失聯**
    （沒有任何地方會報錯，只是模型再也不知道有這份檔）。
  - 所以唯二的手段是 **①縮短 hook 句**（把 nuance 移進該 topic 檔內文）
    **②合併同源條目**（同一模組的多筆併成一行，檔名列一次——CLAUDE.md §8
    已經在用這招）。**禁止刪行**。
  - AI-Projects 那條 620 字的索引是最優先——它一行就佔該檔 11%。

### v5 新增（2026-08-13 晚·user 三個需求：做成工具／包成 skill／通用專案）

> **執行順序（同樣是硬相依，不是建議）**：`P-12 → P-8 → P-9 → P-10 → P-11 →【跑一次留基準】→ P-7/P-7b`。
> ①**P-12 必須最先**：它換掉專案清單的來源，之後所有新能力都吃那個來源；順序顛倒的話
> P-8～P-10 會先繞著寫死路徑長出三份新的耦合，等於把債從 1 處變成 4 處。
> ②**P-11 必須在 P-8～P-10 之後**：skill 是這三支能力的編排器，被編排的東西還不存在時
> 寫出來的步驟無法驗證，只能是散文。
> ③**P-7/P-7b 仍在最後**：R2-13 的理由原封不動——先跑一次留基準，否則歷史第一筆就是壓縮後的值。

- **P-8 跨檔重複偵測（C-12·外部工具有、我們沒有的唯一一項）**：
  對每個 always-loaded 檔的每個區塊，回答「**這段文字是不是別的檔已經有了**」。
  - **這不違反 §2「壓縮不自動化」**：那條禁的是「判斷這句話能不能不在 always-loaded 層」
    ——那是判斷題。而「這段字在不在別的檔裡」是**事實**，查得到、可證偽。
  - **實證（2026-08-13 當日）**：IT-dept MEMORY.md 尾段 6 段散文 5,978 字，逐段 grep
    harness 計畫書後發現**全部有更完整的版本**（`pythonw sys.stdout is None` → `DASHBOARD_IA_PLAN.md:316`／
    cp950 hook 輸出 → `HARNESS_ROLE_ARCH_PLAN.md:460-484`／`deny 綁工具名` → `HARNESS_PROGRESS.md:80`／
    「放 user 層永遠載不到」推翻 → `UNIVERSAL_HARNESS_PLAN.md:129`／hook stderr 綁架對話 →
    `STOP_HOOK_MARKER_PLAN.md:115`／R4 兩次 dead on arrival → `HARNESS_PROGRESS.md:144`）。
    **靠 6 次手動 grep 才敢刪**——這一步正是可自動化的那一步。
  - **輸出分兩欄**（C-12 決定＝兩者都跑）：
    **①「確定重複」**＝正規化後**逐字相同**且 ≥40 個非空白字元 → 這一欄可自動搬（C-13）。
    **②「疑似重複」**＝n-gram 相似度過門檻但非逐字相同 → **一律只報不動**，人判斷。
    兩欄分開印，②不得混進①的自動處理路徑。
  - 🔒 **句子級粒度是硬約束，不是實作偏好**（C-13 選 (b) 之後，這條變成安全性的唯一支點）：
    比對單位必須是**句子**（以 `。！？；\n` 切），**不得以段落或區塊為單位**。
    **理由是當日的實證反例**：「`SOP` repo 仍零 remote 零副本」那一段，多數句子在
    `reference-dev-prod-git-repo-split.md` 有副本（「無 remote（只本機）」），
    **但「零副本＝這顆磁碟壞掉、DEV 全部歷史一起沒」這個風險判斷整個平台只有這一句**。
    段落級比對會判整段重複 → 自動搬 → **那句獨有的風險判斷靜默消失**。
    句子級比對則會把它留在「無副本」那一類，自動搬只搬走有副本的句子。
    ⚠ **這條若被改掉，C-13 的 (b) 就不再安全**——回歸網要有一條專門守它（V-11 的反向案例）。
  - **自動搬的三個前置條件，缺一不搬**：①落在「確定重複」欄 ②來源檔**存在且可讀**
    （不是只在索引裡提過）③搬移後原位置留一句指路。三條都是可機械檢查的。
  - **搜尋範圍**：該專案的 `.aimemory/`／`.claude/rules/`／`.claude/skills/`／`docs/`＋
    harness 的 `*.md`。範圍不寫死，從 C-11 的兩層設定推導。

> ### 🔴 P-8 實測結果（2026-08-13 晚·**假設被推翻，原案作廢**）
>
> 工具寫完了（`rulefile/find_duplicates.py`），跑起來了，**結論是它抓不到要抓的東西**。
>
> | 量測 | 數字 |
> |---|---|
> | 來源檔／索引句 | 299 檔／25,023 句（工具本身正常，樣本查得到） |
> | 拿 git 舊版 MEMORY.md（含那 6 段散文）當 fixture | **確定重複 0 句、疑似 0 句** |
> | 全掃五份 always-loaded 檔 | 確定重複 **3** 句、疑似 5 句 |
> | 那 3 句是什麼 | `見 [[feedback-plan-first-discuss-before-execute]]`／`→ feedback-platform-resources-save-endpoint`／`**保管人→倉位/座位自動帶 → feedback-location-custodian-autofill**` |
>
> **兩個結論，第二個比第一個嚴重：**
>
> **①「有更完整的版本」≠「文字重複」。** P-8 的整段實證寫著「6 段散文全部有更完整的版本，
> 靠 6 次手動 grep 才敢刪——**這一步正是可自動化的那一步**」。錯了。那 6 段是**摘要**，
> 與 harness 計畫書之間連 75% Jaccard 都不到。當日真正做的事是：grep 關鍵詞 → **人讀兩邊** →
> 判斷「那邊講得更完整」。**那是判斷題，正是 §2 說不能自動化的那一類。**
> 換句話說 —— **我把 C-1 已經否決的東西換了個名字（「重複偵測是事實不是判斷」）又提了一次，
> 而且自己沒發現。** 這一輪值得記的不是工具，是這個。
>
> **② 那 3 句「確定重複」全部是不該動的索引。** 檔名引用、wikilink、§8 的規則索引句
> ——**它們本來就該在多處重複出現，那是導航不是冗餘**。而 C-13 授權「確定重複可自動搬」。
> 兩者相乘的結果是：**這支工具目前若照 C-13 執行，第一件事就是刪掉 §8 的規則索引。**
> 🔒 **在 P-8 重新設計並通過 V-11 之前，C-13 的自動搬授權對 P-8 的輸出一律不生效。**
>
> **保留這支工具的理由**：它的索引層（299 檔 25,023 句、shingle 倒排）是對的，
> 且 `--json` 已可用；壞的是「拿它找可搬走的內容」這個用途。改用途不必重寫。
>
> **P-8b（改設計後的方向·待拍板）**：當日真正有效的判準不是「重複」而是**「結構異常」**
> ——MEMORY.md 該是索引（`- [` 列）卻出現大段散文；CLAUDE.md §8 該是速查表卻出現整段敘述。
> 這是**純結構判定、100% 可自動化**，而且當日 5,978 字**全部命中這個形狀**。
> 關鍵是：`check_bloat` 對這些字**完全沒有能見度**（它只認 `- [` 索引列與表格列），
> 所以它報 0 超標的同時，那 6 段散文正躺在檔案裡每則對話收一次費。

- **P-9 系統自動注入偵測（v5·外部三支工具都沒有這一項）**：
  Claude Code 每輪**自動注入** skill 清單（含完整描述）、agent 清單（含完整描述）、
  可用工具清單。**檔案裡再抄一份＝同一份資訊每則對話付兩次錢，而且會漂**。
  - **實證**：IT-dept MEMORY.md 曾有 1,086 字的本地 skill 清單、1,158 字的角色職掌描述，
    兩者都是系統已注入的內容的手抄副本。合計 2,244 字＝該檔 13%。
  - **判準**：一個區塊若同時滿足「列舉 ≥3 個 `/skill-name` 或角色識別字」且
    「其中 ≥N 個能在系統注入的清單裡找到同名項」，就標為候選。
  - **這一項的價值不只省字**：手抄清單會漂（skill 改了描述、角色改名），
    **而漂掉的症狀是模型讀到過期的職掌描述卻不會有任何人收到通知**。

- **P-10 token 與成本換算（v5·借鏡 claude-md-optimizer 的 session cost estimation）**：
  報告把 bytes 換成 tokens 與 $/月。
  - 🔒 **單價表一律 import `dashboard/gen_cost_panel.py` 的 `PRICE_IN`，禁止自己寫第二份**。
    §7 v3 已經量過（$24/月），那次是手算；這裡要的是常設。**第二份單價表 = 第二真相**，
    而價格會改，改的時候沒有人會記得有兩份。
  - 換算要標明前提（cache write 1h ×2／cache read ×0.1／每天幾則），**不給裸數字**。

- **P-11 全域層 skill（user 需求 3·放 `D:\.ai-harness\skills\`）**：
  把今天手動走完的流程固化成可重跑的編排器。**它不含任何規則本體**——規則在
  `check_bloat.py` 與本計畫書，skill 只編排步驟（沿用既有的 15 支專案 skill 的形狀）。
  - **步驟**：①跑 `check_bloat` 拿現況 ②跑 P-8／P-9 拿重複與抄本候選
    ③**逐項用選擇題問 user**（C-13 定的邊界）④照決定搬移 ⑤跑 V-10 驗檔名集合不變
    ⑥重跑 `check_bloat` 比對前後、貼出數字。
  - **層級＝全域**（user 決定）：跟 `visual-check` 同層，每個專案都叫得到。
    作用對象一律從該專案的 `.claude/PROJECT_CONTEXT.md` 讀，**不寫死**（U-1）。
  - ⚠ **P-6b 的邊界仍然成立且要寫進 skill 正文**：`/shougong` 只存在於 IT-department，
    所以「每次收工自動跑」只在這個專案為真；其他專案是**手動叫這支 skill**。
    不寫的話會被讀成「裝了就每個專案都會自己檢查」。

- **P-12 `gen_layers` 去專案化（C-11·解 §7 v3 登記的未償債務）**：
  拆掉 `PROJECT_DIR`(L44)／`EXTRA_PROJECTS`(L72)／`SCAN_ROOT`(L73) 三個字面值。
  - **雞生蛋問題**（C-11 的理由）：`discover_projects()` 回答的是「有哪些專案」，
    這份清單**不能放在專案裡**。所以 harness 層需要自己的設定檔。
  - **U-2 硬要求**：設定檔缺漏一律**拒跑並明說缺什麼**，不得 fallback 到現在這三個值。
    ⚠ 這一條會讓**本機第一次跑就壞**（設定檔還不存在）——所以 P-12 要附一支
    `bootstrap` 或首次跑產生範本，否則看板六支產生器同時停擺。
  - **不改 `survey_projects()` 的呼叫端**：六支產生器都在用，改簽名的爆炸半徑是整個看板。

---

## 5. 驗證方式（動工前就要寫好·每項答得出「怎麼證明它會紅」）

| # | 驗什麼 | 怎麼驗 | 怎麼證明它會紅 |
|---|---|---|---|
| V-1 | 去專案化真的做到 | grep 範圍＝`check_bloat.py` **及它 import 的核心層檔**。⚠ v2 修正：`gen_layers.py` 自己就寫死 `PROJECT_DIR`／`EXTRA_PROJECTS`／`SCAN_ROOT="D:\\"`，只 grep check_bloat 會**綠燈但換部門照樣搬不動**（字面值只是移到 import 之外） | 故意塞回一個字面值 → 必紅。若決定不修 gen_layers，**計畫書要明寫那是未償債務**，不能讓 V-1 的綠燈代表 U-1 達成 |
| V-2 | 沒有專案從報告裡消失 | ⚠ v2 改判準：**不是**「專案數相等」。`discover_projects()` 挑專案的判準是「有 `.claude\` 目錄」**不是「有 CLAUDE.md」**——在 `D:\` 任一目錄開一次 Claude Code 就會多一個，等式當場永久紅。改成「對每個 `survey_projects()` 給的專案，報告必須有一列：有 CLAUDE.md 給數字、沒有的寫『無』」 | 讓某專案的 CLAUDE.md 消失 → 該列必須變成「無」而**不是整列不見**。v1 的變異只動分子（改檔名），分母（看 `.claude\`）不會跟著動，證明的是「兩數不等會紅」而非「靜默略過會紅」 |
| V-3 | 章節解析真的通用 | ⚠ v2 改用**合成 fixture**（自造兩份標題編號不同的 markdown），斷言排名順序正確。不綁 live 專案檔——綁了會被 P-7 推翻：AI-Projects 壓完 §4（12,073→<6,364）後最大節翻成 §6，V-3 當場紅而原因與「通不通用」無關 | fixture 版：把任一標題字面值寫死進解析器 → 另一份 fixture 必排錯 → 紅。（寫成「回傳非空」是**不可證偽**的：寫死 `"## 8."` 的解析器對另一個檔退化成「整檔一節」也非空） |
| V-4 | 歷史比對答得出「反彈」 | ⚠ v2 修正：**至少三個基準點**（原值→降→回升）。兩筆只能表達「一次變化」，**表達不出「上次降過」**——照 v1 字面實作，測試要嘛空跑通過、要嘛逼實作把反彈退化成「只要變大就算」（對正常成長全面誤報） | **先寫這個 case 再寫實作**。另加一個 negative case：`降→沒動→回升`（v1 定義會漏報）必須也被標成反彈 |
| V-5 | 超標訊息真的到得了人眼前 | ⚠ v3 再換一次：v1 驗 `pending_warn` 落檔（投遞不到）、v2 驗 `PENDING_VERIFY.md` 多一列（**落錯節會被 `_NOT_TODO_SECTION` 靜默丟掉而該驗證仍綠**）。v3 定案＝**驗 stdout**：造一個超標情境，`check_bloat` 的**標準輸出**必須含該檔名與超標條數 | 把該檔的超標條目全部壓到 120 字內 → 那幾行必須從 stdout 消失。**唯讀之後這一項才真的驗得到**——訊息的載體就是報告本身，中間沒有第二個會靜默吃掉它的環節 |
| V-6 | 瘦身沒有砍掉觸發力 | ⚠ v2 修正自我矛盾（v1「每一條」vs「抽 3 條」）：改成**逐條機械對帳**——搬走的條目清單 diff，非零遺漏即紅。AI-Projects 全檔 42 條超標，抽 3 條≈7% 覆蓋率，漏掉一條的機率極高 | 故意漏搬一條 → diff 必須抓到 |
| V-7 | **整節刪除**的內容真的有人接住 | ⚠ **v3 大改——v2 的判準防不到它要防的事**。實跑證明：`gen_todos` 四個解析器對 §6 全部回 0，但該專案**其他** `.md` 有 4 項命中 → v2 的「命中數 > 0」會綠燈、閘門解除、十五條待辦一條都沒進看板。v3 改成**逐條對帳**：把 §6 的 A～O 十五條列成清單，`gen_todos --check` 的 AI-Projects 輸出必須**逐條對得上，缺一即紅** | 拿掉其中一條 → 對帳必須抓到。**且刪除前該表格要先改成看板認得的四欄格式**（現況三欄、狀態符號 `🔲🔴⛔❔` 不在 `_OPEN_EMOJI` 白名單，兩個條件任一個都足以歸零） |
| V-9 | **P-4 的建議真的指得對**（v4 新增·C-8·補上唯一沒有回歸網的那一塊） | **用 git 舊版當 fixture，因為答案已知**：IT-department 的 §8 有多條規則在 7/28–7/30 搬進了 `/asset-data-rules`／`/license-rules`／`/platform-resource-rules`／`/verify-rules`。取搬遷**前**那一版 CLAUDE.md（`git show <commit>:CLAUDE.md`）餵給 P-4，它應該建議把那幾節搬到那幾支 skill | 拿一個**與任何 skill 都無關**的節（例如 §5 對話管理）餵進去 → 它**不該**建議搬到任何 skill。⚠ 這一項的紅燈條件有兩個方向：**指錯地方**要紅，**該指卻沒指**也要紅——只驗一個方向會讓「永遠不建議」通過 |
| V-10 | **瘦 MEMORY.md 沒有讓 topic 檔失聯**（v4 新增·C-10） | 瘦身前後各取一次索引裡的檔名集合，**必須完全相同**（只准縮短 hook 句與合併同源，不准刪行——見 P-7b） | 故意刪一行 → 集合差集非空即紅。**這條比字數更重要**：刪行不會報錯，只是模型再也不知道有那份檔 |
| V-8 | **§6 裡「不是待辦」的那一半有去處**（v3 新增·R2-14） | §6 的 A～O 之後接的是發版紀錄（zip SHA256／manifest 檔數）與離線包規格（153 檔／4.63 GB）＋三條 ⚠ 教訓，**四類解析器沒一類收得了**。逐塊指名去處：待辦→看板、發版紀錄→`工作紀錄_2026.md`、離線包規格與教訓→topic 檔 | 三塊各抽一條，在新位置 grep 不到即紅。⚠ 其中一條教訓的原文是「`ITDeploy_Full\` 於 07-30 廢止的教訓仍成立」——**刪掉教訓的帳單要幾個月後才收到** |

| ~~V-11（原案）~~ | ~~P-8 重複偵測既不誤報也不漏報~~ | **作廢**：P-8 原案已被實測推翻（見 🔴 框）。原規格要求「6 段 fixture 的來源檔:行逐一對得上」，而實測是 **0 命中**——那份規格永遠不會通過，因為它要驗的東西不存在 | — |
| **V-11b** | **P-8b 結構偵測：長度累積正確、且不誤報索引** | ①**多行累積**：10 行 × 每行 <120 字、合計 >120 → 必須報成**一個** block 且 `lines==10` ②**段落邊界**：空行分隔的三個短段落 → 0 命中 ③**索引不誤報**：超長的 `- [x](x.md) — hook` 索引列 → 0 命中 | ①逐行判斷的實作（v5 首版）→ ①必紅（實測：修正前該案 `blocks=0`）②把邊界判定拿掉→整檔加總→②必紅 ③把 `_INDEX_ROW` 跳過拿掉 → ③必紅。**⚠ ① 是致命項**：harness 全部 `.md` 硬斷行在 ~90 欄，逐行判準讓全域 CLAUDE.md 被報成「0 塊」而實際有 685 字 |
| V-12 | **P-9 真的在比對系統注入的清單，不是看到 `/xxx` 就算**（v5） | 取 `git show` 的舊版 MEMORY.md（含那 1,086 字 skill 清單）→ 標為候選，且**列出的 skill 名要與系統注入清單逐一對上**；取現版 → 那一段不該再被標 | 把舊版清單裡的 skill 名全部改成不存在的名字（`/zzz-fake` …）→ **命中必須下降到 0**。若仍命中，代表它只是在數 `/` 開頭的字，那個判準對任何含斜線的文字都會誤報 |
| V-13 | **單價表只有一份**（v5·P-10） | AST 掃 `check_bloat.py` 及 v5 新增模組：**價格數值字面值 0 個**，唯一來源是 `import gen_cost_panel.PRICE_IN` | 塞一個 `15.0` 進新模組 → 必紅。⚠ 用 AST 不用字面 grep——`grep "15.0"` 會被註解與版本號污染，而 P-1 已經為「字面 grep 綠燈但字面值只是搬家」付過代價（見 V-1） |
| V-14 | **P-11 的 skill 真的跑得動**（v5） | 照 `/verify-skill` 三層：靜態 → 零件盤點（它引用的每支腳本都存在且可執行）→ **真實 dry-run**。**spawn-agent 型必須每個分支都跑到** | 拿掉 skill 依賴的其中一支腳本 → dry-run 必須**失敗並指名缺哪支**，不是靜默跳過那一步繼續走完。（靜默跳過會讓「步驟少做一半」長得跟「全部做完」一樣） |
| V-15 | **P-12 去專案化是真的，不是把字面值搬到 import 之外**（v5·C-11） | **兩個方向都要驗，缺一不算**：①AST 驗 `gen_layers.py` 執行期字串常數 0 個專案字面值（沿用 P-1 的做法）②**移掉設定檔跑一次 → 必須拒跑並印出缺什麼**（U-2），不得產出空表、不得 fallback 回現在那三個值 | ①塞回一個字面值 → 必紅 ②把拒跑改成「找不到設定就掃 `D:\`」→ 必紅。⚠ **只驗 ① 正是 V-1 踩過的坑**——`check_bloat` 的 `grep = 0` 綠燈，而字面值只是移進了 `gen_layers.py`。這一項若只做 ①，v5 會原地重演一次 |

**兩條紀律**：

1. ⚠ **不得用「兩支自己寫的實作互相比對」當獨立驗證** —— 共享同一個誤解時會一起錯
   （2026-08-06 踩過，當時原訂的 V1 當場降級成單元測試＋變異證明）。
   V-1～V-4 一律走「單元測試＋變異腳本」，變異要**實跑**確認每個都把測試弄紅
   （`tests/mutations/` 的既有形狀）。
2. ⚠ **任何以「> 0 ／非空／有東西」為判準的驗證項一律不算數**（v3 新增·R2-2）。
   V-3 那格自己寫著「『回傳非空』是不可證偽的」，而 v2 的 V-7 用的正是「命中數 > 0」——
   **一輪之內對兩個驗證項套了互相矛盾的標準**，而且實跑證明 V-7 確實會被繞過。
   寫完 §5 要回頭掃一遍：**還有沒有第三個。**

---

## 6. 狀態

<!-- REVIEW_SCOPE_IGNORE_START -->

- 2026-08-13 開稿（v1）。§1 現況已實測落檔；**§3 四項分岔全部拍板**（user 逐項選推薦）。
- 2026-08-13 **v2＝對抗式覆核 Round 1 後改版**（見 §7）。18 個發現，**17 個接受、
  1 個反駁**。掛載點（P-6）整塊推翻重寫。
- 2026-08-13 **v3＝Round 2 後縮範圍**。15 個新發現，其中四個致命項有三個來自
  「從唯讀變成寫入者」這一個決定 → user 拍板**縮成唯讀量測**，那三個當場消失。
  另拍板「§6 先修形狀再搬」與「`gen_layers` 登記成債」。
- **覆核狀態**：兩輪共 33 個發現。收斂理由＝**第二種**（`/adversarial-review` 步驟 5：
  剩下的分歧只能靠實際跑測試解決）——v3 的修法多數要跑起來才知道對不對，
  再猜一輪的邊際價值低於直接進 Execute 用 V-1～V-8 驗。
- 2026-08-13 **Execute 第一段完成**（P-0～P-6 ＋ 回歸網）：

| 項 | 狀態 | 實測 |
|---|---|---|
| P-0 補錨 | ✅ | 三份 CLAUDE.md（IT-dept §8／AI-Projects §4／全域用 `: all`） |
| P-1 去專案化 | ✅ | 執行期字串常數 0 個專案字面值（AST 驗，非字面 grep） |
| P-2 納管範圍 | ✅ | 兩欄（每則都付／開工讀）·`getsize` 不受 CRLF 影響 |
| P-3／P-3b 解析 | ✅ | 章節通用＋無章節退路；條目層綁錨、索引檔自動認 |
| P-4 建議 | ✅ | V-9 用 git 舊版驗過，3/3 指對；不亂指 |
| P-5 快照＋時序 | ✅ | schema 2、三元組 key、逐專案寫入、保留 60 筆 |
| P-6 掛載 | ✅ | shougong 步驟 3（無條件）·exit 1 只對 cwd 專案 |
| 回歸網 | ✅ | `test_check_bloat.py` 29 條＋9 個變異全紅＋1 等價不誤判；主套件 **585/585** |
| 基準 | ✅ | 三專案快照＋5 筆時序（**在壓縮之前**，R2-13 的硬相依） |

**Execute 期發現的兩個實作 bug（靜態看不出來，跑一次就現形）**：
①`--write-snapshot` 也要先讀舊快照，撞上 schema 守門 → **唯一的遷移出口被自己擋死**，
錯誤訊息還叫人去跑那個跑不動的指令。②`trend()` 只有一筆歷史時判成「壓下去了」
（最低＝起點＝現在）——**假趨勢**。兩個都已修並各有變異守著。

- ~~**下一步＝P-7／P-7b 瘦身**（量大：AI-Projects §4 有 36 條超標、兩份 MEMORY.md 合計 44 條）~~
  ⚠ **「44 條」這個數字在寫下的當天稍晚就失效了**——見 §3 C-9 訂正框：判準改成
  「索引列只算 hook 句」之後，IT-dept 那 34 條**全部合格**（實測 88 條 0 超標）。
  剩下的是 AI-Projects 的 10 條（未複驗）與 §4 的 36 條。**P-7 動 §6 前的前置條件
  （建 `PROJECT_CONTEXT.md` 並驗 `gen_todos` 抓得到）仍然尚未做。**

### 2026-08-13 晚：v5 立案（user 三個需求）＋ IT-dept MEMORY.md 散文塊已處理

user 要求「健檢做成工具／編排優化做成工具／包成 skill／通用專案自動瘦身」。查證後
**需求 1 已由 P-0～P-6 完成、需求 2 的自動改寫已由 C-1 否決**，真正的缺口是三個
（見 §4 v5 段）。已上網比對三支外部工具（`claude-md-optimizer`／`cclint`／`AgentLinter`），
結論：**我們的快照＋歷史時序是它們全都沒有的**；**它們的跨檔重複偵測是我們沒有的**（→ P-8）。
外部工具的行數閾值（CLAUDE.md <150 行、MEMORY.md <200 行）**刻意不採**——那是英文專案算的，
中文每行資訊密度完全不同，而我們已有 120 字/條且與檔案自己寫的規則同源。

**同日先做掉的（不等 v5 全案）**：IT-dept MEMORY.md 尾段 6 段散文 → 3 行指路，
**25,196 → 16,808 bytes（-33%）**，常駐層合計 88,622 → 80,234 bytes。
索引列 88→88（V-10 的判準：檔名集合不變）、CRLF 未被翻、`check_bloat` exit 0 趨勢轉「壓下去了」。
**零資訊遺失是逐段 grep 查證的**（6 段的來源檔:行見 P-8），唯一獨有的一句補進
`reference-dev-prod-git-repo-split.md`。這批字 **check_bloat 完全沒在管**——
它們不是 `- [` 索引列也不在 `rules-section` 錨內，**是 P-8／P-9 要接手的那一類**。

| v5 項 | 狀態 | 實測 |
|---|---|---|
| C-11／C-12／C-13 | ✅ 三項拍板（兩層設定／兩欄都報／100% 確定可自動搬＋句子級硬約束） | — |
| **P-12** 去專案化 | ⚠ **部分**（不得讀成 U-1 達成） | `harness.config.json` 兩層設定；`gen_layers` 三個字面值拆掉；順帶抓到 L377 寫死 harness 路徑（讀不到時 `ruleCount` 靜默變 0）。**但覆核 F-4 實測：核心層還有 5 檔 6 處同型債**（`capability_checks.py:40`／`check_freshness.py:37,38`／`gen_cost_panel.py:57`／`gen_roles_topology.py:77`／`refresh_dashboard.py:41`），**加上 AST 抓不到的形狀又有 10+ 處**（`subagent_stats.py:45`／`gen_cost_panel.py:62`／`budget1_daily_usage.py:37`／`capability_checks.py:99`／`eval/*` 多處）。原本只登記 2 處 —— **這正是 V-1 教訓的原地重演**：`check_bloat` 的 grep=0 曾被讀成 U-1 達成，現在換成 `gen_layers` 的 AST=0 |
| **P-8** 跨檔重複偵測 | ❌ **假設推翻·原案作廢** | 見上方 🔴 實測框。工具 `find_duplicates.py` 保留（索引層可用），**但不得拿它的輸出當搬移依據** |
| **P-8b** 結構異常偵測 | ✅ **替代 P-8**（判準改過三版） | **`rulefile/check_prose_blocks.py`**（v8 由 `check_structure.py` 改名·避開 `eval/` 同名）。判準演化：v5 逐行 → **硬斷行整段逃逸**（F-1 致命）；v7 段落累積＋兩段門檻 → **120–239 開了一條縫，折行就能讓 29% 的字消失**（R4-A）；**v8 單一門檻＋報告帶 `lines`／`max_line`**——假陽性不靠門檻解、靠報告讓人分辨。現況：IT-dept CLAUDE.md §8 錨內 **8 塊 1,887 字**，`check_bloat` 從沒報過；**折行不變性已由回歸網守住**（實測折行後 8 塊 1,887 字一字不差） |
| **P-9** 手抄注入清單偵測 | ✅（判準 v8 改過） | 掃 skills／agents 目錄得等價名單。⚠ **只涵蓋目錄型**——plugin 與內建 skill 不在裡面（掃到 17／實際注入約 31），報告尾端已寫明這個界線。判準由絕對數 `>=5` 改為**比例**（`hits>=3 且（涵蓋率>=0.4 或 hits>=5）`）：絕對數對只有 4 支 skill 的部門**永遠不命中**，而分發給其他部門正是前提（F-9） |
| **P-10** 成本換算 | ✅ | 單價一律來自 `gen_cost_panel.PRICE_IN`。⚠ 三次才守住：①`except Exception` 把 `ModuleNotFoundError` 吞成「讀不到」②`.get(model, 5.0)` 的 fallback 就是第二份表 ③AST 只掃 float、行為驗證 patch 掉整支 → **「藏進 `_price_in` 且用 int」兩道防線同時失效**（R4-B）。現有三道：行為驗證＋`ast.Dict` 掃描＋**不 patch 的恆等式** |
| **P-11** 全域層 skill | ✅ | `D:\.ai-harness\skills\context-health\`。**`~/.claude/skills` 整個目錄是 junction 到 harness**，所以新建即生效——同一輪系統注入清單已出現它（不是「檔案存在」而是「真的進了可用清單」） |
| **V-11b**／V-12 | ✅ | **`test_check_prose_blocks.py` 40 項**（v8 新增：折行不變性・多行塊形狀・比例判準四條・列表符號對齊・P-9 整合路徑），已註冊進 `run_hook_tests.py` |
| **V-13** | ✅（v8 補到三道） | 首版 `is not None`＝紀律 2 禁的形狀；v7 加 AST 但只掃 float；**v8 補不 patch 的恆等式** `_price_in() == gen_cost_panel.PRICE_IN`——前兩道都對「藏進 `_price_in` 且用 int」失效（R4-B 實測 7 條斷言全綠） |
| **V-14** | ✅ **v8 補做**（F-15） | `test_context_health_skill.py` **15 項**＝`/verify-skill` 三層全走：frontmatter（含症狀端觸發語）／零件盤點／**真實 dry-run 跑每支腳本**。紅燈條件已驗：拿掉一支依賴腳本 → 盤點失敗並指名。⚠ v6 時這一列寫「❌ 未做」而 P-11 標 ✅——**「沒做」與「做完了」在狀態表上長得一模一樣**，這就是為什麼每個 V- 項都要有自己的一列 |
| **V-15** | ✅ | **`test_harness_config.py` 23 項**。①AST 涵蓋四類路徑上下文（F-3）②三條 `in src` 改 AST 斷言（F-11）③U-1 債務台帳改 **Counter 逐值＋逐次數**——「值集合」會漏掉同檔同值再加一處（R4-D）④`currentProject` 存在性檢查＋`--init` bootstrap＋gitignore（F-8） |

**這一輪最值得記的不是工具，是 P-8 那個錯**：我把 C-1 已經否決的東西換了個名字
（「重複偵測是事實不是判斷」）重新提了一次，寫進計畫書、寫成程式碼、跑起來才發現。
**擋下它的不是覆核也不是規則，是「拿已知答案的 fixture 去跑一次」。**

<!-- REVIEW_SCOPE_IGNORE_END -->

---

## 7. 改版紀錄

### v9（2026-08-14·**Round 4 跑完了**·13 個新發現·已修 6／待修 7）

Round 4 這次沒有中斷。審查者實測後給了 13 個發現，**第一個就是我修法的致命回歸**。

| # | 發現 | 處置 | 狀態 |
|---|---|---|---|
| **R4-A**（高） | **v7 的兩段門檻重新打開了 F-1 的「按 Enter 就達標」漏洞**，範圍 120–239 字。實測：IT-dept CLAUDE.md **只折行、一字未刪**（`_visible` 比對確認）→ `7 塊 1,733 字` 變 `4 塊 1,227 字`，**506 字（29%）憑空消失**。而 SKILL.md 的完成判準還是「散文字數下降」 | **門檻收回單一個（≥120）**；假陽性改用**報告帶 `lines`／`max_line`** 解決——「4 行各 62 字」與「一段折成 4 行」結構上無法區分（中文長段落內部本來就有句號，用標點當邊界會把長段落切碎），但**人一眼能分**，這也正是本工具「只報形狀不報對錯」的定位。新增**折行不變性測試**：實測折行後 8 塊 1,887 字**一字不差** | ✅ |
| **R4-B**（高） | **R4-3 的行為驗證擋不住它自己點名的繞過③**：把單價表寫進 `_price_in()` 且用 int。行為測試把 `_price_in` **整支換掉**所以看不見內容；AST 只掃 float 所以 `5` 抓不到。實測那個變異讓 **7 條價格斷言全綠**，而 v7 表寫著「價格藏在哪都躲不掉」 | 補**第三道防線**：不 patch 的恆等式 `_price_in() == gen_cost_panel.PRICE_IN`；AST 輔助改掃 `ast.Dict` 的 value（int/float 都算） | ✅ |
| **R4-C**（中高） | `PARA_LIMIT` 在 **[193,460] 全區間**都能讓回歸網全綠——沒有測試釘得住那個常數。**「`ECHO_MIN_REFS=5` 改成 4 全綠」的形狀原地重演** | 隨 R4-A 解決：**那個常數不存在了** | ✅ |
| **R4-D**（中） | R4-2 的台帳改記「值集合」**丟掉多重性**：同檔案再寫一次**完全相同**的字面值 → 差集為空 → 綠燈，而**舊的處數版本反而抓得到**。換掉一個繞過，換來另一個 | 台帳改 **`Counter`**，逐值＋逐次數比對。兩種繞過同時堵住 | ✅ |
| **R4-G**（中） | 改名殘留：**工具自己 docstring 的使用範例是壞指令**（實跑 `No such file`）；§6 現況表兩處指著不存在的檔。v8 表寫「殘留 0 處」不成立 | 工具／測試／`gen_layers`／`test_harness_config` 的引用全改；§6 現況表六列更新。**§7 改版紀錄裡的舊名刻意保留**——那是歷史 | ✅ |
| **R4-H**（中） | §7 v6 表 5 列還掛 ⏳ → `gen_todos` 掃 `*_PLAN.md` 的 ⏳ 列 → **看板出現 5 筆幽靈待辦**，其中 2 筆叫人去做已完成的 F-12／F-16，`todo-copy` 還會把不存在的檔名塞進剪貼簿。**與 F-5／F-7／F-15 同型的第四次**，這次有機械消費者把它變成看得見的假工作 | v6 表狀態欄全部更新，⏳ 殘留 **0** | ✅ |
| **R4-E**（中） | 比例判準的新失效面：`ECHO_COVERAGE` 在 **universe ≥ 11 時永遠用不到**（本機 23，需 hits≥10 但 `ECHO_ABS_STRONG=5` 先命中）；**universe ≤ 10 時指路句與全抄在數學上無法區分**（4 支的部門提 3 支 → 75% → 被判成手抄 → **叫新部門去刪自己的導航句**） | 未修 | ⏳ |
| **R4-F**（中） | F-12 只改了「跳過誰」沒改「誰是段落邊界」→ `* `／`+ `／`1. ` 的**短條目會黏成假散文塊**（實測 10 條各 23 字 → 1 塊 251 字），且 head 指到 `<!-- rules-section -->` 錨——**工具正指著一行「不可刪」的東西**。兩支工具對 `* ` 仍然都沒套 120 字/條 | 未修 | ⏳ |
| **R4-I**（中） | F-10 修了「給錯名」沒修「不給」：`--file` **不帶 `--project`** 仍靜默關掉 P-9 且報告不說；**`__global__` 那份 CLAUDE.md 永遠沒有 P-9**（`proj_paths.get('__global__')` = None），而它恰好最可能手抄角色／skill 清單 | 未修 | ⏳ |
| **R4-J／K／L／M**（低） | V-14 的 `exit 0` 斷言會把 `check_bloat` 的**內容閘門 exit 1** 誤報成「skill 零件跑不起來」／零件盤點只涵蓋 SKILL.md 提到的 4 支中的 2 支／變異測試改名的是**版控中**的 `check_bloat.py` 且無 `*.v14bak` 的 gitignore 與還原提示／SKILL.md 說「三支工具」但只列 2 支 | 未修 | ⏳ |

**審查者明確認為「沒問題」的 10 項**（我需要知道哪些穩了）：R4-1 對假陽性確實有效・
台帳正斜線改寫會紅・掃描範圍改對了・行為驗證對三種繞過會紅・F-10 拒跑是真的・
F-12 對三份 live CLAUDE.md 零假陽性・F-14 範圍限制真的印出來・F-15 守門擋得住「全部抽不到」・
V-14 不寫 live 狀態・主套件實跑確認。

**這一輪的形狀**：Round 3 的修法引入 3 個新缺陷（v7），Round 4 又在**修法的修法**裡找到 3 個
（R4-A／R4-B／R4-D 都是「前一版修掉一個繞過、換來另一個」）。
**同一個形狀連續出現三輪**——這不是運氣不好，是「憑我想得到的寫法去擋」這個做法本身的性質。

主套件 **671/671**。

> ⚠ **順手踩到的 marker 陷阱（值得記一筆）**：`pr1_plan_review_marker` 的 marker regex 是
> `<!--\s*ADVERSARIAL_REVIEW_(PASSED|SKIP)\s+sha256=([0-9a-f]{64})[^>]*-->`
> —— **`[^>]*` 不允許 `>` 出現在理由文字裡**。我在 SKIP 的理由寫了 `universe>=11`，
> 那個 `>` 讓整條 marker **匹配不到** → marker 行沒被扣掉 → 計入 hash → 永遠對不上。
> 症狀是「我剛算的 hash 貼進去卻還是說對不上」，看起來像 hash 函式壞了。
> **理由文字裡要用 `≥`／`≤` 而不是 `>=`／`<=`。**

---

### v8（2026-08-14·**待修 6 項全部處理完**）

| # | 修法 | 回歸網 |
|---|---|---|
| **F-9** | **絕對數 → 比例判準**：`hits >= 3 且（涵蓋率 >= 40% 或 hits >= 5）`。首版的「≥5 個名字」對這台機器是對的，**對「分發給其他部門」是致命的**——只有 4 支 skill 的部門即使整份手抄，`count` 最多 4 < 5，**永遠不會命中**。而「換一個部門還成立嗎」正是這整套 harness 的判準 | 4 條：小專案全抄要命中／4-of-17 指路句不命中／2 支名單提 1 支不命中（守 `ECHO_MIN_REFS` 下限）／小專案那條額外斷言「是靠比例不是靠絕對數命中的」 |
| **F-10** | `--project` **統一成專案名**，`--file` 分支改查 `survey_projects()` 的名單，給不認得的名字直接拒跑。首版該分支拿它當**目錄路徑**用 → 照 docstring 打 `--project IT-department` 會**靜默關掉 P-9 且不會說** | 整合測試走 `scan(path, kind, project_path)` 造臨時專案驗接線；**反向**再驗「不給 project_path 就沒有 P-9 結果」，證明真的靠那個參數 |
| **F-12** | `_LIST_ROW` 從 `^[-*+]\s` 收成 **`^-\s`**，與 `check_bloat.parse_entries()` 只認 `- ` 對齊。**互補的前提是邊界對齊，不是各自有各自的漏** | 三種列表符號各驗一次：`- ` 不該報（check_bloat 管）、`* ` 與 `+ ` 要報 |
| **F-14** | docstring 從「注入的清單就是這些目錄，**不必也不能去猜**」改成誠實版：只涵蓋**目錄型**（掃到 17），plugin 與內建 skill（實際注入 31）**不在名單裡**。報告尾端加一行範圍限制——**「沒命中」不等於「沒問題」** | 由報告輸出斷言（`test_dry_run_each_script` 驗有實際輸出） |
| **F-15** | 補 `test_context_health_skill.py`＝`/verify-skill` 三層：①frontmatter（含「description 要有症狀端觸發語」——沒有的話沒人會叫它）②零件盤點 ③**真實 dry-run 跑每一支腳本**。另驗 skill 正文有沒有寫明 P-6b 邊界與兩條硬規則 | **15 項**，含 V-14 的紅燈條件：拿掉一支依賴腳本 → 盤點必須失敗**並指名是哪一支** |
| **F-16** | `rulefile/check_structure.py` → **`check_prose_blocks.py`**（避開 `eval/check_structure.py` 同名）；測試檔同步改名；`run_hook_tests` 註冊名、SKILL.md 引用全數同步（殘留 0 處） | 既有 35 項全綠 |

**主套件 666/666**（`check_prose_blocks` 35＋`harness_config` 23＋`context_health_skill` 15）。

⚠ **F-9 改判準時有兩個既有測試變紅**，那不是回歸：它們用 6／8 支的小名單列 3–4 個，
換成比例後 50% 就命中了。**那是測試資料不真實**（IT-dept 實際 17 支），
改成真實規模後全綠。**判準換了要回頭看測試資料還成不成立**，不能只看紅綠。

---

### v7（2026-08-13 深夜·Round 4 **中斷**·自行接手驗證·又抓到 3 個)

**Round 4 的審查者跑到一半被 session limit 砍掉**，只回傳一句
「Two mutations confirmed」——**沒有具體內容，不可當結論**。
我沒有猜它找到什麼，改成自己把交付給它的檢查項逐條跑一遍。**又抓到 3 個真問題**：

| # | 問題 | 證據 | 處置 |
|---|---|---|---|
| **R4-1** | **F-1 的修法產生了新的假陽性**：段落累積會把**連續的獨立短條目**合併。實例已污染真實輸出——MEMORY.md 檔頭 4 行 `>` 說明各 40–68 字（每行都合法），合併後 176 字被報成散文塊。**那不是膨脹，那是檔頭。** | 造反例實測：兩行各 73 字 → 合併 146 字 → 誤報 1 塊 | ✅ 改**兩段門檻**：`max_line ≥ LIMIT`（單行超標·確定）**或** 合計 `≥ PARA_LIMIT`（2×120·硬斷行的長段落）。修後全域 CLAUDE.md 5 塊→0 塊、合計 4,214→3,015，而 F-1 要抓的 460 字反例仍命中 |
| **R4-2** | **F-4 的債務台帳有兩個繞過**：①key 是 `(檔名, **處數**)` → 同檔「償還一處＋新增一處」處數不變 → **靜默通過** ②掃描範圍是白名單三目錄 → 在 `tools/`／`reviewer/`／新目錄新增看不見（實測未納管頂層目錄有 **9 個**） | 讀 `_KNOWN_U1_DEBT` 與判定式 `actual[k] > known[k]` | ✅ 台帳改記**實際字面值集合**（逐值比對）；掃描改成**全 harness 扣掉排除清單** |
| **R4-3** | **F-2 的 AST 測試有三種繞過**：價格寫成 `int`（只掃 float）／搬到模組層常數／搬進 `_price_in()` 當 fallback ——**結構檢查只擋得住我當初想到的那一種寫法** | AST 實掃 `cost_estimate` 函式體 | ✅ 主防線改成**行為驗證**：把單價表換掉，金額必須跟著等比變動。**價格藏在哪都躲不掉**。AST 那條降級為輔助並註明它抓不到 int |

**另外，我自己寫的守門條件抓到了我自己的測試資料錯誤**：
`test_independent_short_lines_not_merged` 的手寫中文只有 36 字，兩行合計 72 < 120
——**前提不成立，那個 case 什麼都沒測到**。是那條「前提成立：單行 <120 但合計 >120」
的斷言把它照出來的。改用可計算的構造（`"獨立條目內容" * 12`）而不是手寫長度。

**這一輪的教訓**：Round 3 的每一個修法**都引入了新的缺陷**（F-1→假陽性、F-4→兩個繞過、
F-2→三種繞過）。**修一個 bug 的動作本身就是一次新的改動，它需要同等的懷疑。**
三個都是「我當初想到的那一種寫法擋住了，別的寫法沒有」——同一個形狀出現三次。

回歸網：`test_check_structure.py` **26 項**＋`test_harness_config.py` **23 項**，主套件 **642/642**。

---

### v6（2026-08-13 晚·對抗式覆核 Round 3·**16 個發現全部接受**）

審查者：Claude Code `Plan` subagent／opus／effort high —— `reviewer_config.json` 指定的就是這個，
**沒有換人**（Codex 未安裝，但設定本來也沒選它）。觸發者是 **PR-1 閘門**：
v5 改完後 Stop hook 抓到 `ADVERSARIAL_REVIEW_PASSED` 的 hash 對不上。
我在 v5 正文裡自己寫過「sha 已失效，動工前應重跑」——**然後就直接動工了**。
閘門把這件事推回面前，這是它第一次擋到真正該擋的東西。

**這一輪沒有一個發現被反駁。** 16 個裡有 1 個致命、6 個高。

| # | 發現 | 處置 | 狀態 |
|---|---|---|---|
| **F-1** | **`check_structure` 逐行判長度 → 硬斷行的散文整段逃逸。** harness 全部 `.md` 硬斷行在 ~90 欄；當日那 6 段抓得到**只是因為它們剛好是未斷行的單行 blockquote**。更糟：SKILL.md 把完成判準訂成「散文字數下降」→ **重新斷行就能滿足，一個字都沒搬** | 改成**段落累積**（空行／標題／表格／列表為邊界）。實測全掃 2,719 → **4,214 字**，其中**全域 CLAUDE.md 從「✔ 0 塊」變成 5 塊 685 字** | ✅ 已修＋3 條回歸網 |
| **F-2** | **`cost_estimate` 有第二份價格 `5.0`**（＝`PRICE_IN["opus"]` 的複製品），違反 P-10 自己的 🔒。失效形狀：改價或 key 改名時**不報錯、不回 None，默默用舊價算出看起來正常的數字**。且 V-13 規格說要 AST 檢查，實作的卻是 `is not None`——**正是紀律 2 點名禁的形狀，也就是它自己說的「第三個」** | 拿掉 fallback（查不到 key 回 None 並列出現有 key）；補**真正的 AST 測試**（只掃 `cost_estimate` 函式體內的 float，不掃全檔——否則 `CHARS_PER_TOKEN` 會被誤判） | ✅ 已修＋3 條回歸網 |
| **F-3** | V-15① 的 AST 只抓 `Path(<含碟號字面值>)`，**看不見它自己登記那兩筆債的形狀**（`Path.home()/".."` 是 BinOp、`os.path.expanduser()` 不是 Path）。實測五種再引入形式全部漏抓 | 偵測器改看**四類路徑上下文**（`Path()`／`expanduser` 等路徑函式參數・`/` 運算子兩側・`environ.get` default・比對碟號/`d--`/設定檔裡的專案名）。自我驗證改成五種形式逐一斷言 | ✅ 已修 |
| **F-4** | **核心層還有 5 檔 6 處同型債＋10+ 處 AST 抓不到的**，計畫書只登記 2 處，而 §6 把 P-12 記 ✅ | ①§6 的 P-12 改成「⚠ 部分·不得讀成 U-1 達成」②**做成執行期閘門**：`test_u1_debt_does_not_grow` 把實掃的 **7 檔 11 處**凍結成台帳，**只准變少，新增即紅**——把「登記債務」從一句文件變成擋得住的東西 ③`UNIVERSAL_HARNESS_PLAN` §1 債務表同步更新 | ✅ 已修＋閘門 |
| **F-5** | **V-11 規格一字未改卻在 §6 標 ✅**，而 🔴 P-8 框把 C-13 的解鎖條件寫成「通過 V-11」——**改了 V-11 的定義等於偷偷解鎖** | 原 V-11 標作廢並說明「它要驗的東西不存在」；另立 **V-11b** 對應 P-8b，三個判準三個紅燈條件 | ✅ 已改 |
| **F-6** | **C-13 拍板 (b) 但出貨實作是 (a)，且無任何一行記載這個回退**；句子級硬約束的唯一載體 `_SENT_SPLIT` **零回歸網**，改成段落級不會有任何測試變紅。實測 §8 那 7 塊裡**有 5 塊就是規則索引句** | user 裁決：C-13 由 (b) 正式改判 **(a)**（見 §3 C-13 的裁決歷程） | ✅ 已裁決 |
| **F-7** | **D-2 未拍板卻已實作**，且 `UNIVERSAL_HARNESS_PLAN` 零更新：§4 D-2 無決定欄、§6 仍寫「五項待決，未討論前不動工」、§1 債務表仍列 `gen_layers`。而本檔 P-1 還留著「**刻意不新建 `harness.config.json`**：未拍板不動工」——**P-1 說不建、P-12 說建好了，兩段都還在** | 拆法本身審查者認為對（雞生蛋是真的），不回退實作。四處全補：①`UNIVERSAL_HARNESS_PLAN` §4 D-2 加決定欄＋「D-2 定案」段（兩層表格·U-2 四種拒跑）②§6 改成「D-2 已拍板、其餘四項仍待決」③§1 債務表更新 ④本檔 P-1 那段標作廢並保留原文。**「拍板順序是反的」這筆帳明寫在兩份文件裡** | ✅ 已修 |
| **F-8** | `harness.config.json` **untracked 且不在 `.gitignore`**；P-12 承諾的 bootstrap **沒做**（`git ls-files \| grep bootstrap` = 0）；`discover_projects()` 對 `currentProject` 是 `found.insert(0, here)` **無條件插入**（`EXTRA_PROJECTS` 反而有 `exists()` 過濾）→ 換部門時 U-2 的拒跑**不會觸發**，改成報告多一列幽靈專案、CLAUDE.md 印「無」 | 三項全補：①`_load_config()` 加 `currentProject` 存在性檢查 → 拒跑並指名（守門移到 config 層，`survey_projects()` 那條繞道也擋得到）②`--init` bootstrap 產範本，**範本路徑刻意是假的**（逼人去改，不會靜默跑起來）③設定檔進 `.gitignore`——內容是這台機器的路徑，進版控等於把 `D:\IT-department` 當別人的預設值出貨 | ✅ 已修＋4 條回歸網 |
| **F-9** | `ECHO_MIN_REFS=5` **改成 4 全套回歸網照樣綠**（只編碼了 3 個名字那個資料點）；且它是**絕對數**——一個只有 4 支 skill 的新部門即使整份手抄也永遠不命中，而「分發給其他部門」正是 v5 的前提 | v8 改成**比例判準**（`hits>=3 且（涵蓋率>=0.4 或 hits>=5）`）＋四條測試 | ✅ v8 已修 |
| **F-10** | P-9 的**整合路徑零測試**（三條 echo 測試全部直呼 `echo_of_injected`，沒有一條走 `scan()`）→ 接線壞掉仍全綠而報告再也不出現 ⟪P-9⟫；且 `--file` 分支的 `--project` 當**目錄路徑**用、主流程當**專案名**用，照 docstring 打會**靜默關掉 P-9** | 統一 `--project` 語意；補走 `scan()` 的整合測試 | ✅ v8 已修（見 §7 v8） |
| **F-11** | `test_harness_config` 三條是**正向 `in src` 字面比對，一行註解就能滿足**；審查者構造出「真的寫死路徑但七條全綠」的實例。另 `all(...)` 在 `hits` 為空時**恆真**——偵測器整支壞掉時這條「反向自我驗證」自己也是綠的 | 三條 `in src` 改成 **AST 斷言**（驗「那個賦值運算式裡真的有 `_CFG[...]` 下標」，註解滿足不了）；`all()` 那條改成正面列舉不該出現的東西＋`hits` 非空前置 | ✅ 已修 |
| **F-12** | `* `／`+ ` 開頭的長規則**兩支工具都看不見**（`check_structure` 跳過 `^[-*+]`、`check_bloat` 只認 `- `）→ SKILL.md 那句「兩支盲區互補」是錯的 | 現況零命中，但屬「改個排版就失效」型 | ✅ v8 已修（見 §7 v8） |
| **F-13** | **C-12 決定欄與選項欄字母錯位**：選項 (c) 寫「只比對檔名＋關鍵詞共現」，決定欄寫「(c) 兩欄都報」——**照字母執行會做成最容易誤報索引句的那一種**。且 C-12 是**已拍板、無合法消費者、無回歸網**的孤兒 | 選項欄已訂正為實際問過的三個；孤兒狀態已加註 | ✅ 已改 |
| **F-14** | `injected_names()` 掃三個目錄得 **17 項**，而審查者自己收到的注入清單有 **31 項**——多的 14 項是 plugin／內建 skill（`dataviz`／`code-review`／`loop`…），**不在任何一個被掃的目錄裡**。P-9 的 docstring 把「掃同源即可，不必也不能去猜」寫成定論，**錯了** | docstring 要改成「只涵蓋目錄型 skill」，報告要講出這個範圍限制 | ✅ v8 已修（見 §7 v8） |
| **F-15** | **V-14 沒出現在 §6 任何一列，P-11 卻標 ✅**。實際只做了 `/verify-skill` 三層的第一層（靜態），零件盤點與 dry-run 都沒做 | §6 要補 V-14 列並標實際完成度 | ✅ v8 已修（見 §7 v8） |
| **F-16** | `rulefile/check_structure.py` 與 **既有的 `eval/check_structure.py` 同名**；`sys.path` 同時含兩者時 `import check_structure` 拿到哪支取決於順序 | 目前靠 `importlib` 自訂模組名載入，無實際衝突；但同名本身是債 | ✅ v8 已修（見 §7 v8） |

**待 user 裁決（F-6）**：C-13 你拍板的是 **(b) 100% 確定可自動搬**，但我出貨的 `SKILL.md`
步驟 3 寫的是「**沒有人點頭之前不動任何檔**」＝ **(a)**。這個回退我沒有記錄在任何地方。
回退的實質理由是覆核 F-6 查證的那件事：**IT-dept CLAUDE.md 那 8 塊裡有 5 塊是 §8 的規則索引句**
——結構判定「100% 機械可判定」，但它判出來的東西**恰好是最不該自動搬的**。
所以請重新裁決：要維持 (b) 並定義出「哪一類結構塊才可自動搬」，還是把 C-13 正式改成 (a)。

**收斂狀態（2026-08-13 晚·處置後更新）**：16 個發現**沒有一個被反駁**。

| 分類 | 項目 |
|---|---|
| ✅ **已修＋回歸網**（9） | F-1 段落累積・F-2 假價格＋AST・F-3 偵測器四類上下文・F-4 債務台帳閘門・F-5 V-11b・F-7 方向文件四處・F-8 存在性檢查＋`--init`＋gitignore・F-11 AST 斷言・F-13 選項欄訂正 |
| ✅ **user 裁決**（1） | F-6：C-13 由 (b) 正式改判 **(a)**（理由是實測——8 塊裡 5 塊是規則索引句） |
| ✅ **v8 補完**（6） | F-9 改比例判準・F-10 `--project` 統一成專案名＋整合測試・F-12 列表符號與 check_bloat 對齊・F-14 範圍限制寫進 docstring 與報告・F-15 V-14 三層 15 項・F-16 改名 `check_prose_blocks.py`。詳見 §7 v8 |

回歸網（**2026-08-14 更新**）：`test_check_prose_blocks.py` **35 項**
＋`test_harness_config.py` **23 項**＋`test_context_health_skill.py` **15 項**，
主套件 **666/666**。

**2026-08-14：Round 3 的 16 項＋v7 自驗的 3 項，全部處置完畢。**
剩下的收斂條件是「**讓審查者看過這些修法**」——v7 的教訓是每個修法都可能引入新缺陷
（三次都是同一形狀：「我當初想到的那一種寫法擋住了，別的沒有」），
**修完不等於收斂**。以下為 v6 當時的狀態，保留供回溯：

**（v6 當時）仍未收斂**——5 項待修＋1 項部分。收斂判準三選一都還沒達成，
`ADVERSARIAL_REVIEW_PASSED` marker **不更新**，計畫書維持「待審核」。
⚠ **這是刻意的**：marker 過了就等於宣告「這份計畫被審過且結論成立」，
而現在有 6 項已知缺陷還在檔案裡。**PR-1 下次仍會擋，那是對的。**

---

### v5（2026-08-13 晚·user 要求「做成工具＋包成 skill＋通用專案」·**含一個前提失效**）

user 的三個需求逐項對現況查證，結果是**兩個已經存在、一個已被自己否決過**：

| user 的需求 | 查證結果 |
|---|---|
| 健檢做成工具 | **已完成**＝`check_bloat.py` P-0～P-6（回歸網 585/585、三專案基準已留） |
| 編排優化做成工具（自動瘦身） | **C-1 已拍板 (a)+(c)、明確否決 (b) 自動生成／改寫**。但 P-4「該搬去哪」已做且 V-9 驗過 3/3 指對 |
| 包成 skill 給人用 | **真的缺**——全域層只有 `visual-check` 一支，專案層 15 支裡沒有這類 → P-11 |
| 通用專案 | **名義上通用**——`gen_layers.py` 三個字面值未拆，§7 v3 自己登記為未償債務且註明「V-1 的綠燈不得被讀成 U-1 達成」→ P-12 |

**外部工具比對**（user 要求上網確認，2026-08-13）：

| 工具 | 我們可以拿的 | 我們刻意不拿的 |
|---|---|---|
| [`claude-md-optimizer`](https://github.com/geuneda/claude-md-optimizer) | **跨檔重複偵測**（→ P-8）、session cost estimation（→ P-10） | 行數閾值（<150／<200 行）＝英文專案算的 |
| [`cclint`](https://github.com/felixgeelhaar/cclint) | 設定檔可覆寫 severity 的形狀（未來 D-2 參考） | `structure` 規則要求 "Project Overview／Development Commands"＝**對中文專案完全不適用**；`--fix` 只改 code block 語言標籤 |
| [`AgentLinter`](https://agentlinter.com/)／[progressive disclosure](https://alexop.dev/posts/stop-bloating-your-claude-md-progressive-disclosure-ai-coding-tools/) | 方法論用語 | 單次評分（無時序） |

⚠ **值得記一筆**：`claude-md-optimizer` 的 apply 是 **verbatim extraction（逐字搬移）＋zero information loss verification**，
**不是改寫壓縮**。所以它跟 §2「壓縮不自動化」**其實不衝突**——那正是 IT-department
28.5→21.6 KB 用的招，也是 2026-08-13 這次用的招。**C-1 否決的是「改寫句子」，不是「整塊搬走留索引」**，
這個區分之前沒寫清楚，v5 補上。

**本輪最重要的發現不是外部工具，是 C-9 的前提在同一天失效**（詳見 §3 訂正框）：
計畫書寫「IT-dept MEMORY.md 34 條超標」，實測是 **0 條**——判準同日改成「索引列只算 hook 句」。
**這是本檔第三次因為「去讀那個檔／去量那個數」而推翻推理結論**：
第一次 Round 1 質疑 MEMORY.md 不是 always-loaded；第二次 C-9 推翻 Round 2 審查者對 120 字的判斷；
這次推翻的是 **C-9 自己**。三次的共同形狀是——**推理得出的數字撐不過一次實際量測**，
而且三次都發生在同一個檔上。

**沒有跑第三輪對抗式覆核**：v5 新增的五項（P-8～P-12）中，P-8／P-9 有當日的實證資料、
P-10／P-12 是既有債務的償還、P-11 是編排器。真正需要覆核的是 C-11～C-13 三項分岔，
而那要 user 先拍板才有東西可審。⚠ **§7 末的 `ADVERSARIAL_REVIEW_PASSED` sha256 已因本次改版失效**，
v5 動工前應重跑 `/adversarial-review`（PR-1 閘門：M 級＋Design 收尾要標「待審核」，本檔第 3 行已是）。

---

### v4（2026-08-13·計畫書覆閱·補洞＋一個事實修正）

user 要求確認計畫書，我逐節覆閱時自己抓到三件事，逐項拍板（見 §3 的 C-8～C-10）：

| # | 問題 | 處置 |
|---|---|---|
| C-8 | **P-4 沒有任何驗證項**，而它正是 user 三個需求之一（專案專屬建議）。`/design-spec` 的完成判準是「每項都答得出怎麼證明它會紅」——P-4 連驗證項都沒有，是規格的洞 | 新增 **V-9**，用 git 舊版當 fixture（IT-department 的規則搬遷有 ground truth）。**紅燈條件雙向**：指錯要紅、該指沒指也要紅 |
| C-9 | P-3b 把 MEMORY.md 排除在 120 字判準外 —— **沿用了 Round 2 審查者的錯誤判斷** | **事實推翻**：`MEMORY.md` 檔頭自己寫著「一行 ≤~120 字」，與 CLAUDE.md §8 同一個數字。實測 34／10 條超標、最長 620 字。**改成照樣套 120 字**，不另訂判準 |
| C-10 | 只瘦 AI-Projects，但更大的那份 MEMORY.md（25 KB）在 IT-department | 新增 **P-7b**（兩份一起瘦）＋ **V-10**（驗 topic 檔沒失聯）。並寫明**瘦 MEMORY.md 不可套 CLAUDE.md 那一招**：它本身就是索引，刪行＝topic 檔失聯且不會報錯 |

**C-9 值得記一筆**：這是**第二次**「審查者說得很有道理、但去讀那個檔的檔頭就會發現不對」。
第一次是 Round 1 質疑 MEMORY.md 不是 always-loaded（判準其實是 system prompt 的
`claudeMd` 區塊）。**兩次都是關於同一個檔，兩次都是靠實際去看而不是靠推理解決的。**

**沒有再跑第三輪覆核的理由**：這三項都是前兩輪的延伸（補驗證、修判準、擴對象），
不是新設計；且 C-9 是事實查證的結果，再找人推理一輪也推不出檔頭寫了什麼。
收斂理由仍是 `/adversarial-review` 步驟 5 的**第二種**：剩下的分歧要跑起來才知道。

---

### v3（2026-08-13·Round 2 後**縮範圍**·user 拍板三項）

**這一輪的結論不是「再修 15 個 bug」，是「範圍太大了」。** Round 2 的四個致命項裡
有三個來自同一個決定——讓這支工具從唯讀變成寫入者。而已補的成本量測顯示效益是
**$24/月 ＋ 規則變得找得到**，這個比例不支持一個要寫三個檔、要遷移、要鎖的機制。

| 拍板項 | user 決定 | 連帶效果 |
|---|---|---|
| **範圍** | **縮成唯讀量測＋報告** | 🔒 只寫自己的兩個檔。**R2-6／R2-7／R2-8 直接消失**（不再有「寫哪張表」「落誰家」「append 疲勞」），R2-15 只剩自己檔案的保留上限 |
| **AI-Projects §6** | **先修形狀再搬** | V-7 改逐條對帳；表格要先改成看板認得的四欄；另立 V-8 收「不是待辦的那一半」 |
| **gen_layers 三個字面值** | **不修，登記成債** | 見下方「未償債務」——**不藏在驗證表的附註裡** |

**仍要修的（v3 已寫進 §4／§5）**：R2-1（V-7 判準）／R2-2（`>0` 紀律）／R2-3（P-0 補錨）／
R2-4（索引型檔另訂判準）／R2-5（schema 遷移）／R2-9（SKILL.md 同批改參數形狀）／
R2-10（`__global__` 單線）／R2-12（mangle 寫成有測試的函式）／R2-13（執行順序）／R2-14（V-8）。

#### 未償債務（R2-11·**這一項刻意寫在正文而不是附註**）

`gen_layers.py` 自己寫死三個字面值：`PROJECT_DIR = Path(r"D:\IT-department\.claude")`
（L44）、`EXTRA_PROJECTS = [Path(r"D:\AI-Projects")]`（L72）、`SCAN_ROOT = Path("D:\\")`（L73）。

**所以 `UNIVERSAL_HARNESS_PLAN` 的 U-1 在本輪之後仍未達成**——`check_bloat.py` 的
`grep = 0` 只代表字面值移到了一個 import 之外。本輪不修的理由：看板六支產生器都依賴
`survey_projects()`，而 D-2（設定檔格式）尚未拍板。
⚠ **V-1 的綠燈不得被讀成 U-1 達成。** 這條同步登記到 `UNIVERSAL_HARNESS_PLAN` D-3。

同型的第二筆：`capability_checks._p_on_demand`（L99）也寫死 `d--IT-department`——
§1.1 原本引用它當「正確做法的範本」是**錯的**，它不是推導而是寫死。且
`~\.claude\projects\` 下同時存在 `d--AI-Projects`（小寫 d）與
`D--AI-Projects-codebase-health-dashboard`（**大寫 D**）→ mangle 保留啟動時 cwd 的
大小寫，**不是可從 `Path` 物件算出的確定函式**。P-2 的做法改成：寫一支有測試的
mangle 函式，找不到對應目錄時**列出所有候選要求人確認**，不靜默回「無」。

---

### Round 2 發現（2026-08-13·**已於 v3 處置，保留原文供回溯**）

同一組審查者設定，帶上 Round 1 的發現與 v2 全文。**15 個新發現，多數實跑查證**。
關鍵在於：**v2 的修法自己引入了新的靜默失效**，而且有一整類兩輪都沒人看。

| # | 發現 | 嚴重度 |
|---|---|---|
| R2-1 | **V-7 防不到它要防的事**。實跑 `gen_todos` 四個解析器對 AI-Projects §6：**全部回 0**（表格是三欄而解析器要 ≥4 欄且表頭須 `^\|\s*項目`；狀態符號 `🔲🔴⛔❔` 不在 `_OPEN_EMOJI` 白名單；不是破折號 bullet）。但該專案**其他** `.md` 有 4 項命中 → V-7 的「命中數 > 0」綠燈 → 閘門解除 → §6 的 A～O 十五條**一條都沒進看板**，6,355 字照樣蒸發 | **致命** |
| R2-2 | **V-7 用的正是計畫書自己判為「不可證偽」的判準**。V-3 那格白紙黑字寫「『回傳非空』是不可證偽的」，V-7 卻用「> 0」——一輪內對兩個驗證項套了互相矛盾的標準 | 高 |
| R2-3 | **`<!-- rules-section -->` 錨三份 CLAUDE.md 一個都沒有**（實測 grep 皆 0）。P-3b 說「找不到錨就不做條目層」→ 首次跑條目層對**所有檔**失效 → 永遠 exit 0。而現行版本實跑會 exit 1（抓到一條 230 字新增超標）。**這是既有能力倒退**，且 `_p_anti_bloat()` 只看檔案存在 → 看板仍顯示 ✔ | **致命** |
| R2-4 | P-3 退路（`##`≤1 → 退條目層）與 P-3b 退路（無錨 → 不做條目層）**疊起來**，AI-Projects 的 MEMORY.md 兩層都不做 → 又是 P-3 要防的空表 | 高 |
| R2-5 | **舊 `bloat_snapshot.json` 遷移完全沒寫**。舊檔是**合法 JSON**（扁平 63 條），走不到 `exit 2` → 新版三元組 key 查舊扁平 dict 全部 miss → **63 條全報成「新增超標」**，而升級後第一次收工正是最不該噴假警報的一次 | **致命** |
| R2-6 | P-6 沒說寫進 `PENDING_VERIFY.md` 的**哪一張表**。該檔有兩張表，且 62／71 行兩節命中 `gen_todos._NOT_TODO_SECTION`（`不做`／`範例`／`已完成` 等詞）→ 落錯位置會被**靜默丟掉**，而 V-5「檔案多一列」仍綠 —— 與 A-1 同一個病換地方 | 高 |
| R2-7 | **AI-Projects 沒有 `PENDING_VERIFY.md`**，且 `gen_todos.collect()` 的 scope 是檔案所在專案 → 它的膨脹會掛在 IT-department 名下。P-6b 寫了觸發點邊界，沒回答「**結果落在誰家**」 | 高 |
| R2-8 | **該檔已 80,333 B／89 列，而 P-6 沒有去重規則**（P-5 對 history 有，P-6 對它一字未提）。每次收工 append 一列同一件事，且「誰跑」欄含「收工時/下一輪」會命中 `_P_HIGH` → 全被推到看板待辦最上面 ＝ C-2 否決 (c) 時列的那個 WARN 疲勞，換到看板上 | 高 |
| R2-9 | `--write-snapshot` 強制帶 `--project` 後，**收工 SOP 逐字照做會拒跑**（SKILL.md 第 69 行沒有那個參數）→ 步驟 3 完成判準永遠達不到 | 中 |
| R2-10 | **全域 `CLAUDE.md` 屬於哪個專案沒定義**。它是所有專案共用同一實體，但 P-5 的 key 是 `(project, file)` → 同一實體開 N 條線、合計被灌水 N 次、`--write-snapshot` 要跑 N 次才接受得完 | 中 |
| R2-11 | **V-1 的逃生口從沒被兌現**。「若決定不修 gen_layers 就明寫未償債務」——全文搜遍，§3 分岔表、§4、§6、§7 都沒有那一項。Execute 的人會選成本低的那條，U-1 被記成達成而三個字面值原封不動 | 高 |
| R2-12 | **我引用當「正確做法」的 `_p_on_demand` 自己就寫死專案名**（第 99 行 `d--IT-department`）。且 `~\.claude\projects\` 下同時有 `d--AI-Projects`（小寫）與 `D--AI-Projects-codebase-health-dashboard`（**大寫 D**）→ mangle 保留 cwd 大小寫，不是可推導的確定函式 | 高 |
| R2-13 | **§4 沒有執行順序**，至少三對顛倒就出事。最陰險的是「**P-5 先跑一次留基準 → P-7 壓 AI-Projects**」：顛倒的話歷史第一筆就是壓縮後的值，**C-4 選 (a) 的旗艦實例永遠證明不了目標 3**，而且沒有紅燈 | 高 |
| R2-14 | **§6 有將近一半根本不是待辦**（發版紀錄 SHA256／manifest、離線包 153 檔 4.63 GB 規格與三條 ⚠ 教訓）。四類解析器沒一類收得了 → 就算 A～O 全進看板，這幾千字仍無處可去 | 中高 |
| R2-15 | **兩輪都沒看的一整類：這支工具從唯讀變成寫入者**。改版後它在收工寫三個檔，而 §4／§5 對「寫入」一字未提——沒有原子寫、沒有鎖、沒有保留策略。而 `gen_todos.line_sha` 的 docstring 逐字寫「**多 session 並行是這個環境的常態**」、看板的「完成」鈕靠行號＋`line_sha` 寫回同一個檔、`refresh_dashboard` 盯著它每 10 秒重生。另 `bloat_history.jsonl` 是**第二個無上限 append log**——第一個（`_entry_probe`）此刻就掛在待驗表上未結案，原文「會無上限長（463 筆／20KB，每 session +11KB）」 | **致命** |

**審查者刻意不當 finding 的那一句**（列在「我查不到的」）：
> 兩輪都在挑實作與驗證，沒有人問過「44 KB 的 always-loaded 檔，每則付的實際代價是多少，
> 跟建這套機制的成本比起來如何」。§1 有 bytes 但沒換算成成本。

**已補量測**（2026-08-13，從 transcript 實撈，非估算）：每 session context 基底中位數
IT-department **30,731 tokens**／AI-Projects **21,869 tokens**（含 system prompt）。
以 opus `$5/M`、cache write 1h ×2、cache read ×0.1 計：把 AI-Projects 壓掉 8k tokens，
一天 100 則／5 session 約省 **$0.8/天 ≈ $24/月**。
**真實但不驚人 —— 瘦身的主要理由不是錢，是規則太多會找不到、讀不完。**

---

### v2（2026-08-13·對抗式覆核 Round 1）

審查者：Claude Code `Plan` subagent／opus／effort high（`reviewer_config.json` 指定的就是
這個，**沒有換人**；Codex 未安裝但設定本來也沒選它）。

**這一輪最有價值的發現是「整個掛載點不可行」**——v1 選的通道在收工這個時點結構上
100% 靜默失效，而且是我自己憑印象寫的、沒有去讀 `dispatch.py`。

| # | 審查意見 | 處置 |
|---|---|---|
| A-1 | `pending_warn` 綁 session_id＋TTL 90 分＋靠下一次 `UserPromptSubmit`；離線腳本拿不到 session_id，收工又是 session 最後一件事 → 便箋 100% 投不到 | **接受**。P-6 換成寫 `PENDING_VERIFY.md`（session 無關）＋完成判準要求貼數字。V-5 連帶改寫 |
| A-2 | 掛步驟 3.5 錯——它的標題是「只有動過 `D:\.ai-harness` 才跑」 | **接受**。改掛無條件執行的步驟 3。「只改專案檔沒碰 harness」正是最該跑的那天 |
| A-3 | `/shougong` 與 dispatch hook 只存在 IT-department，AI-Projects `dispatchWired: False` | **接受**。新增 P-6b 明寫觸發點邊界——這是刻意接受的限制，但必須寫在畫面上 |
| A-4 | SKILL.md 步驟 3 完成判準實質是閘門，與 C-2 否決的 (c) 打架 | **接受**。exit 1 收窄成「只對 cwd 所屬專案」，SKILL.md 同批改 |
| B-1 | MEMORY.md 路徑假設兩專案都錯；junction 方向是**由外指向內**；AI-Projects 那份根本不在專案磁碟區 | **接受**。§1.1 整表重寫，P-2 改從 `~\.claude\projects\d--<mangled>\memory\` 推導 |
| B-2 | `gen_todos` 對 AI-Projects 命中 0（沒有 `PROJECT_CONTEXT.md`），P-7 刪 §6 會蒸發 6,355 字 | **接受**。P-7 加前置條件；另立 V-7 專驗「整節刪除」 |
| B-3 | `PROJECT_CONTEXT.md` 不是 always-loaded，是角色開工才 Read；算進合計會虛胖，對它下瘦身建議會砍掉五支角色的設定 | **接受**。P-2 改成兩欄，合計只算「每則都付」那欄 |
| C-1 | MEMORY.md 沒有 `##` 標題（AI-Projects 那份 0 個、IT-dept 2 個）→ 章節排名產出空表 | **接受**。P-3 加退路：`##` ≤1 時退回條目層並明講 |
| C-2 | 拿掉 `_SECTION_START` 後條目層**沒有定義掃描範圍**；全檔掃首次噴 93 條假警報，只掃最大節則 P-7 壓完後**最大節翻轉**、規則節退出監控 | **接受**（本輪最深的一個）。新增 P-3b：規則節由檔案自己宣告錨點，MEMORY.md 排除 120 字判準 |
| D-1 | 快照 flat key 跨檔碰撞，實測 369 條已有 1 組，dict 推導靜默後蓋前 | **接受**。key 改三元組，重複時報錯不覆蓋 |
| D-2 | `--write-snapshot` 全域覆蓋，接受 A 專案會順手洗掉 B 專案未處理的成長 | **接受**。強制帶 `--project`，缺參數拒跑 |
| D-3 | `total_bytes` 走 text mode，CRLF 專案系統性少 314 B；`.gitattributes` 明寫 `*.md text eol=crlf` | **接受**。改 `getsize()`。這也解釋了 §1.1 的 43.7 vs 工具的 43.4 |
| D-4 | 反彈定義漏掉「降→沒動→回升」；「接近舊值」無門檻；**V-4 的兩筆表達不出「上次降過」** | **接受**。改與歷史最低點比＋明訂百分比；V-4 改成三基準點＋negative case |
| E-1 | V-1 grep 範圍太窄，`gen_layers.py` 自己寫死三個常數 → 綠燈不代表 U-1 達成 | **接受**。V-1 擴及 import 的核心層檔，否則明寫未償債務 |
| E-2 | V-2 的不變式錯：`discover_projects()` 看的是 `.claude\` 不是 CLAUDE.md，等式會永久紅；變異只動分子 | **接受**。改成「沒有專案從報告消失」 |
| E-3 | V-3 不可證偽；綁 live 檔會被 P-7 推翻（最大節翻轉） | **接受**。改用合成 fixture 驗排名順序 |
| E-4 | V-6 自我矛盾（每一條 vs 抽 3 條）；對「刪除」不適用，最大改動落在驗證縫隙 | **接受**。V-6 改逐條機械對帳，另立 V-7 |
| F-1 | 動快照檔名會讓 `capability_checks._p_anti_bloat()` 靜默翻 False | **接受**。保留 `bloat_snapshot.json` 檔名 |
| （審查者「查不到的」）| MEMORY.md 是否真的 always-loaded 存疑——沒有 `@import`，靠 CLAUDE.md §3 用自然語言叫模型讀 | **反駁**。判準不是有沒有 `@import`，是**它在不在 system prompt 的 `claudeMd` 區塊**。實測本 session 該區塊有三份：全域 CLAUDE.md、專案 CLAUDE.md、`…\memory\MEMORY.md`（標為 user's auto-memory）。審查者是 Plan subagent、看不到主 session 的 context 組成，所以查不到——**但它把這一點誠實列在「我查不到的」而不是當成 finding，這正是那一欄的用途** |

<!-- ADVERSARIAL_REVIEW_SKIP sha256=7b65f25db15fc6e6290e3882a82855623604b6e6dc902a131a3a0dd3581f4a90: Round 4 已跑完（13 發現）。累計四輪 47 個發現、0 反駁；Round 3 的 16 項與 v7 自驗的 3 項全修，Round 4 已修 R4-A/B/C/D/G/H。**仍有 7 項待修**：R4-E（比例判準在 universe ≥ 11 時比例路徑永遠用不到、≤ 10 時指路句變假陽性）／R4-F（`* `/`+ ` 短條目黏成假散文塊、head 指到不可刪的錨）／R4-I（--file 不給 --project 靜默關 P-9、__global__ 永遠沒 P-9）／R4-J/K/L/M（低）。失效情境逐項寫在 §7 v9 表。**刻意不用 PASSED**：審查者明確反對，理由是「Round 4 又抓到 3 個修法自己引入的缺陷，同一形狀連續三輪」——現在蓋章等於消耗這個機制唯一的價值。下次開工先修 R4-E/F/I 再跑 Round 5。 -->
