# 通用化 Harness 計畫書（地基化）

> **2026-08-05 立案。這份是 harness 的方向文件——之後每一個設計決策都以本檔 §0 的目標為準。**
> 現況記錄看 `HARNESS_ROLE_ARCH_PLAN.md`（角色架構）與 `HARNESS_PLAN.md`（閘門工程），
> 那兩份講「做了什麼」，這份講「要往哪去、什麼不准做」。

## §0 目標（user 2026-08-05 定）

**把 harness 做成通用版，可以陸續分發給不同部門使用，當作公司的地基，用來增加公司效率。**

三個推論，之後的設計爭議都回這裡判：

1. **「地基」意味著它不能只服務 IT 資產平台**。任何只在這個專案成立的東西，都不屬於核心層。
2. **「分發給不同部門」意味著安裝成本要低**。一個部門要用它，不該需要我在場、也不該需要讀完
   五份計畫書。裝設流程失敗一次，那個部門就不會有第二次。
3. **「增加公司效率」是驗收標準，不是願景**。一項功能如果只讓 harness 自己更完整、
   但沒有讓使用它的人少犯一次錯或少花一次時間，它就不該優先做。

**2026-09-03 新增第二個目標：作者本人換電腦時能重建這套 harness。**
它與上面的「分發給不同部門」**不是同一件事**，混在一起會讓每個決定都錯一邊
（對抗式覆核第 1 輪 F-c 抓到的）：部門要的是「不還原任何個人設定」，
換電腦要的是「連個人設定一起還原並重寫路徑」。**兩者用安裝剖面分開，不用同一條路徑。**
D-1 定案（§4）只實作 `same-person-new-pc` 這一份；`department` 那一份綁 D-4。

## §1 現況債務（2026-08-05 實測，不是估計）

| 債務 | 量測 | 為什麼擋住通用化 |
|---|---|---|
| **硬編碼專案路徑** | **66 處**字面值，橫跨 **20 個檔案**（`dashboard/`・`eval/`・`hooks/`・`reviewer/`・`rulefile/`・`tests/`） | 換一個部門就要逐檔改，而漏改的地方會**靜默指向錯的專案**——不會報錯，只會產出別人的數字 |
| **通用紀律與業務規則混在同一層** | 8 條規則裡 4 條通用（AWC-1／ENC-1／PR-1／BUDGET-1）、4 條本專案專屬（DB-1 雙改部署／R1 預設值遷移／R3 ops 備份／R4 server DB_PATH） | 別的部門拿到會被自己用不到的規則擋下來，然後把整個 hook 關掉 |
| **角色是專案專屬的** | 5 個自建角色裡，`雙改檢核員`（DEV/PROD 雙改）、`project-auditor`（IT 資產平台文件）**只在這個專案有意義**；`查詢員`／`美編人員`／`harness-auditor` 通用 | 同上——分不清哪些該帶走、哪些該重寫 |
| **看板資料源綁單一專案** | ~~`gen_layers` 掃專案清單~~ **✅ 2026-08-13 已解**（`harness.config.json` 兩層設定·見 D-2）。**`subagent_stats.PROJECT_DIR` 寫死 `d--IT-department` 仍在**，且同日實掃發現核心層**共 7 檔 11 處**同型債（`capability_checks`／`check_freshness`／`gen_cost_panel`／`gen_roles_topology`／`refresh_dashboard`／`subagent_stats`／`budget1_daily_usage`）——**比原本登記的多** | 多部門並用時看板只看得到其中一個。⚠ 現有 `tests/test_harness_config.py::test_u1_debt_does_not_grow` 把這 11 處**凍結成台帳只准變少**，新增即紅 |
| **安裝流程沒被驗過** | `task-memory-model\scripts\bootstrap.ps1` 只在自己這台跑過 | 「新機器首次」這條路徑目前是**未驗狀態**（見 §4 D-4） |

> **2026-08-16 補**：`eval\` 底下有 **4 檔 9 處**同型硬編碼（`check_structure.py`4／`check_contracts.py`3／
> `check_acceptance.py`1／`run_triggers.py`1），副作用是**全域層 2 支 skill 從未被任何一層 eval 檢查過**。
> 這**不是台帳漏登**——`tests\test_harness_config.py:394` 的 `_DEBT_SCAN_SKIP` 明文含 `"eval"`，
> `:406` 註明「性質不同（換部門時它們本來就要跟著換），另案處理」。
> **本補註登記的就是那個「另案」開工了**：還債規格在 `SKILL_EVAL_PLAN.md` §9（W-1／S-4），
> 完成後 `eval` 移出 `_DEBT_SCAN_SKIP`，改由既有的 AST 檢查長期納管。
>
> ⚠ 連帶提醒：`test_u1_debt_does_not_grow` 的保護範圍是「`_KNOWN_U1_DEBT` 台帳 ∩ 未被 skip 的目錄」。
> 被 skip 的目錄（`tests`／`eval`／`state`／`參考`／`SkillViewer`）**連 `repaid` 提醒都不會印**——
> 往那些目錄新增硬編碼不會有任何紅燈。這是刻意的，但要記得它刻意到什麼程度。

## §2 分層原則（核心決策）

harness 切成**三層**，**判準是一句話：換一個部門還成立嗎？**

> **2026-08-05 修正（user 指出）**：初版只有兩層，並把 `project-auditor`／`雙改檢核員`
> 標成專案層，理由是「稽核對象是本平台」。**那是錯的** —— 混淆了兩件事：
>
> | | 是什麼 | 該在哪 |
> |---|---|---|
> | 角色的**能力與工作方法** | 「以程式碼為準、文件是被稽核對象」「先量再改、改完重截一張」 | **全域**——稽核員走到哪都是稽核員 |
> | 角色的**作用對象** | 稽核哪份計畫書、比對哪兩個目錄、讀哪個規則檔 | **專案設定**——注入，不寫死在角色身上 |
>
> 這跟 U-1（不寫死專案路徑）是同一條原則，初版只套用在程式碼上，漏了角色檔。
> user 的說法是對的：**辦公室裡的技術、美編、稽核是跨專案的人員，讀不同專案的資料後執行任何專案。**

| | 全域層 | 核心層 | 專案層 |
|---|---|---|---|
| 放哪 | `~\.claude\`（Claude **自動載入**，跨所有專案） | `D:\Patrick-AI\.ai-harness\`（共用元件，被專案層用絕對路徑引用） | `<repo>\.claude\` |
| 內容 | **角色**（能力＋工作方法）＋ **通用 skill**（2026-08-21 起實裝，原欄位寫「未來的」） | hook 分派機制、event log、看板產生器、eval、通用規則（AWC-1／ENC-1／PR-1／BUDGET-1） | 業務規則（DB-1／R1／R3／R4）、**`PROJECT_CONTEXT.md`**（角色的作用對象）、該專案的 settings |
| 誰維護 | 我（跨部門共用） | 我（改動要想相容性） | 該部門自己 |
| 現況 | 角色 ＋ 通用 skill | hook／產生器／eval 等元件 | 規則 ＋ `PROJECT_CONTEXT.md` |

> **這一列刻意不寫數字**（2026-08-22 改）。原本寫「5 支（角色）／28 支／4 支（規則）」，
> 到 8/22 實測已經是 **6 角色 ＋ 8 skill ／ ── ／ 9 個規則**——三格全錯，而且**沒有任何守門會叫**。
> 架構表要傳達的是「每一層放什麼形狀的東西」，數量是查得到的，寫進來只會爛掉。
> **怎麼重數**（在 `D:\Patrick-AI\.ai-harness`）：
> `ls agents/*.md | wc -l`（全域角色）／`ls -d skills/*/ | grep -v _meta | wc -l`（全域 skill）／
> `ls <repo>/.claude/rules/*.md | wc -l`（專案規則）。
>
> ⚠ **全域層 skill 的 eval 涵蓋**：`config.py:SKILL_DIRS` 已含 `GLOBAL_SKILLS_DIR`，
> 但 **`config.py` 到 8/22 仍是未追蹤檔**（`git status` 顯示 `??`）⇒ **新 clone 拿不到它**。
> 這件事在別的 session 手上（本計畫的 A7／分岔②），此處只記錄，不動。

**`PROJECT_CONTEXT.md` 是新部門導入時唯一必填的檔案**——角色一支都不用改。
它回答的是「這個專案的雙目錄在哪、計畫書在哪、規則檔在哪、驗證慣例是什麼」。

**這一層的硬規則**（新增東西時逐條對照）：

- **全域層與核心層都不准出現任何專案名稱字面值**。要路徑就從設定讀，讀不到就
  **拒跑並說清楚缺什麼**——不准 fallback 到某個預設專案，那會讓「設定漏了」偽裝成「一切正常」。
- **新增規則前先答「換部門還成立嗎」**。成立→核心層；不成立→專案層。**答不出來的一律當專案層**，
  因為把專案規則誤放核心層的代價（別的部門被莫名其妙擋下）遠大於反過來。
- **角色一律是全域層**。發現某個角色「只在這個專案有意義」時，**那不是把它降成專案層的理由，
  是它的作用對象該抽進 `PROJECT_CONTEXT.md`**。角色正文裡出現具體的目錄名、檔名、
  模組名、版號欄位 → 全部搬走，改成「開工先讀 PROJECT_CONTEXT.md 的 X 節」。
- **角色讀不到設定時要停下來回報，不要猜**。猜對了是運氣，猜錯了它會回報一個看起來
  很合理的假結果（例如「兩端一致」的假 PASS），而那份判定會被拿去做決定。
- **核心層的測試不准依賴這個專案的實際檔案**。`tests/` 現在有多支讀 `D:\IT-department`
  真實檔案的，那些是專案層測試。

## §3 設計準則（每條都有可檢查的完成判準）

寫新功能前對一遍。**判準是「怎麼證明它做到了」，不是「有沒有想到」。**

| # | 準則 | 完成判準 |
|---|---|---|
| U-1 | **不寫死專案路徑** | 新增/修改的核心層檔案 `grep -c "IT-department"` 為 0；路徑一律來自設定檔或參數 |
| U-2 | **設定缺漏要拒跑，不要猜** | 移掉設定檔跑一次 → 必須明確報錯說缺什麼；不得產出空表、不得 fallback 到預設專案 |
| U-3 | **新規則先分層** | 規則檔 docstring 第一行寫明「核心層／專案層」與理由（換部門還成立嗎） |
| U-4 | **安裝流程要能被別人跑完** | **兩個剖面兩條判準，不可只留一條**（2026-09-03 拆·D-1 覆核第 2 輪發現 6）：<br>`department`（**尚未實作**·綁 D-4）＝在沒有既有設定的環境跑一次裝設，全程不需要問我；<br>`same-person-new-pc`（**本次要做的**）＝**兩把尺都要過**（2026-09-03 改·第 3 輪發現 6：原本只寫「§4 的八條探針全綠」，而探針清單由同一份定案增刪 ⇒ **循環定義**，探針有洞時準則層抓不到）：<br>① **機器可跑的**：舊機 live 設定帶過來、路徑重寫後，§4 定案第 2 點的探針**沒有 `FAIL`、也沒有 `UNVERIFIED`**（＝`wiring_probe.verdict()` 回 0；`SKIP` 不擋）——2026-09-03 改·第 5 輪發現 2：原本寫「探針全綠」，而四態表明寫「`SKIP` 不是綠、但不擋」⇒ 準則層與探針層會對同一台機器給相反判定；<br>② **不依賴探針的外部觀察**：在新機開一則新對話，記憶讀得到、閘門在 `state\` 留下心跳、看板不是空白、且一次 commit 真的推進新機鏡像。<br>兩條共同：每一步失敗都有可讀的錯誤訊息 |
| U-5 | **功能要對得上「增加效率」** | 說得出「它讓誰少犯哪一種錯／少花哪一段時間」，說不出來就不排優先 |
| U-6 | **文件分兩種讀者** | 使用者要讀的（怎麼裝、怎麼用）與維護者要讀的（為什麼這樣設計）分開；使用者文件裡不出現踩雷史 |
| U-7 | **不破壞既有專案** | 每次通用化改動後，本專案的回歸網（目前 302 項）仍全綠——**地基化不是重寫的藉口** |

## §4 待決分岔（逐項討論，同意才做）

| # | 分岔 | 選項 | 傾向 |
|---|---|---|---|
| D-1 | 分發形式 | ~~(a) 複製資料夾＋裝設腳本 (b) git clone (c) 可安裝套件~~ **三選一已作廢**（框架有缺陷·見表後） | ✅ **已拍板**（2026-09-03）：**git 當內容真相 ＋ fail-closed 接線器 ＋ 只做「本人換電腦」一份安裝剖面**·見表後 |
| D-2 | 設定檔長什麼樣 | (a) 一個 `harness.config.json` 放專案根 (b) 環境變數 (c) 沿用 `.claude/settings.json` 加欄位 | **(a)** ✅ **已拍板·但拆成兩層**（2026-08-13·見表後） |
| D-3 | 先通用化哪一塊 | (a) 先拆路徑（66 處） (b) 先拆規則分層 (c) 先做裝設流程 | **(a)**：路徑不拆，其餘兩項做完仍然搬不動 |
| D-4 | 第一個試點部門 | 待定 | 需要一個**真的會用**的部門，而不是我自己再開一個資料夾假裝 |
| D-5 | 多部門的資料要不要匯總 | (a) 各部門各自看板 (b) 加一個跨部門總覽 | 先 **(a)**：跨部門總覽要先有兩個部門在用才有意義 |

### D-1 定案（2026-09-03·user 拍板·對抗式覆核第 1 輪的產物）

**原本的三選一作廢，理由不是選錯，是題目問錯。** (a)(b)(c) 三個都只回答
「內容怎麼運」，真正會靜默失敗的是「這台機器怎麼接線」，而那一軸不在任何一個選項裡：

| 軸 | (a) 複製 | (b) clone | (c) 套件 | 判定 |
|---|---|---|---|---|
| 內容怎麼運 | 複製資料夾 | `git clone` | wheel／plugin | 三個都回答了 |
| **這台機器怎麼接線** | — | — | — | **三個都沒回答，而它是唯一會靜默失敗的那一軸** |
| 之後怎麼更新 | 再拷一次 | `git pull` | `pip install -U` | 附屬於第一軸 |

⇒ **運輸成功會被讀成安裝成功**。這正是 U-4 要擋、而 D-1 舊寫法擋不到的形狀。

**拍板內容（三件，缺一不可）**

1. **內容走 git**——原 (b) 對的那半句留下。
   ⚠ **但「走 git」不等於有來源**（第 2 輪發現 3）：本機唯一的 remote 叫 `backup`，
   指向這台機器 C 槽的 bare repo。**換機那台沒有可 clone 的 URL。**
   ⇒ `same-person-new-pc` 的取得來源明定為**把舊機那顆 bare 鏡像整個搬過去**
   （隨身碟或網路複製），新機從它 clone。接線器裝完 `post-commit` 之後
   **必須在新機建一顆新的 bare 鏡像並加 `backup` remote**——
   `post-commit` 第一件事就是「沒有叫 `backup` 的 remote 就 exit 0」，
   **裝了而沒有 remote＝裝了等於沒裝，而且靜默**。
2. **一支冪等、fail-closed 的本機接線器**（現在完全不存在，見處置 A1）。

   ⚠ **這一節是接線器規格的唯一真相**（2026-09-03 收斂·第 3 輪發現 1）。
   下面三張表（要做的事／設定檔怎麼處理／探針）之外，**任何處置格、待辦表、
   交接檔都不得再寫修法**，只能寫「判定 ＋ 指向這裡哪一列」。
   第 2、3 輪反覆被打穿的就是這個形狀：**同一件事有兩個地方在寫，修一處另一處就長回來**。

   **「單一真相」講的是修法，不是觀察**（第 3 輪發現 6 的配套，先寫在這裡免得被讀成矛盾）：
   接線器要做什麼只寫這一處；**驗收則允許也需要多把尺**——探針是機器可跑的那把，
   U-4 另有一把不依賴探針的外部觀察（見 §3）。修法單一、觀察多重，兩者不衝突。

   **接線器要做的事**（只做機器局部，逐項都要冪等）：

   | # | 動作 | 對應探針 |
   |---|---|---|
   | W1 | 建／修 `~\.claude\agents`、`~\.claude\skills` 的 junction | P1、P7 |
   | W2 | `gen_layers.py --init` 產 `harness.config.json` | P3 |
   | W3 | 確保 `state\` 存在且可寫 | P4 |
   | W4 | 安裝 `tools/githooks/post-commit` | **P9**（2026-09-03 訂正·第 4 輪發現 2：原本寫 P2 是錯的——P2 只走 live `settings.json` 的 Claude hook，`.git\hooks\post-commit` 根本不在那條路徑上） |
   | W5 | **在新機建一顆 bare 鏡像並加 `backup` remote** | P9 |
   | W6 | 同步 `cursor-agents\` → `~\.cursor\agents\` | P8 |
   | W7 | 把 `global/CLAUDE.md` restore 到 live | P6 |
   | W8 | **把 `global/output-styles\` restore 到 live**（第 3 輪發現 2 補） | P10 |
   | W9 | **重量測 skill-watch 基準，不沿用舊機那份**（第 3 輪發現 1 改寫，見下） | P11 |
   | W10 | 帶舊機 live `settings.json` 過來**逐欄改寫路徑** | P2、P5 |

   **W9 不在這裡定做法——這件事早就定案了**（2026-09-03 訂正·**跨文件的第三真相**）：

   `SkillViewer/platform_skills.json` 現在在版控裡（`git ls-files` 命中），
   所以第 2 輪寫在 B5 那格的「刪掉基準檔重量測」**做不到**（刪了 `git restore` 會回來、
   重量測後還會經 `backup` 蓋掉舊機那份）。但**接著寫的「整個基準檔加進 `.gitignore`」
   也是錯的**——那個檔同時是 **SkillViewer 的顯示清冊**（`skills[]` 現有 54 支，
   `SkillViewer.ps1` 直接讀它），整檔移出版控會讓新機的 SkillViewer 沒有資料。

   ⇒ **正解在 `SKILL_WATCH_PLAN.md` 的 W-5 訂正**（2026-08-23·票 10 推翻 W-5 字面；
   2026-09-04 訂正票號——原文誤寫「票 06」，那支票號指的是另一件事：三種初次體驗的預設）：
   **基準搬到 `state/skill_watch_baselines.json`（gitignore），清冊留在原檔原位。**
   它給的理由與本案第 3 輪發現 1 **一模一樣**——別部門 clone 下來第一次跑就對著
   我這台機器的快照比、撞收縮守衛，而程式建議的出口正是規格禁止的 `--force`。

   ✅ **2026-09-04 已實作**（原文寫「那個決定至今沒實作」，2026-09-03 實查為真，
   已於次日處理）。搬家走一次性冪等工具 `tools/migrate_skill_baselines.py`；
   `baselines` 與 `officialCrossCheck` 兩個鍵都搬——後者沒被票文點名，但它記的是
   「上次跟官方文件比的差集」，同樣是本機快照，留在版控裡別部門會拿到我這台的
   `missingLocally`，而判定看的是「鍵在不在」（R3-2）⇒ **鍵在但內容是別人的**
   正好落進那條註解說的洞。P11 四條判準現在全綠。
   ⇒ **W9 ＝ 去把票 10 實作完**，不是在這裡發明第三種做法。
   **本節不重述修法**（同本節開頭的唯一真相宣告）——那是 `SKILL_WATCH_PLAN` 的事。

   **設定檔怎麼處理，三種對象不要混**（第 2 輪發現 1 拆出前兩列；第 3 輪發現 4 補第三列）：

   | 對象 | 做法 |
   |---|---|
   | repo 的 `global/settings.json` | ❌ **禁止整檔還原**。它是舊絕對路徑的快照，還原＝把舊路徑裝進新機（原 B3） |
   | **舊機的 live `settings.json`** | ✅ **帶過來，逐欄改寫路徑**——hook `command`、`additionalDirectories`、permissions 裡的路徑。**改寫不掉的欄位要紅，不准靜默保留舊值** |
   | **新機已經有一份 live**（先裝 Claude 用了幾天才跑接線器） | ⛔ **拒跑並列出衝突項，不准自動選邊**。整檔蓋掉會吃掉新機這幾天的設定；「已有檔就跳過」會留下沒有絕對路徑的預設 hook。同理 `~\.claude\agents` 若已是**實體目錄**：Windows 無法在同名上建 junction ⇒ **拒跑並要求人先處理**，不得改成往實體目錄拷檔（那會讓 P1 的 `samefile` 與 P7 的「非空」互相打架）。`backup_global_config.py:137-149` 的 `_mixed_gate` 已經是這個態度（方向不一致就整批拒跑），接線器沿用 |

   §0 的第二個目標是「連個人設定一起還原並重寫路徑」，不是「不要還原」。

   **結束條件不是 exit 0，是探針「沒有 `FAIL`、也沒有 `UNVERIFIED`」**（第 2 輪發現 2
   從 3 條擴到 8 條；第 3 輪發現 2、3 再擴到 11 條並改嚴三條判準）。
   ⚠ 措辭 2026-09-03 改過（第 5 輪發現 2）：原本寫「探針全綠」，與下面四態表的
   「`SKIP` 不是綠、但不擋」互斥——`SKIP` 存在時探針會放行、而準則層讀「全綠」會過不了，
   同一件事兩套結束條件。**唯一的判定實作是 `wiring_probe.verdict()`**，這行只是它的白話：

   | # | 探針 | 判準——**「存在／非空」不算數的特別標** | 擋掉的失效形狀 |
   |---|---|---|---|
   | P1 | junction 真的指到 harness | `samefile(~\.claude\agents, <harness>\agents)`，skills 同理 | 斷掉／指舊路徑的 junction，畫面像「還沒建角色」 |
   | P2 | hook 打得到實體檔 | live 每條 hook command **裡的實體檔**（不是整段字串）`Test-Path` | hook 打空，對話正常但閘門一條都不跑 |
   | P3 | 設定指向真專案 | `harness.config.json` 存在且 `currentProject` 在本機是真目錄 | 看板掃到錯專案 |
   | P4 | 狀態目錄可寫 | hook 程式內部的 `STATE_DIR` 指向本機真目錄且**實際寫得進去** | 路徑對、狀態目錄錯 ⇒ 寫入失敗被吞（`hooks/dispatch.py:365-366` 是 `pass`），看板顯示「沒發生過」 |
   | P5 | 記憶目錄是**改寫的結果** | `additionalDirectories` 每一條存在、**非空**，且**逐條對得上舊機那份的改寫來源**——**只驗存在不算數**（第 3 輪發現 3：新機使用者名相同時 Claude 會自建空的 `projects\…\memory`，照樣綠）。⚠ **「對得上」的判準 2026-09-03 重寫過**（第 5 輪發現 4）：原本比「最後 3 段」，等於讓**路徑長度**決定哪一截是前綴，兩頭都錯——長路徑（`C:\Users\<名>\AppData\…\Startup` 共 9 段）使用者名落在比對範圍外，換成別人的帳號照樣吻合、而那資料夾每台機器都有且非空；短路徑（`D:\Patrick-AI\.ai-harness` 只有 3 段）碟符落在比對範圍內，合法改碟符反而紅。現在的判準是**改寫＝一組前綴替換一致地套用到每一條**，三件：① 每一對的共同尾段不得是 0 段 ② 每一條都要吻合**最一般**的那條適用規則（取最長規則＝每對套自己那條＝恆真檢查）③ 改寫後不得落在**別人的家目錄**底下（這條不需要 `--source`）。⚠ **能力上限寫在明處**：擋不掉「一致地錯」——整組都映到同一個不是本機、卻真的存在的帳號。要擋它得靠接線器留下對照表（W10） | 記憶整批失聯，畫面像「這個專案還沒有記憶」 |
   | P6 | `CLAUDE.md` 真的被 restore | live 與 `global/CLAUDE.md` **內容相同**（`filecmp.cmp`，`backup_global_config.py:92` 已有這個判法）——**非空不算數**（第 3 輪發現 3：新機自己寫的那份也非空） | 工作方式規則整份不生效，看不出來 |
   | P7 | 目錄真的列得出來 | agents／skills 目錄可列且非空 | 平台收緊 symlink 政策時整批消失（C2） |
   | P8 | Cursor 側**三態分開** | 已接上／裝了但沒接上／沒裝 Cursor，**三者不得混成同一個綠**：第三態印 `SKIP`，**`SKIP` 不計入綠、也不擋結束條件**（2026-09-03 訂正·第 5 輪發現 2：原寫「結束條件只數 `OK`」，那是 `SKIP` 拆成四態之前的寫法，會讓沒裝 Cursor 的機器永遠印不出「裝好了」）（第 3 輪發現 5：現有 `check_cursor_agents.py:90-94` 在 live 目錄不存在時 `return 0`，直接拿來當探針會讓「沒裝」冒充全綠）。**且 P8 不是一次性的**——`git pull` 更新 repo 後 live 複本不會跟著動，接線器要能在 pull 後重跑 | Cursor 派出去的是舊複本，沒有紅燈 |
   | P9 | **跨碟備份真的會發生** | 四條缺一不可：① `tools/githooks/post-commit` **已安裝到 `.git\hooks\` 且與 repo 版本逐位元相同**（W4；`.git\hooks\` 不進版控）② `git remote` 有 `backup` ③ 沒有 `state\mirror_sync_failed.txt` ④ 鏡像 HEAD 與本機相同。⚠ **刻意不真的 push**（2026-09-03 訂正·第 4 輪發現 3：原本寫「實際推一次驗證推得進去」，與同節實跑說明互斥）——**探針要唯讀、不製造副作用**，③④ 合起來是「最後一次推的結果」這個唯讀代理。代價寫在明處：**從未推過、也從未失敗時，沒有標記＝該項綠**，所以①才必要 | 裝了等於沒裝，而且靜默 |
   | P10 | **輸出風格帶得過來** | `~\.claude\output-styles\` 與 `global/output-styles\` 內容一致，且 `settings.json` 的 `outputStyle` 值在裡面找得到（第 3 輪發現 2：`backup_global_config.py:56-62` 把它與 `CLAUDE.md` 當同一級，接線器與探針原本只收 `CLAUDE.md`）。⚠ **`outputStyle` 這顆鍵不見時的分界 2026-09-03 訂正**（第 5 輪發現 5）：原本一律 `SKIP`，但「舊機設過、接線器漏帶」與「舊機本來就沒設」在本機這一份裡**長得一模一樣**，而前者正是加 P10 要擋的畫面。⇒ **「這台不適用」是需要舉證的主張，證據只能來自舊機那份**：舊機有這顆鍵、本機沒有＝`FAIL`；沒給 `--source`＝`UNVERIFIED`；舊機也沒有才是 `SKIP` | 對話看起來正常，風格靜默變回預設 |
   | P11 | **skill-watch 基準是本機量的** | **基準不在版控物裡**——`baselines` 已搬出 `SkillViewer/platform_skills.json`、住在 gitignore 的 `state/skill_watch_baselines.json`（見 W9 指向的票 10）；且其 `capturedAt` 晚於本次接線時間（＝新機重量測過）。⚠ **判準不是「整個 platform_skills.json 不在版控」**——那個檔還兼著 SkillViewer 的顯示清冊，本來就該留在版控。⚠ **加驗「執行期讀取路徑」**（2026-09-03·第 5 輪發現 6）：上面三條全是「檔案擺在哪」，票 10 **做一半**（搬了檔、沒改讀取點）時三條照樣全綠，而工具一跑就撞缺基準——U-2 說那會把「還沒建立基準」偽裝成「什麼都沒變」。⇒ 加驗 `tools/skill_watch.py` 的 `DEFAULT_BASELINE` 真的指向 `state\` 那份 | 沿用舊機基準 ⇒ 滿屏「平台真的變了」，然後照訊息加規格禁止的 `--force`，連守衛一起關掉 |

   **結束條件（四態）**——2026-09-03 訂正·第 4 輪發現 4：原本 `SKIP` 一個碼扛兩種語意，於是「這台沒裝 Cursor」與「該驗的沒驗」擋住同一件事，**只裝 Claude Code 的新機會永遠印不出「裝好了」**。現在拆開：

   | 碼 | 意思 | 擋不擋「裝好了」 |
   |---|---|---|
   | `OK` | 通過 | — |
   | `FAIL` | 判定失敗 | **擋** |
   | `SKIP` | **選配不適用**（P8 的「沒裝 Cursor」；P10 的「舊機也沒設 `outputStyle`」——⚠ 要有舊機那份**舉證**才算，見下） | 不擋 |
   | `UNVERIFIED` | **該驗但這次沒驗**（P5 沒給 `--source`、P11 沒給 `--wired-at`、P2 抽不出路徑、**P10 沒有 `outputStyle` 又沒給 `--source`**） | **擋** |

   ⚠ **`SKIP` 不是綠**——它宣告的是「這條在這台機器上沒有適用對象」。判不出來屬於 `UNVERIFIED`，不是 `SKIP`。
   ⚠ **「不適用」是一個需要舉證的主張**（2026-09-03 補·第 5 輪發現 5）。原本這張表拿
   「P10 沒設 `outputStyle`」當 `SKIP` 的定義例，而那正是分界選錯的地方：本機少一顆鍵，
   可能是「這台不用風格」，也可能是「舊機設過、接線器漏帶」，**兩者在本機這一份裡同形**。
   ⇒ 判準是**證據從哪裡來**，不是「這條看起來像不像選配」：拿得出舊機那份、且舊機也沒有，
   才是 `SKIP`；拿不出來就是 `UNVERIFIED`。**`SKIP` 現在唯一不需要外部舉證的實例是 P8 的
   「這台沒裝 Cursor」**（本機自己就看得到 Cursor 裝沒裝）。
   **十一條探針全部實作並實跑**（P1／P2／P5／P9／P10／P11 於 2026-09-03；
   P3／P4／P6／P7／P8 於 2026-09-04 補齊·`tools/wiring_probe.py`）。
   這是 user 拍板「不要純文件打磨到蓋章」的落地：**三輪覆核打的全是規格文字，
   第一次真的跑就在這台舊機上紅了。**

   ⚠ **補齊那五條同時修掉一個在驗收條件裡的洞**（2026-09-04）：
   `verdict()` 只看拿到的結果，而**沒實作的探針一條結果都不產生** ⇒ 它在判定眼裡
   不存在。也就是說 W2／W3／W6／W7 全部做完那天，驗收會回 0 並印「全綠」，
   而那五條從頭到尾沒驗過——**「裝了等於沒裝，而且靜默」發生在這份計畫自己的
   驗收條件裡**。現在改成先宣告 `EXPECTED_PROBES`，缺席的一律 `UNVERIFIED`。

   ⚠ **同日一併修掉兩件會讓新機第一次跑就白費的事**：①探針的輸出帶 `⇒`／`⚠`，
   在 cp950 console 上不加 `-X utf8` 會 `UnicodeEncodeError` 然後 exit 1 ——
   **拿到的不是判定是 traceback**（U-4 要擋的正是這個）；程式自己接管 stdout 編碼，
   不靠人記得加旗標。②程式裡四處使用者看得到的訊息還寫「票 06」，
   而本節 2026-09-04 已訂正成票 10——**文件改了碼沒改**。

   舊機實跑結果：**OK 44｜FAIL 1｜SKIP 0｜UNVERIFIED 2**（共 47 項·2026-09-04 W9 完成後）。
   ⚠ **分項表的碼與總計現在有測試在對帳**（2026-09-03 補·第 5 輪發現 1）。
   那一輪抓到的互斥就在這兩段之間：總計寫 `SKIP 0`、分項表同一輪卻標 `SKIP`，
   而**兩個碼的後果相反**（`SKIP` 不擋、`UNVERIFIED` 擋），過期的碼和真的還沒拆同形。
   ⇒ `tests/test_wiring_probe.py` 會解析這兩段並比對；
   `tests/mutations/mutate_plan_runlog.py` 證明它在三種漂法下都會紅
   （含發現 1 的原形）。**「唯一真相」宣告擋不住這件事——宣告是意圖，對帳才是機制。**

   | 項 | 結果 | 說明 |
   |---|---|---|
   | P1 agents／skills junction | 2 OK | `samefile` 兩條都通過 |
   | P2 六條 hook 的實體檔 | 6 OK | 抽取邏輯全部命中，沒有一條落進「抽不出路徑」的 UNVERIFIED |
   | P3 `harness.config` 指向真專案 | 2 OK | `currentProject` 是真目錄、1 個 `scanRoots` 也是。⚠ 判準比「檔案存在」嚴一級是有前例的：2026-09-03 這個檔被留在 `--init` 範本態約 12 小時，而 `is_file()` 與 `json.loads()` 全程都過 |
   | P4 hook 的 `STATE_DIR` | 2 OK | 字面值指到這一顆 harness 的 `state\`，而且**實際寫進一個檔再刪掉**。⚠ 不能只驗「目錄存在」：`dispatch.py:365-366` 把寫入失敗 `pass` 吃掉，看板顯示「沒發生過」，與「很乾淨」同形 |
   | P6 live `CLAUDE.md` | 1 OK | 與 `global/CLAUDE.md` `filecmp` 相同。**非空不算數**（第 3 輪發現 3：新機自己寫的那份也非空） |
   | P7 agents／skills 列得出來 | 2 OK | 6 項／16 項。與 P1 量的不是同一件事：P1 問「連到對的地方嗎」，P7 問「連過去之後真的看得到東西嗎」（C2 追蹤點） |
   | P8 Cursor 角色複本 | 5 OK | 五支逐檔 `filecmp` 相同。**三態分開**：沒裝 Cursor＝`SKIP`、裝了但沒接上＝`FAIL`、內容漂掉＝`FAIL`。live 側多出來的 `.bak-…` 不判紅（票 10-5 同型） |
   | P5 `additionalDirectories` 13 條 | **12 OK／1 FAIL** | ⚠ **第 0 條是 `"\\tmp"`，本機不存在**——live 與 repo `global/settings.json:153` 兩份都有它 |
   | P5 「改寫沒有指到別人的帳號」 | 1 OK | 第 5 輪發現 4 新增；**不需要 `--source`** ⇒ 舊機上也驗得到 |
   | P5 「對得上舊機改寫來源」那半 | 1 UNVERIFIED | 需要 `--source` 給舊機那份；舊機上沒有「舊機」可比 ⇒ **明說沒驗，不當通過**（⚠ 2026-09-03 訂正碼名·第 5 輪發現 1：這格原本寫 `SKIP`，而 `SKIP` 不擋結束條件——過期的碼和真的還沒拆長得一模一樣） |
   | P9 跨碟備份 | 4 OK | post-commit 已安裝且與 repo 相同、`backup` remote 在、無失敗標記、鏡像 HEAD 與本機相同。⚠ **這條探針一寫出來就抓到本機真的漂了**：裝的是舊版（8 行差、**0 行是行為碼**，註解還停在舊路徑寫法 `D:\.ai-harness`），已把 repo 版本拷過去 |
   | P10 `output-styles` | 3 OK | 兩支風格檔 live 與 repo `filecmp` 相同，`outputStyle=PM-Challenger` 對得到 `pm-challenger.md` |
   | P11 skill-watch 基準 | **4 OK／1 UNVERIFIED** | ✅ **W9 已完成**（2026-09-04·`tools/migrate_skill_baselines.py`）：基準搬進 `state\skill_watch_baselines.json`、清冊只剩 `skills[]`、執行期讀取點跟著改。⚠ **搬家順帶暴露一條新機必死的路**：`--capture` 是建立基準的唯一入口，而它一開頭就 `load_doc()` ⇒ 檔不在就拒跑，新機永遠建不了基準。搬家前那個檔在版控裡、一定存在，所以這條路從來沒被走過。「基準晚於接線時間」那半仍是 `UNVERIFIED`（要 `--wired-at`，接線器還不存在） |

   **回歸網 86 項、二十個變異各自轉紅**（`tests/test_wiring_probe.py`；變異腳本 `tests/mutations/mutate_wiring_probe.py`）。86 項裡有 3 項是上面那組文件對帳，另有 `mutate_plan_runlog.py` 專門證明那 3 項會紅。
   ⚠ **2026-09-04 那七個新變異裡有三條第一次寫出來抓不到東西**，而且形狀值得記：
   前兩條退化的是**測試的斷言**不是探針的判準——P3 只斷言「有 FAIL」，但範本值那個
   目錄本來就不存在，範本判準拿掉之後「目錄不存在」照樣紅；P4 同理。
   第三條更彎：用管線跑子行程證明不了編碼問題（管線編碼與真實 console 不同），
   要用 `PYTHONIOENCODING=cp950` 才叫得出來，而**當時連變異腳本自己都在管線裡被吃掉**
   （它跑測試沒加 `-X utf8`，測試印失敗訊息時自己先炸，於是回報「沒轉紅」）。
   ⇒ **驗證器自己有三層都可能是假的**：判準、斷言、跑判準的那支腳本。
   ⚠ **第 5 輪處置新增的六個變異都是「把新判準退化回被打穿的舊判準」**——
   前綴替換退回固定尾段 3 段、規則排序改取最長（＝每對套自己那條，恆真檢查）、
   拿掉家目錄語意、恆等改寫改回 `UNVERIFIED`、P10 缺鍵一律 `SKIP`、P11 不驗讀取點。
   **退得回去而測試轉紅，才證明新的那一層真的在守東西。**
   ⚠ **變異驗證兩次抓到「測試自己是假綠」，四件，形狀都一樣**——
   斷言「有跑到」而不是斷言「判定對」：

   | 何時 | 哪一條 | 假綠的原因 |
   |---|---|---|
   | 首寫（第 3 輪處置後） | P9 沒有 `backup` remote | 用「不是 git repo 的空目錄」測，`git remote` 本身就會失敗而走前一個分支 ⇒ 判準拿掉仍會紅 |
   | 首寫 | P11 版控檢查 | 只斷言「有這條標題」，拿掉檢查後走 else 分支，標題一樣、結果變 `OK` |
   | 第 4 輪處置 | P9 post-commit 沒安裝 | 只斷言 `code == FAIL`，把「沒安裝」誤判成「內容不同」時照樣綠——而訊息會把人引去比對檔案內容，不會去想「這台根本沒裝過」 |
   | 第 4 輪處置 | 同上的變異錨點 | **跨行錨點在 CRLF 檔上對不到**（`\n` vs `\r\n`），而「錨點對不到」在變異腳本裡是**靜默的**——只說這條沒驗到，不說為什麼。判定已收成單行 |

   ⚠ 另一個同型的：變異一度只讓測試**拋例外**而不是判定變錯（`filecmp` 拿不存在的檔）。
   **用例外換來的紅證明不了判定對不對**，探針已改成預先算好、不炸。

   ⚠ **實跑逼出一個規格缺口，本文在此補上**（原本三張表都沒有這一態）：
   **來源本身就有壞條目時怎麼辦。** `\tmp` 不是路徑改寫失敗，是這條設定**本來就壞**。
   規格原本只分「改寫成功」與「改寫不掉要紅」——照那個寫法，新機的 P5 會**永遠紅**，
   而且紅的原因指不到真正的問題（會被讀成「接線器沒改寫好」）。
   ⇒ **W10 增訂**：帶舊機 live 過來之前**先跑一次來源健檢**，
   在舊機上就不存在的路徑要**當場列出來讓人決定刪或留**，
   **不得靜默帶過去**，也不得由接線器自己刪（那是在改人的個人設定）。
   ⇒ **P5 增訂**：紅的訊息要分得出「來源本來就壞」與「改寫失敗」兩種，
   否則新機第一次跑就會拿到一條指錯方向的紅燈。

   ⚠ **`\tmp` 這條本身尚未處理**：它在 live 與 repo 兩份都在，屬個人設定，
   由 user 決定刪不刪 ⇒ 落在待辦表，不在本輪自行動手。

   ⚠ **W10 再增訂：改寫器要留下它實際用的前綴對照表**（2026-09-03·第 5 輪發現 4 的
   配套·user 拍板「A 現在做、B 記票」）。P5 現在的判準是從**舊新兩份設定回推**前綴替換，
   那是**單一來源**——它驗得出「不一致」與「指到別人的帳號」，驗不出「一致地錯」
   （整組都映到同一個不是本機、卻真的存在的帳號；回推出來的規則會完美自洽）。
   ⇒ 接線器改寫時把它用的對照表（舊前綴 → 新前綴，通常 2–4 條）落成檔案，
   探針拿它做**第二個獨立來源**：逐條套用後必須與 live 那份逐字相等，
   且每條新前綴要能對得上本機的家目錄／碟符。
   **尚未實作**（接線器本體整支都還不存在）⇒ 落在待辦表，判定指回本段。

   **四態的語意見上面探針表後的結束條件表**（2026-09-03 第 4 輪發現 4 拆開後，本節不再自己寫一套——那正是「唯一真相」要防的形狀，而第 4 輪就是在這一節裡抓到它）。

3. **只做 `same-person-new-pc` 一份安裝剖面**。`department` 剖面**留白，綁 D-4**。
   ⚠ 剖面收窄了，**U-4 的完成判準也要跟著拆成兩條**（第 2 輪發現 6），見 §3。

**為什麼收成一份剖面**（審查者建議兩份，user 拍板一份）：§5 明文
「沒有第二個部門真的要用之前，不做抽象化重構」。department 剖面現在沒有人裝、
沒有人回報壞掉 ⇒ 它的正確性只能靠想像檢查，而**想像中的裝好**正是這輪覆核的主題。
D-4 定案前不動它。

**同時消掉 D-1／D-3 的內部矛盾**：舊 D-1 說 (b) 是「最少的新概念」，
舊 D-3 傾向卻寫「路徑不拆，其餘兩項做完仍然搬不動」。定案後兩者一致——
**接線器就是 D-3 (a) 拆路徑的落地物**，不是另一件事。

**(c) 保留的用途**：可以當接線器的包裝（一個 `pip install` 呼叫同一支探針），
不能替代 git；plugin 只適合 skills／agents 的官方載入，替代不了 hook 絕對路徑
與 `harness.config.json`。

⚠ **2026-09-04 增訂：上面「plugin 替代不了 hook 絕對路徑」這半句站不住**（官方文件實查，非推測）：
`plugins.md` 明寫 plugin 可帶 `hooks/hooks.json`（格式與 settings.json 的 `hooks` 相同），
`hooks.md` 明寫 hook command 可用 `${CLAUDE_PLUGIN_ROOT}`；plugin 同時可帶 `skills/` 與 `agents/`。
⇒ plugin 能拿走 **W1 的兩條 junction ＋ W10 裡 12 條指令路徑改寫**（6 條 hook command＋6 條角色 frontmatter），
正是最容易靜默失效的那一半。**拿不走的**：`harness.config.json`（W2）、`state\`（W3）、post-commit 與
bare 鏡像（W4／W5）、Cursor 複本（W6）、CLAUDE.md 與 output-styles 的 restore（W7／W8）、5 處 `STATE_DIR` 字面值（B4）。
官方**沒寫**的：plugin 與 user 層 hooks 並存的順序與合併規則、安裝後執行一次的機制、`additionalDirectories` 的 `~` 支援
（本機實測可用，見 `CLOUD_BACKUP_PLAN.md` §2）。
**spike 已跑（2026-09-05·實跑非推測）**：在暫存目錄做一個最小 plugin
（`.claude-plugin/plugin.json` ＋ `hooks/hooks.json`，hook 指令是
`py -3 "${CLAUDE_PLUGIN_ROOT}/mark.py" <事件>`），以 `claude -p ... --plugin-dir <暫存>`
在 harness 之外的工作目錄開一則 headless 對話。**完全不動 harness 本體、不動 live 設定。**

| 問題 | 結果 |
|---|---|
| plugin 帶的 hook 會不會觸發 | ✅ 會。`SessionStart` 與 `PreToolUse` **兩個事件都落了標記** |
| `${CLAUDE_PLUGIN_ROOT}` 會不會展開 | ✅ 會，展開成 plugin 目錄的絕對路徑 |
| 與 user 層 hooks 並存會不會互相壓掉 | ✅ **不會**。同一則對話裡 harness 的 user 層閘門照樣跑完整輪（`state/events.<那則>.ndjson` 有 `dispatch`／`AWC-1`／`DECL-1`／`WIN-1`），plugin 的標記也同時落地 |

⇒ **「plugin 替代不了 hook 絕對路徑」這句正式被推翻**，不再是文件推論而是實跑。
plugin 拿得走 W1 的兩條 junction ＋ W10 裡 12 條指令路徑改寫。

**spike 沒有回答的（不要當成一起驗過了）**：

* **驗的是 `--plugin-dir`（單則對話的旗標），不是安裝**。換機真正要的是裝一次就常駐，
  那條路徑（`claude plugin install`／marketplace）**沒驗**。旗標能跑不等於裝得起來。
* 並存的**順序與合併規則**沒驗——只證明兩邊都跑，沒證明誰先誰後、衝突時誰贏。
* 安裝後執行一次的機制（W2／W3／W5 那類「跑一次就好」的動作）官方沒寫，spike 也沒碰。
* 拿不走的那一半原封不動：`harness.config.json`（W2）、`state\`（W3）、post-commit 與
  bare 鏡像（W4／W5）、Cursor 複本（W6）、CLAUDE.md 與 output-styles 的 restore（W7／W8）、
  5 處 `STATE_DIR` 字面值（B4）。**接線器還是要做，只是變小。**
換機失效點的完整盤點（6＋6＋5＋13＋看板 6＋不進 clone 3＋junction 7＋IT 專案 1）在 2026-09-04 晚的說明頁與 `.scratch/handoff/20260904-cloud-autopush.md`。

### D-1 對抗式覆核·第 1 輪逐項處置（2026-09-03）

審查者 Cursor CLI `cursor-grok-4.6-high`；守門 `adversarial_exchange_gate.py --check` exit 0。
素材 `.scratch/harness-portability/`（`round-1-ask.md` 已凍結 `ask-sha256=a166f169…`）。
**處置前本則重新實查了 A1／A2／B2／B3／B4 的證據**，結果見「依據」欄；
與交接檔對不上的數字以本表為準。

| # | 發現 | 處置 | 依據／後續 |
|---|---|---|---|
| A1 | 裝設腳本不在 harness、路徑寫死、缺目標只警告不中止 | **接受** | 實查：`git ls-files` 找不到任何裝設腳本；`bootstrap.ps1` 第 19／34／76 行寫死 harness 路徑，第 37–38／79–80 行缺目標 `Write-Warning` 後 skip、腳本照常結束。修法＝定案第 2 點的接線器**取代它**（它在 IT 專案裡，屬專案層） |
| A2 | 腳本就算 junction 成功也不寫 hook 接線 | **接受** | 實查 live／repo `settings.json`：6 條 hook command 全是絕對路徑，`bootstrap.ps1` 完全不碰它。**修法見定案第 2 點 W10 與設定檔三態表**（⚠ 這格原本自己寫「展開模板合併進 live」，與定案的「舊機 live 帶過來逐欄改寫」並存 ⇒ 第 3 輪發現 1 的第二真相，已刪除） |
| A3 | 整夾複製會把 gitignore 的本機狀態一併帶走 | **部分接受**（第 2 輪修正） | 成立：`state/`、`harness.config.json`、`session-archive/` 確實被排除。⚠ **第 1 輪的處置只寫在這一格、沒改到定案本文，等於沒修**（第 2 輪發現 1）。**修法見定案第 2 點的設定檔三態表與 W2**，本格不重述（2026-09-03 收第二真相） |
| A4 | 連 `~\.claude` 一起複製時 junction 目標仍指舊絕對路徑 | **接受** | 斷掉的 junction `is_dir()` 回 False，會被當成「還沒建角色」靜默跳過（`dashboard/capability_checks.py:575-576`）。這就是 **P1** 的 `samefile` 存在的理由 |
| B1 | clone 只拿到版控樹，執行期接線全部缺席 | **接受** | 「三選一框架有缺陷」的核心證據，已寫進上方定案 |
| B2 | 傾向理由「已有跨磁碟鏡像」把本機備份通道誤當分發通道 | **接受** | 實查 `git remote -v`：只有 `backup` → `C:/Users/<USER>/git-mirrors/JEFF-Harness.git`，**沒有 origin**；`tools/githooks/post-commit` 自己的註解就寫「`.git/hooks/` 不進版控，換機器或重新 clone 都不會有它」。**舊傾向欄已直接改寫，不留附錄** |
| B3 | 把 repo 的 `global/settings.json` 當「clone 後還原」用會裝進舊路徑 | **接受** | 實測該檔：**6 條 hook command、20 處絕對路徑出現、14 個相異真路徑**（⚠ 交接檔寫的「13 條路徑／7 個 hook」是錯的，以本行為準）。**修法見定案第 2 點設定檔三態表第 1 列**（⚠ 這格原本自己寫「展開模板而不是還原檔案」⇒ 第 3 輪發現 1 的第二真相，已刪除） |
| B4 | hook 程式內部仍寫死舊 `state\` | **接受**，第 2 輪**收回「不在接線器範圍」** | `hooks/dispatch.py:71` `STATE_DIR = r"D:\Patrick-AI\.ai-harness\state"`，寫入失敗 `:365-366` 被 `pass` 吃掉。⚠ 拆字面值仍是另案，但**探針必須驗它**（定案 **P4**）——不驗的話「hook 路徑對、狀態目錄錯」會全綠通過，而看板顯示「沒發生過」與「很乾淨」同形 |
| B5 | skill-watch 基準是原機量的，新 clone 會撞錯出口 | **第 2 輪撤回原處置，改為完全接受** | ⚠ 原處置寫「同一人同一組基準，不觸發」是**把「同一人」誤讀成「同一台機器上的平台技能清單」**（第 2 輪發現 4）。新電腦裝的 Claude／Cursor 版本不同就會撞，與剖面無關。**修法見定案第 2 點 W9 與 P11**。⚠ 第 2 輪寫在這格的「刪掉基準檔重量測」**第 3 輪證明做不到**——`SkillViewer/platform_skills.json` 在版控裡，刪了 `git restore` 會回來、重量測後 commit 還會經 `backup` remote 蓋掉舊機那份。已改為版控模型變更（基準檔各機一份、不進版控·user 2026-09-03 拍板），細節不在這格 |
| C1 | 套件安裝成功的位置不是 Claude 的載入位置 | **接受** | 作為不把 (c) 當主線的理由，已寫進定案；(c) 保留為接線器的包裝 |
| C2 | 官方 plugin／symlink 模型不保證容器層 junction 讀得到 | **接受為外部相依風險，非本輪修項** | 接線器擋不住平台改政策，但探針要偵測得到（skills 目錄可列且非空）。追蹤點：`TODOS.md` 的 `claude plugin validate` 那筆 |
| C3 | 套件凍結版本與「改 repo＝改執行期」的 junction 模型打架 | **部分反駁**（第 2 輪收回一半） | 「兩份樹」那半仍反駁：定案沒選 (c)，`git pull` 就是更新。⚠ **「Cursor 是 D-1 以外」那半是錯的**（第 2 輪發現 5）——定案第 2 點自己把 `cursor-agents\` 同步收進接線器，卻只驗 Claude 側。**修法見定案第 2 點 P8**。⚠ 「兩份樹那半仍反駁」第 3 輪也被推翻一半（發現 5）：`git pull` 對 Claude 側 junction 成立，但 `~\.cursor\agents\` 是**複本**，pull 後 live 不會跟著動 ⇒ P8 已改寫成三態＋可重跑 |
| F-a | 三個選項不互斥、混了三條軸 | **接受** | 改寫框架的直接原因，見定案表 |
| F-b | 傾向 (b) 不成立 | **接受** | 傾向欄已改寫，舊字面刪除 |
| F-c | §0「分發部門」與新增的「本人換電腦」兩個目標塞同一題 | **接受** | 用「安裝剖面」拆開，且本次只做一份 |
| F-d | 第四案 (d) | **部分接受** | 採用「git ＋ fail-closed 接線器 ＋ 探針過得了結束條件」三件（⚠ 2026-09-03 訂正措辭·第 5 輪發現 2：原寫「探針全綠」，那是四態拆開前的講法 ⇒ **結束條件的唯一寫法在定案第 2 點的四態表**，本格不重述）；**不採用**「兩份剖面一次做齊」（§5） |

**審查者自報打不穿的 6 條，下一輪不要重複去打**：D-2 拆兩層與缺設定拒跑、U-2 不准
fallback、「核心層 Claude 不會自動載入」、U-1 債務閘門只准變少、§5「沒有第二個部門就
不抽象化」、「空 junction 是最危險狀態、畫面像還沒建角色」。

**衍生待辦（實作清單——每列只寫「做了沒」，修法一律指向定案的 W／P 編號）**

⚠ 這張表犯過兩次病：**先寫表、後才修，寫完沒回頭改**（第 2 輪發現 7——過期的表和真的沒做
長得一模一樣）；以及**把「還沒做的接線步驟」跟「就算做了也驗不到的洞」混成同一種 ⏳**
（第 3 輪發現 7）。⇒ 現在分工固定：**修法在定案第 2 點的 W 表、驗收在 P 表、這裡只記狀態**。
現況欄一律標日期。

| 項目 | 對應 | 現況 |
|---|---|---|
| ~~U-1 硬編碼閘門只掃 `.py`~~ | — | ✅ **已補 JSON 維度**（2026-09-03·commit `cb75d67`）。設定檔的寫死路徑已凍進台帳、只准變少，測試 32 通過 0 失敗 |
| ~~HND-1 已在跑但沒註冊~~ | — | ✅ **已註冊為 enforce**（別的 session·commit `e6ded68`），規則鍵現為 19 個 |
| ~~`global/settings.json` 的探針移除未提交~~ | — | ✅ **已提交**（commit `c62f2ac`） |
| **接線器本體**（W 側一支都還沒有） | W1–W10 | ⏳ 未動。2026-09-03 user 拍板：**先寫探針實跑，拿真實輸出回頭修規格，再派確認輪**（不要純文件打磨到蓋章） |
| **plugin 路線 spike**（能否拿走 W1＋W10 的指令路徑那一半） | W1／W10 | ✅ **已跑（2026-09-05）**：plugin 帶的 hook 真的觸發、`${CLAUDE_PLUGIN_ROOT}` 正確展開、與 user 層 hooks 並存不互相壓掉——三項都是實跑。⚠ 驗的是 `--plugin-dir` 旗標**不是安裝路徑**，裝一次就常駐那條沒驗。詳見定案末段增訂 |
| ~~探針一支都沒有~~ | P1／P2／P5／P9／P10／P11 | ✅ **六條已實作並實跑**（2026-09-03）。**數字見定案第 2 點的實跑節，本欄不複述**（第 4 輪發現 5：本欄的數字與實跑節對不上，而過期的表和真的沒做長得一模一樣） |
| ~~探針剩下五條~~ | P3／P4／P6／P7／P8 | ✅ **2026-09-04 補齊並實跑**，數字見定案第 2 點的實跑節（本欄不複述——第 4 輪發現 5：過期的表和真的沒做長得一模一樣）。⚠ **同時補了 `EXPECTED_PROBES` 缺席守門**：在這之前沒實作的探針一條結果都不產生，`verdict()` 看不見它們 ⇒ 少驗五條仍會印「全綠」 |
| **`additionalDirectories` 的 `\tmp` 殘留條目** | P5 | ⏳ 未動·**等 user 決定**。live 與 repo `global/settings.json:153` 兩份都有，本機不存在 ⇒ P5 實跑的那一條 FAIL。屬個人設定，接線器不得自行刪 |
| **來源健檢**（帶舊機 live 過來前先列出壞條目） | W10／P5 | ⏳ 未動。**實跑逼出的規格缺口**，已寫進定案；不做的話新機 P5 會永遠紅且指錯方向 |
| **換機取得來源**（bare 鏡像怎麼搬、新機怎麼重建 `backup` remote） | W5／P9 | ⏳ 未動 |
| **`additionalDirectories` 的路徑重寫** | W10／P5 | ⏳ 未動 |
| **改寫器要留下前綴對照表** | W10／P5 | ⏳ 未動·**2026-09-03 user 拍板記票**（第 5 輪發現 4：「A 現在做、B 記票」）。P5 現在的判準只有「兩份設定回推」這一個來源，擋不掉「一致地錯」。判定與代價見定案第 2 點 W10 的第二則增訂 |
| **skill-watch 基準搬到 `state\`** | W9／P11 | ⏳ 未動，**但已有紅燈**（數字見實跑節，本欄不複述）。⚠ **票 10「做一半」也會紅了**（第 5 輪發現 6）：搬檔而不改 `skill_watch.py` 的讀取點，P11 第 4 段判準會擋下來。⚠ **這不是 D-1 的工項**，做法也不在這裡——見 `SKILL_WATCH_PLAN.md` 票 10（2026-08-23 定案）。⚠ 本欄 2026-09-03 刪掉過一段自己寫的修法步驟（第 4 輪發現 5：唯一真相宣告禁止待辦表寫修法，而我在同一輪的處置裡違反了自己剛立的規矩） |
| **live `CLAUDE.md` 的 restore** | W7／P6 | ⏳ 未動。它沒有 junction，靠 `backup_global_config.py` 管，接線器要接手 |
| **`output-styles` 的 restore** | W8／P10 | ⏳ 接線器那半未動；**P10 已實作且本機全綠**（第 3 輪發現 2 補列）。它與 `CLAUDE.md` 在 `backup_global_config.py` 是同一級，之前整項漏在表外 |
| **Cursor 側改成三態＋pull 後可重跑** | W6／P8 | ⏳ 未動。現有 `check_cursor_agents.py` 不能直接當探針 |
| B4 的 `STATE_DIR` 字面值 | P4 | ⏳ 未動，**且刻意不做**。拆字面值是另案；P4 先擋住它的失效形狀 |
| **Cursor User Rules 雲端那份** | — | ⏳ 未動，**且探針碰不到**。貼雲端是人工步驟 ⇒ 只能列進安裝說明，**不准假裝有探針** |

**尚未完成**：第 3 輪**不是**零改動輪（提出 7 條新發現，已逐項處置），
⇒ 還要再跑確認輪才能蓋 `ADVERSARIAL_REVIEW_PASSED`；`review_inflight.py` 便箋維持掛著。
⚠ **下一輪之前要先寫探針**（user 2026-09-03 拍板），理由見上表「接線器本體」那列。

### D-1 對抗式覆核·第 2 輪逐項處置（2026-09-03）

審查者 Cursor CLI `cursor-grok-4.6-high`；守門 `--check` exit 0、兩輪都齊全。
本輪 7 條**全部打在第 1 輪的處置上，不是打原計畫**——這正是零改動輪存在的理由。

| # | 發現 | 處置 | 改到哪裡 |
|---|---|---|---|
| 1 | 「禁止還原檔案」與 same-person 剖面互相否定，A3 的處置沒改到定案本文 ⇒ 等於沒修 | **接受** | 定案第 2 點改寫成兩列表：repo 快照禁止還原／舊機 live 帶過來逐欄改寫 |
| 2 | 三條探針全綠仍可接線失敗（不看 `STATE_DIR`、`additionalDirectories`、live `CLAUDE.md`、Cursor） | **接受** | 探針從 3 條擴到 8 條；第 3 輪再擴到 **11 條**並改嚴 P5／P6／P8，見定案第 2 點的表 |
| 3 | 「內容走 git」在唯一要做的剖面上沒有來源；`post-commit` 沒有 `backup` remote 就 exit 0 | **接受** | 定案第 1 點補明取得來源＝搬舊機 bare 鏡像，並要求新機重建鏡像 remote |
| 4 | B5「只在 department 才發生」不成立——把「同一人」誤讀成「同一組平台基準」 | **接受·撤回原處置** | B5 那一列改寫。⚠ 當時填的修法（刪基準重量測）**第 3 輪證明做不到**，現行修法見定案第 2 點 W9／P11 |
| 5 | C3 反駁把 Cursor 踢出 D-1，定案卻又收進來，探針不驗 | **部分接受** | C3 改為部分反駁；補 **P8**，並要求「沒裝」與「沒接上」不得同綠（第 3 輪再改寫成三態） |
| 6 | U-4 仍以「陌生人、沒有既有設定」為判準，與只做 same-person 互斥 | **接受** | §3 U-4 拆成兩條剖面判準；第 3 輪發現 6 再補 same-person 的第二把尺（探針之外的外部觀察） |
| 7 | 衍生待辦四項不完整，且兩項已過期 | **接受** | 上表重寫，過期三項標完成並附 commit，補上五項漏列 |

**證據漂移一則（審查者主動回報，不算發現）**：`bootstrap.ps1` 第 19–22 行對缺
`link_memory.py` 現況已是 `Write-Error` ＋ `exit 1`，不是只警告；agents／skills
兩段（37–38、79–80）仍是 warning 後 skip。第 1 輪 A1 的描述以後者為準。

**審查者本輪自報打不穿的 6 條**（下一輪不要重複去打）：「運輸成功 ≠ 安裝成功、
三選一框架有缺陷」、§6 明寫接線器還沒寫、B3 的「repo 快照不可整檔還原」那半句、
D-2 缺設定拒跑與 `--init` 假範本、§5「沒有第二個部門就不要靠想像驗收」、
「空 junction 必須能被偵測」。

### D-1 對抗式覆核·第 3 輪逐項處置（2026-09-03）

審查者 Cursor CLI `cursor-grok-4.6-high`；守門 `adversarial_exchange_gate.py --check` exit 0、三輪齊全。
素材 `.scratch/harness-portability/round-3-{ask,reply}.md`（`ask-sha256=680db8d7…` 已凍結）。

**本輪的處置方式與前兩輪不同。** 第 2、3 輪連續被同一個形狀打穿——「修法寫在處置表那一格、
定案本文照舊」——`adversarial-review` skill §5.6 明文：同一邏輯反覆被打穿時應**移除第二真相**，
不要再訂正個案。⇒ **本輪先把接線器規格收成單一真相**（定案第 2 點加了「這一節是唯一真相」
的宣告與 W 表），**再處置 7 條**；兩件事改的是同一段文字，分兩趟做等於改兩遍。

**本輪額外實查（不是審查者給的，是處置前自己驗的）**：
`git ls-files` 確認 `SkillViewer/platform_skills.json` **在版控**、`.gitignore` 沒排除它
⇒ 第 2 輪 B5 那格寫的「刪基準檔重量測」**做不到**。這條沒被審查者點名，但它讓發現 1 的
處置不能是「把修法搬過去」。

| # | 發現 | 處置 | 改到哪裡 |
|---|---|---|---|
| 1 | B5 修法只在處置格與待辦表；A2／B3 依據欄仍寫「展開模板」，與定案兩列表並存 | **接受**（本輪主軸） | 定案第 2 點加**唯一真相宣告** ＋ **W1–W10 表**；A2／B3／B5／C3／A3 五格退成「判定 ＋ 指向」。B5 修法本身**推翻重寫**（見上段實查），改為版控模型變更 W9（user 拍板：基準檔各機一份、不進版控） |
| 2 | 八條全綠仍可：沒建 `backup` remote、沒 restore `output-styles`、沿用 skill-watch 基準 | **接受** | 補 **P9／P10／P11** 三條探針與 **W5／W8／W9** 三個動作。⚠ 其中 `backup` remote 是**定案第 1 點自己點名的靜默失敗，卻不在探針裡**——這是本輪最該紅臉的一條 |
| 3 | 探針 5、6 把「存在／非空」當成「已改寫／已 restore」 | **接受** | **P5** 加「非空 ＋ 對得上舊機改寫來源」，**P6** 改成與 `global/CLAUDE.md` `filecmp` 相同（沿用 `backup_global_config.py:92` 的判法）。兩條的表頭欄名也改成「判準——存在／非空不算數的特別標」 |
| 4 | 設定檔兩列表沒有第三態：新機已經有一份 live | **接受** | 兩列表擴成**三列**，第三態＝**拒跑並列出衝突項**（沿用 `_mixed_gate` 的態度），並涵蓋「`~\.claude\agents` 已是實體目錄」的 junction 衝突 |
| 5 | C3 的「兩份樹仍反駁」對 Cursor 複本不成立；`check_cursor_agents.py` 第三態 exit 0 | **接受** | **P8 改寫**：三態分開、第三態印 `SKIP`、`SKIP` 不計入綠也不擋（⚠ 2026-09-03 訂正措辭·第 5 輪發現 2：原寫「結束條件只數 `OK`」，四態拆開後那句會讓沒裝 Cursor 的機器永遠印不出「裝好了」；**結束條件的唯一寫法在定案第 2 點**）、且**要求 pull 後可重跑**（複本不會跟著 `git pull` 動）。C3 那格退成指向 |
| 6 | U-4 的 `same-person-new-pc` 用本文件的探針定義完成（循環定義） | **接受** | §3 U-4 補**第二把尺**：不依賴探針的外部觀察（新對話讀得到記憶、`state\` 有心跳、看板非空、commit 真的推進鏡像）。⚠ 同時在定案第 2 點寫明「**單一真相講的是修法，不是觀察**」——否則下一輪會把「剛收單一真相又加第二把尺」當成矛盾 |
| 7 | 衍生待辦把 B5 寫成純未實作，且漏了「就算做了也驗不到」的洞；`output-styles` 整項不在表上 | **接受** | 待辦表**重寫**：每列加「對應」欄指向 W／P 編號、只記狀態不寫修法，補上 `output-styles` 與 Cursor 三態兩列 |

**審查者本輪自報打不穿的 7 條**（下一輪不要重複去打）：第 2 輪發現 1／2／3／6 確實改到定案正文
（不是只改表格）、「repo `global/settings.json` 禁止整檔當還原物」、**P2** 驗「command 裡的實體檔」、
**P4** 把 `STATE_DIR` 寫進結束條件、**P8** 禁止「沒裝」與「沒接上」同綠這個禁令本身、
D-2 缺設定拒跑與 `--init` 假範本、§6 明寫接線器一行都還沒寫、§5 不靠想像驗收。

**⚠ 覆核路徑本輪改了**（2026-09-03·user 拍板）：**不直接派第 4 輪**。三輪打的全部是規格文字，
而 §6 明寫接線器一行都還沒寫 ⇒ **就算蓋了 `ADVERSARIAL_REVIEW_PASSED`，證明的也只是這份
文件內部一致，不是換機真的裝得起來**；U-4 的判準要靠探針，而探針本身還不存在
（⚠ 這行寫於 2026-09-03 探針實作之前；「全綠」的講法已於第 5 輪發現 2 改掉，見 §3 U-4）。
⇒ **先寫 2–3 支在舊機就跑得動的探針**（P1 junction `samefile`、P2 hook 實體檔、P5
`additionalDirectories`），拿**真實輸出**回頭修規格，再派確認輪。
**上限 4 輪，目前用掉 3 輪；第 4 輪若又有新且不重複的發現，要先向 user 說明再決定續不續。**

### D-1 對抗式覆核·第 5 輪逐項處置（2026-09-03）

審查者 Cursor CLI `cursor-grok-4.6-high`；素材 `.scratch/harness-portability/round-5-{ask,reply}.md`
（`ask-sha256=da1c4c6f…` 已凍結）。**6 條全部成立**，處置前逐條回查行號證據，沒有一條是記錯。

**本輪是第一次「發現打的是程式不是文字」**：第 4 輪之前三輪打的全是規格措辭，
第 5 輪 6 條裡有 4 條指到 `wiring_probe.py` 的實際判準。

| # | 發現 | 處置 | 改到哪裡 |
|---|---|---|---|
| 1 | 實跑分項表還寫 `SKIP`，總計已改 `UNVERIFIED` | **接受** | 分項表改碼並補兩列（新增的「改寫沒有指到別人的帳號」與 P11 第 4 段）。⚠ **這是同型第六次發作**，所以處置不是改字：`test_wiring_probe.py` 新增三條**對帳** case 解析總計行與分項表並比對，`mutations/mutate_plan_runlog.py` 證明它在三種漂法下都會紅（含本條的原形）。**宣告是意圖，對帳才是機制** |
| 2 | U-4 ①「探針全綠」沒跟著四態改 | **接受** | §3 U-4 ①、定案第 2 點結束條件那行、P8 判準格三處全改成「沒有 `FAIL` 也沒有 `UNVERIFIED`（＝`verdict()` 回 0，`SKIP` 不擋）」；第 1／3 輪處置格裡的舊措辭加訂正註記並指回定案 |
| 3 | 同名使用者的正確改寫被標 `UNVERIFIED` 且擋住；且沒有測試也沒有變異 | **接受·撤回原判定** | 使用者名與碟符都沒變時**恆等就是改寫的正確結果**，硬擋會讓這半永遠到不了綠（不給 `--source` 擋、給了且全原樣也擋，而 `verdict()` 沒有給人放行的口）⇒ 改判 `OK`。「改寫到底跑過沒」由 P2 回答，不歸 P5。補測試 2 條、變異 1 條 |
| 4 | P5 尾段固定 3 段，兩個真實反例 | **接受·判準重寫**（與發現 3 同根：**拿路徑形狀猜改寫語意**） | 判準改成「**改寫＝一組前綴替換一致地套用到每一條**」：共同尾段由資料決定不再寫死、每條要吻合**最一般**的適用規則、改寫後不得落在別人的家目錄。⚠ **能力上限寫在明處**：擋不掉「一致地錯」⇒ user 拍板「A 現在做、B 記票」，B（接線器留下對照表）寫進 W10 第二則增訂與待辦表。補測試 5 條、變異 4 條 |
| 5 | P10 缺 `outputStyle` 標 `SKIP` 是分界選錯 | **接受** | 分界改成**證據從哪裡來**：舊機有這顆鍵而本機沒有＝`FAIL`；沒給 `--source`＝`UNVERIFIED`；舊機也沒有才是 `SKIP`。⚠ 四態表**拿它當 `SKIP` 的定義例**，一併改掉並補寫「不適用是需要舉證的主張」——`SKIP` 現在唯一不需外部舉證的實例是 P8 的「這台沒裝 Cursor」。補測試 3 條、變異 1 條 |
| 6 | P11 不驗執行期讀取路徑 | **接受** | P11 加第 4 段：`tools/skill_watch.py` 的 `DEFAULT_BASELINE` 要真的指向 `state\` 那份。**一寫出來就紅**（票 10 至今沒實作）。前三段全是「檔案擺在哪」，做一半時照樣全綠。補測試 2 條、變異 1 條 |

**本輪量到的數字**：探針那組**見定案第 2 點的實跑節，本欄不複述**
（第 4 輪發現 5 的紀律：兩個地方寫同一組數字，改一處另一處就過期，
而過期的表和真的沒做長得一模一樣——第 5 輪發現 1 又是同一個病）。
回歸網 46 項全過；`mutate_wiring_probe.py` 13 個變異全部 PASS；
`mutate_plan_runlog.py` 3 個全部 PASS；錨點守門 219/219。
全套 `run_hook_tests.py` 1712/1713——**唯一那條紅不是這條線的**
（`global/user-rules-reconcile.md:29` 的 SHA8 `97eece54` 被 `check_commit_refs.py` 誤判成 git hash）。

**⚠ 覆核輪次已用 5、預設上限 4。第 6 輪要先問 user**，不得自行續派。

**⇒ 2026-09-04 user 裁定：不派第 6 輪，D-1 這條覆核線到此為止。**
⚠ **這是「決定停」不是「條件達成」，兩件事不要混**：`adversarial-review` skill §7
要求處置完再跑一輪**零改動確認輪**才能蓋 `ADVERSARIAL_REVIEW_PASSED`，
那一輪**沒有跑過**，五輪裡也沒有任何一輪是零改動 ⇒ **marker 不蓋、也不准補簽**。
往後誰要重開這條線，起點是「第 6 輪」而不是「重跑一輪確認」——前五輪的處置都在
§4 的逐輪處置表裡，不必重做。`review_inflight.py` 的便箋依原規矩在收尾時清。

### D-2 定案（2026-08-13·`CONTEXT_HEALTH_PLAN` C-11 的實例化）

**選 (a)，但原本寫的「放專案根」不成立——必須拆成兩層：**

| 層 | 檔案 | 回答什麼 | 進版控？ |
|---|---|---|---|
| harness 層 | `D:\Patrick-AI\.ai-harness\harness.config.json` | **有哪些專案**（`currentProject`／`scanRoots`／`extraProjects`） | ❌ 內容是這台機器的路徑，跨機無意義。`--init` 產範本 |
| 專案層 | `<repo>\.claude\PROJECT_CONTEXT.md` | 這個專案自己的設定（雙目錄、待辦來源、角色作用對象） | ✅ **不新發明**——它已經是「新部門導入唯一必填檔」 |

**為什麼不能只有專案根那一份**：`gen_layers.discover_projects()` 要回答的是
「**有哪些專案**」，而那份清單**不可能放在還沒被發現的專案裡**（雞生蛋）。

**U-2 的落實**（缺設定拒跑，不要猜）：缺檔／schema 不符／缺欄位／`currentProject`
指向不存在的目錄，四種都 `SystemExit` 並說出缺什麼。⚠ 最後一種是覆核抓到的——
原本 `discover_projects()` 對 `currentProject` 是**無條件插入**，換部門時不會拒跑，
而是報告多一列幽靈專案、CLAUDE.md 印「無」，**跟「那個專案很乾淨」長得一模一樣**。

**⚠ D-2 拍板的順序是反的**：實作（2026-08-13）早於本欄的決定紀錄。
形式上違反了 §6「五項待決，未討論前不動工」——這是 `CONTEXT_HEALTH_PLAN`
覆核 F-7 抓到的。實質上它是 D-2 (a) 的實例化而非另闢新路，故不回退實作，
但**這筆順序顛倒要記著**：下一個看方向文件的人若照 §6 的舊字面，
會以為設定格式還沒定案而自己發明第二種。

## §5 不做

- **為了通用而通用**：沒有第二個部門真的要用之前，不做抽象化重構。U-5 是防這件事的——
  抽象層本身不會讓任何人少犯錯。
- **重寫**：既有 302 項回歸網與八條規則是資產，通用化是**搬移與分層**，不是從頭寫。

## §6 狀態

- 2026-08-05 立案，§1 現況債務全部實測（66 處硬編碼、8 條規則分類、5 個角色分類）。
- §2 分層原則與 §3 設計準則已定，**即刻對後續所有 harness 設計生效**。
  上線當天第一個被擋下的是我自己：§8 索引句寫了 140 字，防膨脹閘門（上限 120）當場退回。
- ~~§4 五項待決，未討論前不動工。~~ ~~2026-08-13 更新：D-2 已拍板。~~
  **2026-09-03 更新：D-2、D-1 已拍板**（D-2 (a)·拆兩層；D-1 三選一作廢·改為
  git ＋ fail-closed 接線器 ＋ 只做 `same-person-new-pc` 剖面·均見 §4 表後），
  **D-3／D-4／D-5 仍待決，未討論前不動工**。
  ⚠ D-1 的**實作尚未開始**——拍板的是方向，接線器一行都還沒寫；
  §4 那張「本輪衍生的待辦」是與 D-1 無關、任一案都要做的獨立工項，不要當成 D-1 已落地。
  ⚠ 對抗式覆核**已跑到第 3 輪**，三輪都**不是零改動輪**⇒ 本檔**不得**蓋
  `ADVERSARIAL_REVIEW_PASSED`。預設上限 4 輪，**目前用掉 3 輪**。
  ⚠ **第 3 輪的處置動了做法本身**（2026-09-03）：接線器規格已收成**單一真相**
  （定案第 2 點的 W／P 兩表），處置表只准寫「判定 ＋ 指向」；
  且 **user 拍板不直接派第 4 輪**——先寫 2–3 支探針實跑，拿真實輸出回頭修規格再派。
  理由：三輪打的全是規格文字，蓋章只能證明文件內部一致，不能證明換機裝得起來。
  ⚠ D-2 的實作早於決定紀錄（`CONTEXT_HEALTH_PLAN` 覆核 F-7 抓到的順序顛倒）；
  不回退實作，但這一行**必須維持最新**——舊字面會讓下一個人以為設定格式還沒定案。

### 已完成：分層標註 ＋ 角色去專案化（2026-08-05）

**37 支全標完：全域層 5、核心層 28、專案層 4。** 每支在 docstring／正文寫明理由。

專案層剩下的 4 支全是規則（`db1_deploy`／`r1_default_migration`／`r3_ops_backup_scp`／
`r4_server_dbpath`），各自綁本平台的部署形狀、設定儲存形狀、備份路徑、資料層形狀。

**5 個角色全部改成全域層並抽掉專案綁定**：

| 角色 | 抽走了什麼 | 改成 |
|---|---|---|
| `雙改檢核員` | `SOP/05_UI_Demo/`／`SOP_PROD/05_UI_Demo/`、`?v=`、`node --check`、比對指令 | 開工讀 `PROJECT_CONTEXT.md`「雙目錄同步」節；讀不到就回報「這個專案沒有定義雙目錄結構」 |
| `project-auditor` | `SOP_PROD/…/docs/*_PLAN.md`、`CLAUDE.md §8`、記憶檔位置 | 開工讀「規則與文件在哪」節；讀不到就停，不自己掃 repo 猜 |
| `美編人員` | 「開工先讀」那張本平台規則檔表、可改檔案清單 | 開工讀「前端與樣式」節（harness 看板仍直接歸它，那是跨專案共用元件） |
| `查詢員` | 「本平台 DEV/PROD 是雙份的」 | 開工讀，有「雙目錄同步」節才當雙份處理 |
| `harness-auditor` | 無（本來就稽核 harness 自己） | 標註理由改寫 |

新增 `<repo>/.claude/PROJECT_CONTEXT.md`：五節（專案是什麼／雙目錄同步／規則與文件在哪／
前端與樣式／部署／驗證慣例）。**新部門導入時這是唯一必填的檔案。**

### 兩項實測（2026-08-05・都通過）

**① 全域層角色在 VSCode 成立** —— `~/.claude/agents/` 放探針角色，headless 與 **VSCode 都派得到**，
工具清單與 frontmatter 逐字一致（`tools: Read` → 只有 Read），且讀得到 user 層檔案。
**7/30「放 user 層永遠載不到」的結論確定推翻**（那次 `~/.claude/agents/` 目錄根本不存在）。

⚠ 同時訂正一個當天稍早的誤判：角色檔改動**不是「既有 session 內不會重載」，是有延遲後會生效**
（探針建檔約半小時後自己出現在既有 session 的可用清單裡）。這與 7/30 記憶檔那條
「拿一次 not found 就斷言要重啟是錯的」是同一件事。

**② 角色真的會去讀設定** —— 派改造後的 `雙改檢核員` 做一次真實檢核：

- 回報第一行即「依據來源：`PROJECT_CONTEXT.md`「雙目錄同步」節」，並列出讀到的目錄與檔案清單
- 四項檢核全跑，**29 次工具調用**，依據欄都是實際指令
- 版號那項字面上不成立時，它**推理出原因並把依據攤開**（「讓你能推翻我」），沒有硬判也沒含混
- **碰到設定沒寫的東西（`version.json` 兩端差異）選擇回報而不是自己猜** ——
  這正是整個改造要防的失敗模式，而它做對了

它的建議也採納了：`version.json` 是刻意的兩條版號流、`bust` 本來就落後一格、`db/` gitignored
要用 `cmp` 比 —— 都補進 `PROJECT_CONTEXT.md` 的「不在雙改清單裡的東西」，
**免得每個檢核員都問一次同樣的問題**。

### 已完成：角色搬到全域層（2026-08-05・user 選 junction 方案）

實體 `D:\Patrick-AI\.ai-harness\agents\`（跟著 harness 版控）→ `~/.claude/agents` 用 **junction** 接過去
→ **既是全域層（Claude 每個專案自動載入）又有 git 歷史**。專案層 `<repo>/.claude/agents/` 已清空。

- **搬法刻意分三步**：先複製、再建 junction 並驗內容一致（`cmp` 五支全 byte-identical）、
  **最後才刪原檔**。中途失敗時角色不能兩邊都沒有。
- `bootstrap.ps1` 補了 agents junction 段（新機器一次到位），並在 junction 解析到**空目錄**時警告
  —— 那個狀態最危險：角色會**靜默地不存在**，而畫面上跟「還沒建角色」一模一樣。
- 順手拆掉一個會靜默失效的推導：`available_skills()` 原本用 `AGENTS_DIR.parent / "skills"`，
  **角色搬走後那個「旁邊一定有」的假設就錯了**。改成獨立常數 `SKILLS_DIR`，
  同時把散落的專案路徑集中成單一 `PROJECT_ROOT`（往 U-1 前進，不是新增債務）。
- 測試綁**實體路徑**不綁 junction：junction 斷掉時要看得出「角色不在該在的地方」，
  走 junction 讀會讓「連結斷了」偽裝成「角色目錄是空的」。
- 驗證：派 `查詢員` → 讀到新版正文的【全域層】標註、確認專案層 0 個 `.md`、仍派得動。
  回歸網 308/308、分層 37/37。

### 已完成：角色正式命名（2026-08-05・user 要求中英文都要、顯示以中文為主）

**識別字統一英文（好打、跨環境穩、與內建角色同風格），畫面一律顯示中文。**

| 中文正式名 | `subagent_type` | 舊識別字（`aliases:`） |
|---|---|---|
| 查詢員 | `locator` | 查詢員 |
| 美編人員 | `visual-designer` | 美編人員 |
| 雙改檢核員 | `sync-checker` | 雙改檢核員 |
| 專案稽核員 | `project-auditor` | — |
| 平台稽核員 | `harness-auditor` | — |
| 規劃師／探查員／通用助手 | `Plan`／`Explore`／`general-purpose`（內建，改不了） | — |

- **檔名也改英文**：中文檔名在 git 裡會被轉義成 `\346\237\245…`，diff 與 log 都難讀。
- **舊識別字用 `aliases:` 接住**，看板 `merged_stats()` 把改名前的次數併回同一個人 ——
  不接的話畫面會出現「`locator` 從未被派過」旁邊掛一個沒人認得的外援角色「查詢員 11 次」。
- **卡片顯示中文名 ＋ 識別字小字**（`.rt-id`，點一下整段選取好複製）：
  顯示以中文為主，但識別字必須看得到，否則要派工時得回去翻角色檔。
- 測試補兩條**負向檢查**：每個自建角色都要有中文顯示名、內建角色也要有 ——
  沒填 `display_name` 會退回識別字，而「沒填」跟「刻意同名」在畫面上長得一樣。
- 順手治掉第五個「兩份判準」發作點：`subagent_stats` 與 `gen_roles_topology` 各存一份
  測試 session 過濾規則且**已經漂開**（後者多三個前綴）。收成單一真相，前者為準。

配套（**標註不能只做一次**）：

- `rulefile/check_layers.py` —— 掃描 37 支、列出未標的、零目標拒跑。
- `tests/test_layer_marks.py` 接進回歸網（現在 **306/306**），驗四件事：覆蓋率 100%、
  核心層非空、專案層非空、掃描範圍沒縮水（防「把 `SCAN_DIRS` 改窄讓覆蓋率假性變好看」）。
- **變異證明會紅**：拿掉任一支的標註 → 測試 exit 1 且指名該項。
- ⚠ **這一層守不到「標錯層」**：把 `db1` 從專案層改成核心層，覆蓋率仍 100%、分佈仍非空 →
  測試會綠。個別檔案標得對不對只能靠 review，測試只擋「沒標」與「整批消失」。
  誠實記在這裡，免得下次有人以為它什麼都擋得住。
