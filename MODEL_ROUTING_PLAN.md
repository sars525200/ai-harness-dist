# Model Routing 與成本精算化計畫書

> 2026-08-05 立案。來源：`D:\Patrick-AI\.ai-harness\參考\Model routing\` 20 張圖（user 提供）。
> 與 [`WORKFLOW_5STAGE_PLAN.md`](WORKFLOW_5STAGE_PLAN.md) 是同一套系統的兩半——
> 那份講**流程與交接**，這份講**每階段派誰上場、花多少錢**。
> **本檔為計畫書，未逐項討論前不動工。**

---

## §0 核心主張（三句話）

1. **同一份工作要拆成「處理未知方向」與「純執行」兩段**，前段需要判斷力、後段不需要。
2. **派誰上場是二維決策**：模型強度（誰）× 推理強度（想多久）。四種組合各有適用情境。
3. **真正的成本是「交付總成本」不是 API 單價**：Token 成本 ＋ 成品能不能用 ＋ 重跑次數 ＋ Review 時間。

---

## §1 四象限路由矩陣

|  | 便宜模型（快·大量） | 強模型（貴·判斷力好） |
|---|---|---|
| **高推理**<br>（多想幾步·反覆檢查） | **③ Spec 清楚·執行有難度**<br>例：依既有規格補複雜邏輯<br>→「多給思考預算，細節做好」 | **① 方向不明·走錯代價高**<br>例：產品策略／系統架構／重要 Spec／高風險 Review<br>→「**判斷錯 → 再快都白做**」 |
| **低推理**<br>（快速回答） | **④ 機械·重複·容易檢查**<br>例：整理資料／分類／摘要／改格式／固定檢查 | **② 需要好判斷·不用想很久**<br>例：3 個產品方向 → 快速初篩<br>→「**Reasoning 先別拉滿**」 |

**判準**：先問「方向明不明」→ 決定 X 軸；再問「錯了要重做多少」→ 決定 Y 軸。

---

## §2 五階段的編制與模型配置

| 階段 | 編制 | 模型配置 | 交付物 |
|---|---|---|---|
| **Research** | **Manager（強模型）＋ Scout ×N（便宜模型）**<br>Scout 分頭查（競品／使用者需求／相關技術／法規案例）<br>Manager 負責**去重·找矛盾·補缺口** | Scout：④<br>Manager：① | **決策報告** |
| **Design & Spec** | 主導者單人 | **①強×高**<br>「逼出模糊地帶」 | **工作規格**（好 Spec 六問，見下） |
| **Execute** | 按任務類型分（頁面／文案整理／資料轉換／Code·測試）<br>每個任務一組**四件套** | Spec 清楚 → ③或④ | **可檢查成品** |
| **Review** | **Checker 小隊 ×N（便宜模型）**<br>各查一個面向，**只交證據不下判決**<br>→ **強模型判決** | 一般初檢：④<br>衝突·安全·法規·商業：**拉高推理** | **清單 ＋ 證據** |
| **Fix & Deploy** | 主導者單人 | **方向已定 → 便宜模型就夠** | **修完上線** |

### 好 Spec 的六問

哪些頁面？／服務誰？／資料哪來？／設計原則？／**Success Criteria**？／**什麼不能做**？

### Execute 的四件套（每個任務都要有）

**輸入** → **交付** → **禁區** → **驗收**

### Review 的三個出口

Checker 交證據 → 強模型判決：**退回修改** ／ **可以接受** ／ **交給人決定**

### Fix & Deploy 四步

01 問題清單 → 02 改 Code·補格式 → 03 重跑測試 → 04 部署

---

## §3 成本結構與精算

### 3.1 任務成本 ＝ 四部分

**Token 成本 ＋ 成品能不能用 ＋ 重跑次數 ＋ Review 時間**

只算第一項＝只看得到帳單，看不到「便宜模型做壞了重跑三次」的真實代價。

### 3.2 不要背價格，要看成本結構

價格會變。要問的是三個問題：**哪一段 Token 量最大？哪一段 Output 最多？哪一段最值得 Routing？**

**額度桶不只一個**（2026-08-28 從全域 `CLAUDE.md` §4.2 搬入——它是平台事實不是工作規則，
放常駐層每則都付，但它不改變任何一次決策）：**Fable 5 燒獨立的 Weekly Fable 桶、權重 2 倍**，
與 Opus／Sonnet 不同池。⇒ 算「這個月還剩多少」時它不佔 Opus 的額度，
但**它自己的桶消耗得比看起來快一倍**。

### 3.3 執行階段：關鍵在 Output

Input 與 Output 的單價差距通常在 **5 倍**量級（圖例：$10/M vs $50/M）。
**Output 越多，模型選擇的差距越大**——所以「只看 Input 單價」會誤判。

### 3.4 圖上的試算（方法 A vs B）

| | 配置 | 成本 |
|---|---|---|
| **A** 全用最強模型 | 82 萬 In × $10 ＋ 27 萬 Out × $50 | **$21.70** |
| **B** Model Routing | Research+Planning → 強模型 $5.00<br>Execute → 中階 $2.10<br>Review → 中階 $1.35<br>Fix+Deploy → 中階 $0.70 | **$9.15** |

**差異 −$12.55（−58%）**：「只換模型 ＝ 帳單砍半」。

### 3.5 ⚠ 這個試算模型少了一段：**cache**

圖上的公式是 `Input × 單價 + Output × 單價`。但在 Claude Code 這種**每回合重送對話歷史**的形態下，
真正的大宗是 **cache read**。本專案 2026-08-05 實測：

| | input | output | cache write | cache read |
|---|---|---|---|---|
| opus | 6,210 | **4,064,220** | 29,948,688 | **1,442,965,817** |
| sonnet | 20 | 11,271 | 115,001 | 608,800 |

**cache read 是 output 的 355 倍。** 精算模型必須是四項而不是兩項：

```
成本 = input×P_in + output×P_out + cache_write×P_cw + cache_read×P_cr
```

**這筆資料 harness 已經在收**（`gen_cost_panel.aggregate_tokens()` 的 `in`／`out`／`cw`／`cr`），
只是目前沒有拿來做階段歸因。

### 3.6 單價表（2026-08-06 補·來源 `/claude-api` skill 官方快取 2026-06-24）

**不要憑記憶寫單價**——寫錯的儀表比沒有儀表更危險。實體在
`dashboard/gen_cost_panel.py` 的 `PRICE_IN`，本節只是給人看的對照。

| 家族 | input $/M | output（×5） | cache read（×0.1） | cache write 5m（×1.25） | cache write 1h（×2） |
|---|---|---|---|---|---|
| fable | $10.00 | $50.00 | $1.00 | $12.50 | $20.00 |
| opus | $5.00 | $25.00 | $0.50 | $6.25 | $10.00 |
| sonnet | $3.00 | $15.00 | $0.30 | $3.75 | $6.00 |
| haiku | $1.00 | $5.00 | $0.10 | $1.25 | $2.00 |

output 與各 cache 項都是 input 的固定倍數（官方定價規則），所以**只存 input 一欄**、
其餘用倍數推——單價調整時只有一個地方要改。
（sonnet-5 到 2026-08-31 有 $2/$10 優惠價；本表沿用標準價與 ccusage 同口徑，
差額落在對帳差裡而不是被藏起來。）

**§3.5 的四項公式要再細一格**——cache write 兩種 TTL 不同價：

```
成本 = input×P + output×5P + cw_5m×1.25P + cw_1h×2P + cache_read×0.1P
```

ccusage 把 `ephemeral_1h` 與 `ephemeral_5m` 併成一欄（正是它反解殘差 36% 的原因），
但**原始 transcript 的 `usage.cache_creation` 有分**——這正是自算比外包準的地方。

---

## §4 ★ 現況對照：harness 缺什麼

### 4.1 已經有的（不用重做）

| 能力 | 現況 |
|---|---|
| 角色機制 | 5 自建＋3 內建，全域層，可設 `model` |
| 角色能力邊界 | `tools:` ＋ agent-scoped hook 閘門 |
| 觀測 | 平台 subagent 紀錄（派工次數／工具／目標／實際模型） |
| Token 資料 | **已拆 in／out／cache write／cache read**，按日按 family |
| 成本 | 按模型、按日（$8,744.64 / 32 天） |
| Review 的強模型判決 | `/adversarial-review`（Plan ＋ effort high） |

### 4.2 缺的（按難度排序）

| # | 缺口 | 難度 | 說明 |
|---|---|---|---|
| **G1** | ~~推理強度在 subagent 層無法設定~~ **已推翻（8/5 查證）** | 低 | 初版寫「平台限制」是**只查了 Agent 工具參數就下結論**。實際上**角色 frontmatter 支援 `effort:`**（`low`/`medium`/`high`/`xhigh`/`max` 或數字，官方 agent-sdk/subagents 文件），另有全域環境變數 `CLAUDE_CODE_EFFORT_LEVEL`。**Y 軸做得到，但綁在角色上**（每支角色一個固定值），不能逐次派工調 —— 對本設計反而剛好：五階段的角色本來就各守一格象限。⚠ 文件宣稱，**尚未實測**（待驗：frontmatter 加 `effort:` 後派工，行為有沒有變） |
| **G2** | ~~按階段的成本歸因~~ **✅ 已做（8/6）** | 中 | 全域 CLAUDE.md §2 自我宣告加「階段」欄；`gen_cost_panel.stage_attribution()` 離線從 transcript 撈宣告行歸因，五項公式自算金額，看板新增「階段成本歸因」區塊。**未標記佔比本身就是宣告紀律的量測**，照實顯示不藏。細節見 §4.4 |
| **G3** | Scout 平行搜集模式 | 中 | 現在派 subagent 是一次一個、序列的。沒有「一次撒 N 個便宜模型分頭查、再由強模型去重找矛盾」的編排 |
| **G4** | Checker 小隊 ＋「只交證據不下判決」 | 中 | Review 角色有三個，但**沒有並行小隊的形狀**，也沒有把「不下判決」寫成角色紀律 |
| **G5** | Execute 四件套（輸入／交付／禁區／驗收） | 低 | 角色檔各自有輸出格式，但**沒有統一的四件套結構**。這是寫規範就能補的 |
| **G6** | Design 階段的執行器（好 Spec 六問） | 低 | 14 支 skill 裡 **Design 階段 0 支**。六問可以直接寫成 skill |
| **G7** | 交付總成本的另外三項 | 高 | 「成品能不能用／重跑次數／Review 時間」目前**完全沒有量測**。重跑次數理論上可從 event log 推（同一任務被重派幾次），另外兩項需要新的資料來源 |

### 4.3 最要緊的一條（8/5 修正）

初版斷言「G1 是平台限制」被查證推翻——**四象限兩軸都做得到**：

- ✅ X 軸選模型：派工參數 `Agent(model=...)` 或角色 frontmatter `model:`
- ✅ Y 軸選推理強度：角色 frontmatter `effort:`（**每支角色固定一格，不能逐次調**）

「綁角色不綁派工」對本設計剛好：Scout＝便宜×低、Checker＝便宜×低、
Manager/判決＝強×高、初篩型＝強×低——**角色本身就是象限的載體**。
教訓照 U 系準則記下：**「工具參數沒有」≠「平台做不到」，同一能力常有第二個掛載點**
（跟 7/30「user 層角色載不到」被推翻是同一型錯誤：查了一條路徑就下全稱結論）。

### 4.4 G2 實作記錄（2026-08-06）——五個踩到的坑

做的時候踩到五件事，每一件單獨看都會**靜默算錯**（表照樣長得很正常）：

1. **同一則 API 訊息在 transcript 拆成多筆**（每個 content block 一筆、`message.id`
   與 `usage` 完全相同）。實測 45% 是重複、同 id 的 usage 零不一致。
   mix 是比例還能互相抵消，**金額不去重就是直接灌水近一倍**。
2. **transcript 同一個檔同時有兩種中文編碼**——字面 UTF-8 與 `\uXXXX` 逃逸。
   第一版拿原始行做 `"階段" in line` 前置過濾，會**靜默漏掉逃逸那一半的宣告**，
   而漏掉的部分安靜地落進「未標記」——看起來只是紀律差，其實是解析器壞了。
   **教訓同型於 §4.3**：查了一條路徑（一種編碼）就下全稱結論。
3. **宣告要兩段式解析**。一則訊息的 thinking block 排在 text block 前面且各存一筆，
   邊走邊判的話**宣告那一則的 usage 會被算進上一個階段**。改成先解出
   「哪個 message id 宣告了哪個階段」，再照順序歸因。

4. **分母要跟規則同齡**。第一版把 32 天歷史全算進「未標記」，於是那個佔比
   **永遠是 100%**——歷史成本會把新資料淹掉好幾週，「未標記佔比＝宣告紀律」
   這個量測從第一天起就是壞的。改成只看規則生效日以後。
5. **起算日是「當地日期」、transcript 是 UTC**。直接拿 `timestamp[:10] < "2026-08-06"`
   比，會把**當地今天的前 8 小時整段砍掉**（01:15 當地＝前一天 17:15 UTC），
   表格靜默變空。換算用本機時區、不寫死 +8（換一個部門就錯）。

另外**宣告只認 assistant 訊息**：user 貼一句規則文或引用舊對話就會命中「階段 X」，
讓一句引用改寫成本歸因是最陰的一種錯。以上都有回歸測試 ＋ 變異測試守
（`test_cost_panel.py` 4 個 case、`mutate_cost_panel.py` 新增 12 個變異，26 個全紅）。

**第 4、5 兩條是同一次事故的兩半**：修完 4 之後表格變空，若當時把「空表」當成
「還沒有資料」就會漏掉 5。**「改完之後數字變成 0」永遠要先當成 bug 查一次**——
這也是為什麼零紀錄時整段不能消失（消失就沒有人會發現時區設錯了）。

**自算 vs ccusage 對帳**：自算 US$9,372、ccusage US$8,745，差 +7%。兩個已知來源——
ccusage 有 7/148 個 session 對不上（它少算）、sonnet-5 優惠價未計（我方多算）。
**對帳仍以 ccusage 為準**，這張表要回答的是「錢花在哪個階段」。

---

## §5 待決分岔（逐項討論，同意才做）

| # | 分岔 | 選項 | 傾向 |
|---|---|---|---|
| ~~M-1~~ | ~~G1 怎麼繞~~ | **已由查證解決（8/5）**：角色 frontmatter 就支援 `effort:`，不必繞。每支角色釘一格象限（model×effort），首次使用前先實測一次 frontmatter effort 真的生效 | — |
| M-2 | 階段歸因怎麼標 | (a) 自我宣告加「階段」欄位 (b) hook 從工具序列推斷 (c) 每階段開新 session | **(a)**：現行已有自我宣告，加一欄成本最低；(b) 不準、(c) 破壞脈絡 |
| M-3 | 先補哪個缺口 | (a) G2 成本歸因 (b) G5+G6 規範與 skill (c) G3+G4 並行編制 | **(a)**：沒有階段成本，其餘優化都無法驗證有沒有效 |
| M-4 | 成本模型要不要納入 cache | (a) 納入（四項公式） (b) 沿用圖上的兩項 | **(a)**：本專案 cache read 是 output 的 355 倍，不納入等於算錯 |
| M-5 | 「交付總成本」的另三項要不要量 | (a) 只量重跑次數（可從 event log 推） (b) 三項都量 (c) 先不量 | **(a)**：重跑次數有現成資料源；另兩項需要人工輸入，先不做 |

---

## §6 狀態

- 2026-08-05 立案，20 張來源圖已全部讀過並轉成本檔的 §1–§3。
- §4 現況對照已實際查證：**token 四項資料確認存在**；初版「`Agent` 工具無 `effort` ＝ 平台限制」
  被同日查證**推翻**——角色 frontmatter 支援 `effort:`（見 §4.3 修正）。
- **§5 五項全部定案（2026-08-05 逐項討論）**：
  - ~~M-1~~ 查證解決：角色 frontmatter 釘 `effort:`，每支角色一格象限；首用前實測生效。
  - M-2 ✅ **自我宣告加「階段」欄**（Research/Design/Execute/Review/Fix），
    成本面板離線批次從 transcript 撈宣告行做歸因；「未標記」佔比本身就是宣告紀律的量測。
  - M-3 ✅ **先補 G2 成本歸因**——沒有階段成本，其餘優化都無法驗證有沒有效。
    之後順序：G5/G6 規範與 skill → G3/G4 並行編制 → G7。
  - M-4 ✅ **四項公式**（in／out／cache_write／cache_read）——cache read 是 output 的
    355 倍，兩項公式等於算錯。
  - M-5 ✅ **先只量重跑次數**（event log 有現成資料源）；「成品能用？」「Review 時間」
    需人工回報，漏填比沒有更誤導，暫不做。
- **2026-08-06：G2 完工**（M-3 排的第一項）。全域 §2 自我宣告加「階段」欄、
  產生器加 `stage_attribution()` 與 `PRICE_IN` 單價表、看板加「階段成本歸因」區塊。
  回歸網 328/328、eval 六級全過、23 個變異全紅。踩到的三個坑記在 §4.4。
  **現況：宣告 0 次、未標記 100%**——規則今天才上線，這是正確的初始狀態，
  下一輪開始就會看到分佈。
- **順帶修掉一個假紅**：`test_layers.py` 把「AI-Projects 沒有 `.claude`」寫死進斷言，
  那個目錄今天被建出來就變成假紅（產生器是對的、環境變了）。改成量測恆真性質
  （`exists` 與磁碟一致 ＋ 用臨時目錄造受控樣本），並用兩個變異證明它還會紅。
  **環境現況禁寫死**這條規則本來就在 `feedback-execution-test-before-deploy`，
  這是它第一次真的被觸發。
- **下一項（M-3 順序）**：G5 Execute 四件套 ＋ G6 好 Spec 六問 skill。

---

## §7 派工的實測理由與環境障礙（2026-08-14 從全域 `CLAUDE.md` §4.1 搬入）

搬入理由同 `WORKFLOW_5STAGE_PLAN.md` §12：常駐層只留「一律派、不先問」那條指令，
**支撐它的兩組實測數字與一個環境坑**放這裡。（`check_bloat` 2026-08-14 報這兩條 181 字／130 字超標。）

### 7.1 為什麼預設派出去（兩組實測）

- **2026-08-07：單一 session 的 API 時間是工具時間的 11.6 倍**
  ⇒ **慢在模型推理，不在跑指令**。優化跑指令的速度是在優化 8% 的那一段。
- **不派工會讓它指數惡化**：檔案全文、grep 輸出、測試 log 全堆在主 session 的 context 裡
  **每輪重送**。派工**同時解兩邊**——便宜模型跑粗活（單價低），
  且**原始資料留在 subagent 那側**（主 session 的 context 不長）。
- **2026-08-06：每 token 成本差 2.9 倍**（Opus vs Sonnet 實測，非牌價推算）。

### 7.2 `CLAUDE_CODE_CHILD_SESSION=1` 會帶一條預設的禁派工指令

**VSCode 面板是常態**（不是例外情況）。那條 session 指令會說「除非 user 要求，否則不要派 subagent」。

- **全域 `CLAUDE.md` §4.1 的常設授權就是它要的那個「user requested」** ⇒ 宣告一句後照派。
- ⚠ **被擋住時必須當場說**，別默默自己做完——
  「安靜地退回單 session 模式」會讓上面那兩組數字的優化全部失效，而且不會有任何徵兆。

---

## §8 子 effort：task-identity-cost-attribution（2026-08-23 開圖）

規劃層走 wayfinder map，不在本檔展開：
`D:\Patrick-AI\.ai-harness\.scratch\task-identity-cost-attribution\map.md`

它從本檔長出來的理由：到達點是「判斷派工準不準、錢花在哪個任務」，
正是 §1 四象限與 §4.4 階段成本歸因沒有回答的那一半 —— 現有兩軸都是「實際用了多少」，
缺「該用多少」那一軸，而那個值（規模欄）早就在宣告裡。

<!-- REVIEW_SCOPE_IGNORE_START -->
### 決策票待辦（票開出來就回寫一列，關掉改成 ✅ 已完成）

<!-- 票檔在 .scratch\task-identity-cost-attribution\issues\ 底下；
     說明格刻意保持精簡 —— 產生器取「狀態格以外最長的那一格」當標題，
     說明格寫太長會把票名整個搶走。 -->

| 狀態 | 項目 | 說明 |
|---|---|---|
| ✅ 已完成 | 票 01 「任務」欄的字面形狀與解析錨點 | 第二欄；要求分隔符 |
| ⏳ 待做 | 票 02 未結案任務的狀態檔：格式、結案與改名 | issues/02 |
| ⏳ 待做 | 票 03 歸因資料層：兩條游標的資料結構 | issues/03 |
| ✅ 已完成 | 票 04 「任務分類」死欄位的消費端要長什麼樣 | 拆標籤·值域外置 |
| ⏳ 待做 | 票 05 看板那張表要回答什麼問題 | issues/05 |
| ✅ 已完成 | 票 06 任務身分 vs effort_of_path 兩套 key | 裁決 (c)：任務欄取代 |
| ✅ 已完成 | 票 07 宣告分隔符契約與既有髒資料 | 吃三種；20%→0% |
| ✅ 已完成 | 票 08 任務的專案維度該從哪裡來 | 寫檔路徑；harness +69 段 |
| ⏳ 待做 | 票 09 規模 × 派工強度第一版要存哪些欄位 | issues/09 |
| ✅ 已完成 | 票 10 cost_panel 的語料範圍與宣告偵測口徑 | 單一真相；未標記 6.5%→1.2% |
<!-- REVIEW_SCOPE_IGNORE_END -->

## §9 MOD-1 模型錯配提醒（2026-09-05 開·**計畫段·等逐項同意才動程式**）

### 9.1 現況（實測，非推論）

近 7 天加權用量（唯讀掃 `~/.claude/projects/**/*.jsonl`，讀 0.1／寫 2／輸出 5）：

| 類別 | 模型 | 佔比 | 回合 |
|---|---|---|---|
| 主 session | Opus | **71.5%** | 374 |
| 主 session | Sonnet | 13.2% | 62 |
| 主 session | Fable | 6.3% | 37 |
| subagent | Fable | 4.3% | 16 |
| subagent | Sonnet | 2.5% | 25 |
| subagent | Opus | 1.7% | 12 |
| 主 session | Haiku | 0.5% | 2 |

**Opus 之內 97.7% 是主 session。** 全域 `settings.json` 的 `model` 一直是 `opus[1m]`
⇒ 每一則新對話開場就是 Opus，§4.2「預設 Sonnet、M 級才升」從來沒有承載體，
只寫在散文裡。**2026-09-05 21:35 已改成 `sonnet[1m]`**（兩份副本：`~\.claude\settings.json`
與 `global/settings.json`，JSON 皆驗過）。Fable 合計 10.6%，§4.2 目標 <5%。

### 9.2 官方可行性（2026-09-05 查 `sub-agents`／`hooks` 官方頁）

- **沒有任何 hook 能換模型**：`PreModelSwitch` 只能擋、`PostModelSwitch` 只能觀察、
  `PreToolUse` 不能改 tool input。官方明寫沒有依難度或卡住自動換模型的機制。
- subagent 模型解析順序：**派工時的 `model` 參數 → 角色 frontmatter → `CLAUDE_CODE_SUBAGENT_MODEL` → 主對話模型**。
  ⇒ 判斷難度的人本來就是派工的主 session，把手已經有，缺的是紀律。
- 主 session 只有人用 `/model` 或 `opusplan` 能換（後者每次切換破快取重付全部 input）。
- hook 輸入的 `model` 欄**只有 `SessionStart` 有、且不保證給**；但每個 hook 都拿到
  `transcript_path`，jsonl 每則 assistant 訊息都帶 `model` ⇒ 讀最後一則就是當前模型。

⇒ **能做的只有「偵測錯配、提醒人切」**，不能自動換。名字叫 MOD-1。

### 9.3 做法

新增 `hooks/rules/mod1_model_scale_match.py`，`UserPromptSubmit`（走現有 `dispatch.py`，不新增 hook 佈線）：

1. 從 `transcript_path` 反向讀最後一則 assistant 訊息的 `message.model` → 當前模型家族。
2. 從本輪 assistant 文字取宣告的 `規模` 欄。**正則不自己寫**——沿用
   `dashboard/gen_workflow_compliance.py:174` 的 `scale`，比照 DECL-1 的做法各留一份
   ＋測試驗逐字相同（`hooks/` 不 import `dashboard/`）。
3. 判準（只有兩種錯配會講，其餘沉默）：
   - `規模 L` ＋ Opus／Fable → 「輕量任務用重模型」
   - `規模 M` ＋ Sonnet／Haiku → 「大型任務用輕模型」
   - `規模 待定`／無宣告 → **沉默**（DECL-1 已經在管沒宣告這件事，不重複叫）
4. `warn` 不 `block`。訊息帶「現在是什麼、宣告是什麼、建議切成什麼」三段。
5. **同一則對話同一個方向只講一次**（比照 BUDGET-1 的去重，但 key 是 `session_id + 方向`，
   不是「一天一次」——換方向要能再講）。

### 9.4 驗證（動工前寫好）

`tests/test_mod1.py`，每條都要答得出「怎麼證明它會紅」：

| 案例 | 輸入 | 期望 |
|---|---|---|
| a | 規模 L ＋ model=claude-opus-5 | WARN，訊息含「L」與「Opus」 |
| b | 規模 M ＋ model=claude-sonnet-5 | WARN，方向相反 |
| c | 規模 M ＋ model=claude-opus-5 | 沉默（這是對的配置） |
| d | 規模 待定 | 沉默 |
| e | 無宣告 | 沉默（交給 DECL-1） |
| f | transcript 讀不到 | 沉默，**不猜模型**（比照 QUOTA-1 資料源不新鮮就閉嘴） |
| g | 同一 session 第二次同方向 | 只講一次 |
| h | 正則與 `gen_workflow_compliance.py` 逐字相同 | 一邊改了另一邊沒跟 → 紅 |

跑 `py -3 tests/test_mod1.py` ＋ `py -3 tests/run_hook_tests.py` 回歸網不掉，
再用 `hooks/` 既有的假 payload 機制端到端餵一次。

### 9.5 不做什麼

- 不自動換模型（官方做不到，硬做就是假承載體）。
- 不管 subagent 的模型（占 Opus 2.3%，量體不值得）。
- 不擋（BLOCK）——錯配不是錯誤，是取捨，人有權選。

### 9.6 狀態

- 2026-09-05 21:35：預設值已改（見 9.1）。**MOD-1 本體＝計畫段，一行程式都還沒寫，等 user 逐項同意。**
- 待驗：預設值改動要**開一則新對話**才看得到效果（本則已在 Opus 上，不受影響）；
  七天後對帳看 Opus 佔比有沒有從 71.5% 掉下來。誰跑：user 開新對話時自然驗到。

### 9.7 ⚠ 設定檔改動可能從未被讀取：**關視窗 ≠ 結束程式**（2026-09-05 23:15 實測·**未分辨，懸置**）

9.6 的「開一則新對話就驗得到」**不成立**。桌面版 app 是 Electron，設定檔只在
**程式啟動時讀一次**；關掉視窗在 Windows 上只是縮到系統匣，主行程沒死、設定沒重讀。

**實測（唯讀，指令見下）**：

| 項目 | 值 |
|---|---|
| 主視窗行程 | `claude.exe` PID 19384，`C:\Program Files\WindowsApps\Claude_1.46388.2.0_x64__pzs8sxrjxfjjc\app\claude.exe` |
| 啟動時間 | **2026-09-04 23:27:26** |
| 量測時間 | 2026-09-05 23:14:51（**連續存活逾 24 小時**） |
| `model` 改成 `sonnet[1m]` 的時間 | 2026-09-05 21:35 |
| 判定 | 21:35 的改動**在此行程的生命週期內從未被讀取** |

**同時段八則對話的開場模型（23:12 量）**：七則 `claude-opus-5`，一則 `claude-sonnet-5`
（`d75c1b64`，22:58，舊對話延續、非新開）。含 23:09 全新開的 `341659d8`(73,177)、
對話 `ac525d89`(69,396) 都是 Opus。

**這組數字不能下判定**——在主行程沒重啟的前提下，「還是 Opus」是必然結果，
**既不能證明假設 B（介面選單優先），也不能排除假設 A（啟動時讀一次）**。
2026-09-05 23:10 user 以為已重開，實為關視窗；23:15 查行程才發現。

**⚠ 以下「分辨方法（唯一）」已於 2026-09-06 00:25 被 §9.9 推翻——不必重開 app 就分辨得出來。
保留原文只為記錄推理過程，不要照做。**

**分辨方法（原判·已作廢）**：系統匣右鍵 Claude 圖示 → Quit（**不是視窗右上角 ✕**）→ 重新開啟
→ 開一則全新對話打一句話 → 跑下面兩支指令。

```
# ① 確認主行程真的換了 PID / StartTime
Get-Process | Where-Object { $_.ProcessName -like '*laude*' } | Sort-Object StartTime |
  Select-Object -First 5 ProcessName, Id, StartTime | Format-Table -AutoSize
```

② 量開場模型與地板：見 `.scratch/handoff/20260905-token-saving-research.md`
「重開後怎麼量（可直接貼）」那段的 `py -3` 指令，不重抄。

**判準**：①的 StartTime 必須是重開之後 → 才看②。②開場出現 `claude-sonnet-5` ＝ 假設 A
成立、五個旋鈕生效；仍是 `claude-opus-5` ＝ 假設 B，介面選單才是第一真相，
**今晚五個旋鈕在介面層被架空、要重新想承載體**。

**對 9.6 的修正**：「開一則新對話就驗得到」要改成「**結束程式並重啟 app 之後，
開一則新對話才驗得到**」。這一條同時適用於今晚所有寫進 `settings.json` 的旋鈕，
不只 `model`。

### 9.8 待驗清單（2026-09-05 23:15 入列）

| 項目 | 為何沒驗 | 驗證指令逐字 | 誰跑 |
|---|---|---|---|
| `model: sonnet[1m]` 是否生效 | app 主行程未重啟，設定檔未重讀 | 見 9.7 的 ① ＋ ② | user 結束程式重開後，叫 Claude 跑 |
| 今晚其餘四個 `settings.json` 旋鈕 | 同上，同一個行程生命週期 | 同上（②的地板欄比對） | 同上 |
| 假設 A / B 分辨 | 前提（重啟）未達成 | 同上，看②的開場模型欄 | 同上 |

### 9.9 ✅ 分辨完成：**假設 A 死、假設 B 成立**——設定檔有讀，只有 `model` 被蓋掉（2026-09-06 00:25 實測）

不必重開 app。用**同一個 `settings.json` 裡、改動時間更晚的其他鍵**當對照組就分辨得出來。

**時間軸（全部有 commit 佐證）**：

| 時間 | 事件 | 出處 |
|---|---|---|
| 09-05 21:17 | `model` 由 `opus[1m]` 改 `sonnet[1m]` | commit `846941f` |
| 09-05 22:45 | 加入 `env.CLAUDE_CODE_SIMPLE_SYSTEM_PROMPT`、`skillOverrides`(13 支)、`autoCompactWindow`、`switchModelsOnFlag`、`crossSessionInbound` | commit `773b302` |
| 09-05 23:09 | **本則對話開場**（在以上全部改動之後） | jsonl `ac525d89` |
| 09-06 00:21 | 主行程 PID 19384 仍是 09-04 23:27 那顆，**從未重啟** | `Get-Process` |

**對照組實測（本則 session 內直接量）**：

| 鍵 | 加入時間 | 在本則生效？ | 證據 |
|---|---|---|---|
| `env.CLAUDE_CODE_SIMPLE_SYSTEM_PROMPT` | 22:45 | **✅ 生效** | `$env:CLAUDE_CODE_SIMPLE_SYSTEM_PROMPT` 讀到 `1` |
| `skillOverrides`（13 支轉 user-invocable-only） | 22:45 | **✅ 生效** | `code-review`／`design`／`simplify`／`loop`／`init` 均已從模型可見的 skill 清單消失 |
| `model: sonnet[1m]` | **21:17（更早）** | **❌ 沒生效** | 本則開場 `claude-opus-5` |

**推論（強）**：22:45 才寫進去的鍵，在 23:09 開的對話裡生效了，而主行程 24 小時沒重啟
⇒ **`settings.json` 是每則對話開場時重讀的，不是 app 啟動時讀一次。假設 A 被推翻。**
比它更早寫入的 `model` 卻沒生效 ⇒ **`model` 這個鍵被更高優先權的東西蓋掉。**

**已排除的覆寫來源**（都查過，都沒有 `model` 鍵）：
`~\.claude\settings.local.json`、`D:\Patrick-AI\.claude\settings.json`、
`D:\Patrick-AI\.claude\settings.local.json`、`~\.claude.json` 的 `projects.*`
（那裡只有 `lastModelUsage` 用量統計）。

**⇒ 覆寫者在桌面版 app 自己的儲存區**，由 app 在開 session 時以顯式參數傳入。
佐證：`CLAUDE_CODE_CHILD_SESSION=1` 也是 app 注入的，證明 app 確實會對 session 行程注入環境。

### 9.10 兩個尚未分辨的子假設（下一步只要做 B1 一件事）

| 假設 | 說明 | 怎麼分辨（便宜） |
|---|---|---|
| **B1** | `sonnet[1m]` 這個**值本身**不被接受（Sonnet 5 未必有 100 萬窗的權限），被靜默退回預設 | 把 `model` 改成**純 `sonnet`**（拿掉 `[1m]`）→ 開一則全新對話 → 量開場模型。**不必重開 app** |
| **B2** | app 介面的模型選單有顯式選定，優先於設定檔任何值 | B1 改完仍是 Opus ⇒ B2 成立。此時**唯一承載體是介面選單**，設定檔對 `model` 純屬裝飾 |

**注意 `[1m]` 的既有教訓**：交接檔「更正的三條錯誤事實」第 2 條已證實 `[1m]` 是 100 萬
context 窗、不是快取 TTL。**`opus[1m]` 一直有效** ⇒ Opus 有 1m 權限；Sonnet 有沒有，沒查過。
B1 的成本是改一個字串加開一則對話，**應該先做**。

### 9.11 對 §9.8 待驗清單的修正（2026-09-06 00:25）

- **「app 主行程未重啟」不再是任何一條的擋路原因**——設定檔每則對話重讀，已證實。
- 今晚五個旋鈕之中，**`env`、`skillOverrides` 兩項已驗到生效**；
  `autoCompactWindow`、`switchModelsOnFlag`、`crossSessionInbound` 三項與它們同一顆 commit、
  同一份檔案，**推定生效但未逐項量到**（推定，不是事實，別往下傳成事實）。
- **只剩 `model` 一項確定沒生效**，下一步是 §9.10 的 B1。

### 9.12 B1 已施作，等新對話驗（2026-09-06 00:35）

`model` 由 `sonnet[1m]` 改為純 **`sonnet`**，兩份副本同時改、改前後皆過 `json.loads`、
鍵數 14 不變：`~\.claude\settings.json` 與 `global\settings.json`。

**驗法（不必重開 app）**：user 開一則**全新對話**打一句話 → 回本則叫我跑
`.scratch/handoff/20260905-token-saving-research.md`「重開後怎麼量」那支 `py -3` 指令。

| 量到 | 判定 | 接下來 |
|---|---|---|
| 開場 `claude-sonnet-5` | **B1 成立**：`[1m]` 對 Sonnet 是非法值，被靜默退回預設 | 旋鈕生效，收尾。順帶把「非法值靜默退回」寫成教訓 |
| 開場仍 `claude-opus-5` | **B2 成立**：介面選單優先於設定檔 | `settings.json` 的 `model` 對桌面版純屬裝飾，MOD-1 要改用介面選單當承載體 |

**已知代價**：若 Sonnet 本來就有 1m 權限，拿掉 `[1m]` 會讓字窗回到 20 萬。
但目前**每則對話開場都是 Opus、Sonnet 根本沒被用到**，此刻等於零損失。
B1 驗完若成立、且確認 Sonnet 有 1m 權限，再決定要不要加回去。

### 9.13 ⚡ B1 驗完：**第三種結果——設定檔有效，但值解析不對**（2026-09-06 00:31 實測）

`settings.json` 寫入 00:29:07；user 於 **00:31:31** 開新對話打 `hi`（`0c2278d7`，`D--Patrick-AI`）。

| 設定值 | 實際開場模型 | 判定 |
|---|---|---|
| `opus[1m]`（09-05 21:17 前） | `claude-opus-5` | 相符 |
| `sonnet[1m]`（21:17–00:29） | `claude-opus-5` | 不符 |
| **`sonnet`（00:29 起）** | **`claude-haiku-4-5-20251001`** | **不符，且與上一格不同** |

`0c2278d7` 全部 4 個回合都是 Haiku，地板 52,418（**非** title 產生器那種小呼叫，是完整 session 脈絡）。

**⇒ 兩個假設同時被推翻**：

- **B2 死**：改設定檔的值**確實改變了開場模型**（Opus → Haiku）⇒ 介面選單不是唯一真相，`model` 這個鍵是活的。
- **B1 只對一半**：`[1m]` 確實有影響，但拿掉之後**沒有落在 Sonnet**，落到 Haiku。

**新假設 C（最可能）**：桌面版**不接受裸別名 `sonnet`**，無法解析時靜默退到某個預設（此處為 Haiku）。
佐證：`~\.claude.json` 的 `additionalModelOptionsCache` 用的是**完整 ID＋窗後綴**（`claude-fable-5[1m]`），
不是裸別名。**下一步：把 `model` 改成完整 ID `claude-sonnet-5`，再開一則新對話量。**

**⚠ 最重要的一條教訓**：`model` 填錯值**不會報錯、不會警告**，只會靜默換一個模型跑。
今晚兩個錯值分別跑成 Opus 與 Haiku ——**成本差 10 倍以上，而畫面上沒有任何提示。**
這條要進 `CLAUDE.md` §4.2：改 `model` 後**必須開新對話量一次**，不准假設它生效。

**地板附帶觀察（未隔離變因，不可當結論）**：對話 `0c2278d7` 地板 52,418，
同期 Opus 對話 69k–86k。差距可能來自 Haiku 工具集較小、也可能來自今晚的精簡旋鈕，**兩者未分離**。

### 9.14 ❌ 撤回 §9.13 的結論：**模型是 user 在介面手選的**（2026-09-06 00:40）

user 於 00:40 指出：對話 `0c2278d7`（那則 `hi`）**的模型是他自己在介面手選的**。

⇒ **§9.13「改設定檔就改變了開場模型（Opus → Haiku）」的因果推論不成立。**
Opus→Haiku 的變化來自**人手選**，不是我 00:29 那次改檔。§9.13 的「B2 死」**作廢**。

**同時失效的還有**：§9.9–§9.13 所有拿「開場模型」當設定檔生效證據的推論。
對話 `0c2278d7`、`ac525d89`、以及 09-05 那批「八則全 Opus」，**只要模型是手選的，就都不是對照組**。

**§9.9 有一段仍然成立、不受影響**：`env.CLAUDE_CODE_SIMPLE_SYSTEM_PROMPT` 與 `skillOverrides`
（22:45 寫入）在 23:09 開的 session 裡讀得到 ⇒ **`settings.json` 每則對話重讀，這條是硬證據，不撤回。**
撤回的只有「`model` 這個鍵的行為」那部分。

**重新定位問題（這才是真的槓桿）**：
Opus 佔配額 93–95% 的原因，**不是設定檔沒生效，是開新對話時人手選了 Opus**。
`settings.json` 的 `model` 只有在**人沒手選**的情況下才可能是承載體。
⇒ MOD-1 的下一步不是繼續試設定值，而是先問清楚：

| 待答（只有 user 知道） | 為什麼重要 |
|---|---|
| 是「每一則都手選」還是「只有這則」？ | 決定 09-05 那批 Opus 樣本能不能當證據，也決定手上所有數字要不要重取 |
| 介面選單有沒有「記住上次選擇／設預設」？ | 有的話，把它設成 Sonnet 就是真正的承載體，比改設定檔可靠 |

**⚠ 現況警告**：`settings.json` 的 `model` 目前停在 **`sonnet`**（00:29 改），
這個值**從未被驗證過是不是合法值**。在上表兩題答完之前，不要把它當成已知good的設定。

### 9.15 ✅ 承載體找到了：**介面模型選單（會記住上次選擇）**（2026-09-06 00:45·user 回答）

user 回答兩題（**這是 user 陳述的事實，不是我量到的**）：

| 問題 | 回答 |
|---|---|
| 每則新對話都手選模型嗎？ | **幾乎每則都手選** |
| 介面選單會記住上次選擇嗎？ | **會記住上次選的** |

**⇒ 三個結論**：

1. **所有把「開場模型」當設定檔證據的推論，全部作廢。** 包含 §9.9 關於 `model` 的部分、
   §9.10（B1／B2）、§9.12、§9.13。09-05 那批「八則全 Opus」、00:31 那則 Haiku，
   **全都是手選的結果，沒有任何一個是乾淨對照組**。
2. **仍然成立、不受影響**：`settings.json` **每則對話重讀**（證據＝`env.CLAUDE_CODE_SIMPLE_SYSTEM_PROMPT`
   與 `skillOverrides` 在 22:45 寫入、23:09 的 session 讀得到，而主行程 24 小時沒重啟）。
   09-05 那個「要重開 app 才生效」的判斷確實是錯的，這條撤回維持。
3. **桌面版的 `model` 鍵實質上是死鍵**：選單既然 sticky，每則新對話都會套用記住的選擇，
   設定檔那個值**沒有機會被用到**。我們手上**沒有任何一筆乾淨樣本**能證明它會不會生效。

**⇒ MOD-1 重新定位：這不是設定問題，是介面預設值問題。**

Opus 佔配額 93–95% 的真正成因＝**介面選單被記成 Opus，此後每則新對話自動沿用**。
`CLAUDE.md` §4.2「預設 Sonnet、M 級才升」的承載體**一直都在介面選單上**，
不在 `settings.json`、不在 hook、不在任何程式裡。

**唯一有效的動作（只有 user 能做，一次性）**：
在介面模型選單把它切成 **Sonnet**。因為 sticky，之後每則新對話都會是 Sonnet；
碰到 §4.2 列的硬任務（DB／邏輯／硬規則區／多檔協調／根因診斷／架構規劃）再手動升 Opus。
**這比任何設定檔改動都可靠，而且今天就生效。**

### 9.16 待處理：`settings.json` 的 `model` 目前是未驗證值

現值 **`sonnet`**（2026-09-06 00:29 由 `sonnet[1m]` 改成，為了測 B1）。

- **合法性未知**：整條驗證線因為手選而失效，`sonnet`／`sonnet[1m]`／`opus[1m]` **沒有一個被乾淨驗過**。
- **影響面**：桌面版用不到（選單 sticky 蓋掉）；**CLI 直接跑 `claude` 時沒有選單，那裡才會讀到它**。
- **待決**：要不要改回 `opus[1m]`（唯一長期在用、未見異常的值），還是留 `sonnet` 以符合 §4.2 預設。
  兩份副本要同步：`~\.claude\settings.json` 與 `global\settings.json`。

### 9.17 待驗清單（2026-09-06 00:45 覆蓋 §9.8、§9.11）

| 項目 | 為何沒驗 | 驗證指令逐字 | 誰跑 |
|---|---|---|---|
| 介面選單切 Sonnet 後，新對話是否沿用 | 只有 user 能操作介面 | 切完開一則新對話**不要碰選單**，再跑 `.scratch/handoff/20260905-token-saving-research.md` 的 `py -3` 量測 | user 切、Claude 量 |
| ~~`settings.json` 的 `model` 到底會不會生效~~ **已驗，見 §9.19** | — | — | ✅ 2026-09-06 02:50 |
| `autoCompactWindow` 300000 是否生效 | 未逐項量 | 新對話跑 `/context` 看壓縮門檻 | user |
| `switchModelsOnFlag: false`、`crossSessionInbound: hold` | 未逐項量 | 與上兩項同 commit（`773b302`），**推定生效但未證實** | 待安排 |

### 9.18 §9.16 已處理：`model` 定為完整 ID（2026-09-06 00:50）

user 選「改成完整 ID」。兩份副本同步改成 **`claude-sonnet-5`**，改前改後皆過 `json.loads`、
鍵數 14 不變：`~\.claude\settings.json`、`global\settings.json`。

理由：`~\.claude.json` 的 `additionalModelOptionsCache` 用的就是完整 ID 形式，
裸別名 `sonnet` 是否被接受無法查證，完整 ID 風險較低。**仍未驗證**——
桌面版取不到乾淨樣本（選單 sticky），要在**終端機**直接跑 `claude` 才驗得到（見 §9.17 第二列）。

**本輪到此結束。剩下的兩件事都在 user 手上，Claude 做不到**：
① 介面模型選單切 Sonnet（§9.15，真正的槓桿）
② 終端機開一則驗 `model` 值是否合法（§9.17）

### 9.19 ✅ 已驗：`claude-sonnet-5` 在設定檔【合法且生效】（2026-09-06 02:50·headless 實跑＋官方文件雙路）

覆蓋 §9.17 第二列、解除 §9.18 的「仍未驗證」。兩條互不參照的路徑得到同一結論。

**路徑一：headless CLI 實跑（CLI `2.1.247`）**

| 組別 | 指令 | 量到的 model | 判讀 |
|---|---|---|---|
| 基準 | `claude -p "回一個字：ok" --output-format json` | `canonicalModel: claude-sonnet-5`、`is_error:false` | 設定檔的值生效 |
| 對照 A | 同上加 `--model haiku` | `claude-haiku-4-5-20251001` | **量法會變**，綠不是假的 |
| 對照 B | 同上加 `--model claude-sonnet-99` | `modelUsage:{}`、`api_error_status:404` | 不合法值**當場報錯** |
| 對照 C | `--settings <探針>` 內容 `{"model":"haiku"}` | `claude-haiku-4-5` | **設定檔的 `model` 欄位確實被讀** |
| 對照 D | 探針內容 `{"model":"claude-sonnet-99"}` | 404 報錯 | 設定檔的不合法值**不會靜默忽略、不會 fallback** |

⇒ 「靜默忽略」這個可能性被 B/D 兩路排除。基準組沒報錯 ⇒ `claude-sonnet-5` 是合法 id；
對照 C 證明來源是設定檔而非 CLI 內建預設巧合。探針檔寫在 scratchpad，跑完已刪，正本一個 byte 未動。

**路徑二：官方文件**（`code.claude.com/docs/en/model-config.md`、`settings.md`、`errors.md`）

- `model` 欄位**別名與完整 ID 都吃**，文件明列 `{"model": "claude-opus-5"}` 這種寫法。
- 不合法值報 `Model ... is not a recognized model id`，**不是靜默忽略**——與實跑一致。
- 優先順序（高→低）：Managed → CLI `--model` → project local → shared project → user `~/.claude/settings.json`。
- `sonnet[1m]` 的 `[1m]` 是 **1M context 變體**標記，仍受支援。

**附帶發現（原本最可能踩的坑，結果安全）**：基準組回報 `contextWindow: 1000000`。
從 `sonnet[1m]` 改成 `claude-sonnet-5` **沒有丟掉 1M context window**。

**仍未驗（不要當成已驗）**

| 項目 | 為何沒驗 | 驗證指令逐字 | 誰跑 |
|---|---|---|---|
| 桌面版 App「改設定檔沒用」這條規則 | 本次只驗 headless CLI，觀察不到 App | 介面選單切 Sonnet 後開新對話不碰選單，看開場模型 | user 切、Claude 量 |
| 互動式終端機（不帶 `-p`）的開場模型 | 只驗了 `-p` 這條路徑 | 終端機直接跑 `claude`（互動模式），問它是哪個模型 | user |
| 兩份 `settings.json` 誰同步誰 | 只比對文字相同，沒查時間戳與 git | `git log -1 -- global/settings.json` 與 `~\.claude\settings.json` 的 mtime 對比 | 待安排 |
| `claude-sonnet-5` 是否為長期穩定 id | 只證明今天這台機器這個帳號可用 | 無現成指令，看官方 deprecation 公告 | 待安排 |
