# Model Routing 與成本精算化計畫書

> 2026-08-05 立案。來源：`D:\.ai-harness\參考\Model routing\` 20 張圖（user 提供）。
> 與 [`WORKFLOW_5STAGE_PLAN.md`](WORKFLOW_5STAGE_PLAN.md) 是同一套系統的兩半——
> 那份講**流程與交接**，這份講**每階段派誰上場、花多少錢**。
> **本檔為計畫書，未逐項討論前不動工。**

---

## §0 核心主張（三句話）

1. **同一份工作要拆成「處理未知方向」與「純執行」兩段**，前段需要判斷力、後段不需要。
2. **派誰上場是二維決策**：模型強度（誰）× 推理強度（想多久）。四種組合各有適用情境。
3. **真正的成本是「交付總成本」不是 API 單價**：Token 成本 ＋ 成品能不能用 ＋ 重跑次數 ＋ Review 時間。

---

## §1 四象限路由矩陣

|  | 便宜模型（快·大量） | 強模型（貴·判斷力好） |
|---|---|---|
| **高推理**<br>（多想幾步·反覆檢查） | **③ Spec 清楚·執行有難度**<br>例：依既有規格補複雜邏輯<br>→「多給思考預算，細節做好」 | **① 方向不明·走錯代價高**<br>例：產品策略／系統架構／重要 Spec／高風險 Review<br>→「**判斷錯 → 再快都白做**」 |
| **低推理**<br>（快速回答） | **④ 機械·重複·容易檢查**<br>例：整理資料／分類／摘要／改格式／固定檢查 | **② 需要好判斷·不用想很久**<br>例：3 個產品方向 → 快速初篩<br>→「**Reasoning 先別拉滿**」 |

**判準**：先問「方向明不明」→ 決定 X 軸；再問「錯了要重做多少」→ 決定 Y 軸。

---

## §2 五階段的編制與模型配置

| 階段 | 編制 | 模型配置 | 交付物 |
|---|---|---|---|
| **Research** | **Manager（強模型）＋ Scout ×N（便宜模型）**<br>Scout 分頭查（競品／使用者需求／相關技術／法規案例）<br>Manager 負責**去重·找矛盾·補缺口** | Scout：④<br>Manager：① | **決策報告** |
| **Design & Spec** | 主導者單人 | **①強×高**<br>「逼出模糊地帶」 | **工作規格**（好 Spec 六問，見下） |
| **Execute** | 按任務類型分（頁面／文案整理／資料轉換／Code·測試）<br>每個任務一組**四件套** | Spec 清楚 → ③或④ | **可檢查成品** |
| **Review** | **Checker 小隊 ×N（便宜模型）**<br>各查一個面向，**只交證據不下判決**<br>→ **強模型判決** | 一般初檢：④<br>衝突·安全·法規·商業：**拉高推理** | **清單 ＋ 證據** |
| **Fix & Deploy** | 主導者單人 | **方向已定 → 便宜模型就夠** | **修完上線** |

### 好 Spec 的六問

哪些頁面？／服務誰？／資料哪來？／設計原則？／**Success Criteria**？／**什麼不能做**？

### Execute 的四件套（每個任務都要有）

**輸入** → **交付** → **禁區** → **驗收**

### Review 的三個出口

Checker 交證據 → 強模型判決：**退回修改** ／ **可以接受** ／ **交給人決定**

### Fix & Deploy 四步

01 問題清單 → 02 改 Code·補格式 → 03 重跑測試 → 04 部署

---

## §3 成本結構與精算

### 3.1 任務成本 ＝ 四部分

**Token 成本 ＋ 成品能不能用 ＋ 重跑次數 ＋ Review 時間**

只算第一項＝只看得到帳單，看不到「便宜模型做壞了重跑三次」的真實代價。

### 3.2 不要背價格，要看成本結構

價格會變。要問的是三個問題：**哪一段 Token 量最大？哪一段 Output 最多？哪一段最值得 Routing？**

### 3.3 執行階段：關鍵在 Output

Input 與 Output 的單價差距通常在 **5 倍**量級（圖例：$10/M vs $50/M）。
**Output 越多，模型選擇的差距越大**——所以「只看 Input 單價」會誤判。

### 3.4 圖上的試算（方法 A vs B）

| | 配置 | 成本 |
|---|---|---|
| **A** 全用最強模型 | 82 萬 In × $10 ＋ 27 萬 Out × $50 | **$21.70** |
| **B** Model Routing | Research+Planning → 強模型 $5.00<br>Execute → 中階 $2.10<br>Review → 中階 $1.35<br>Fix+Deploy → 中階 $0.70 | **$9.15** |

**差異 −$12.55（−58%）**：「只換模型 ＝ 帳單砍半」。

### 3.5 ⚠ 這個試算模型少了一段：**cache**

圖上的公式是 `Input × 單價 + Output × 單價`。但在 Claude Code 這種**每回合重送對話歷史**的形態下，
真正的大宗是 **cache read**。本專案 2026-08-05 實測：

| | input | output | cache write | cache read |
|---|---|---|---|---|
| opus | 6,210 | **4,064,220** | 29,948,688 | **1,442,965,817** |
| sonnet | 20 | 11,271 | 115,001 | 608,800 |

**cache read 是 output 的 355 倍。** 精算模型必須是四項而不是兩項：

```
成本 = input×P_in + output×P_out + cache_write×P_cw + cache_read×P_cr
```

**這筆資料 harness 已經在收**（`gen_cost_panel.aggregate_tokens()` 的 `in`／`out`／`cw`／`cr`），
只是目前沒有拿來做階段歸因。

### 3.6 單價表（2026-08-06 補·來源 `/claude-api` skill 官方快取 2026-06-24）

**不要憑記憶寫單價**——寫錯的儀表比沒有儀表更危險。實體在
`dashboard/gen_cost_panel.py` 的 `PRICE_IN`，本節只是給人看的對照。

| 家族 | input $/M | output（×5） | cache read（×0.1） | cache write 5m（×1.25） | cache write 1h（×2） |
|---|---|---|---|---|---|
| fable | $10.00 | $50.00 | $1.00 | $12.50 | $20.00 |
| opus | $5.00 | $25.00 | $0.50 | $6.25 | $10.00 |
| sonnet | $3.00 | $15.00 | $0.30 | $3.75 | $6.00 |
| haiku | $1.00 | $5.00 | $0.10 | $1.25 | $2.00 |

output 與各 cache 項都是 input 的固定倍數（官方定價規則），所以**只存 input 一欄**、
其餘用倍數推——單價調整時只有一個地方要改。
（sonnet-5 到 2026-08-31 有 $2/$10 優惠價；本表沿用標準價與 ccusage 同口徑，
差額落在對帳差裡而不是被藏起來。）

**§3.5 的四項公式要再細一格**——cache write 兩種 TTL 不同價：

```
成本 = input×P + output×5P + cw_5m×1.25P + cw_1h×2P + cache_read×0.1P
```

ccusage 把 `ephemeral_1h` 與 `ephemeral_5m` 併成一欄（正是它反解殘差 36% 的原因），
但**原始 transcript 的 `usage.cache_creation` 有分**——這正是自算比外包準的地方。

---

## §4 ★ 現況對照：harness 缺什麼

### 4.1 已經有的（不用重做）

| 能力 | 現況 |
|---|---|
| 角色機制 | 5 自建＋3 內建，全域層，可設 `model` |
| 角色能力邊界 | `tools:` ＋ agent-scoped hook 閘門 |
| 觀測 | 平台 subagent 紀錄（派工次數／工具／目標／實際模型） |
| Token 資料 | **已拆 in／out／cache write／cache read**，按日按 family |
| 成本 | 按模型、按日（$8,744.64 / 32 天） |
| Review 的強模型判決 | `/adversarial-review`（Plan ＋ effort high） |

### 4.2 缺的（按難度排序）

| # | 缺口 | 難度 | 說明 |
|---|---|---|---|
| **G1** | ~~推理強度在 subagent 層無法設定~~ **已推翻（8/5 查證）** | 低 | 初版寫「平台限制」是**只查了 Agent 工具參數就下結論**。實際上**角色 frontmatter 支援 `effort:`**（`low`/`medium`/`high`/`xhigh`/`max` 或數字，官方 agent-sdk/subagents 文件），另有全域環境變數 `CLAUDE_CODE_EFFORT_LEVEL`。**Y 軸做得到，但綁在角色上**（每支角色一個固定值），不能逐次派工調 —— 對本設計反而剛好：五階段的角色本來就各守一格象限。⚠ 文件宣稱，**尚未實測**（待驗：frontmatter 加 `effort:` 後派工，行為有沒有變） |
| **G2** | ~~按階段的成本歸因~~ **✅ 已做（8/6）** | 中 | 全域 CLAUDE.md §2 自我宣告加「階段」欄；`gen_cost_panel.stage_attribution()` 離線從 transcript 撈宣告行歸因，五項公式自算金額，看板新增「階段成本歸因」區塊。**未標記佔比本身就是宣告紀律的量測**，照實顯示不藏。細節見 §4.4 |
| **G3** | Scout 平行搜集模式 | 中 | 現在派 subagent 是一次一個、序列的。沒有「一次撒 N 個便宜模型分頭查、再由強模型去重找矛盾」的編排 |
| **G4** | Checker 小隊 ＋「只交證據不下判決」 | 中 | Review 角色有三個，但**沒有並行小隊的形狀**，也沒有把「不下判決」寫成角色紀律 |
| **G5** | Execute 四件套（輸入／交付／禁區／驗收） | 低 | 角色檔各自有輸出格式，但**沒有統一的四件套結構**。這是寫規範就能補的 |
| **G6** | Design 階段的執行器（好 Spec 六問） | 低 | 14 支 skill 裡 **Design 階段 0 支**。六問可以直接寫成 skill |
| **G7** | 交付總成本的另外三項 | 高 | 「成品能不能用／重跑次數／Review 時間」目前**完全沒有量測**。重跑次數理論上可從 event log 推（同一任務被重派幾次），另外兩項需要新的資料來源 |

### 4.3 最要緊的一條（8/5 修正）

初版斷言「G1 是平台限制」被查證推翻——**四象限兩軸都做得到**：

- ✅ X 軸選模型：派工參數 `Agent(model=...)` 或角色 frontmatter `model:`
- ✅ Y 軸選推理強度：角色 frontmatter `effort:`（**每支角色固定一格，不能逐次調**）

「綁角色不綁派工」對本設計剛好：Scout＝便宜×低、Checker＝便宜×低、
Manager/判決＝強×高、初篩型＝強×低——**角色本身就是象限的載體**。
教訓照 U 系準則記下：**「工具參數沒有」≠「平台做不到」，同一能力常有第二個掛載點**
（跟 7/30「user 層角色載不到」被推翻是同一型錯誤：查了一條路徑就下全稱結論）。

### 4.4 G2 實作記錄（2026-08-06）——五個踩到的坑

做的時候踩到五件事，每一件單獨看都會**靜默算錯**（表照樣長得很正常）：

1. **同一則 API 訊息在 transcript 拆成多筆**（每個 content block 一筆、`message.id`
   與 `usage` 完全相同）。實測 45% 是重複、同 id 的 usage 零不一致。
   mix 是比例還能互相抵消，**金額不去重就是直接灌水近一倍**。
2. **transcript 同一個檔同時有兩種中文編碼**——字面 UTF-8 與 `\uXXXX` 逃逸。
   第一版拿原始行做 `"階段" in line` 前置過濾，會**靜默漏掉逃逸那一半的宣告**，
   而漏掉的部分安靜地落進「未標記」——看起來只是紀律差，其實是解析器壞了。
   **教訓同型於 §4.3**：查了一條路徑（一種編碼）就下全稱結論。
3. **宣告要兩段式解析**。一則訊息的 thinking block 排在 text block 前面且各存一筆，
   邊走邊判的話**宣告那一則的 usage 會被算進上一個階段**。改成先解出
   「哪個 message id 宣告了哪個階段」，再照順序歸因。

4. **分母要跟規則同齡**。第一版把 32 天歷史全算進「未標記」，於是那個佔比
   **永遠是 100%**——歷史成本會把新資料淹掉好幾週，「未標記佔比＝宣告紀律」
   這個量測從第一天起就是壞的。改成只看規則生效日以後。
5. **起算日是「當地日期」、transcript 是 UTC**。直接拿 `timestamp[:10] < "2026-08-06"`
   比，會把**當地今天的前 8 小時整段砍掉**（01:15 當地＝前一天 17:15 UTC），
   表格靜默變空。換算用本機時區、不寫死 +8（換一個部門就錯）。

另外**宣告只認 assistant 訊息**：user 貼一句規則文或引用舊對話就會命中「階段 X」，
讓一句引用改寫成本歸因是最陰的一種錯。以上都有回歸測試 ＋ 變異測試守
（`test_cost_panel.py` 4 個 case、`mutate_cost_panel.py` 新增 12 個變異，26 個全紅）。

**第 4、5 兩條是同一次事故的兩半**：修完 4 之後表格變空，若當時把「空表」當成
「還沒有資料」就會漏掉 5。**「改完之後數字變成 0」永遠要先當成 bug 查一次**——
這也是為什麼零紀錄時整段不能消失（消失就沒有人會發現時區設錯了）。

**自算 vs ccusage 對帳**：自算 US$9,372、ccusage US$8,745，差 +7%。兩個已知來源——
ccusage 有 7/148 個 session 對不上（它少算）、sonnet-5 優惠價未計（我方多算）。
**對帳仍以 ccusage 為準**，這張表要回答的是「錢花在哪個階段」。

---

## §5 待決分岔（逐項討論，同意才做）

| # | 分岔 | 選項 | 傾向 |
|---|---|---|---|
| ~~M-1~~ | ~~G1 怎麼繞~~ | **已由查證解決（8/5）**：角色 frontmatter 就支援 `effort:`，不必繞。每支角色釘一格象限（model×effort），首次使用前先實測一次 frontmatter effort 真的生效 | — |
| M-2 | 階段歸因怎麼標 | (a) 自我宣告加「階段」欄位 (b) hook 從工具序列推斷 (c) 每階段開新 session | **(a)**：現行已有自我宣告，加一欄成本最低；(b) 不準、(c) 破壞脈絡 |
| M-3 | 先補哪個缺口 | (a) G2 成本歸因 (b) G5+G6 規範與 skill (c) G3+G4 並行編制 | **(a)**：沒有階段成本，其餘優化都無法驗證有沒有效 |
| M-4 | 成本模型要不要納入 cache | (a) 納入（四項公式） (b) 沿用圖上的兩項 | **(a)**：本專案 cache read 是 output 的 355 倍，不納入等於算錯 |
| M-5 | 「交付總成本」的另三項要不要量 | (a) 只量重跑次數（可從 event log 推） (b) 三項都量 (c) 先不量 | **(a)**：重跑次數有現成資料源；另兩項需要人工輸入，先不做 |

---

## §6 狀態

- 2026-08-05 立案，20 張來源圖已全部讀過並轉成本檔的 §1–§3。
- §4 現況對照已實際查證：**token 四項資料確認存在**；初版「`Agent` 工具無 `effort` ＝ 平台限制」
  被同日查證**推翻**——角色 frontmatter 支援 `effort:`（見 §4.3 修正）。
- **§5 五項全部定案（2026-08-05 逐項討論）**：
  - ~~M-1~~ 查證解決：角色 frontmatter 釘 `effort:`，每支角色一格象限；首用前實測生效。
  - M-2 ✅ **自我宣告加「階段」欄**（Research/Design/Execute/Review/Fix），
    成本面板離線批次從 transcript 撈宣告行做歸因；「未標記」佔比本身就是宣告紀律的量測。
  - M-3 ✅ **先補 G2 成本歸因**——沒有階段成本，其餘優化都無法驗證有沒有效。
    之後順序：G5/G6 規範與 skill → G3/G4 並行編制 → G7。
  - M-4 ✅ **四項公式**（in／out／cache_write／cache_read）——cache read 是 output 的
    355 倍，兩項公式等於算錯。
  - M-5 ✅ **先只量重跑次數**（event log 有現成資料源）；「成品能用？」「Review 時間」
    需人工回報，漏填比沒有更誤導，暫不做。
- **2026-08-06：G2 完工**（M-3 排的第一項）。全域 §2 自我宣告加「階段」欄、
  產生器加 `stage_attribution()` 與 `PRICE_IN` 單價表、看板加「階段成本歸因」區塊。
  回歸網 328/328、eval 六級全過、23 個變異全紅。踩到的三個坑記在 §4.4。
  **現況：宣告 0 次、未標記 100%**——規則今天才上線，這是正確的初始狀態，
  下一輪開始就會看到分佈。
- **順帶修掉一個假紅**：`test_layers.py` 把「AI-Projects 沒有 `.claude`」寫死進斷言，
  那個目錄今天被建出來就變成假紅（產生器是對的、環境變了）。改成量測恆真性質
  （`exists` 與磁碟一致 ＋ 用臨時目錄造受控樣本），並用兩個變異證明它還會紅。
  **環境現況禁寫死**這條規則本來就在 `feedback-execution-test-before-deploy`，
  這是它第一次真的被觸發。
- **下一項（M-3 順序）**：G5 Execute 四件套 ＋ G6 好 Spec 六問 skill。

---

## §7 派工的實測理由與環境障礙（2026-08-14 從全域 `CLAUDE.md` §4.1 搬入）

搬入理由同 `WORKFLOW_5STAGE_PLAN.md` §12：常駐層只留「一律派、不先問」那條指令，
**支撐它的兩組實測數字與一個環境坑**放這裡。（`check_bloat` 2026-08-14 報這兩條 181 字／130 字超標。）

### 7.1 為什麼預設派出去（兩組實測）

- **2026-08-07：單一 session 的 API 時間是工具時間的 11.6 倍**
  ⇒ **慢在模型推理，不在跑指令**。優化跑指令的速度是在優化 8% 的那一段。
- **不派工會讓它指數惡化**：檔案全文、grep 輸出、測試 log 全堆在主 session 的 context 裡
  **每輪重送**。派工**同時解兩邊**——便宜模型跑粗活（單價低），
  且**原始資料留在 subagent 那側**（主 session 的 context 不長）。
- **2026-08-06：每 token 成本差 2.9 倍**（Opus vs Sonnet 實測，非牌價推算）。

### 7.2 `CLAUDE_CODE_CHILD_SESSION=1` 會帶一條預設的禁派工指令

**VSCode 面板是常態**（不是例外情況）。那條 session 指令會說「除非 user 要求，否則不要派 subagent」。

- **全域 `CLAUDE.md` §4.1 的常設授權就是它要的那個「user requested」** ⇒ 宣告一句後照派。
- ⚠ **被擋住時必須當場說**，別默默自己做完——
  「安靜地退回單 session 模式」會讓上面那兩組數字的優化全部失效，而且不會有任何徵兆。

---

## §8 子 effort：task-identity-cost-attribution（2026-08-23 開圖）

規劃層走 wayfinder map，不在本檔展開：
`D:\.ai-harness\.scratch\task-identity-cost-attribution\map.md`

它從本檔長出來的理由：到達點是「判斷派工準不準、錢花在哪個任務」，
正是 §1 四象限與 §4.4 階段成本歸因沒有回答的那一半 —— 現有兩軸都是「實際用了多少」，
缺「該用多少」那一軸，而那個值（規模欄）早就在宣告裡。

<!-- REVIEW_SCOPE_IGNORE_START -->
### 決策票待辦（票開出來就回寫一列，關掉改成 ✅ 已完成）

<!-- 票檔在 .scratch\task-identity-cost-attribution\issues\ 底下；
     說明格刻意保持精簡 —— 產生器取「狀態格以外最長的那一格」當標題，
     說明格寫太長會把票名整個搶走。 -->

| 狀態 | 項目 | 說明 |
|---|---|---|
| ✅ 已完成 | 票 01 「任務」欄的字面形狀與解析錨點 | 第二欄；要求分隔符 |
| ⏳ 待做 | 票 02 未結案任務的狀態檔：格式、結案與改名 | issues/02 |
| ⏳ 待做 | 票 03 歸因資料層：兩條游標的資料結構 | issues/03 |
| ✅ 已完成 | 票 04 「任務分類」死欄位的消費端要長什麼樣 | 拆標籤·值域外置 |
| ⏳ 待做 | 票 05 看板那張表要回答什麼問題 | issues/05 |
| ✅ 已完成 | 票 06 任務身分 vs effort_of_path 兩套 key | 裁決 (c)：任務欄取代 |
| ✅ 已完成 | 票 07 宣告分隔符契約與既有髒資料 | 吃三種；20%→0% |
| ✅ 已完成 | 票 08 任務的專案維度該從哪裡來 | 寫檔路徑；harness +69 段 |
| ⏳ 待做 | 票 09 規模 × 派工強度第一版要存哪些欄位 | issues/09 |
| ✅ 已完成 | 票 10 cost_panel 的語料範圍與宣告偵測口徑 | 單一真相；未標記 6.5%→1.2% |
<!-- REVIEW_SCOPE_IGNORE_END -->
