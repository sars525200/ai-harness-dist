# WIN-1「規模 M 降噪」設計覆核計畫書

> 2026-09-06 立案，源自 IT-department SG-098 成本個案追查（見
> `D:\Patrick-AI\IT-department\.scratch\cost-audit-followup\PLAN.md` §7.3）。
> 對應 `TODOS.md`「全域·需求」表新增列（2026-09-06）。
> **本檔為計畫書，未逐項討論前不動 `hooks/rules/win1_total_input.py`。**

## §1 現況（實查，見 `win1_total_input.py` 原始碼與 git log）

- WIN-1 於 2026-08-28 上線、同日轉正（`dispatch_config` shadow=false），早於 SG-098（09-05）一週以上
- `_is_large_plan()`：讀 transcript 尾端**最後一次**自我宣告的「規模」值，若為 `M`，
  **150k／180k 兩檔提醒整檔略過，只保留 210k 強烈陳述線**
- 設計理由（docstring 原文）：「M 級任務本來就跨 session、本來就長，在 150／180 催它交接是雜訊」
- **個案矛盾**：SG-098 原判 `規模 S`，Round 1 覆核後**中途重估為 `規模 M`**（見該案 PLAN.md 開頭）。
  其中一段（23 則訊息／4.3 分鐘）密集把整段程式碼貼進對話——這正是 150k/180k 提醒該攔的情境，
  但因為宣告已經是 M，兩檔被略過
- **§3 分岔 1 已驗證（2026-09-06，逐行查證 `78fc26cc-2630-4b26-bb4e-c9bee09a02f3.jsonl`）**：
  結論**推翻了本節原本的舉證**——
  - 「23 則密集貼 code」那段（19:20:41–19:24:54 UTC）當下的合計 input 只有 **125k–136k**，
    **連 150k 都沒到**。它不是「被規模 M 略過的 150k/180k」，是**根本還沒碰到任何門檻**。
    上面「這正是 150k/180k 該攔的情境，但因為宣告已經是 M，兩檔被略過」這句話**不成立**，
    原始舉證作廢。
  - 這個 session 隔天（09-06）另一輪確實跨過 210k，`win1_state.json` 也記到
    `last_strong_prompt`；但**整份 transcript 逐行搜尋，找不到任何一次 WIN-1 警告字面
    被投進 additionalContext**（只有 3 處命中，全是 skill 說明文字的靜態描述，不是
    dispatch 真的送出的內容）。且該 session 在跨線那一則之後**沒有下一輪**——WIN-1
    機制是「這輪判定、下輪投遞」，沒有下一輪，連「投遞失敗」都無法確認，只能確認
    「沒有機會投遞」。
  ⇒ **判準本身沒有「因為規模 M 而漏接」的證據**（因為那段從未達標）；但「210k 判定寫進
  state 之後有沒有真的送到模型」這件更底層的事，在這個案例裡**查無實據**，
  對應 §3 分岔 2（系統性觸發缺陷），不是分岔 3。
- **§3 分岔 2 已驗證（2026-09-06，逐行查證 `f881086e-5aa0-42f7-aa4c-7d7590c68072.jsonl.part1-before-rename`）**：
  找到**正例**——`:1069` 該輪 `UserPromptSubmit` 的 `hook_additional_context` 確實出現
  「WIN-1：本回合合計 input 約 305k tokens，已越過 210k 強烈陳述線……」原文，`:1071`
  助理回應**直接引用並承認**這段警告，證實**至少有一次**投遞成功、且模型讀到了。
  ⇒ 機制**沒有系統性壞掉**的證據；SG-098 那次「查無實據」的正確解讀是「剛好沒有下一輪
  可驗」，不是「機制失效」——兩個案例並存，代表結果因「有沒有下一輪」而異，不是全面性缺陷。
- **舊旁證仍未能獨立佐證，但也未被推翻**：本計畫書誕生那則對話（自稱 240k 時看到 210k
  觸發）的 session transcript 在磁碟上已找不到（只剩刪除標記／改名前殘檔），無法逐行覆核；
  但既然已在別的 session（f881086e）找到同款警告字面的正例，此點**不再是判斷的關鍵路徑**，
  不需要再追。

## §2 目標

判斷「規模 M 略過低兩檔」這個設計，對「中途從 S 升級成 M、但仍在單一 session 內密集操作」
這種情況是不是反效果；決定要不要改判準。

## §3 待決分岔（逐項討論，同意才做）

| # | 分岔 | 選項 | 傾向 | 理由 |
|---|---|---|---|---|
| 1 | 210k 那次到底有沒有觸發 | 驗／不驗直接改設計 | **已驗：查無實據**（見 §1） | 沒驗清楚就改判準，等於用一個同樣未經證實的假設換掉另一個 |
| 2 | 「210k 判定寫進 state 後有沒有真的送到模型」 | 再找一個「跨過 210k 且確實有下一輪」的 session 逐行查證投遞 | **已驗：找到正例，機制未證實壞掉**（見 §1） | SG-098 那次沒有下一輪可驗，樣本不足；需要一個乾淨案例才能定案「機制壞了」還是「剛好都沒下一輪」 |
| 3 | ~~若確認「210k 有觸發，只是低檔被略過」~~ | ~~(a) 維持現狀 (b) 略過條件加密集操作指標~~ | **作廢**——前提（150k/180k 被略過）不成立 | 原始舉證的那段從未達到 150k，不存在「被略過」這件事 |

## §4 做法（分岔 1 定案後才動工）

1. 找一個真的宣告過「規模 M」且對話長度確實超過 210k 的 session（可用
   `tools/token_usage_breakdown.py` 或直接掃 `state/win1_state.json` 找 `last_strong_prompt` 有值的紀錄）
2. 對照該 session 的 transcript，確認 210k 那則 WARN 訊息有沒有真的出現在
   下一輪的 `additionalContext`（不是只看 dispatch 有沒有 return warn()，要看訊息真的投遞到模型）
3. 依 §3 分岔 2／3 的結果決定要不要改 `_is_large_plan()` 的判準

## §5 驗證方式（動工前先寫好）

| 要驗的 | 怎麼驗 | 判準 |
|---|---|---|
| 210k 是否真的觸發過 | 找到一個 M 級長 session，逐輪掃 transcript 找 WARN 訊息字面 | 找到 ⇒ 分岔走§3-3；找不到 ⇒ 分岔走§3-2 |
| 改判準後不誤傷正常 M 級任務 | 挑一個真正跨 session 的大計畫（非中途從 S 升級），確認新判準下低檔提醒仍被正確略過 | 沒有對這類任務誤發提醒 |

## §6 不在範圍內

- 不重跑 IT-department SG-098 的任何數字（那條線已在該案 PLAN.md 結案）
- 不順手改 WIN-1 其他部分（門檻數值、便箋去重邏輯）——本檔只處理「規模 M 降噪」這一條

## §7 狀態（結案）

- 2026-09-06 立案並同日結案。§3 分岔 1、分岔 2 皆已驗證：
  1. 原始舉證（「23 則密集貼 code 被規模 M 略過」）**作廢**——那段從未到 150k，不存在「被略過」。
  2. 210k 投遞機制**找到正例**（`f881086e` session `:1069/:1071`），未發現系統性缺陷；
     SG-098「查無實據」是因為那次剛好沒有下一輪，不是機制壞掉。
- **結論：沒有證據支持修改 `_is_large_plan()`**。本檔源頭（SG-098 個案）的舉證不成立，
  分岔 2 的檢驗也沒有找到反面證據。`win1_total_input.py` **維持現狀，不改**。
- 若未來再遇到「規模 M 略過低檔造成問題」的個案，需要**新的、獨立驗證過的舉證**才能重開本題，
  不能沿用本檔已作廢的 SG-098 案例。
