# D:\.ai-harness 的 Python 依賴（2026-08-15 Round 7 起）
#
# ══════════════════════════════════════════════════════════════════════════
# ⚠ 先讀這一段：**「pip install」在這台機器上是有歧義的**
# ══════════════════════════════════════════════════════════════════════════
#
#   指令       實體                                                          版本
#   ---------  ------------------------------------------------------------  --------
#   python     D:\IT-department\.venv\Scripts\python.exe                      3.12.10
#   py -3      C:\Users\<你>\AppData\Local\Python\pythoncore-3.14-64\python.exe 3.14.5
#
# **harness 的 SOP 與 `/context-health` skill 跑的都是 `py -3`**
# （`rulefile/*.py` 檔頭那行 `py -3 -X utf8 …` 就是），
# 所以只 `pip install` 進 venv **等於沒裝**。
#
# 症狀提醒：裝錯直譯器的表現是
#     ModuleNotFoundError: No module named 'markdown_it'
# **看起來像工具壞了、不像環境沒裝**——這是已經踩過的坑，不是假設。
#
# 正確安裝（**認明 `py -3`**）：
#
#     py -3 -m pip install -r D:\.ai-harness\requirements.txt
#
# 裝完驗一句（印得出版本才算裝好，沒有輸出就是還沒好）：
#
#     py -3 -c "import markdown_it; print(markdown_it.__version__)"
#
# 兩支直譯器都要用到時（本機目前是這種狀態）就各跑一次：
#
#     python -m pip install -r D:\.ai-harness\requirements.txt
#
# ── 依賴 ──────────────────────────────────────────────────────────────────
#
# markdown-it-py：`check_bloat.py` / `check_prose_blocks.py` 的**量測單位**改由
# CommonMark 規格定義（CONTEXT_HEALTH_PLAN §8.1）。在這之前判準靠列舉 markdown
# 標記，連續六輪對抗式覆核每輪都找得到新的繞過寫法（空行＋縮排續段、巢狀子條目、
# blockquote 續段、`~~~` fence…）——**列舉法的軸永遠列不完**，所以改成問 parser。
#
# 版號釘死不是保守：**量測單位一變，bloat 快照的條目身分就會位移**，
# 升版要當成一次基準重寫來處理（§8.2）。要升版先跑
# `py -3 D:\.ai-harness\tests\run_hook_tests.py` 確認七種寫法契約仍成立。
#
# 表格規則（GFM table）是 markdown-it-py 內建的，**不必另外裝 mdit-py-plugins**；
# 但預設 preset 沒開，要 `MarkdownIt("gfm-like")` 或 `.enable("table")`。
markdown-it-py==4.2.0
