# Token 成本最佳化計畫書

> 2026-09-06 立案。承接 [`MODEL_ROUTING_PLAN.md`](MODEL_ROUTING_PLAN.md) §9——
> 那條線只回答「開場模型的承載體在哪」（答案：介面選單），已於 §9.19 結案。
> 本檔問的是**更上層的問題：錢到底花在哪、還有哪些沒被碰過的槓桿**。
> 與 [`CONTEXT_HEALTH_PLAN.md`](CONTEXT_HEALTH_PLAN.md)（常駐層瘦身）、
> [`COST_OBSERVABILITY_PLAN.md`](COST_OBSERVABILITY_PLAN.md)（歸因看板）分工見 §0.2。
>
> **本檔為計畫書，未逐項討論前不動工。**

---

## §0 立案理由

### 0.1 user 已經做到底的兩條（不要再投入）

| 方向 | 現況實測 | 判定 |
|---|---|---|
| 降單位單價（模型／effort 路由） | `MODEL_ROUTING_PLAN.md` 整份，§9 追到承載體 | **到頂** |
| 減常駐層（規則檔、記憶、技能簡介） | `~\.claude\CLAUDE.md` **205 行**、14,229 bytes；官方建議 **200 行內**；記憶索引 270 bytes | **到頂** |

user 自陳「這 2 種都做到爛了」，本機量測支持這個說法。**本計畫書刻意不重複這兩條。**

user 另外自己想到、也**不列入本檔新增項**的：雲端快取、任務路由、重複性規則、技能去重、贅詞、專案 token 對照表。
其中「對照表」有現成替代品，見 §4 C-1。

### 0.2 與既有三份計畫書的分工

- `CONTEXT_HEALTH_PLAN.md`＝**常駐層檔案**的量測與瘦身 → 本檔不碰，已到頂。
- `COST_OBSERVABILITY_PLAN.md`＝**歸因看板**（誰花了錢） → 本檔只引用它的產出，不重做。
- `MODEL_ROUTING_PLAN.md`＝**派誰上場** → 本檔 §3 要修正它其中一條規則。

---

## §1 現況（2026-09-06 實測，不是推測）

### 1.1 本機常駐層

| 項目 | 量到的值 | 來源 |
|---|---|---|
| 全域規則檔 | 14,229 bytes／205 行 | `wc` 實跑 |
| 回覆風格檔（PM-Challenger） | 5,048 bytes | 同上 |
| 記憶索引 | 270 bytes | 同上 |
| 本機 16 支技能的 description 合計 | 2,539 字元 | 逐檔抽 frontmatter |
| `effortLevel` | **high** | `global/settings.json:253` |
| `autoCompactWindow` | 300000 | 同上 `:168` |

### 1.2 九個成本旋鈕，**一個都沒設**

`promptCacheTtl`／`subagentPromptCacheTtl`／`bashOutputMaxChars`／`skillListingMaxDescChars`／
`skillListingBudgetFraction`／`alwaysThinkingEnabled`／`autoCompactEnabled`／
`CLAUDE_CODE_SUBAGENT_MODEL`／`CLAUDE_CODE_GOAL_CHECKIN_MINUTES`

### 1.3 【本機實測】錢實際花在哪——拆 session 逐字檔算出來的

資料來源：`~\.claude\projects\D--Patrick-AI\*.jsonl` 每則 assistant 訊息的 `usage` 欄位。
樣本＝本則對話（`7f3bfeb7`）**100 個回合**，全部 `claude-opus-5`。

**原始 token（硬數字，無任何換算）**

| 項目 | 量 |
|---|---|
| `cache_read_input_tokens` | **10,885,196** |
| `cache_creation_input_tokens` | 226,709 |
| `output_tokens` | 131,654 |
| 未命中快取的 `input_tokens` | **200** |
| 最大前綴（單回合 cache_read 峰值） | **173,089** |

⇒ 快取命中率接近 100%（未命中只有 200 token）。**現況的快取已經很健康，這條沒有優化空間。**

**換算成 input 當量後的佔比**（比例用業界通用的 cache read 0.1×／cache write 1.25×／output 5×，
⚠ **本次沒有查證官方費率頁**，比例可能有出入，用於排序而非報價）

| 項目 | 當量 | 佔比 |
|---|---|---|
| **cache_read** | 1,088,520 | **53.6%** |
| **output**（含 thinking） | 658,270 | **32.4%** |
| cache_write | 283,386 | 14.0% |

### 1.4 這份拆解改寫了優先順序

1. **最大宗是 cache_read（53.6%），而它正比於「回合數 × 平均前綴」。**
   每一個回合都要把整段對話重讀一次。100 個回合、前綴長到 17.3 萬 ⇒ 累計 1,089 萬。
   **⇒ 真正的槓桿是「不要讓一則對話又長又多回合」，不是任何設定旋鈕。**
   同樣的工作拆成三則短對話，這一項大約掉到三分之一（前綴成長是累加的）。
2. **第二大是 output 32.4%，而 `effortLevel` 目前是 high**——thinking token 算 output。
   100 回合吐了 13.2 萬 output，平均每回合約 1,300。
3. **切一次模型的代價可以算了**：前綴 17.3 萬要整段重寫，
   單次多付約 **199,052 當量＝本則對話總量的 9.8%**；一個任務切兩次＝**19.6%**。
   ⇒ §3 那條規則的成本從「理論上不好」變成**每個任務燒掉五分之一**。

### 1.5 不可壓縮的地板

【第三方實測·中信度】Claude Code 在讀到 user 第一個字之前，system prompt 本身約 **33,000 token**
（對照 OpenCode 約 7,000）。方法論：在 agent 與 API 之間加日誌記錄請求。
出處：HN #48883275。**這是地板 ⇒ 常駐層瘦身的天花板很低，印證 §0.1。**

---

## §2 證據等級標示法（本檔通用）

| 標記 | 意思 |
|---|---|
| 【官方明文】 | code.claude.com 官方文件白紙黑字 |
| 【官方部落格】 | Anthropic 自家部落格，可信但非規格 |
| 【第三方實測】 | 有方法論、有前後對照的外部測試 |
| 【本機實測】 | 這台機器上跑出來的 |
| 【單一來源·未證實】 | 只有一處這樣說，無法交叉驗證 |

**§4 每一項都必須帶標記。沒有標記的不准進清單。**

---

## §3 核心發現：**規則本身在製造 cache miss**

這是本次唯一一條「方向相反」的發現，優先於所有旋鈕。

### 3.1 事實

【官方明文】`prompt-caching.md` 列出 9 種讓快取整段失效的動作，其中兩條直接命中本機規則：

1. **切換模型**（`/model`）——每個模型各有一份快取，切了下一輪整段重算。
2. **切換 effort level**——大多數模型每個 effort 各存一份快取。

【官方明文】快取是**前綴精確匹配**，沒有逐檔或逐段快取；前面任一段變了，後面全部重算。

【官方部落格】Anthropic 自己舉的反例：已在 Opus 對話花掉 100k token 後，中途換 Haiku 回答簡單問題
**反而更貴**，因為要重建整條快取。出處：`claude.com/blog/lessons-from-building-claude-code-prompt-caching-is-everything`（2026-04-30）。

### 3.2 與現行規則的衝突

`global/CLAUDE.md` §4.2 現行寫法：

> **升 Opus 的時機**：任務碰到 DB／邏輯／硬規則區／多檔協調／根因診斷／架構規劃
> → **起頭切 Opus、收尾切回 Sonnet。**

⇒ 每個任務**主動製造兩次全段重算**。任務越長、對話越大，這兩次越貴。
**這是一條以為在省、實際在花的規則。**

### 3.3 候選修法（待 user 逐項討論）

| 選項 | 做法 | 代價 |
|---|---|---|
| A | 改成「一則對話從頭到尾不換模型」，需要高階就**另開對話** | 多一道開對話的手續；跨對話要交接 |
| B | 保留切換但限定「只在對話很短時切」 | 需要一個「多短算短」的判準，而判準會被自我豁免 |
| C | 不改，接受這個成本 | 明知在燒仍繼續，且會抵銷其他所有旋鈕 |

**傾向 A**。理由：判準是「有沒有換」這個事實，不是「複雜不複雜」這種要判斷的東西。

---

## §4 候選清單（分三批）

### A 批：零設定變更、方向明確

| 編號 | 做法 | 依據 | 預估效果 | 代價 |
|---|---|---|---|---|
| A-1 | 一則對話不換模型、不調 effort（§3） | 【官方明文】＋【官方部落格】 | 每個任務省兩次全段重算 | 硬任務要另開對話 |
| A-2 | 用 `/clear` 取代 `/compact` 換場 | 【官方明文】`/compact` 要重讀整段對話產生摘要，冷快取時很貴；`/clear` 免費 | 換場成本歸零 | 對話記憶全丟 ⇒ **交接檔必須先寫好**，這正是 `chat-handoff` 的用途 |
| A-3 | prompt 指名檔案，不要下「改善這個 codebase」 | 【官方明文】模糊指令觸發大範圍掃描 | 未量化 | 無 |

### B 批：要改設定，效果可量

| 編號 | 旋鈕 | 依據 | 預估效果 | 代價 |
|---|---|---|---|---|
| B-1 | `bashOutputMaxChars` 調低 | 【官方明文】控制指令輸出直接進 context 的量 | 本則對話就出現過 43.9KB 的單次輸出 | 太低會截斷需要的資訊 |
| B-2 | `subagentPromptCacheTtl` 拉長 | 【官方明文】子代理／workflow 預設 **5 分鐘**，主對話是 1 小時 | 本則四個角色各燒 47k–99k，全部冷啟動 | 1h 的 cache write 費率較高 |
| B-3 | `promptCacheTtl` 明確設 `1h` | 【官方明文】訂閱制在額度內本來就是 1h，超額掉回 5m | 避免超額後靜默變貴 | 同上 |
| B-4 | `CLAUDE_CODE_SUBAGENT_MODEL` 設便宜模型 | 【官方明文】子代理模型可指定，優先序：臨時參數 > frontmatter > 此環境變數 > 繼承主對話 | 粗活不再用最貴模型 | ⚠ 見 §5 陷阱 1 |
| B-5 | `skillListingMaxDescChars` 調低 | 【官方明文】技能清單開場全載入 | 本機 16 支合計 2,539 字元，量不大 | 描述被截可能讓技能叫不出來 |

### C 批：改工作方式，不是改設定

| 編號 | 做法 | 依據 | 預估效果 | 代價 |
|---|---|---|---|---|
| C-1 | **停做手工 token 對照表**，改用內建歸因 | 【官方明文】`/usage` 在 Pro/Max/Team/Enterprise 會給**依技能／子代理／外掛／個別 MCP 分列**的佔比，加上快取未命中次數與**推測原因**，以及超過 10% miss 的行為旗標 | 對照表可退休 | 需確認本帳號方案等級看得到歸因區塊 |
| C-2 | 派工優先用「繼承主對話前綴」的方式 | 【官方明文】`fork` 直接繼承 parent 整段前綴，第一輪就吃到快取；一般 subagent 前綴不同、必須自己暖機 | 子代理冷啟動成本降低 | fork 不適合要獨立視角的對抗式覆核 |
| C-3 | 長 log／大檔先用 hook 或指令過濾再進對話 | 【官方明文】官方舉例：一份長 log 從數萬 token 降到數百 | 效果大 | hook 要寫要維護，寫錯會漏內容 |
| C-4 | 能用 CLI 就不要掛 MCP server | 【官方明文】建議用 `gh`／`aws`／`gcloud` 取代對應 MCP；`/mcp` 停用不用的 | 【第三方實測】一顆沒用到的 MCP 白吃 18.3k token（9.2% context）；7 顆共吃 67,300 token | 停用＝那些工具當場不能用。⚠ 註：官方後來推出 MCP Tool Search（deferred 載入），此數字可能已過期。**【本機實測·2026-09-06】查證結果：不適用。**`settings.json`（兩份）與 `.claude.json` 各專案的 `mcpServers` 欄位全部是空 `{}`——本機沒有掛任何使用者自訂 MCP server。對話裡看到的 `claude-in-chrome`／`mcp-registry`／`scheduled-tasks`／`ccd_session_mgmt`／`ccd_directory`／design-sync 連結器是桌面版 App 內建一級功能，不在任何我能編輯的設定檔裡，要關只能 user 去 App 的 Settings › Connectors 手動操作。**這條從可施作清單移除。** |
| C-5 | 減少背景耗量 | 【官方明文】goal check-in 每個 goal 最多 3 次背景檢查，可用 `CLAUDE_CODE_GOAL_CHECKIN_MINUTES=0` 關；排程任務**每次觸發都送整包 context** | 未量化 | 關掉就沒有背景提醒 |

---

## §5 反直覺：以為省、其實沒省的三個陷阱

1. **`CLAUDE_CODE_SUBAGENT_MODEL` 會蓋掉技能自己指定的模型，且不會警告**
   【單一來源·未證實】出處：youcanbuildthings.com。
   ⚠ **與 `global/CLAUDE.md` §4.2「skill frontmatter 的 model／effort 只准往上調」直接衝突。**
   若屬實，B-4 一裝，所有刻意指定高階模型的審查型角色會**靜默降級**。
   **這一項在裝 B-4 之前必須先驗**（驗法見 §6）。

2. **「電報體省 token」是行銷數字**
   【第三方實測】JetBrains 用 SkillsBench 86 項任務、配對 A/B、沙箱固定版本：
   官方聲稱省 65%，**實測只省 8.5%**；總成本因一次任務暴衝反而**貴了 11.6%**（$40.60 vs $36.39）；
   品質差異無統計顯著性（p=0.82）。出處：blog.jetbrains.com，2026-07。
   ⇒ **社群流傳的省 token 百分比，預設當它是假的。**

3. **子代理不一定省**
   【單一來源·未證實】子代理可佔重度 session 成本的 85%；3 個子代理做同一任務約為單線程 4 倍花費。
   【官方明文】的正面說法是：子代理讀了 6,100 token 的檔案，parent 只收到 420 token 摘要。
   ⇒ 兩者不矛盾——**省的是 parent 的 context，燒的是總量**。若目標是配額而非單則 context，派工要更節制。

---

## §6 驗證方式（動工前先寫好）

| 要驗的 | 怎麼驗 | 判準 | 誰跑 |
|---|---|---|---|
| §5 陷阱 1（環境變數蓋掉 frontmatter） | 設 `CLAUDE_CODE_SUBAGENT_MODEL=haiku`，派一個 frontmatter 寫 `model: opus` 的角色，用 `/tasks` 看它實際跑哪個模型 | 若顯示 haiku ⇒ 陷阱屬實，B-4 不可裝 | Claude 派、user 看 `/tasks` |
| A-1 的效果 | 連續兩週：一週照舊切換、一週不切換，各自用 `/usage` 的 cache miss 次數與原因比較 | miss 次數應明顯下降 | user |
| B-1 的副作用 | 調低後刻意跑一個大輸出指令，看有沒有截掉需要的資訊 | 截斷處要能被 Claude 察覺並改用檔案落地 | Claude |
| C-1 是否可行 | 跑一次 `/usage`，確認有沒有「依技能／子代理／MCP」的歸因區塊 | 看得到 ⇒ 手工對照表可退休 | user |
| 整體 | 每批只裝一項，裝完量一週再裝下一項 | **同時裝多項＝分不出誰起的作用**，上一輪就是這樣栽的 | — |

⚠ **前置條件**：`MODEL_ROUTING_PLAN.md` §9.15 的「介面選單切 Sonnet」尚未執行。
在那件事完成前裝任何旋鈕，量到的變化都無法歸因。**這是硬性排序，不是建議。**

**【2026-09-06 更新】user 已切選單，前置條件解除**（user 口頭確認，未經 `/tasks` 或其他工具驗證選單實際狀態——這是本回合唯一的資料來源）。
⚠ 但「整體」列的規則沒有跟著解除：A-1／`effortLevel: medium` 的效果本回合稍早驗證仍是「疑似生效，未定案」（見交接檔 2026-09-06 追加段落），
**選單切換本身也還沒量過一週**。三個變數（不換模型規則、effortLevel、選單切 Sonnet）目前疊在一起，
**現在裝任何 B 批項目＝第四個變數疊上去，之後更分不出誰起作用**。

---

## §7 待決分岔（等 user 逐項討論）

| # | 分岔 | 選項 | 傾向 | 理由 |
|---|---|---|---|---|
| 1 | §3 的規則要怎麼改 | A 不換模型／B 短對話才換／C 不改 | **A** | 判準是事實不是判斷，不會被自我豁免 |
| 2 | 先做哪一批 | A 批／B 批／C 批 | **A 批** | 零設定變更、零風險，且 A-1 的效果蓋過所有旋鈕 |
| 3 | B-4 要不要裝 | 裝／先驗 §5 陷阱 1 再說／不裝 | **先驗再說** | 若陷阱屬實，裝了會靜默降級審查型角色 |
| 4 | 手工 token 對照表要不要退休 | 退休／保留／並行一段時間 | **先確認 `/usage` 看得到歸因再決定** | 事實可查證，不該用猜的 |
| 5 | 本檔要不要升級成 wayfinder map | 要／不要 | **不要** | 動的是規則與設定，範圍在一則對話內可收斂，還不到跨 session |

### 7.1 user 裁定（2026-09-06）

| # | 裁定 | 狀態 |
|---|---|---|
| 1 | **選 A：一則對話從頭到尾不換模型、不調 effort。** 需要高階模型就另開對話 | 待改 `global/CLAUDE.md` §4.2 與 hub 副本 |
| 2 | 先做哪一批 —— user 要求**先給估算**，見 §1.3／§1.4 | **估算已出，等 user 定調** |
| 3 | **B-4 先驗 §5 陷阱 1 再說**，未驗完不裝 | 驗法見 §6 第一列 |
| 4 | **手工 token 對照表直接退休，改用內建歸因** | 待確認本帳號看得到歸因區塊（§6 第四列） |
| 5 | 不升級成 wayfinder（Claude 決定，user 未推翻） | 已定 |

⚠ **分岔 1 的裁定與 §1.4 第 3 點互相印證**：實測顯示一個任務切兩次模型約燒掉本則對話總量的
19.6%。這條規則不是「理論上不好」，是**實際上每個任務燒掉五分之一**。

---

## §8 狀態

### 8.1 已施作（2026-09-06，commit `ed7bc5a`）

| 動了什麼 | 為什麼是這個值 | 落點 |
|---|---|---|
| 刪掉「起頭切 Opus、收尾切回 Sonnet」，改成**開場選定並全程用它** | 實測前綴 17.3 萬時，一個任務切兩次＝該則總量的 19.6% | `global/CLAUDE.md` §4.2＋hub 副本＋`~\.claude\CLAUDE.md` 三份同步 |
| 新增「對話長度是最大的成本」一條 | cache_read 實測 53.6%，是最大單一項 | 同上 |
| `effortLevel` **high → medium** | output 實測 32.4%，thinking 算 output | `global/settings.json`＋`~\.claude\settings.json` 兩份，改後過 `json.loads`、鍵數 14 不變 |
| 新增 `tools/token_usage_breakdown.py` | 量測要能重跑，不能只留在對話裡 | harness `tools/` |
| 介面選單切 Sonnet（`MODEL_ROUTING_PLAN.md` §9.15 前置條件） | 解開 B 批的硬性排序前提 | user 操作選單，口頭確認（未經 `/tasks` 或其他工具驗證選單實際狀態） |
| C-4（能用 CLI 就不掛 MCP）查證：**不適用** | `settings.json`（兩份）與 `.claude.json` 各專案 `mcpServers` 全部是空 `{}`，本機沒有自訂 MCP server 可停用 | 從可施作清單移除，附查證依據於本表格 C-4 列 |
| **B-2**：`subagentPromptCacheTtl` **未設 → `"1h"`** | 官方明文子代理預設 5 分鐘快取，主對話 1 小時；本機實測子代理各燒 47k–99k token 全部冷啟動 | `global/settings.json`＋`~\.claude\settings.json` 兩份，改後過 `json.loads`、鍵數 14→15 |
| **B-3**：`promptCacheTtl` **未設 → `"1h"`** | 官方明文訂閱制在額度內本來就是 1h，這項只鎖死「超額後靜默掉回 5m」的降級行為，正常情況無副作用 | `global/settings.json`＋`~\.claude\settings.json` 兩份，改後過 `json.loads`、鍵數 15→16，兩份僅 `additionalDirectories` 路徑寫法（`~/...` vs 展開路徑）不同，其餘欄位逐一比對相符 |

⚠ **B-2／B-3 是在三個未量完效果的變數（不換模型規則、`effortLevel: medium`、選單切 Sonnet）疊加之上再裝的第四、第五項**——
user 明確選擇接受這個代價（「疊加變數，現在就裝」），**不是符合 §6「每批只裝一項、量一週再裝下一項」的施作**。
下次量到效果時，五個變數的作用無法個別歸因，只能看整體方向。B-3 本身風險極低（純安全網，非額度內無行為差異），
不額外增加需要歸因的維度，但仍計入「已疊加變數數量」供未來對照時提醒自己樣本不純淨。

**為什麼敢兩項一起動**（user 選「兩個一起做」，我原本標了歸因風險，這裡撤回）：
兩項落在逐字檔的**不同欄位**——對話長度動的是 `cache_read_input_tokens`，
effort 動的是 `output_tokens`。同一支腳本一次跑出兩欄，**事後分得開**，
不構成「同時變多項就分不出誰起的作用」那一型。

### 8.2 沒做的

- **B 批裝了 B-2、B-3**（`bashOutputMaxChars`／`CLAUDE_CODE_SUBAGENT_MODEL`／
  `skillListingMaxDescChars` 仍未裝）。理由：B-4 卡在 §5 陷阱 1 未驗；B-1 要先訂截斷閾值、
  B-5 本機技能描述僅 2,539 字元、性價比低，兩項先不動。
- **C 批只落實 C-1，C-4 查證後標不適用**：手工 token 對照表**從未實際存在**（全 repo grep 只命中本檔），
  所以「退休」等於「不要新建」，改用 `tools/token_usage_breakdown.py` 與 `/usage` 歸因。
  C-4 查了本機 `mcpServers` 全空，沒東西可停，從可施作清單移除。C-2／C-3／C-5 未動。
- **§5 陷阱 1 未驗**，B-4 因此不得裝。

### 8.3 待驗清單（四欄齊全）

| 項目 | 為何沒驗 | 驗證指令逐字 | 誰跑 |
|---|---|---|---|
| §4.2 新規則與 `effortLevel` 是否生效 | **【2026-09-06 第三輪更新：方法論 bug，前兩輪判定作廢】**前兩輪把 3ce4b514 當「已結束的 Sonnet session」混進平均值，但 `/clear` 在同一分頁**不會**產生新 session id，3ce4b514 其實是持續累積的同一份檔——第三輪重跑它已從 6 回合漲到 51 回合，證實它從未封存過。前兩輪的混合平均（665／446 那兩組）因此**沒有比較意義，撤回**。唯一乾淨樣本只剩 **341659d8**（全 Opus、13 回合、三次量測數字完全不變、真正封存）：平均每回合 output 1,312→524.5（↓60%）、cache_read 123,693→74,270（↓40%），方向與前兩輪一致但樣本更乾淨（同模型、真封存），**仍偏小**（13 回合 vs 基準線 145 回合），未達定案門檻 | 開一個**全新的分頁／視窗**（不是同分頁 `/clear`——那樣量到的還是 3ce4b514 這個黑箱），跑 `py -3 tools/token_usage_breakdown.py -n 5`，找另一個全 Opus、`effortLevel: medium` 的**已封存**新 session，跟 341659d8 的 524.5／74,270 做第二點對照 | 我（下一個全新分頁） |
| §5 陷阱 1：環境變數是否蓋掉 skill frontmatter 的 model | 需要看 `/tasks` 面板，桌面版這則看不到 | 設 `CLAUDE_CODE_SUBAGENT_MODEL=haiku`，派一個 frontmatter 寫高階模型的角色，開 `/tasks` 看實際模型 | user |
| `/usage` 有沒有「依技能／子代理／MCP」歸因區塊 | 桌面版不接終端機型斜線指令 | 終端機跑 `claude`，進去打 `/usage` | user |
| 介面選單切 Sonnet 後的效果 | 只有 user 能操作選單，且選單狀態本身沒工具可查證 | 切完開新對話不碰選單，跑同一支腳本比對 | user 切、我量 |
| B-2：`subagentPromptCacheTtl: 1h` 是否真的生效、有沒有降低子代理冷啟動成本 | **【2026-09-06 查證：原始基準查無實據，無法同條件比對，結案為「不可比」】**派了 `locator` 做一次單一 Grep 的冷啟動任務，回報 `subagent_tokens: 12901`，遠低於原本記錄的 47k–99k。全 repo 搜尋「四個角色」「47k」「99k」，**只有本檔自己提過這句話，找不到當初是哪四個角色、做了什麼任務的原始記錄**——那組基準線本身就是「沒留證據的斷言」。**判定：這次數字沒有比較基準，不能拿來說 B-2 生效或無效，也不會再去補測**（找不回原始任務就永遠湊不出同條件對照） | 無（已結案，不再驗） | — |
| §5 陷阱 1：`CLAUDE_CODE_SUBAGENT_MODEL` 蓋掉 frontmatter 的 model | **2026-09-06 user 裁定擱置**：曾短暫寫進兩份 `settings.json` 的 `env`（未跨過對話邊界生效、未實際驗到），user 決定先不驗，已移除 | 開新對話，設 `CLAUDE_CODE_SUBAGENT_MODEL=haiku`，`Agent` 派 `locator`（frontmatter `model: sonnet`，不必改檔），開 `/tasks` 面板看它實際跑 sonnet 還是 haiku，**驗完不論結果都要移除環境變數** | user 開新對話派、user 看 `/tasks` |
| B-4 | 因 §5 陷阱 1 未驗，**維持不可裝** | 同上 | 同上 |
| B-3：`promptCacheTtl: 1h` 是否有實際效果（相對於訂閱制原本額度內就是 1h 的基準行為） | 純安全網設定，正常情況下（未超額）**理論上無可觀測差異**——只有超額掉回 5m 時才會看出差別，短期內不易驗到 | 需要一段時間的使用量資料佐證是否曾經超額掉回 5m，短期無法驗；長期可對照 `/usage` 額度紀錄與同期是否有 cache_write 異常升高 | 我（長期觀察，非下一則） |

### 8.4 基準線（下次對照用這組）

⚠ **佔比不能跨 session 比**：短對話的 `cache_read` 佔比天生就低。`tools/token_usage_breakdown.py`
已補一列不受對話長度影響的指標，**驗 `effortLevel` 只准看那一列**。

| 指標 | 基準值 | 條件 |
|---|---|---|
| **平均每回合 output** | **1,312** | 全 `claude-opus-5`、`effortLevel: high`、145 回合樣本 |
| 平均每回合 cache_read（＝平均前綴） | 123,693 | 同上 |
| 佔比（僅供了解結構） | cache_read 54.7%／output 29.9%／cache_write 15.4% | 同上 |

---

## §9 原始狀態欄

- **2026-09-06 立案。§1 的數字全部是本機實測，§4／§5 的依據全部標了等級。**
- **未動工。** 未經 user 逐項討論前不改任何規則、不裝任何旋鈕。
- 前置阻塞：`MODEL_ROUTING_PLAN.md` §9.15（介面選單切 Sonnet，只有 user 能做）。

### 沒做的／沒找到的

- **沒驗**：§4 全部候選項的實際效果，一項都沒量過。本檔是清單不是結論。
- **沒找到**：Sonnet／Opus／Haiku 的精確費率倍率（官方成本頁只定性說 Sonnet 較便宜）。
- **沒找到**：子代理啟動的固定成本門檻——「多小的任務不值得派」官方沒給界線。
- **沒找到**：Reddit 原始討論串全文（爬蟲政策擋下）、X 上的開發者討論串（搜不到原生內容）。
- **可能過期**：C-4 的 MCP token 數字出自 2025-09 的 issue，官方之後推出 MCP Tool Search，改善幅度**查不到後續追蹤**。
- **【2026-09-06 新增】§1.3／§8.1 引用的「四個角色各燒 47k–99k」沒有原始記錄**：全 repo 搜尋找不到當初是哪四個角色、做了什麼任務，B-2 因此永遠湊不出同條件對照，已在 §8.3 該列結案為「不可比」。**以後寫『N 個角色燒了 X token』這種話，當下就要留角色名單與任務，不能只留結論數字。**
