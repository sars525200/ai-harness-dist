# 角色頁改架構圖計畫書（可觀測路由）

> 2026-07-31 立案。需求：角色頁從清單表格改成**架構圖**，每個角色可點開自己的設定
> （技能工具／沙箱範圍／說明／觸發歷史），並且看得出「一個任務開啟後路由怎麼跑、
> 誰在工作誰在空閒」。**未經逐項討論同意前不動任何程式碼。**

## §0 現況（2026-07-31 實測）

### 有的

角色檔 `<repo>\.claude\agents\*.md` 的 frontmatter **已經是結構化的**，四類需求有三類有現成來源：

| 需求 | 現成來源 | 缺什麼 |
|---|---|---|
| ① 使用的技能、工具 | `tools:`（Read/Grep/Glob/Bash…）＋ `model:`（查詢員 `sonnet`，其餘 `inherit`） | **skill 那半沒有**——角色不能呼叫 skill（無一角色帶 `Skill` 工具），這欄實際上是空的 |
| ② 沙箱範圍 | `hooks:` 的 agent-scoped 閘門（3 個角色掛 `agent_readonly_gate.py`） | 閘門「實際擋過什麼」沒有按角色分開記 |
| ③ 說明欄位 | `description:`（寫給模型判讀觸發用，很長） | 無 |
| ④ 觸發歷史 | event log 的 `SubagentStop`＋`agent_type`＋`agent_id` | **只有「結束」沒有「開始」**，見下 |

### ★ 缺的：沒有「開始」事件，所以算不出忙閒

實測 event log：`dispatch` 記到的 `tool_name` 只有 `Bash 2365`／`Edit 955`／`PowerShell 671`／
`Write 297`——**`Agent` 一次都沒有**，而 `SubagentStop` 有 22 次。派出去的那一刻沒被記下來。

根因在 `dispatch.py` 的順序：

```
candidates = [規則裡 events 含本事件、tools 含本工具的]
if not candidates:
    return 0            # ← 在這裡就退了
_log_event(kind="dispatch", ...)   # ← 心跳記在後面
```

`settings.local.json` 的 matcher 確實含 `Agent`（hook 有被呼叫），但 **REGISTRY 裡沒有任何規則的
`tools` 含 `Agent`** → `candidates` 為空 → 心跳沒記。這與 `Skill` 的處理方式對照很清楚：
`Skill` 有一個**純觀測分支**（`kind="skill"`，記完 return 0），`Agent` 沒有。

⇒ **「誰在工作誰在空閒」在補上 spawn 事件之前做不到**，只能做到「誰被派過幾次、最後一次何時」。

### 另一個既有缺口（看板上已註明，這次不解）

dispatch matcher **不含 Read/Grep/Glob** → 唯讀角色讀了哪些檔一行都沒記。
所以「它實際碰過什麼」拿不到，圖上只能畫「它被派去做什麼」。

## §1 目標

1. 角色頁看得出**拓樸**：誰能派誰、誰掛什麼閘門、誰的工具邊界到哪。
2. 每個角色可展開看四類設定（①技能工具 ②沙箱 ③說明 ④觸發歷史）。
3. 看得出**忙閒**：這一刻誰在跑、跑多久了；歷史上誰常被派、誰建好沒人用。
4. 不破壞既有紀律：數字一律從來源產生（不手寫）、零目標拒跑、冪等、深淺色。

## §2 核心約束（先講，因為它決定形態）

- **看板是 artifact，它沒有狀態、也讀不到本機檔**（7/31 向 control plane 查證：可用 capability
  只有 `downloads`／`mcp`）。所以看板上的圖**必然是快照**，「這一刻誰在跑」在 artifact 上
  只能顯示「產生當下誰在跑」。要真即時，出口必須是本機服務。
- **角色設定改不了**：同上，artifact 寫不回 `.claude/agents/*.md`。頁面上能做的是「選好→下載／複製」，
  或把改設定放到本機頁（`reviewer/` 已有這個形狀可沿用）。
- **spawn 事件是前置**：§3 的 D1 若選「要忙閒」，就得先改 `dispatch.py`（純觀測分支，比照 `Skill`）。

## §3 待決分岔（逐項用選擇題討論，同意才做）

| # | 分岔 | 選項 | 傾向 |
|---|---|---|---|
| D1 | 忙閒要做到什麼程度 | (a) 先補 spawn 事件＋看板顯示快照 (b) 再加本機即時頁 (c) 只做歷史統計不做忙閒 | **(a) 先補事件**：沒有 spawn 事件，(b)(c) 都只是不同畫法的同一份殘缺資料。補完先看快照夠不夠用 |
| D2 | 圖的形態 | 拓樸圖（節點＋連線，看得到派工關係）／泳道時間軸（看得到一個任務怎麼跑）／兩者切換 | **拓樸圖優先**：需求主語是「誰在工作」，那是狀態不是時序 |
| D3 | 設定能不能從頁面改 | 唯讀展示／看板選好後下載／本機頁直接寫檔 | **唯讀展示 ＋ 本機頁改**：角色檔是 harness 的一部分，改它應該走版控，不該有一個會繞過 git 的寫入路徑 |
| D4 | 對抗式覆核審查者要不要併進角色頁 | 併（當成一個角色顯示）／維持獨立區塊 | **併**：它確實是被派出去的 subagent（event log 裡叫 `Plan`，已 11 次），跟其他角色同一件事 |
| D5 | 內建角色（Plan／Explore／general-purpose）要不要畫 | 畫／只畫自建 | **畫但標明**：`Plan` 被派 11 次比自建角色都多，只畫自建會漏掉最常用的那條路由 |

## §4 做法（待 §3 定案後展開）

### Phase 1 — 補觀測（D1(a) 的前置）

- `dispatch.py` 加 `Agent` 純觀測分支：記 `kind="agent_spawn"`＋`subagent_type`＋`description` 前 60 字。
  比照 `Skill` 分支的既有形狀（記完 `return 0`，不進規則流程）。**不記 prompt 內容**——
  那是任務內容，跨 session 共用 log 不該收（同 `kind="decision"` 只在非乾淨 ALLOW 留痕的理由）。
- 配對 `agent_spawn` ↔ `SubagentStop`（靠 `agent_id`）→ 未配對者即「進行中」。

### Phase 2 — 圖

- `gen_roles_topology.py`：讀 `.claude/agents/*.md` frontmatter ＋ event log → 產生拓樸圖與展開面板，
  注入既有 marker 契約（找不到 marker `SystemExit`、冪等、零目標拒跑）。
- 圖用內嵌 SVG 或 CSS grid，**不引外部庫**（artifact CSP 擋所有外部請求）。

### Phase 3 — 設定入口（D3）

- 沿用 `reviewer/` 的本機服務形狀，擴成「角色設定頁」。

## §5 驗證判準

1. 冪等（固定輸入連跑兩次雜湊不變）——注意 event log 會長，要用快照驗（同成本分頁的坑）。
2. spawn↔stop 配對正確：手動派一個 subagent，圖上該出現「進行中」，結束後轉「空閒」。
3. 零目標拒跑：角色目錄空 → 拒絕產出空圖。
4. 深淺色各實截一張。
5. `test_dashboard_structure.py` 補斷言；變異腳本證明會紅。

## §6 不做

- **唯讀工具的細粒度追蹤**（Read/Grep/Glob 不進 matcher）：加進去會讓 dispatch 每次讀檔都跑一次，
  成本與雜訊都不划算。圖上只畫「被派去做什麼」，不畫「實際碰了哪些檔」。
- **artifact 上的即時輪詢**：沒有狀態能力，也沒有可用的資料來源，做不到。

## §7 狀態

- 2026-07-31 立案，§0 現況已實測（含 spawn 事件缺口的根因定位）。
- 2026-07-31 §3 討論完畢：**D1 一次做到即時**（非推薦值，user 選的）／D2 拓樸圖／
  D5 內建角色也畫並標明；D3 唯讀展示＋本機頁改、D4 審查者併進角色頁 採傾向值。
- **2026-07-31 三個 Phase 全部完成**：
  - **Phase 1**：`dispatch.py` 加 `Agent` 純觀測分支（`kind="agent_spawn"`，只記
    `subagent_type`＋`task` 60 字，**不記 prompt**）。上線當天即在真實環境命中
    （`Plan spawn=1`，13:51 另一條 session 派的）。
  - **Phase 2**：`dashboard/gen_roles_topology.py` → 看板「角色」分頁最上方的拓樸圖。
    主 session 為中樞、自建／內建兩欄、節點可展開四類設定、狀態三重編碼
    （`●` 進行中／`○` 空閒／`×` 從未被派過）。既有角色表格保留在下方（並排比較視圖仍有價值）。
  - **Phase 3**：`reviewer/roles_live.py` → 本機即時頁（4 秒輪詢，唯讀）。
    與看板**共用同一份判定邏輯**（`gen_roles_topology.activity()`），不留第二份 copy。
- 驗證：回歸網 **255/255**；冪等 PASS；結構驗證 8 頁籤 PASS；拓樸圖與即時頁各實截一張。
  新增 2 個變異（Agent 心跳消失／prompt 外洩進 log），都證明會紅。
- 被既有閘門當場擋下一次：`BUILTIN` 的說明裡寫了 markdown 粗體，會被 `_esc()` 轉義成字面星號
  —— 看板結構驗證有一條負向檢查專門擋這個。已改純文字並在程式碼註明。

### 仍未做

- **spawn↔stop 的 FIFO 配對只保證「進行中的數量」是對的**，不保證哪一筆對哪一筆
  （同型角色同時多開時）。所以頁面只用數量，不宣稱「這一筆跑了多久」。要做到後者，
  得讓 spawn 與 stop 共享一個 id —— 平台在 spawn 當下還沒有 `agent_id`，需要另想辦法。
- **唯讀工具（Read/Grep/Glob）仍不進 matcher**，所以「它實際碰了什麼」還是看不到。
  §6 已說明這是刻意的取捨（成本與雜訊）。**→ v2 §V0-② 已找到零成本替代路徑，這條取捨可以撤銷。**

---

## v2（2026-08-04）：角色頁改成「可編輯決策的四層視圖」

> user 需求原文：①角色欄位顯示方式優化，「像一個辦公室有部門有人員分佈」
> ②角色點開要看得到工具名稱，才知道「我設計的角色是否在正確地使用工具」。
> 目標是**更好地編輯優化人員，讓角色更專精更實用、分工明確，達到省錢的效果**。
> **未經逐項討論同意前不動任何程式碼。**

### §V0 現況實測（2026-08-04，全部實跑過）

### ① user 的心智模型 vs 平台實際

user 提出角色由四層組成：**規範（人腦記憶）／技能（專業技能）／工具（依技能需要）／沙箱（辦公室辦公桌，
跨部門要找人或通知主管）**。骨架與平台結構對得上，四層確實是四件不同的事。四處不符：

| 層 | 對應到平台的什麼 | 現況 |
|---|---|---|
| 規範 | 角色檔 frontmatter 以下的正文（＝system prompt）＋ CLAUDE.md | 自建角色**會**載入 CLAUDE.md，內建角色（Plan／Explore／general-purpose）**不會**。而 Explore 是全場被派最多的角色（48 次）——它在完全不知道任何硬規則的狀態下工作 |
| 技能 | `Skill` 工具 ＋ `/asset-data-rules` 這類參考型 skill | **這一層目前是空的**：五個自建角色沒有一個的 `tools:` 帶 `Skill`，所以寫好的專業規則它們呼叫不到（只能自己 Read 那個檔，而它們不知道要讀） |
| 工具 | `tools:` | 正常運作，是唯一四層裡完整的 |
| 沙箱 | `tools:` 邊界 ＋ `hooks:` 的 `agent_readonly_gate.py` | **是兩層不是一層**：tools 決定「手上有沒有這個工具」，閘門決定「有工具但不准做某些事」。**而且沒有目錄範圍限制**——查詢員能 Read 任何路徑包含 C 槽。user 以為有「辦公室」邊界，實際沒有 |
| 跨部門找主管 | — | **不存在**。自建角色的 `tools:` 都沒有 `Agent`，做不到就是做不到，不會問也找不了人。**唯一例外：general-purpose 實測派過 6 次子角色**（`spawnDepth: 2`） |

### ② ★ 看板現在顯示的派工次數是錯的（低估），且有零成本的正確來源

看板的 `activity()` 讀 `D:\.ai-harness\state\events.*.ndjson`（hook 記的），而 **`agent_spawn` 是 2026-07-31
才開始記**、`SubagentStop` 也只在 hook 掛上之後才有。平台自己的
`~/.claude/projects/d--IT-department/<session>/subagents/agent-*.jsonl` ＋ `.meta.json`
**從一開始就有完整紀錄**，且含逐筆 `tool_use`（**Read／Grep／Glob 全都有**）。

| 角色 | 看板現在說 | 實測真值 | 實際工具使用 |
|---|---|---|---|
| Explore | × 從未被派過 | **48 次** | Grep 564・Read 547・Bash 339・Glob 46・PowerShell 3 |
| Plan | ● 進行中 ×1 | **26 次** | Bash 772・Read 283・Grep 78・PowerShell 7・Glob 2 |
| general-purpose | × 從未被派過 | **14 次** | Read 145・Grep 121・Bash 37・Glob 12・**Agent 6**・Write 4・WebSearch 4・WebFetch 4 |
| 查詢員 | ○ 派過 7 次 | 8 次 | Read 85・Grep 82・Glob 29（**完全在宣告邊界內，零越界**） |
| claude-code-guide | **圖上沒有這個角色** | 7 次 | WebFetch 38・Grep 4・Read 3・WebSearch 2 |
| project-auditor | ○ 派過 3 次 | 3 次 | Bash 120・Read 56・Grep 12 |
| 雙改檢核員 | × 從未被派過 | **0 次（看板是對的）** | 唯二兩筆紀錄都在測試 session（`11111111-…`／`22222222-…`），是閘門驗證餵料 |
| 美編人員 | × 從未被派過 | 0 次（8/4 才建，屬實） | — |
| harness-auditor | × 從未被派過 | 0 次（屬實） | — |

⚠ **2026-08-05 訂正**：本表初稿把雙改檢核員寫成「派過 2 次、只跑 Bash 沒讀檔」，
那是**沒有過濾測試 session** 造成的。`gen_roles_topology` 既有的 `_TEST_SESSION`
判準（手寫規律 UUID）必須在新解析器裡原樣沿用——**少一道過濾，測試餵料就會變成
「這個角色的真實行為」，而它讀起來完全合理，不會有人懷疑。**

`.meta.json` 另外帶 **`model`（實際解析後的值，不是 `inherit`）** 與 `spawnDepth`、`description`、`toolUseId`。
7/31 之後的紀錄才有 `model`（平台版本升級加的），之前為空。

**這正是 user 要的「用得對不對」訊號**：

- **查詢員 ＝ 用得對的樣板**：宣告 Read/Grep/Glob，實際就只用這三個，比例也合理（定位工作本來就是讀＋找）。
- **general-purpose ＝ 邊界最寬的那個**：實測用過 9 種工具，含 `Agent` 6 次（會再派下一層）
  與 `Write` 4 次。它同時**不載入 CLAUDE.md**——寫入能力＋不知道硬規則，是清單裡風險最高的組合。
- **只看次數會誤判**：雙改檢核員的角色檔第 37 行明確要求用 `git diff --no-index` 比對兩端，
  所以它就算真的跑起來，`Read` 也應該是 0 —— **「該讀沒讀」與「設計就不用讀」在次數欄長得一模一樣**，
  要看到它 Bash 實際打了什麼才分得出來。這是 V-G 選「次數＋實際目標」的直接理由。

### ③ 成本現況（對應「省錢」目標）

| 角色 | 宣告 model | 實測跑什麼 | 備註 |
|---|---|---|---|
| Plan | 平台內建 inherit | **opus**（有記錄的 9 筆全是） | 26 次、Bash 772 次，是最大宗支出。`/adversarial-review` 又疊 `effort: high` |
| project-auditor | `inherit` | **opus** | inherit ＝ 跟主 session，而主 session 常是 Opus |
| 查詢員 | `sonnet` | — | 唯一已釘住便宜模型的角色 |
| harness-auditor／雙改檢核員 | `inherit` | 未有紀錄 | 做的事是「跑腳本＋比對文字」 |
| 美編人員 | `sonnet` | — | |

⚠ **`inherit` 會讓人以為「沒設定＝沒事」，實際上它繼承的是當下最貴的模型。**

### ④ 資料源量體

108 個 subagent 紀錄檔。**它每回合都在長** → 按 `dashboard-generators.md` 硬規則
「上游會每回合都在長的產生器不准接進 `refresh_dashboard.py` 的 SOURCES」，
這個來源**只能走離線批次**（`/shougong` 步驟 3.5 ＋隨時手動跑），不能掛 Stop hook。
冪等也只能在快照上驗（同成本分頁的坑）。

### §V1 已定案（2026-08-04 user 選定，不再討論）

- **分組來源＝職能分組，寫進角色檔 frontmatter `department:`**。單一真相與 `tools:`／`model:` 同層，
  新增角色時在同一個檔裡填。沒填的歸「未編組」並**在畫面上明講**，不靜默吞掉。
  內建角色無角色檔 → 沿用現有 `BUILTIN` 手寫並標日期（既有先例）。
- **版面＝部門區塊 ＋ 人員卡片格狀**。部門數量增減時版面自己收攏，不必調欄數。

### §V2 待決分岔（逐項用選擇題討論，同意才做）

| # | 分岔 | 選項 | 傾向 |
|---|---|---|---|
| V-A | 部門怎麼切、誰歸誰 | (a) 五組：查證／稽核／品管／設計／規劃 (b) 四組：查證／稽核／執行／規劃 (c) 依 user 現場想法定 | **(a)**：對得上現有五個自建角色的實際職責，且每組都有明確的「不做什麼」。插件角色（claude-code-guide）另立「外援」或歸「查證」待定 |
| V-B | 次數資料源怎麼換 | (a) 全面改用 `subagents/`，hook log 只留「進行中」判定 (b) 兩源並存、畫面標明各自涵蓋範圍 (c) 維持現狀只補註解 | **(a)**：hook log 的優勢只有「即時」（`running` 要靠它），歷史統計全面輸給 `subagents/`。並存會讓同一個數字有兩個版本，正是這批產生器要根治的病 |
| V-C | 技能層要不要補 | (a) 先實測「角色的 `tools:` 能不能配 `Skill`」，能就給稽核／檢核角色配對應的參考型 skill (b) 只在看板顯示缺口不補 (c) 不做 | **(a) 但先實測**：現在沒有任何證據說明角色配得了 `Skill`（只知道沒人配過）。**這是 user「讓角色更專精」的最大施力點——專精不是給更多工具，是給它專屬規則**。實測成本一分鐘：暫時給查詢員加 `Skill`、派它呼叫 `/verify-rules`、看 event log 有沒有 `kind="skill"` |
| V-D | 沙箱層顯示什麼 | (a) 顯示三件事：tools 邊界／閘門有沒有掛／**目錄範圍未受限（明講）** (b) 只顯示前兩件 (c) 順便做目錄範圍限制 | **(a)**：目錄範圍沒有限制是事實，**看板不講的話 user 會繼續以為有辦公室邊界**。(c) 是另一個工程（要改 `agent_readonly_gate.py` 加路徑白名單），不塞進這輪 |
| V-E | 「跨部門找主管」要不要做 | (a) 不做，但在畫面上講明「角色做不到只會做不到，不會回頭問」 (b) 做：給特定角色配 `Agent` 讓它能派下一層 (c) 做：讓角色回報時附「我需要但沒有的能力」 | **(a)**：(b) 會讓能力邊界失守（能派人＝能繞過自己的 tools 限制，general-purpose 已經是這個形狀）。(c) 是 prompt 層的事，不是看板的事，要做也該進角色檔正文 |
| V-F | 省錢要做到什麼程度 | (a) 看板顯示「宣告 model vs 實測 model」落差 (b) (a) ＋ 對 `inherit` 的角色標示可降級建議 (c) 直接改角色檔把 model 釘死 | **(b)**：(a) 只是把事實攤開；(b) 多一句判定但不動檔，改不改仍是 user 決定。(c) 是執行不是看板，等 user 看完落差再說 |
| V-G | 工具明細顯示到多細 | (a) 次數 ＋ 每個工具最近幾筆實際目標（Read 讀了哪些檔／Bash 跑了什麼） (b) 只補次數 (c) 次數 ＋ 產生器算出的越界訊號摘要 | **(a)**：user 的原始問題是「是否在正確地使用」，**光看次數答不出來**——雙改檢核員那個例子就是證據。(c) 的判定規則寫死在產生器裡，誤判時要回頭查原始 log 才知道為什麼 |
| V-H | 更新時機 | (a) 離線批次：`/shougong` 步驟 3.5 ＋手動跑 (b) 接進 Stop hook | **(a)**：(b) 直接違反 `dashboard-generators.md` 的硬規則（上游每回合都在長 → Stop hook 的 109ms 秒退會失效）。畫面要標明「這份數字更新到哪一刻」 |

### §V3 做法（待 §V2 定案後展開）

- **新產生器或擴充既有**：`subagents/` 的解析是新資料源，但輸出仍注入同一組 marker
  （`ROLES_TOPOLOGY_START/END`）。既有契約全部照舊：找不到 marker `SystemExit`、零目標拒跑、冪等、
  深淺色各實截一張、`test_dashboard_structure.py` 補斷言、變異腳本證明會紅。
- **角色檔改動**：五個自建角色各加一行 `department:`（V-A 定案後）。若 V-C 實測通過，另加 `Skill` 到
  相關角色的 `tools:` 並在正文寫明「什麼時候該呼叫哪支 skill」。
- **彈窗改四層結構**：①規範（載不載入 CLAUDE.md ＋ 角色檔正文行數）②技能（配了哪些 skill／未配置）
  ③工具（宣告 vs 實際次數 vs 最近目標）④沙箱（tools 邊界／閘門／目錄範圍未受限）。

### §V4 驗證判準（沿用 §5 並補）

1. 冪等（快照上驗，因為上游會長）。
2. 次數換源後：抽查三個角色的數字與 `subagents/` 手動點算一致。
3. 零目標拒跑：`subagents/` 目錄不存在 → 拒絕產出，不把次數寫成 0。
4. **V-C 的實測要先於實作**：沒證明角色配得了 `Skill` 之前，不寫「技能層已補」。
5. 深淺色各實截一張；部門區塊在窄視窗要能收攏不橫向捲。

### §V5 定案（2026-08-04 逐項討論完畢，全部八項）

| # | 定案 | 與傾向值的差異 |
|---|---|---|
| V-A | **五組**：查證（查詢員・Explore）／稽核（harness-auditor・project-auditor）／品管（雙改檢核員）／設計（美編人員）／規劃（Plan）。插件與兜底角色歸**外援**（claude-code-guide・general-purpose） | 同傾向值 |
| V-B | **全面換 `subagents/`**，hook log 只留「進行中」判定（`subagents/` 沒有結束時間，算不出 running） | 同傾向值 |
| V-C | **先實測角色能不能配 `Skill`**，能就給稽核／檢核角色配對應的參考型 skill；不能就在看板寫明「平台不支援」 | 同傾向值。**實測未過之前不寫「技能層已補」** |
| V-D | 沙箱三件事（tools 邊界／閘門／目錄範圍未受限）**要盡量圖形化呈現**，不是文字列表 | ★ user 追加要求：圖形化。純 CSS／內嵌 SVG（artifact CSP 擋外部庫），沿用「門禁卡」形狀對上 user 的辦公室心智模型 |
| V-E | **做**：每個角色正文加一段「碰到邊界時要在回報末尾列〈需要但沒有〉＋理由」。**不給 `Agent` 工具**——能派人就能繞過自己的 tools 限制 | ★ 選了 (c) 而非傾向值 (a)。這是改**角色檔正文**不是改看板，看板側只顯示這個機制有沒有配上 |
| V-F | 看板顯示「宣告 model vs 實測 model」落差 ＋ 對跑腳本／比對文字類角色標可降級建議，**不動角色檔** | 同傾向值。改不改由 user 看完再決定 |
| V-G | 工具區列**次數 ＋ 每個工具最近幾筆實際目標**（Read 讀哪些檔／Bash 跑什麼指令） | 同傾向值 |
| V-H | **離線批次**（`/shougong` 步驟 3.5 ＋手動跑），畫面標明數字更新到哪一刻 | 不是分岔：`dashboard-generators.md` 硬規則直接排除「接進 Stop hook」 |

### §V6 狀態

- 2026-08-04 立 v2，§V0 四項現況全部實跑（含看板次數低估的根因定位、四層心智模型比對、成本現況、量體）。
- §V1 兩項＋§V5 八項全部定案。
- **2026-08-05 全部實作完成**：
  - **V-C 實測先行**（定案要求）：暫時給查詢員加 `Skill` → 派它回報工具清單。**前兩輪都說「沒有 Skill」，
    但那是舊定義** —— 同時植入的探針指令也沒到，證明**角色檔改動在既有 session 內不會重新載入**。
    換獨立 headless session 再驗，工具清單立刻出現 `Skill` ⇒ **平台支援角色配 Skill**，技能層可以補。
    （順帶：那個查詢員識破探針是 prompt injection 測試並拒絕盲從，行為正確。）
  - `dashboard/subagent_stats.py` 新增：解析 `~/.claude/projects/…/subagents/`，
    出次數／工具／**最近目標樣本**／實測 model／**活動範圍 roots**。含機密字樣的目標整筆不收
    （看板會發布成 artifact）。沿用 `_TEST_SESSION` 過濾。
  - `gen_roles_topology.py`：`activity()` 保留給即時頁（hook log 語意），新增 `history()` 讀新來源；
    版面改部門區塊＋人員卡片；彈窗改四層；`_boundary()` 由 `tools:` 推導四級能力邊界。
  - 五個角色檔加 `department:`；稽核／檢核三個加 `Skill` 與「什麼時候呼叫哪支」；
    五個都加 V-E 的〈需要但沒有〉回報段落。
  - `reviewer/roles_live.py` 跟著換源 —— 兩個頁面同一份判定，不讓同一角色在兩處顯示不同次數。
- **驗證**：回歸網 **302/302**；冪等 PASS（改用寫死的 hist 快照 —— 真實歷史每回合在長，
  拿它驗冪等會永遠紅）；JS 語法 PASS；**payload↔JS 欄位雙向對帳零缺口**（27 個欄位全有消費者）。
- **修了兩個測試判準**（都是判準本身看不見新情況，不是放寬）：①`**` 負向檢查排除 `#rt-data`
  —— Glob 的 `**/x.md` 是合法輸入，不排除的話「只要有人用過 Glob 就紅」；
  ②section 平衡改數 `<section`，因為部門區塊帶 class 屬性。**兩者都跑了變異證明仍會紅、且紅的理由正確。**

### §V7 仍未做（已落 `PENDING_VERIFY.md`）

- **深淺色沒實截**：這台機器的 Edge headless 本輪三種模式都不產檔（exit=0 但無輸出）。
  改用靜態檢查代替（57 行新 CSS／0 個寫死色值／16 個 token 深色全有定義）——機制對，但沒看過。
- **角色檔的新設定沒驗到真的載入**：要新 session 才生效，本輪驗不了。
- **`Skill` 配上了但沒實際呼叫過一次**：只驗到「工具清單裡有它」。
### §V8 收成一個網頁（2026-08-05・user 追加）

user 要求「全部內容統一到一個網頁，不要分散」→ **收進看板 artifact**，本機即時頁退役。

- **搬了什麼**：即時頁唯一比看板多的是「工作視窗卡片」（哪個視窗在跑、正在做什麼、
  把活交給了誰、跑多久）。`sessions_detail()` 從 `reviewer/roles_live.py` 搬進產生器，
  看板角色分頁最上方畫成卡片。**不搬過來的話「統一」等於刪功能。**
- **`reviewer/roles_live.py` 退役但不刪**：看板本質上答不了「誰**現在**在跑」
  （artifact 無狀態能力），真要盯著看時再開。它的 `sessions_detail` 已改成 delegate，
  **判定邏輯一份都不留在那裡**。
- **順手治掉「兩套算法」的病**（原本列在仍未做）：`running_by_role()` 跨檔全域配對 vs
  `sessions_detail()` 逐 session，同一件事兩個答案 —— 畫面一邊說「0 個進行中」、
  另一邊某角色掛著「進行中 ×1」。兩份資料原本在不同頁面所以沒人發現，**併成一頁後
  無所遁形**。統一改用 `running_from_sessions()`（只看活動中的視窗，已關閉視窗的殘留
  spawn 自然消失）。**同一個病在這輪出現三個發作點**：看板 build_html、產生器 --check、
  roles_live.snapshot()，三處都改完才一致。
- **順手統一測試 session 過濾**：產生器的 `_TEST_SESSION` 少了 `e2e-`／`test-`／`warnchan-`
  三種前綴，同一批檔案在兩個頁面被算進不同分母。取嚴格的那份。
- 驗證：回歸網 **302/302**；`--check` 的「目前 N 個進行中」與角色清單逐項加總一致。
