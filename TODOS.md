# 待辦登記簿（TODOS）

> **這個檔存在的理由**：2026-08-06 前，全域待辦是**手寫在看板 HTML 裡**的 10 個
> `<div class="todo-item">`。手寫在顯示層的東西只有兩種下場——沒人改（靜默過期），
> 或改了顯示層卻沒人知道資料原本在哪。這裡是**唯一真相**，看板由
> `dashboard\gen_todos.py` 讀本檔產生。
>
> 規則：
> - **做完就把該列刪掉**，不留「已完成」歷史（那是 git log 的職責）。
> - 一列一件事。**「下一步」欄要寫得能直接照做**——寫「再看看」等於沒登記。
> - 屬於某個專案的待辦**不要寫在這裡**：待驗的寫該專案的 `PENDING_VERIFY.md`，
>   其餘寫該專案 `.claude\PROJECT_CONTEXT.md`「待辦來源」表指到的檔案。
>   這裡只放**跨專案／harness 本體**的事。
> - 欄位裡要出現半形 `|` 請寫成 `\|`（markdown 轉義；產生器認得）。
> - **每列都要有「分類」與「優先」**（2026-08-23 起）。這兩欄是**溝通的基準點**：
>   指一件事時說「閘門那三條高的」，不用貼標題。
>
>   **分類＝領域**（回答「要動哪一塊」，同一類可以一批做完）：
>
>   | 值 | 指什麼 |
>   |---|---|
>   | 閘門 | `hooks\` 底下的規則與白名單（會擋人／會判定的東西） |
>   | 看板 | `dashboard\` 的產生器與呈現 |
>   | 角色 | `agents\` 的角色檔與它們的能力邊界 |
>   | 流程 | skill、SOP、覆核、eval —— 人與模型怎麼走一遍 |
>   | 備份 | 版控、鏡像、異地、未 commit 的成果 |
>   | 文件 | 規範檔本身的正確性與長度 |
>
>   **優先＝不做會怎樣**（不是「有多想做」）：
>
>   | 值 | 判準 |
>   |---|---|
>   | 高 | ①不做會**失去東西**（不可逆）②或工具**正在讓人做錯事** ③或**每次都必然發生** |
>   | 中 | 每天／每次都在付摩擦，但繞得過去 |
>   | 低 | 改善。不做也不會壞，只是沒有變好 |
>
>   ⚠ **沒填的話產生器會自己推導優先度，而且畫面上標「（推導）」** —— 推導值是從
>   「誰」欄的措辭猜的，猜不準。**看到「（推導）」就代表這一列沒人真的排過。**
>   分類沒填則整個 chip 不出現（空 chip 比沒有 chip 更吵），該列掉進看板的
>   **「未分類」桶**。**`gen_todos.py` 會點名漏填的列**（2026-08-24 起）——
>   在此之前沒有任何東西會發現，實際上 8/23 標完 20 列後又靜靜多出 4 列沒填。
>
>   ⚠ **兩欄一律加在表尾**：產生器的前四欄是位置固定的（項目｜現況｜下一步｜誰），
>   插到前面會讓所有欄位錯位，而且**不會報錯**——它只會把「下一步」當成標題顯示。

【核心層】格式與解析規則跟被服務的專案無關；內容是本機 harness 的。

---

## 全域（harness 共用層）

| 項目 | 現況／為何還沒做 | 下一步（逐字指令或動作） | 誰 | 分類 | 優先 |
|---|---|---|---|---|---|
| 🔧 **`TODOS.md` 這一列已過期：CTX-1 早就轉正了**（2026-09-03 稽核發現） | 本檔另一列標題仍寫「**CTX-1 還在 shadow**」、行動欄還寫「決定轉正就在 `dispatch_config.json` 加 `"CTX-1": {"shadow": false}`」——**那件事已經做完**，設定檔裡 CTX-1 是 `shadow: false`。⚠ **這一列已經害人抄錯一次**：2026-09-03 重寫 `TRIGGER_MECHANISMS_REFERENCE.md` 時照抄本列，寫成「CTX-1 還在 shadow 觀察期」，與同一份文件自己寫的「18 條全 enforce」互相矛盾，獨立稽核才抓出來。**待辦沒回收＝下一個人會再抄一次** | ①把那一列的「還在 shadow」改成「已轉正（日期待查 `git log -- hooks/dispatch_config.json`）」，或整列刪除 ②順手掃一次本檔還有沒有別的「已完成但沒刪」的列——判準是拿列裡寫的驗收指令實跑一次 | 我 | 閘門 | 中 |
| 🔧 **【需要但沒有】沒有任何腳本能量 skill description 的長度**（2026-09-03 稽核角色喊的） | `tools/skill_inventory.py` 只數支數不吐長度。⇒ 「描述總量超過預算會被靜默砍掉」這個機制**永遠只能靠手抄**，而 `TRIGGER_MECHANISMS_REFERENCE.md` 的整個立論就是「手抄的數字會過期兩個月沒人發現」。⚠ 現況已經因此出錯一次：文件原本只算自訂 30 支（合計 10,037 bytes），漏掉平台與外掛 **24 支**，把風險低估將近一半；而且「字元」與「bytes」混用（`ui-rules` 目測 182 字元、526 bytes） | ①在 `tools/` 寫一支唯讀量測腳本，涵蓋 global＋project＋platform 三層，**輸出 bytes 與字元兩欄**（混用是這次出錯的成因）②要能被唯讀角色跑（放 harness 底下、不寫檔、不吃 `-c`）③驗收：拿它的輸出回填 `TRIGGER_MECHANISMS_REFERENCE.md` §1.1(a)，並與 `/context` 的 Skills 列對照 | 我 | 工具 | 中 |
| 🔧 **【需要但沒有】唯讀閘門放行了會寫檔的腳本**（2026-09-03 實例） | `hooks/agent_readonly_gate.py` 的放行判準是「在 harness 底下的既有 `.py` ＋ 不帶已知寫入旗標」。`tools/skill_inventory.py` 兩條都過，**卻會自己回寫** `SkillViewer/platform_skills.json`——稽核角色這輪就意外寫了一次（+88/-38，內容是把 8/22 舊快照更新成 9/03 實況，**判定不需還原**，但那是運氣好）。⇒ 「唯讀角色」這個宣稱有洞，而洞的形狀是「腳本自己決定要不要寫」，旗標黑名單抓不到 | ①維護一張「已知會寫檔的 harness 腳本」名單，或讓這類腳本預設 dry-run、要寫檔得帶明確旗標 ②**先證明它會紅**：讓唯讀角色跑 `skill_inventory.py`，必須被擋或必須不寫檔 ③順手決定 `skill_inventory.py` 的回寫要不要改成 opt-in | 我 | 閘門 | 中 |
| 🔧 **【需要但沒有】`locator` 角色拿不到檔案的修改時間**（2026-09-03 角色喊的） | `locator` 的工具只有 Read／Grep／Glob，**沒有 Bash 也沒有 stat**。派它去盤點「每份索引檔最後改於何時」時，它只能退而用檔案內容裡自述的日期（例如開頭寫「建立：2026-07-28」）——那是**寫的人手打的**，與檔案系統時間無關，而且正是會過期的那種東西。這輪它明講了這個限制，沒有假裝查到 | ①決定要不要給 `locator` 一個受限的時間查詢能力（例如允許 `Bash(stat:*)`、或另寫一支唯讀的 mtime 查詢腳本讓它跑）②或者接受這個限制，但在 `agents/locator.md` 的邊界一節明寫「查不到 mtime，要問就派別的角色」——**現在兩者都沒有，所以每次派它都會重撞一次** | 我 | 角色 | 中 |
| 🔧 **IT 專案 `CLAUDE.md` 說 skill 覆寫「2 筆」，實際 4 支**（2026-09-03 稽核發現） | `D:\Patrick-AI\IT-department\CLAUDE.md` §7 寫「本專案 skill 覆寫現況 **2 筆**：`diagnose-bug` `effort: high`；`codebase-health: disable-model-invocation`」。實際鎖手動的是 **4 支**——8/25 commit `47fc3c1b` 又加了 `suggestion-inbox`／`verify-skill`／`dry-run-migrate`，**沒回頭改 CLAUDE.md**。⚠ 諷刺點：這正是常駐層自己在講「現況」的地方，而它是常駐的、每輪都在付錢的那一層 | ①改 §7 那句為 4 支並列出名字 ②⚠ 這是常駐層，改完會被 `CTX-1` 預算閘門看到，**字數要控制**——用「鎖手動 4 支：a／b／c／d」一行寫完，不要展開理由 ③驗收：`grep -c 'disable-model-invocation: *true' .claude/skills/*/SKILL.md` 的支數要與那句話一致 | 我 | 文件 | 中 |
| ⏳ **`~\.claude\skills` 整個目錄做成 Junction，不在官方保證範圍**（2026-09-03 查證） | 官方文件只承諾「`skills\` 是真目錄、底下**單一 skill 資料夾**可以是 symlink」，**從未承諾容器層**。硬證據：`claude plugin validate ~\.claude\skills` 直接拒讀（回「This path is a symlink. Component directories are read without following symlinks, so nothing here was validated」），傳真實路徑 `D:\Patrick-AI\.ai-harness\skills` 才 pass。官方 issue anthropics/claude-code#38051：v2.1.69 修 symlink 安全漏洞後，這種形態的 user-level skill **整批靜默消失**，回報者的 workaround 正是改成容器真目錄＋各支 symlink。⚠ 現在能用（15 支實測全載入、實跑呼叫 `explainer-style` 成功），但沒人承諾下一版還能用，而且壞掉的症狀是**靜默**的 | ①先備份 `D:\Patrick-AI\.ai-harness\skills` ②`cmd /c rmdir C:\Users\<USER>\.claude\skills` 拆掉容器 junction ③`mkdir` 成真目錄，逐支 `cmd /c mklink /J "C:\Users\<USER>\.claude\skills\<name>" "D:\Patrick-AI\.ai-harness\skills\<name>"` ④驗收兩關缺一不可：`claude plugin validate C:\Users\<USER>\.claude\skills` 要 exit 0，**且**開一則新對話數 skill 是否 15 支全在——只驗前者會漏掉「驗證過但載不到」 | 我 | 流程 | 中 |
| ⏳ **30 支 skill 的描述共約 10,000 字常駐，溢出時官方會靜默砍掉最少用的那幾支**（2026-09-03 查證＋實測） | 官方機制：skill listing 有預算上限＝模型 context window 的 **1%**，超過時「drops descriptions starting with the skills you invoke least」，**不報錯**。被砍掉描述的 skill 從此不會被自動觸發——**惡性循環：越少用越先被砍，砍了就更不會被用**。實測 30 支 description 合計 10,037 字元，最長 `audit`(649)、`to-tickets`(596)、`ui-rules`(526)、`wayfinder`(515)。本則對話只載全域 15 支（合計 5,323 字元）**沒有溢出**，15 支描述肉眼確認完整；IT-department 那側會同時載 30 支，風險較高但**未實測** | ①在 `D:\Patrick-AI\IT-department` 開一則對話跑 `/context`，看 Skills 那一列套用預算後的實際大小；或 `claude --debug` 抓溢出警告 ②真的溢出：調 `skillListingBudgetFraction`（例 `0.02`）或把最長那幾支的 description 砍短 ③長期解：極少被自動觸發、只靠打斜線叫的 skill 加 `disable-model-invocation: true`，它的 description 就**完全不進 context** ④⚠ 別靠感覺判斷有沒有溢出，要拿 `/context` 的數字 | 我 | 流程 | 中 |
| ⏳ **規則檔被別的 session 改過，正在跑的對話不會知道**（2026-09-03 親身踩到） | `CLAUDE.md` 在對話開始時載入一次，之後檔案改了**不重載、不提醒**。實例：本則對話載到的是舊版（寫 `D:\.ai-harness\tools\push_cloud_title.py`），而磁碟上 00:08 已被另一 session 修成 `D:\Patrick-AI\.ai-harness\...`；照舊指令跑才發現檔案不存在。⚠ 而且這種失敗**很容易被吞**：管線接 tail 之後取 `$?` 拿到的是 tail 的 exit code，要寫 `${PIPESTATUS[0]}` 才是腳本的。⚠ **skill 檔案不受影響**——官方有 live change detection，skill 改動當場生效。⚠ user 原本選「開場比對」，但開場當下規則是對的、抓不到，掛點必須在**每次動工前** | ①在 `hooks/dispatch.py` 加一條規則：SessionStart 記下 `~\.claude\CLAUDE.md` 的 hash 到 `state/`，PreToolUse 每次比對，不一致就提醒重讀 ②**先證明它會紅**：手動改規則檔一個字，下一次工具呼叫必須出現提醒；不會紅就是規則沒掛上 ③fail-open：比對本身失敗一律放行 exit 0，擋住工具呼叫比漏報嚴重得多 | 我 | 閘門 | 中 |
| ⏳ **`/clear` 後的【閒置】改名：程式已改、實機未驗**（2026-09-03） | 封存那條路改成讀「這則對話自己的檔尾 custom-title」再組 `【閒置】任務名`（舊版讀跨對話側寫檔 `state/lasttask.*.txt`，而寫它的 hook 掛載 2026-08-28 已退役⇒ 名字永遠取不到、側邊欄只剩「等待任務」把真名蓋掉）。**已驗**：單元 7 條＋端到端 5 條、兩個變異各自精準轉紅、全套 1552/1552；`_last_custom_title()` 對真封存檔實跑抽得出 `SG-093 流程說明稿`。⚠ **沒驗到的是真的 /clear 一次**——那條路要 `SessionEnd` 真的觸發、真 token、真 PUT，測試裡的 urlopen 是假的。⚠ 另一半**被 auto mode 分類器擋下**：`global/CLAUDE.md` 已含新規則但**沒有部署到 `~/.claude/CLAUDE.md`** ⇒ 新對話讀不到「【閒置】不是給你用的」那條，可能把它當平台快取名蓋掉 | ①部署（被擋，要 user 允許或自己跑）：`cp D:/Patrick-AI/.ai-harness/global/CLAUDE.md ~/.claude/CLAUDE.md`，驗收 `grep -c 閒置 ~/.claude/CLAUDE.md` 要是 1 ②實機：在任一專案 `/clear`，等 sweep 跑完（首個 tick 之後）跑 `py -3 D:/Patrick-AI/.ai-harness/tools/push_cloud_title.py --cse <cse> --check`，雲端應為 `【閒置】<原任務名>` ③對不上就看 `session-archive/archive.log` 的 `idle-title` 那幾行——成功會印 HTTP 200＋title | 我 | hook | 中 |
| ✅ **封存那支的雲端改名遇憑證過期會安靜跳過**（2026-09-03 發現·同日修完） | **已修**：`session_archive._push_idle_title()` 原本 token 取不到就只寫一行「跳過」然後 return ⇒ user 說的「Clear『等待任務』有時候都沒有觸發」查不出成因。①續命實作從 `tools/push_cloud_title.py` **搬進** `hooks/session_title.py`（`_renew_token`／`_token_or_renew`，緊鄰 `_access_token`），封存與推雲端兩條路共用同一份、共用同一把防遞迴旗標；原處保留同名別名，呼叫端與既有測試不動。②封存端改叫 `_token_or_renew()`，換發成功留痕，換不到的訊息改成「這一列的名字沒有改到」而不是「跳過」——「跳過」讀起來像正常判定。③順手清掉搬走後沒人用、卻被測試偷偷依賴的兩個 import，測試改 patch 實作所在的模組。**已驗**：新增 3 條端到端；四個變異各自精準轉紅（拿掉續命 39/41、訊息改回「跳過」40/41、拿掉防遞迴 6/7、續命後不重讀 6/7）；全套 1558/1558。⚠ **沒驗到真的憑證過期**——stub 驗的是分支邏輯，真 CLI 換發那條路仍是交接檔票 06 的範圍。⚠ 換發會真的發一次 API 請求、最多等 120 秒；只因為 sweep 已 detached 才敢這樣做 | ①真正的驗收要等憑證自然過期後 `/clear` 一次，看 `session-archive/archive.log` 有沒有「已叫官方 CLI 換發成功」那一行 ②⚠ 不准為了改名去寫 `~/.claude/.credentials.json`——風險是作廢舊 refresh token 導致整台登出 | 我 | hook | 低 |
| ⏳ **Cursor 角色副本落後 9 天，而且這一縫沒有任何守門**（2026-09-03 盤點發現） | Claude 的 `agents/`、`skills/` 是 junction，改 repo 等於改執行期；**Cursor 沒有這條線**——`cursor-agents/*.md` 要人工複製到 `~\.cursor\agents\`，而每個檔自己的第一句就寫著「那不是 junction」。**實跣不是推測**：`harness-auditor.md` 與 `visual-designer.md` 的 repo 版本 09-02 更新，live 停在 08-25 ⇒ Cursor 這九天派出去的稼核員與美編讀的是舊規格。⚠ **`sync-checker` 不查這一組**（它查的是專案前端雙目錄），沒有工具、沒有測試、沒有閘門。⚠ 別拿 `agents/` vs `cursor-agents/` 的 diff 當漂移，**那兩份的差異是刻意的**（frontmatter 與平台專屬段落）；要比的是 `cursor-agents/` ↔ `~\.cursor\agents\`。斷點已寫進 `COLLAB_HANDOFF.md`「共用層現在有哪些斷點」一節 | ①先重量：`for f in cursor-agents/*.md; do n=$(basename "$f"); diff -q <(sed 's/\r$//' "$f") <(sed 's/\r$//' "$HOME/.cursor/agents/$n") >/dev/null 2>&1 &#124;&#124; echo "DRIFT or MISSING: $n"; done` ②有輸出就把那幾個檔複製過去，先看一眼 diff 確認不是 Cursor 自己改的 ③要真的陣門就寫一支檢查工具並挂進回歸網；**先證明它會紅**（改一個 live 副本的一個字，它必須轉紅） ④順手決定 `cursor-agents/` 要不要補 `locator`，或在那側寫一行「為什麼沒有」——現状是讀的人只能猜是設計還是漏掉 | 我 | 工具 | 中 |
| ⏳ **`global/settings.json`（repo）落後 live，而且 repo 那份還寫著 D 槽搬遷前的舊路徑**（2026-09-03） | 同一支 `backup_global_config.py` 管兩個檔，而兩個檔的同步方向**相反**：`CLAUDE.md` 是 repo→live（已於 2026-09-03 --restore，修掉三處指向不存在目錄的路徑），`settings.json` 是 live→repo。**工具偵測到方向不一致時拒跑整批**（這次真的擋下來了）。⚠ repo 那份的差異不只是舊：它寫的是 `D:\IT-department\.aimemory`，而實際位置是 `D:\Patrick-AI\IT-department\.aimemory` ⇒ **哪天有人拿 repo 還原到 live，記憶目錄會整批失聯**，而失聯的症狀是静默的（載不到記憶不會報錯）。另外 live 多了三個 memory 路徑與 `model` 項，那些是真的設定改動 | ①先看差異：`py -3 tools/backup_global_config.py`（不加旗標＝只比對不寫） ②確認 live 那些新增項都是要的之後：`py -3 tools/backup_global_config.py --backup --only settings.json` ③**不要直接 --restore settings.json**——那會把舊路徑裝回去 ④驗收：`grep -n 'IT-department' global/settings.json`，不該再出現 `D:\IT-department` | 我 | 規則 | 中 |
| ⏳ **對話改名規則已改，Cursor 雲端那份還沒貼、reconcile 未回寫**（2026-09-02） | 起因是實測：`D:\AI-Projects` 兩則對話從 9/1 晚上開到 9/2 都沒有名字。根因不是模型偷懶——`global/hub/20-modes.md` 舊句寫著「**對話名稱自己從宣告組（不必手動改名）**」，而做這件事的 hook 已於 2026-08-28 整套退役（`hooks/session_title.py` 檔頭有記，`~\.claude\settings.json` 現行掛載清單裡確實一個 `session_title` 都沒有）⇒ **每一則對話都以為有程式在做，於是都不改**。補救原本只存在於 harness 專案的記憶檔（`feedback_session_title_rename`），`D:\AI-Projects` 的記憶索引與 `CLAUDE.md` 兩邊都沒有 ⇒ 只有一個專案吃得到。**已改**：`20-modes.md` 拿掉兩處過期句、`21-title-claude.md` 補回 body（呼叫 `set_session_title`，標題格式與判定順序比照 Cursor 那份）、`22-title-cursor.md` 修掉「Claude 端改用平台自產標題」的過期交叉引用、兩支 output-style 的同一句一起修。產出已重跑、`--check` 一致、`eval/run_all.py` 六層全 PASS、live 副本已同步。⚠ **沒生效的是 Cursor 那一半**：`global/CURSOR_USER_RULES.md` 已含修正，但貼進 Cursor 雲端 User Rules 是**人工步驟**（票 04 明訂產生器不准寫雲端），而快照 `global/user-rules-inventory.md:34/50` 仍是舊文 ⇒ Cursor 側現在讀到的還是「不必手動改名」。⚠ 我**不能**代改 inventory——它的定義是「雲端現在貼著什麼」的快照，先改它會讓對帳失去意義 | ①user 把 `global/CURSOR_USER_RULES.md` 去掉 GENERATED 檔頭後的正文貼進 Cursor User Rules ②貼完把同一份正文回寫進 `global/user-rules-inventory.md` 並 `git add` ③在 `global/user-rules-reconcile.md` 補一列，產出檔正規化雜湊欄填 `7cc03dee3e0a`（本次已量）④驗生效：Cursor 開新對話，看它是否主動呼叫 `rename_chat`——**沒叫就是沒吃到**。⚠ Claude 側同樣未驗：要開**新** session 才會載入改過的規則，現行對話載入的是舊的 | user＋我 | 規則 | 中 |
| ⏳ **介面注入的禁派指令：規則已交人手貼，未驗生效**（2026-09-02） | 桌面版在 system 層注入 `Do not call the AgentTool unless the user requested it`＋禁 workflow，直接覆蓋全域 §4.1「唯讀盤點一律派、不先問」。**實測不是推測**：user 一句「先實測」後派 locator 成功（47k token／13 次工具／52 秒／回報含「沒找到的」）⇒ 該條要的是 user 開口、不是硬性禁令。⚠ 要補進 §4.1 的那四行**被 auto mode 分類器擋兩次**（Edit 與 Bash 寫入都擋，理由都是修改全域設定），已備份 `CLAUDE.md.bak.20260902_dispatch_auth`、檔案本身沒動 ⇒ 2026-09-02 交由 user 自己貼。⚠ **補了也不會讓我自動派**——注入指令仍在，補的價值只在「user 開口後我不再回頭確認一次」 | ①先確認 user 貼了沒：`grep -n "介面注入" C:/Users/<USER>/.claude/CLAUDE.md`，沒有命中＝還沒貼 ②驗生效：下一則對話由 user 說一次「派角色去查」，看模型是否直接派而不再反問——**反問了就是沒吃到** ③⚠ 若手貼後仍每次反問，這條規則等於裝飾，改走「每次口頭講」那條路 | user＋我 | 規則 | 中 |
| 🔧 **D19 反向檢查沒被 `run_hook_tests.py` 收** | 2026-08-28 上線（`tests/test_d19_ratchet.py`，17 支裡 6 支命中判準全過）。它是**獨立腳本**，全套不會自動跑它 ⇒ 沒人跑就等於沒裝，而 D19 的價值正是「新增閘門的人不會記得去讀它」。⚠ 掛進去要改 `tests/run_hook_tests.py`，而收工當下那個檔有別條線的在製品，沒動。⚠ 另一個要留意的：這支的變異驗證**必須拿掉全部抑制才會紅**（改單一個關鍵詞不紅是對的，`_DAMPER` 是「或」的關係）——不知道這件事的人跑一次變異看到全綠，會得出「這支是假綠燈」的相反結論，檔頭已寫 | 等 `run_hook_tests.py` 乾淨後把它加進案例收集；驗收＝跑全套要看得到 `D19` 那幾行，且照檔頭的變異指令（三個 sed 一起下）能讓它從全套裡轉紅 | 我 | eval | 中 |
| 🔧 **`tools/link_memory.py` 沒有回歸網** | 2026-08-28 上線並實際套用在 AI-Projects（16 份記憶檔接進版控）＋ `IT-department` 的 `bootstrap.ps1` 已改成呼叫它。**四個守門是手動驗的**（沒給 `--repo`／路徑不存在／不是 git repo／已經是 junction 回 0），沒有自動測試釘住。⚠ **它會刪目錄**——回歸價值比一般工具高，而且這種東西壞掉的症狀是「某台機器的記憶沒接上」，靜默。⚠ 上線當天實跑就炸過一次：PowerShell 下印中文 `UnicodeEncodeError`（已修＋雙向驗過），**語法檢查、dry-run、單獨跑三種都照不到那一條**——回歸網要涵蓋「從 PowerShell 呼叫」這條路徑，不能只測 python 內部行為 | 造暫存 repo（`git init` ＋假的 `~\.claude\projects\<enc>\memory`）跑四個守門＋一次真的接線＋冪等重跑；再加一條「用 `powershell -Command "& py -3 <tool>"` 呼叫，stdout 要有內容且 exit 0」的端到端。**先證明它會紅**：把 reconfigure 那段拿掉，那條端到端必須轉紅 | 我 | 工具 | 中 |
| 🔧 **CTX-1 還在 shadow，且抓不到產生器那條寫入路徑** | 2026-08-28 上線（`hooks/rules/ctx1_resident_budget.py`，掛 PostToolUse 改檔工具，與 ENC-1／HTML-1／UI-1 同槽位）。**動機是實測數字**：當天一輪人工瘦身砍 653 bytes，同期另一條線加 768 ⇒ 淨 +39，趨勢線上完全看不出成效 ⇒ 手動清理追不上增長速度，要改斜率只能在寫入的當下叫。門檻 `max(基準×1.10, 基準+800)`，基準取 `rulefile/bloat_snapshot.json`（不另存一份）。已驗：回歸網 20/20＋四個變異精準轉紅（拿掉小檔底線 19/20、門檻放到不會越線 14/20、對不上基準改硬猜 18/20、拿掉全域路徑辨認 19/20）；端到端三態實跑（shadow 靜默但 event log 有 `decision WARN shadow:true`／暫時轉正輸出 646 bytes 的 `additionalContext`／還原後設定乾淨）。⚠ **已知不涵蓋**：`tools/gen_rule_hub.py` 走 Bash 寫檔、不經過改檔工具 ⇒ 產生器改大常駐層時這條不會叫，而那正是今天實際發生的路徑之一。⚠ **轉正前要看的是誤觸率不是有沒有用**（同 IDX-1）：常駐層本來就會長，若警告長期都是「這次成長是必要的」，它會退化成每次改規則都出現的噪音然後被整條無視 | ①觀察一段時間後跑 `py -3 hooks/report.py` 看 CTX-1 的 applies／would-block 比，決定轉正就在 `hooks/dispatch_config.json` 加 `"CTX-1": {"shadow": false}` ②產生器那條路徑要不要補：補的話得掛 PreToolUse Bash 並辨認產生器命令，**誤判面大**（任何 `py -3 ...` 都要判一次），先評估值不值得 ③⚠ 驗收要先造一個超標寫入再跑，**沒叫就是沒生效** | 我 | hook | 中 |
| 🔧 **WIN-1 門檻按 20 萬視窗訂，實際視窗 100 萬 ⇒ 63% 的發言都會收到便箋**（2026-08-28 轉正當天回放量到） | **實測不是推測**：單次請求合計 input 最高量到 999,128（session id `bb8d3376`，不是 git hash）後自動壓縮回 172,429 ⇒ 單次 input 不可能超過視窗 ⇒ 視窗至少 100 萬，140K＝14%。把 WIN-1 的判斷回放到 `d--IT-department` 最近 12 則對話：**84 次真人發言中 53 次會收到便箋＝63%**，最長那則 40 次發言中 37 次（92%）。⚠ 規則自己的註解寫著「140／160 每輪都講會被無視（BUDGET-1 同一病）」——140／160 有「同一則一次」防護，但 **180K 是設計上每輪再講**，在 20 萬視窗裡撐到 180K 的對話本來就快結束，在 100 萬視窗裡才剛熱身，後面八成路程一路被念。⚠ 另一半是文案：「較早的決定與檔案內容更容易被擠出視窗」在 140K／100 萬的當下**不成立**，講當下不成立的事會被當背景音，等真的 800K 危險時已經沒有可信度。**user 2026-08-28 決定維持原樣、先實地被念幾次再判斷**（不是沒發現，是刻意緩） | ①累積幾次真實觸發後跑 `py -3 hooks/report.py` 看 WIN-1 的 applies／WARN 比，並自問「這幾次有沒有一次讓我真的去交接」②要改就三處一起：`hooks/rules/win1_total_input.py` 的 `TIER_*`、`tests/test_win1.py` 的門檻常數、便箋文案那句因果（改成重送成本、或搬到真的會被擠出去的位置）③⚠ 驗收方式是回放不是感覺：拿 `C:\Users\<USER>\.claude\projects\d--IT-department\*.jsonl` 重算「真人發言中會收到便箋的占比」，目標降到個位數 | 我 | hook | 中 |
| ✅ **便箋投遞：狀態型不過期 ＋「講過了」以投遞成功為準**（2026-08-28 當天發現當天修完） | 起因：WIN-1 於 13:06:38 真機首次觸發（session `895e6ee8`，是 session id 不是 git hash），量到 149k、越 140K 線、訊息正確落進便箋；使用者離開約兩小時，15:06 回來時便箋已過 `_PENDING_TTL_MIN = 90` 被丟，模型只收到「另有 1 則沒能投遞」。**真正的洞是記帳時機**：140／160 的 fired 記號蓋在排入佇列的當下，投遞失敗＝那一則對話永遠不會再收到那一檔。**已修四處**：①`contract.py` 新增回執四支（`note_delivered`／`record_delivered`／`clear_delivered`／路徑），寫的是 dispatch、讀的是規則，同檔定義避免漂移 ②`dispatch.py` 便箋改帶 `rule／kind／key`、Stop 逐條排入（原本 join 成一串就分不開是誰講的）、狀態型不吃 TTL（`_expired`）、超量先丟事件型（`_evict_index`）、投遞成功才 `record_delivered` ③WIN-1 宣告 `NOTE_KIND = "state"` 與 `note_key()`（鍵是門檻檔位，**不能用訊息當鍵**——訊息裡的量每輪都在變）、140／160 改查回執 ④舊格式便箋（無 rule／kind）一律當事件型，改版不吃掉正在排隊的舊便箋。**驗到的**：全套 1486／1489（另 3 紅是既有債，測試數 1475→1489）；`mutate_warn_channel.py` 18 個變異全紅（新增 4 個守這次的改動）；端到端重演那次遺失（真 transcript ＋ 真 dispatch ＋ 把便箋時間往回撥 120 分鐘）→ 送到了、沒回報遺失、回執 `{WIN-1: [160]}`、下一次 Stop 不再排 | ⏳ **仍未驗的最後一哩**：便箋真的送到模型眼前之後，模型會不會因此去交接。要等一則真的過線、且人在 90 分鐘內回來的對話 —— 這次修完之後連隔夜回來也會送到，所以驗收窗口比原本大。⚠ 觀察時同時記下「這幾次有沒有一次讓我真的去交接」，那個答案是上一列門檻校準題的資料 | 我 | hook | 低 |
| ⏳ **AWC-1 裝了臨時探針，答完就要拆**（2026-08-28） | 防迴圈 A（平台重跑時帶不帶 stop_hook_active）與防迴圈 B 互相遮蔽：旗標若有帶 A 先回、B 永不觸發；旗標若沒帶則 B 觸發而 A 看不到。探針在兩個分支各記一筆到 `state/awc1_probe.ndjson`，哪個 branch 記到就知道答案，**不必拆掉任何一道防護**。目前 0 筆——要等下一次自然被擋才會有資料。 | 下次被擋後看 `type D:/Patrick-AI/.ai-harness/state/awc1_probe.ndjson`：有 branch=A ⇒ 平台有帶旗標；只有 branch=B ⇒ 沒帶、A 是死碼。得到答案後刪掉 `_probe` helper 與兩處呼叫，並更新待驗清單那一列 | Claude | 閘門 | 中 |
| ✅ **回歸網長期三條紅 ⇒「全綠」這個訊號已經失效**（2026-08-28 量到·同日清零） | `run_hook_tests.py` 固定 1417/1420，三條都不是當次改動造成的：①F-4 抓到六支硬編絕對路徑（`.scratch\` 五支＋`tools/run_claude_reviewer.py`）②八支檔案沒標分層（`extract_shell.py`／`html_paths.py`／`open_in_ide.py`／`win_subprocess.py`＋四支角色 md）③**全域層是空的**——角色能力本來就跨專案，全空代表分層判定漂了。另有 `_KNOWN_U1_DEBT` 台帳過期（五支 dashboard 檔已償還沒更新）。**代價已實際付過**：改制時必須逐檔比對點名清單，才敢說紅燈不是自己造成的。⚠ **2026-08-28 全數處置，全套 1491/1491**（commit `e279530`）：①六支角色檔標全域層、四支看板基礎設施標核心層（70/70 零未標）②「角色都在全域層」原本是**空真**——生成式把未標的檔濾在外面、迴圈跑零次，於是它與「全域層非空」長期一綠一紅、互相矛盾卻並存三週，掃描範圍已補上 missing ③U-1 閘門改成跳過「gitignore 排除且未進版控」的拋棄物（豁免必留痕、已追蹤的不豁免、拿不到 git 就照掃），並還掉 `tools/run_claude_reviewer.py` 的 harness root 硬編碼、台帳 11 筆清到 8 筆。三個變異各自驗過。 | ✅ 已完成（2026-08-28）。回歸判準＝`py -3 D:/Patrick-AI/.ai-harness/tests/run_hook_tests.py` 要 1491/1491，且 `py -3 rulefile/check_layers.py --summary` 要印「未標 0」 | ✅ 已完成 | 文件 | — |
| 🔧 **閘門總開關已搬到全域，但「在 harness／AI-Projects 內真的生效」沒驗到** | 2026-08-28 搬遷。原本 `dispatch.py` 只掛在 `d:\IT-department\.claude\settings.local.json`（而且 `.local` 通常不進 git ⇒ 換機器整套閘門消失且無提示），已改掛 `~\.claude\settings.json` 的五個事件、並從專案層移除同名重複（`session_title.py` 原封不動）。**已驗**：三種 payload 在 `D:\Patrick-AI\.ai-harness`／`D:\AI-Projects` cwd 下跑 `dispatch.py` 皆 exit 0 無誤報（四支【專案層】規則都有 `applies()` fail-open 第一道，且靠 `resolve_remote_ref("vm","master")` 解不出就放行）；反向測試 R4 在 IT cwd 下 exit 2 並印完整訊息 ⇒ 搬遷沒有改壞既有行為。⚠ **沒驗到的是「新覆蓋範圍真的會擋」**——那要在 harness 或 AI-Projects 開一個**新** session（現行 session 啟動時載入的是舊設定），而且要真的踩一條核心層規則。⚠ 順帶一提本次也證實 `~\.claude\CLAUDE.md`、`global\CLAUDE.md`、`global\hub\10-comm.md` 是三份各自獨立的檔，改一份不會傳到另外兩份 | 在 `D:\Patrick-AI\.ai-harness` 開新 session，故意用開放式問句結尾（不呼叫 AskUserQuestion）⇒ **AWC-1 必須擋下**；再於 `D:\AI-Projects` 開新 session 寫一支含 `import server` 而無 `server.DB_PATH =` 的 `.py` ⇒ **R4 必須放行**（專案層規則不該跨部門開火）。兩邊都要跑到，只跑一邊分不出是「生效」還是「都沒載入」 | 我 | 閘門 | 高 |
| 🔧 **收工的「逐檔驗證」全部答對，孤兒仍然掉出去** | 2026-08-27 實證。`hooks/session_title.py` 有 38 行未提交躺了兩小時（`_repo_root`＋`project_name` 改用 repo 根），期間**兩則收工各自逐檔驗證後都宣告「未提交的 harness 檔沒有一筆是我的」**（`13409e48` 說 30 檔、`1bac1ea4` 說 27 檔），**而且兩份判定都是對的** —— 它們用內容特徵比對（`git show HEAD:"$1" | grep -c 我的特徵` ＋ `git diff -- "$1" | grep 我的`），問的是「這截未提交的東西裡有沒有我的字」，答案確實是沒有。**沒有人問下一個問題：那這是誰的、還有沒有人要回來收？** ⇒ 每個人的檢查各自正確，孤兒正好從縫裡掉下去。⚠ **留著不是中性選項**：`1bac1ea4` 同日回報主 repo 的 auto-commit 把它做到一半的東西**收走了兩次**，還讓它 stage 的快照過期、差點回退掉別人 59 行 ⇒ 孤兒等的就是被掃進不相干的 commit，untested 且 unattributed。⚠ **結局訂正（22:5x）**：我原本判定「作者已走、沒人會回來收」——**錯了**。孤兒躺了約 2.5 小時後，另一條線用 `9ca8036`「佔位名用 repo 根、超長截斷補省略號」**刻意收走了它**（`session_title.py` 差異 38+/8- 與孤兒完全一致）。⇒ 缺口比我原本寫的小：孤兒最後有人認領，但**那 2.5 小時裡沒有任何訊號說它無主**，而且期間有兩則收工從它旁邊走過去。⚠ **同一顆 commit 順手把我的測試檔整檔帶走**（我補的 5 條 `_repo_root` 案例，`_repo_root` 原本**零覆蓋** —— 既有兩條 `project_name` 案例只斷言 fallback，整段拿掉也不會紅；我補完後三個變異精準轉紅）。測試在 HEAD 存活 5/5、87/87 綠，內容沒少，但**掛在別人的 commit 下** ⇒ 這是同日**第三例**跨線收走（前兩例是 `1bac1ea4` 回報的），這次輪到我。⚠ **同日第四例（8/28 收工時），形態比前三例更精確**：不是孤兒被掃走，是**我 `git add` 完、`git commit` 之前被插隊**。序列可重現：`git add` 兩個檔 → `git diff --cached --name-only` 印出來的卻是**別人的檔**（`SOP_PROD/…/test_issue_skip_menu.js`）→ `git commit -F` 回「no changes added to commit」→ `git show HEAD` 是別人的 commit。事後查證我的內容**沒丟**（日誌 235 行工作區與 HEAD 一致、未驗兩列都在），但分別掛在 `149734d1`（別人的收工）與 `104358e7`（別人的 pending-verify）底下。⇒ **「只 stage 自己的 hunk」這條規則擋不住這個形態**——它管的是我 stage 什麼，管不了 stage 完到 commit 之間有誰把 index 清空。真正的判準是**`git add` 與 `git commit` 之間不要插入任何指令**（我中間跑了一個 `git diff --cached --name-only` 想對帳，那一秒就被插隊了），或者改用 `git commit -o <路徑…>` 一次完成 stage+commit。 | 在收工 SOP 步驟 1 之後加一問：**逐檔驗證判定「不是我的」的檔，要再判一次「有沒有人會回來收」**。判準用現成訊號即可 —— `tools/check_before_start.py` 已經印「殘留·N 小時前寫過（沒人在動）」，只是沒有人拿它問這一題。落地形態二選一：①`check_before_start.py` 對「殘留＋無人在動＋超過 N 分鐘」的檔主動提示「這是孤兒，要認領還是登記」②`/shougong` 步驟 1 加一行檢查。⚠ 驗收要先造一個孤兒（改一個檔不 commit、關掉那個 session）再跑，**沒提示就是沒生效** | 我 | 流程 | 中 |
| 🔥 **`backup_global_config.py --restore` 無視自己 `--check` 印的逐檔方向，覆蓋較新的 live** | 2026-08-27 實地咬到。`--check` 明明逐檔印了「`CLAUDE.md` 建議 `--restore`／`settings.json` 建議 `--backup`」，但 `--restore` 把**兩個檔都**往 live 寫 ⇒ live `settings.json` 5,637→5,393，**吃掉 `outputStyle: PM-Challenger` 與另一條線剛裝的 `SessionStart` 探針 hook**。⚠ 更壞的是**它是靜默的**：輸出寫「已 --restore 到 live（原狀態：有差異）」，兩個檔長得一模一樣，看不出其中一個是逆向覆蓋。救回靠的是另一條線碰巧留的 `~\.claude\settings.json.bak.20260827_210404_teardown_probe`，**不是這支工具留的**——它自己不備份。票 04 說「方向 bug 這張票修」，修掉的只有「無旗標不寫」那一半，~~逐檔方向這一半沒修~~ → **同日 22:0x 另一條線已修**（`3aa1fde` 「備份工具加兩道閘門——方向不一致與來源較短一律拒跑」，`backup_global_config.py:138` `跨檔方向不一致時拒跑整批`，附 48 行新測試）。⚠ **我沒有實跑驗過那兩道閘門**——驗它要對 live 設定檔下寫入命令，而它正是今天被吃掉一次的那個檔。驗法：`py -3 tools/backup_global_config.py --restore` 應**拒跑並非零 exit**（現況 repo `settings.json` 比 live 少一整段 `SessionStart`），且跑完 `grep -c SessionStart ~/.claude/settings.json` 必須仍是 1。⚠ `--check` 的**建議文字**似乎沒跟著改：22:0x 實跑仍印「建議 `--restore`」，閘門擋的是寫入不是建議 ⇒ 人照著建議打下去才會知道被擋，**收工 SOP 步驟 4.0 每次都會遞這個反向建議**。⚠ **同日 21:37 第二次踩到，這次更糟——工具的「建議」本身是反的**：repo `global/settings.json` mtime 較新（21:37:33 vs live 21:34:10）所以印「建議 `--restore`」，但 repo 那份**少了 live 才有的 `SessionStart` hook**（`state\sessionstart_probe.py`）⇒ 照建議做會第二次刪掉同一個 hook。**根因：拿 mtime 當「較新」的代理，而 mtime 只說「誰後被寫」不說「誰內容較全」。**收工 SOP 步驟 4.0 每次都會叫人跑這支，等於每次收工都遞一個反向建議。 | 兩件事：①`--restore`／`--backup` 對「方向與該檔建議相反」的檔要先擋下（印出來要人加 `--force` 或指名單檔），不得預設整批同向；②寫入前一律先備份被覆蓋的那一側（`<檔>.bak.<時間戳>`），現在完全沒有；③**JSON 類的檔不要只比 mtime**——至少要偵測「一邊有、另一邊整段沒有的 top-level key」並改印「兩邊各有對方沒有的內容，需人合併」而不是指一個方向。驗收：造一個 live 較新的 `settings.json` ＋ repo 較新的 `CLAUDE.md`，跑 `--restore` 必須拒絕動 `settings.json`，且 `CLAUDE.md` 的舊 live 有 `.bak` 落地 | 我 | 工具 | 高 |
| 🔧 **IDX-1 還在 shadow，要看資料才知道該不該轉正** | 2026-08-27 上線。`hooks/dispatch.py:350` 明訂「設定檔缺該規則 ID → 預設 shadow」，那是刻意的畢業儀式。端到端已驗：enforce 下走 `hookSpecificOutput.additionalContext` 吐完整 staged 清單（368 bytes），shadow 下靜默。⚠ **轉正前要回答的是「誤觸率」而不是「有沒有用」**：這條每次 `git commit` 都發動，而 commit 很頻繁 ⇒ 若清單長期沒有 ⚠ 標記，它就退化成每次都出現的噪音，然後被整條無視（`check_bloat` 的 WARN 疲勞同型）。判準應該是「⚠ 標記的出現率」而不是「applies 次數」。 | 觀察一段時間後跑 `py -3 hooks/report.py` 看 IDX-1 的 applies／would-block 比，並抽樣看幾則訊息裡有沒有 ⚠ 標記；決定轉正就在 `hooks/dispatch_config.json` 加 `"IDX-1": {"shadow": false}` | 我 | hook | 中 |
| 🔧 **eval L2 分不出「必須存在的依賴」與「該被產出的檔名／歷史文物」** | B-4 收尾（2026-08-26）全量 eval，L2 唯二兩條紅**都是偽陽性**：①`skills/adversarial-review/SKILL.md:98` 的 `round-N-reply.md` 是**審查者該產出**的檔名，不是既有依賴 ②`.claude/skills/data-incident/SKILL.md:99` 的 `restore_cjf.py` 寫著「實作參考**本次的**…」＝過去事故的一次性腳本，全 repo 已無此檔。⇒ 檢查器把「所有反引號裡像檔名的東西」都當成必須存在的依賴。**代價是紅燈失去意義**：真的斷線會混在偽陽性裡。 | 在 `eval/check_contracts.py` 加豁免判準（先想清楚判準本身——「上下文有『產出／參考／本次的』就豁免」可能過寬），或改成把這兩類標成第三種狀態而非 ❌ | 我 | eval | 中 |
| ✅ **P-12 U-1 債閘門掃了已 gitignore 的暫存腳本** | 同一輪 hook 全量回歸 1319/1323，其中一紅是「沒有新檔案引入 U-1 債」抓到 4 支 `.scratch/*.py`（`apply_ops_nav.py`／`global-rule-hub/_print_map_hash.py`／`measure_skill_listing.py`／`wrap_desk.py`），**四支都是未追蹤且已被 gitignore 的拋棄腳本**，其中一支是當天 13:44 別條線剛寫的。⇒ 暫存區寫死路徑是**正常的**（拋棄物不必去專案化），閘門卻對它開火 ⇒ **這條紅會長期常駐**，而長期常駐的紅等於沒有紅。 | ✅ 已完成（2026-08-28·commit `e279530`）。判準取「gitignore 排除**且**未進版控」而不是寫死 `.scratch/`——跟著 gitignore 走，不必再維護第二份清單。兩道防線防後門：已追蹤的檔不豁免（`check-ignore` 只看規則不看追蹤狀態）、拿不到 git 就回空集合照掃（fail-open 方向＝寧可誤報）。豁免會印出哪幾支。⚠ 實作踩到 `subprocess` 的 `text=True` 在 Windows 把 stdin 的 `\n` 轉成 `\r\n`，git 收到帶 `\r` 的路徑全部對不上——5 支只豁免到 1 支，已改走 bytes 模式。變異驗過：`tools/` 的未追蹤新檔照樣紅 | ✅ 已完成 | hook | — |
| 🔧 **Cursor CLI 現在每一個工具呼叫都被 hook 擋死**（2026-08-26 08:0x 實測·**Cursor 側全滅**） | **先講一個推翻舊結論的發現：Cursor CLI 會讀 `~\.claude\settings.json` 的 hook 註冊。** 下面那一列原本寫「Cursor 側閘門等於不存在、fail-open 靜默」——**兩點都要修正**：閘門接上了，而且失效是 **fail-closed**。機制：註冊的指令本身乾淨（`py -3 "D:\Patrick-AI\.ai-harness\hooks\session_title.py"`），但 Cursor CLI 執行時**自己把它包成 PowerShell**（`$OutputEncoding = …; Get-Content -LiteralPath '…payload.json' -Raw \| & { $input \| py -3 … }`）再交給 **bash** 跑 ⇒ `eval: syntax error near unexpected token '&'` ⇒ CLI 把 hook 執行失敗當成 **block** ⇒ 每個工具呼叫都停。**實測兩次**：①`/adversarial-review` 的審查者完全讀不到題目檔 ②我另派一個只要求 `ls effort/` 的最小任務，同樣被擋。同一支 hook 餵 `PreToolUse` payload 在 Claude 這側 **exit 0 正常**，所以 Claude 側完全無感——**只有一半的人被擋，另一半不知道**。起點是 `settings.json`（06:39）與 `session_title.py`（07:13）的改動，都是另一條線 8/26 的新工。**直接後果：跨模型族的對抗式覆核整條斷掉**（`cursor-cli` 是這台機器唯一真正跨族的審查者） | ✅ **2026-08-26 18:5x 定案（那條線就是我，已處置）**：`PreToolUse` 那一發**是必要的**——根因查清楚了，client 2.1.237 在每個新 prompt 把自己的快取標題回寫檔尾，`Stop`＋`UserPromptSubmit` 兩發寫完就被蓋，側欄永遠停在舊名（詳見 [[reference-claude-code-disk-hygiene]] 第 1 節）。**但掛法換了**：從全域 `~\.claude\settings.json` 的**無 matcher** 改成專案 `.claude\settings.local.json` 裡與 `dispatch.py` **同一個 matcher** 的 entry（`Bash|PowerShell|Skill|Write|Edit|MultiEdit|NotebookEdit|Agent`）⇒ 依本表下一列的實測，有 matcher 只有 Shell 類工具中彈，而那類**早就被 `dispatch.py` 擋著**（自 07-28），所以 Cursor 的受害範圍沒有擴大、Read/Grep/Glob 不受影響。⚠ **空 matcher 一律不可用**，那才是 8/26 早上 Cursor 全滅的形態。✅ **2026-08-26 23:0x~23:4x 已驗，本列可關**：派了三個 `cursor-agent` 探針（同一句 prompt、只要求 `ls`）——①乾淨沙箱 workspace：exit 0、列出三個標記檔，`hook_errors` **零新增** ⇒ 沙箱下一個 hook 都不會註冊 ②`--workspace d:\IT-department`：exit 0、**沒有被擋**，但 `hook_errors` 長出一筆 `Unexpected UTF-8 BOM` ⇒ **擋點不是 fail-closed 而是相反：閘門有被呼叫、解析失敗、fail-open 靜默放行** ③修好解析與名稱正規化後重跑：exit 0、`hook_errors` 2873→2873 bytes 一個位元組沒長，且 event log 出現 `kind="dispatch" event="PreToolUse" tool_name="Bash"`（Cursor 送的是 `preToolUse`／`Shell`）⇒ 正規化生效、真的選到候選規則。**結論**：8/26 早上那個 fail-closed 擋點（全域無 matcher 的 `session_title.py`）已被 18:5x 那次搬遷移除，之後剩下的是反方向的問題。原「仍未驗」敘述保留於後供對照：**仍未驗**：改完沒派 `cursor-agent` 實測（Shell 類本來就被擋，測了也分不出是誰擋的）——要分辨得先處置 `dispatch.py` 那一半。原下一步保留於後：⚠ **動手前先跟另一條線對過**——`session_title.py` 與全域 `settings.json` 是他們 40 分鐘前的在製品。①先確認 **`PreToolUse` 那一發是不是必要的**：這支 hook 的用途是讓對話標題跟隨任務，`Stop` 與 `UserPromptSubmit` 兩發應該就夠，而 `PreToolUse` 無 matcher ＝ 每個工具都跑一次，成本也高。不必要就只移除 `PreToolUse` 那一個註冊，Cursor 立刻復活 ②若必要，問題在 **Cursor CLI 的包裝方式**而不在指令（指令現在已經是不依賴 shell 語法的單一 `py -3 <路徑>`），要嘛回報上游、要嘛在 Cursor 側避開 ③**先證明它會紅**：處置後派一個只要求 `ls` 的 `cursor-agent` 任務——**改之前必須被擋、改之後必須列得出檔名**，兩邊都要跑到 | ✅ 已完成（2026-08-26） | 閘門 | — |
| **Cursor 端 13 條 hook 從未生效**（2026-08-25 查 `state\hook_errors.unknown.log` 發現） | Cursor 有 third-party hook 機制、也真的呼叫了 `dispatch.py`，但 **8/24 起 38 次呼叫 38 次全掛**。雙重失效：①payload JSON 解不開（`Expecting ',' delimiter` 落在 char 286～31159、另有 `Invalid \escape`，指向字串值裡沒跳脫的反斜線或引號）②**事件名與工具名都對不上**——8/25 14:34 Cursor 落了一份完整 payload （`.scratch\cursor-payload-sample.json`，1322 bytes、帶 UTF-8 BOM、`parse_ok:true`）：**`hook_event_name` 是有的**，但值是 `preToolUse`（camelCase）而非 Claude 的 `PreToolUse`，`tool_name` 是 `Shell` 而非 `Bash`／`PowerShell` ⇒ 解析成功也不會 match 任何 matcher。⚠ **前一版這裡寫「payload 整份沒有 hook_event_name」是錯的**——那是從 log 的 `raw[:200]` 截斷 head 推論來的，該欄位排在第 9 位、被切掉了。**截斷資料不能拿來下否定結論**。**失效是靜默的**：三層 fail-safe 第 2 層一律 exit 0 放行，只寫錯誤 log，跑了兩天沒人發現。⚠ 同一個 log 裡另外 308 筆 `Expecting property name` **不是問題**——raw 是 `{ 123}`／`{ 壞 payload`，是 `tests\test_agent_gate.py` 刻意餵的 fixture。⚠ **2026-08-26 根因從推測變實證**（13 筆 FAIL 樣本在 `~\.cursor\hooks\captures\`，含失敗點前後各 200 字）：①原始位元組**全部是合法 UTF-8** ⇒ 壞在 Cursor 送出前，不是傳輸也不是我方解碼（`dispatch.py:673` 早就用 `utf-8-sig`，BOM 從來不是原因——BOM 會炸在 char 0，實際落在 202～9561）②大 payload **12.6% 是 Unicode 私有區字元**＝中文被誤解碼後的亂碼 ③三種錯誤形狀全都在 `tool_input` 的字串值裡：`"pattern":"<亂碼>,"file_path"`（**收尾引號整個不見**）／`\SKILL`（落單反斜線＝非法跳脫）／`Expecting property name` ④**7 筆的出錯位置都是 pos 9561**，而總長 108906～114792 各不相同 ⇒ 同一個固定欄位、固定內容 ⑤**log 裡 Cursor 開頭的 raw 有 185 筆**，不是 38。⇒ **本 repo 通篇中文，Cursor 只要碰到中文就送出不合法 JSON**，閘門靜默不存在。⚠ **2026-08-26 08:0x 這句話被上面那一列推翻了一半**：Cursor **CLI** 其實會讀 `~\.claude\settings.json` 的 hook 註冊 ⇒ 閘門不是「不存在」而是「接上了但執行方式壞掉」，而且是 **fail-closed**（每個工具呼叫都被擋），不是這裡寫的 fail-open。**本列講的 payload 解析失敗仍然成立**（那是 Cursor IDE 端走 `.cursor/hooks.json` 的路徑），但「Cursor 側沒有任何閘門」這個總結論不再正確——兩條路徑的失效方向相反，別把它們混成一句話。⚠ **這不是「解析寫寬鬆一點」能修的**——收尾引號是真的不見了，任何寬鬆解析都只是在猜原本的邊界在哪 | ①~~抓完整 payload~~ **已完成**（Cursor 8/25 落檔，見左欄）**2026-08-26**：`~/.cursor/hooks.json` 的 `capture_payload` 探針已滿移除條件 1（`captures/` ≥3 筆 FAIL；現場是 UTF-8 BOM + `tool_input` 中文／路徑把 JSON 截斷）。該 hook **每個工具都 powershell+conhost**，已從 `hooks.json` 拿掉以停黑窗；FAIL 樣本仍在 `~\.cursor\hooks\captures\`。②~~先查那 38 筆為何解析失敗~~ **已完成**（`_log_error` 的 `raw[:200]` 8/25 就加大成切失敗現場前後 120 字了，但那個改動落在最後一次失敗之後、零筆命中；真正取到證據的是 8/26 的 capture 探針。根因見左欄⚠那段）。**剩下的問題不是「為什麼解析失敗」而是「Cursor 送不合法 JSON 時我方要怎麼辦」**。⇒ **2026-08-26 user 拍板走 (c)：判定 Cursor 側 hook 閘門不可靠，改用平台無關訊號（`git status` ＋ mtime），不依賴 hook。** 落地情況：`tools/check_before_start.py` 已做（逐檔判定熱／殘留），`tools/peek_sessions.py` 已補同一個訊號並修掉 cp950 半截輸出，`CLAUDE.md`／`COLLAB_HANDOFF.md` 的指向已改。**未選的兩案留痕**：(a) fail-open＋計數告警——閘門實質仍不存在，且該 hook 每個工具都 powershell+conhost 閃黑窗 (b) 容錯解析抽 `tool_name`／`cwd`——收尾引號真的不見了，**任何寬鬆解析都是猜邊界**，用它做閘門等於用猜的結果擋人 ⚠ **③④ 隨 (c) 一起凍結，不要照做**——它們是「把 Cursor hook 修好」那條路的步驟，而那條路已經不走了。留著原文只為了讓後人看得到當初打算怎麼修：~~③`dispatch.py` 加一層 schema 偵測：認得 `conversation_id` 走 Cursor 對映表、認得 `hook_event_name` 走現行路徑~~ ~~④造一筆 Cursor payload fixture，改之前必須失敗、改之後才通~~。**要解凍的唯一條件**：Cursor 修好它的 payload 序列化（自己拿一筆中文 `tool_input` 餵 `json.loads` 驗，過得了才算）。**優先度已從「高」降為「中」**——原本的「高」是建立在「閘門要修好」這個前提上，前提已經換掉了。⚠ **2026-08-26 23:4x user 拍板解凍，已落地** —— 三個實測推翻本列部分敘述：①**Cursor CLI 也會讀專案層 `.claude\settings.local.json`**（本列原本只寫全域 `~\.claude\settings.json`）：沙箱 workspace 零 hook 註冊、專案 workspace 才接上，兩個探針各一次實測 ②**「BOM 從來不是原因」對現行活路徑不成立**：實測 payload 帶**兩個** BOM（Cursor 一個、PowerShell 管線再一個），`utf-8-sig` 只剝掉第一個，第二個 U+FEFF 留在 char 0 → 解析失敗 → fail-open；0／1／2 三種 BOM 數量已重現（0、1 解得開，2 必炸）。原句對 8/24-25 那批**中段**壞掉的仍然成立，兩者不可混為一句 ③修好後端到端實測拿到正面證據：`kind="dispatch" event="PreToolUse" tool_name="Bash"`（Cursor 送的是 `preToolUse`／`Shell`），證明正規化生效且真的選到候選規則 DB-1／R1／R3。**落地**：`hooks\dispatch.py` 新增 `_decode_payload()`＋`_normalize_payload()`、回歸網 `tests\test_cursor_payload.py` 11/11（雙向變異：解碼退回舊實作精準紅 3 條、正規化改 no-op 精準紅另外 3 條）、全套 `run_hook_tests.py` 1319/1323 與基線同 4 筆既有債、零新增回歸。**仍不涵蓋**中段壞掉那一類，且**刻意保留**它解析失敗＋留錯誤 log——反向守門已釘住，不要為了 log 好看去加寬鬆解析。**受影響範圍比想像小**：`/adversarial-review` 的審查者跑乾淨沙箱＋`--mode ask`，實測沙箱下一個 hook 都不會註冊 ⇒ 審查者那條路修不修都一樣；真正生效的是「`cursor-agent --workspace d:\IT-department` 直接做事」那種場合。⚠ **2026-08-27 訂正這句話的射程**：上面那個實測證明的是「**沙箱 workspace 不會註冊專案 hooks**」，**不是**「審查者被關在沙箱裡」。兩件事被同一個「沙箱」字眼綁住了。當天三組實測：審查者用**絕對路徑讀得到**沙箱外任何檔、沙箱內 **junction 指向外部也讀得到**、`--sandbox enabled` 在 Windows 直接 `exit 1`。⇒ 「hook 不註冊」這個結論仍然成立，但**不可以再拿它推論「審查者那條路沒有風險」** | ✅ 已完成（2026-08-26） | 閘門 | — |
| ✅ **第 2 批候選 hook 誤判率已量·四條全部不做**（2026-08-26） | 語料 41,927 筆工具呼叫（585 個 transcript、排除 21 個探針專案；本機原本沒有通用的工具呼叫 dump 器，現有四處都有損 ⇒ 新寫 `.scratch\context-slimming\dump_tool_calls.py`＋`replay_b2_matchers.py`，命中原文全部落檔可覆算）。**①B2-sed**：全部 `sed` HIT 4,419（全語料 10.5%）、誤判率約 **96.6%**——88.8% 的命中是 `sed -n` 只讀列印行區間；收窄到 `sed -i` 剩 147 筆仍 **85.7%**（126 筆是明列 1–3 檔的窄操作）；再收窄到「檔案集合無界」剩 21 筆，逐筆看仍大多不是在跑 sed（heredoc 內文提到、`ssh` 指令的 sed 在別處、**甚至有一筆是 commit message 在描述這條規則本身**）⇒ **文字 matcher 分不出「在跑 sed」與「在寫關於 sed 的字」，不是閾值問題** **②B2-del** 刪 `.py` HIT 373，76.4% 打到 `scratchpad`／`__pycache__`，其餘多數根本不是刪 `.py`（`rm -f .refresh.lock` 同一條指令裡別處出現 `.py`）⇒ **matcher 壞掉，`rm` 與 `.py` 沒有綁定關係** **③B2-eol** `sed -i` 打到 CRLF 熱檔 HIT 94，樣本清一色是必要的 DEV/PROD 版號同步 ⇒ **形態是 WARN 不是 BLOCK** **④B2-scp** 改 `db/*.py` HIT 38、只落在兩支檔（`formal_excel_tools.py`／`import_archive_to_sqlite.py`），matcher 最乾淨；但序列回放顯示 **38/38 改完都真的 scp 了** ⇒ 誤判率 **100%**、無事可擋。⚠ **方法論發現（比六個數字更重要）**：語料是在「規則還寫在常駐層」的期間產生的 ⇒ **誤判率量得到、漏擋量不到**。B2-scp 的 38/38 合規是「§8:112 那句話在起作用」的證據，**不是「hook 不必要」的證據**——讀成後者就會刪掉常駐層那句、換上一條永遠不命中的 hook。這正是覆核 C-5 對 CQ-1 提的同一個坑，現在有數字：**「shadow 期誤判 0」當畢業判準對這一整類「動作型」規則恆真，換一條規則先試也不會變好** | **直接後果：`d:\IT-department\CLAUDE.md` §8:112／113／120／129 四行都不能拆**（原計畫是「hook 落地後拆常駐層那半」，四條都不會落地）。第 2 批 hook 這條路到此為止。若日後要翻案，兩個缺口要先補：①B2-sed″ 那 21 筆只看了去重後 16 筆、剩 5 筆沒逐筆標 TP/FP ②**完全沒量 PowerShell 側的等價動作**（`Set-Content`／`-replace`／`Remove-Item`），5,004 筆 PowerShell 呼叫沒進統計。✅ **2026-08-27 兩個缺口都補完了，結論不變而且更強**：①**無界 `sed -i` 那 21 筆逐筆判完**——TP 1／FP 20＝**誤判率 95.2%**，而且**唯一那筆 TP 犯的是 §8:129 行尾那條、不是 §8:113 的「過度匹配」** ⇒ B2-sed 在整個月的語料裡**一次都沒抓到它自己要防的東西**。FP 的四種形狀：sed 出現在 heredoc 內文／commit message 裡根本沒執行（3 筆）、`*` 出現在下游 `grep` 才誤觸（3 筆）、單檔或錨定 `^…$` 的窄替換（11 筆）、刻意的全樹密碼清除與行尾修復與 `/dev/null` no-op（3 筆）。②**PowerShell 側 5,004 筆已量**（等價動作＝`-replace`＋寫回／`Set-Content`／`Remove-Item`）：`-replace`＋寫回 76 筆→真的相關 **2**（誤判率 97.4%，74 筆落在 AI-Projects／scratchpad）、`Set-Content` 打 CRLF 熱檔 16 筆→真的寫回專案內熱檔 **2**（87.5%）、`Remove-Item .py` 20 筆→1 筆而且是 scratchpad 檔（≈100%）。**那 2 筆是同兩筆**（`Get-Content index.html -Raw ｜ … ｜ Set-Content` 升 `?v=`，7/29 與 7/31）⇒ PowerShell 側一整個月只產生 **2 個**相關事件。⚠ **而且那兩筆正是 `feedback-python-write-crlf-preserve.md` §「7/29 再犯·PowerShell 端」已經記載的同一次事故**（連 BOM、CRLF 5173→0、判準與修法都寫好了）⇒ 兩層意義：既有常駐層規則＋記憶檔**已經抓到並處置過**這一類、hook 補不到東西；而且**回放方法本身有效**——它在零提示下從 41,927 筆語料裡獨立重新找出一個已知事故，這支工具日後量別的規則可以信。**新的沒做的**：`-replace` 寬判準那 211 筆只判了「＋寫回」的 76 筆子集（不寫回的本來就不改檔）；**`Edit`／`Write` 工具本身會不會翻行尾沒在語料上驗過**（8,905＋1,773 筆），目前只有 `.gitattributes` 兜底這個既有結論。完整數字與逐條形狀見 `.scratch\context-slimming\batch2-fp-measure-spec.md`、`layer-plan.md` 待決 3 已據此結案 | ✅ 已完成 | 閘門 | — |
| **`dashboard-generators.md` 的「驗不了冪等」例外清單漏了兩個資料源**（2026-08-22 群 C 實測） | 該規則只把成本／mix 那頁列為「上游每回合都在長、驗不了連跑兩次雜湊相同」。實測連跑兩次 `refresh_dashboard.py --force`，差異落在 **`ROLES_TOPOLOGY`（2 處）＋時間戳徽章（1 處）**，**不是**成本面板——`gen_roles_topology` 從 event log 反推角色實派次數，而 event log 每跑一個工具就在長，是同一類但規則沒寫到。後果：照規則字面驗冪等會得到「不冪等＝壞了」的假結論，而真正該驗的是「**扣掉這幾個資料源之後**冪等」 | 在 `dashboard-generators.md` 的那一條補上 `gen_roles_topology` 與時間戳／session 徽章；或更好——把「哪些區塊驗不了冪等」做成一份機器可讀清單，讓冪等測試自己排除它們，不要靠人記得規則裡那句話 | 下次施工 | 看板 | 中 |
| ⏳ **`check_bloat` 對表格列把「記憶檔」指標欄算進 120 字**（8/13 量到·**8/20 複核仍成立**） | `parse_entries` 對表格列走的是整個 row 節點（全欄）：`check_bloat.py:715` 的索引列特例寫死 `u["kind"] == "entry"`，`table` 走不進去。實測合成 fixture：規則欄 30 字＋指標欄 60 字 → `chars=91`。照這個數字壓等於**刪真規則來替檔名騰位置**——正是這支工具已經為 MEMORY.md 索引列修掉的同一個病（link 前綴 76 字佔上限 63%），表格列沒比照辦理 | 比照索引列：表格列只計**第一欄**。⚠ **原本寫的驗收句已經過期**（8/20 發現）：它說「改完 §8 超標應從 4 降到 1」，但 commit `0612f9a` 把 IT-dept §8 壓到 `over_limit_count: 0`，那 4 條樣本不存在了。**新驗收要自己造 fixture**：一條「規則欄 100 字＋指標欄 60 字」的表格列，改前判超標、改後不判，且「規則欄本身 130 字」那條**仍要被抓到** | 下次施工 | 文件 | 高 |
| **R1 的 `.py` 側從未取樣過**（8/23 清帳時從已完成列救回來的殘留項） | R1 判準 8/20 改成比對整個賦值區塊後，在 `app.js` 的歷史上驗到 4/4（四筆真實事故全中）、400 個 commit 只有 4 個會出聲。但**那批樣本全是 `.js`**；新判準對 `.py` 的 `DEFAULT_*` 同樣適用、**卻一個樣本都沒驗過**。⚠ 這一項差點隨「已完成」整列被刪掉 —— 刪列前要看「誰」欄有沒有寫殘留 | 從 `db/**/*.py`、`server.py` 的歷史裡撈幾個動過 `DEFAULT_*` 的 commit，跑 R1 看會不會出聲；**先找一個「已知該被抓到」的樣本**（`.js` 那四筆就是這樣驗的），不要只跑「不會誤報」那一半 | 下次施工 | 閘門 | 中 |
| 🔧 **PR-1 在長輪次裡形同虛設 —— 2MB 尾窗找不到輪次起點**（2026-09-01 稽核抓到＋量到數字） | `hooks/rules/pr1_plan_review_marker.py:833` 是**唯一**呼叫 `note_failopen("讀不到 transcript 或找不到輪次起點（尾窗 2MB）", …)` 的規則，而 PR-1 是「唯一會擋住對話結束的閘門」。`contract.py:25` 的 `_TRANSCRIPT_TAIL_BYTES` 寫死 2,000,000——只讀 transcript 檔尾這段位元組去找「這一輪」真人訊息的起點，找不到就回 `None`，PR-1 選擇 fail-open（不擋）。實跑 `hooks/report.py` 量到：真 session 這個原因累計 66 次，且**2026-08-30／08-31 兩天真 session 的 Stop 事件 100% 命中**（10/10、2/2，分母小但比率是滿的），8/28 也到 34.7%（25/72）。⚠ 這不是雜訊——`_load_failopen_rows()` 的 `source` 欄已能把單元測試自檢（`source=test`）濾掉，這 66 次全是真 session。代價：**輪次裡工具輸出量大時，PR-1 對「這輪有沒有寫 plan.md 卻沒跑審查」直接放棄判斷，且不留使用者可見訊號** | 先想清楚再動，不要倉促調大數字治標：①先確認觸發原因是不是輪次工具輸出量真的變大（例如多派 subagent、報告貼回 transcript），跑 `py -3 hooks/report.py` 對照近期分數 ②要根治得想「有沒有辦法不用線性掃 tail 也能可靠定位輪次起點」（例如另外記錄輪次邊界的 offset，而不是每次從檔尾往回猜）③改完要能重現「觸發→修好」：造一個輪次工具輸出 >2MB 的 fixture，改前 `iter_turn_tool_uses` 回 None、改後回得出正確輪次 | 我 | 閘門 | 中 |
| **PR-1 只剩兩段未驗**（7/28 登記·**8/20 大幅縮小**） | 原本記「整條鏈未驗」，8/20 稽核在**生產設定**（`shadow:false`）的 event log 找到三段的證據：真擋（`state\events.0c9e00f3-….ndjson:74`、`:181`、`:203`）／蓋 marker 放行（同檔 `:83`／`:149`／`:186`，`bypassed:true`）／改一個字 hash 失效再擋（同一批 BLOCK 訊息就是 hash 對不上）。**仍沒證到的只剩兩段** | ①`/design-spec` 步驟 5 會不會真的產出標記——`SKILL_EVAL_PLAN.md:701` 有 PASSED marker，但查不出是誰蓋的，要走一次 M 級流程才知道 ②`tests\pr1_e2e\SAMPLE_PLAN.md` 那條路徑至今仍 `shadow:true`（`events.6de0ac63-….ndjson:32`），要在**非 `tests\`** 的 cwd 跑一輪才算生產設定 | 下次施工 | 閘門 | 低 |
| **Skill Eval L4：共用 Skill 逐支驗收**（進度以 `SKILL_EVAL_PLAN.md` §9.6b 的 B-4 隊列為準） | user 已終止規範覆核並授權施工。`adversarial-review`＝`skipped:user`；**第 1～9 列全數 `verified`**（本地批 7 支＋外部批 `grilling`·`research`）。外部批強度已調低（只做必留契約缺口＋真缺陷），四條操作定案見 §9.6b 校準節。⚠ **第 13 列的前置發現已到手**：`~\.agents\skills\wayfinder` 經獨立復算確認是**乾淨的 upstream 副本**（不抓網路即閉環），該列有 byte-exact 基準。**目前沒有 active 列** | 下一支是 `prototype`（第 10 列·bundle 有 4 檔·`SKILL.md`／`LOGIC.md`／`UI.md` 各帶一個 LOCAL EDIT）。⚠ 它與 `to-tickets` 是**僅存兩支既無免費基準、亦無 vendored 副本**的，依校準第 3 條走匯入 commit 並明記缺口。**要不要開由 user 說**；開列前先跑 `git status` 與 `tools\peek_sessions.py`（**這個 repo 現在很活躍**） | 等 user 指示 | 流程 | 高 |
| **R2（平台資源 key ＋ dump 同步守門）未動工** | 需先盤點 key 清單才能定判準；I1–I2 優先度已下修 | 先 `grep -n "ALLOWED_KEYS" -A 40` 盤出 key 清單，貼進 `HARNESS_PLAN.md` 再定守門判準 | 下次施工 | 閘門 | 低 |
| **claude.ai／Google Drive 兩個 MCP 尚未授權**（**8/20 訂正：比登記的更前面一步**） | 原記「需互動式 session 跑 OAuth」。8/20 實查 `C:\Users\<USER>\.claude.json`：`mcpServers` 只出現在 `projects.*` 底下且都是 `{}`（L870、L909），**無 top-level `mcpServers`**；`d:\IT-department` 下也沒有 `.mcp.json` ⇒ **連 server 都還沒設定**，不只是沒授權 | ①先設定 server（`claude mcp add …` 或寫 `.mcp.json`）②才在互動式 session 開 `/mcp` 跑 OAuth，或到 claude.ai 連結器設定授權。⚠ 少了第①步，第②步會找不到東西可授權 | 待你 | 流程 | 低 |
| 🔥 **harness 跨機同步仍未解，而且唯一的鏡像已靜默過期六天**（2026-09-02 量到） | 原記「已有跨實體磁碟鏡像（C 槽 NVMe · post-commit 自動同步）」——**「自動同步」這四個字在 8/27 20:47 之後就不成立了**。實測：本機 HEAD `d1a51e5` vs 鏡像 `81896ea`，**本機領先 73 顆、鏡像獨有 1 顆＝已分叉**。成因是 8/27 那顆之後本機改寫過歷史，鏡像那顆變孤兒 ⇒ 之後每次 `git push` 都被 non-fast-forward 拒絕，被 `2>/dev/null \| true` 吞掉。舊 hook 的註解自己寫著補償措施是「收工時順手看一眼」——**六天沒有人看，因為沒有東西提醒他要看**。⚠ 孤兒那顆已逐檔驗過**沒有獨有內容**：`user-rules-inventory.md`／`user-rules-reconcile.md` 與 HEAD 逐行一致，另 3 列 TODO 在本機歷史的 `9f90ba7`、`bf6e964` 裡 ⇒ `--force` 覆蓋不會失去東西。⚠ **已補上偵測（2026-09-02）**：①`tools/check_before_start.py` 新增 [4] 備份鏡像新鮮度，逐 remote 比 tip、分「落後 N 顆／已分叉／連不上／沒有 remote」四態，實跑已正確報出這次的分叉 ②`post-commit` 改成失敗時 stderr 印警告＋寫 `state/mirror_sync_failed.txt`（成功則刪），仍 fail-open 不擋 commit，實跑驗過（exit 0、標記檔內容正確）。⚠ **偵測補了，斷掉的鏡像本身還沒接回去**——force push 被權限層擋下，要人自己下 | ①**人自己跑**：`git -C D:/Patrick-AI/.ai-harness push --force backup main`，跑完 `py -3 tools/check_before_start.py --no-vm` 的 [4] 要變 `[OK]` ②`.git/hooks/` 不進版控 ⇒ 換機器或重新 clone 這個 hook 不會跟著走，正本放在 `tools/githooks/post-commit`（`18aa5aa`），**安裝這一步目前沒有任何東西會提醒**（要不要掛進 `bootstrap` 之類的入口待決）。⚠ 同一支檔還有第二個洞：**它沒有副檔名**，所以 `rulefile/check_layers.py` 與 U-1 硬編路徑掃描**兩支都掃不到它**，而它本文就帶 `D:\Patrick-AI\.ai-harness` 與 `C:\Users\<USER>\git-mirrors\...` 兩個絕對路徑。行尾那一類同病已於 `af53b20` 修掉（`.gitattributes` 改用路徑比對），但**掃描器那兩支仍是靠副檔名收檔**——驗收＝在 `tools/githooks/` 放一支帶絕對路徑的無副檔名檔，兩支掃描必須點名它③異地本題仍未解，且**候選只剩一個**：⛔ user 2026-09-02 明確否決推到 IT 那台 VM——**那台只放 IT-portal 平台，不要拿來放別的東西**。剩下的路是納入既有 GPG 異地包（目前只涵蓋 `it_asset_platform.sqlite`），或另找一個落點。**提異地方案前先排除 VM，不要再問一次** | 我 | 備份 | 高 |
| **`/adversarial-review` 的分工並行只有設計、沒有實測**（2026-08-23 寫進 skill §5.5） | 一輪之內的 N 個攻擊方向現在是**序列**跑，牆鐘＝相加。實測基準（同一個 effort）：R1 18.0 分／66 次工具、R2 16.8 分／54 次、R3 15.4 分／36 次，**耗時不隨輪次惡化**（每輪全新 agent、context 不累積），慢的來源是它真的在讀千行檔案並自己寫量測腳本。⚠ **序列有 4 個實測資料點、並行有 0 個** —— 說並行比較好是預測不是量測，所以 §5.5 明標「設計不是結論」 | 比照 DB-1 的紀律：在**一個真實 effort** 上並行跑一輪（N 個專科審查者 ＋ **一個統合者**；統合者不可以是主 session——實測 45 個發現裡有 6 個是主 session 處置時新造的，跨面向統合正是那個弱點的正中央），與同一輪的序列結果對照三件事：①發現數 ②必改數 ③**有沒有漏掉跨面向的那種**（R3 最重的 F4 是把三件事串成一個發現，各看一角的專科串不起來）。代價先算清楚：N 支專科各約 100k token ⇒ 一輪從 ~130k 變 ~400k（約 3 倍）。**驗收＝那三個數字並排寫進 §5.5**，不是「感覺比較快」 | 下次跑覆核時 | 流程 | 低 |
| **看板新元件 `.rt-sum` 卡片列沒有人用眼睛看過**（8/08·未驗） | 交接契約那節新加的四張統計卡是**伺服端第一個發 `.rt-sum` 這個 markup 的地方**（在此之前只有角色彈窗的 client JS 在用）。CSS 是 `auto-fit minmax(118px,1fr)`，四張卡在窄欄會擠成兩行；`.rt-sum .k` 還帶 `text-transform:uppercase`（標籤全中文所以應該安全，但沒驗過）。結構測試只驗標籤平衡，量不到「看起來對不對」 | 走 `/visual-check`：開 `http://127.0.0.1:8099/` →「工作流程」→「遵循度」，截淺色與深色兩張，逐張用 Read 打開看四張卡有沒有擠行、標籤有沒有被畫成全大寫 | 下次施工 | 看板 | 低 |
| **規模欄的分母已經有了，問題換成「大量缺欄」**（8/08 登記·**8/20 推翻並改寫**） | 原記「分母還是 0，等自然樣本」。8/20 實查：`規模 S｜`／`規模 L｜` 這個 pattern 在 `~\.claude\projects\d--IT-department\*.jsonl` 有 **232 命中／49 檔**，而 `gen_workflow_compliance.py:612` 的 `scale_cutoff()` 只要有一筆帶 scale 就綁真實第一筆 ⇒ **分母早就 >0**。真正的問題是**遵循度**：473 段對帳裡「缺規模欄」54 段、「缺修改檔案欄＋缺規模欄」再 11 段 | ⚠ **原本的下一步照著做會撲空**：它說「跑 `--check` 看【規模分布】」，但那支的 `main()` 只印【專案】【宣告對帳】【階段軌跡】【覆蓋率】【交接契約】五節，**沒有【規模分布】**。要嘛補那一節，要嘛改成讀【宣告對帳】的旗標統計。這是行為問題不是程式問題，先決定「要不要把它做成看得見的指標」 | 下次施工 | 看板 | 中 |
| **DECL-1 掃整輪之後可能掃到 subagent 的訊息**（8/08·未驗） | 改成掃整輪的副作用：若 subagent 的 assistant 訊息會落進主 session 的 transcript，DECL-1 會把角色回報也當成宣告來源。`dispatch.py` 刻意不把 DECL-1 掛 SubagentStop，理由正是「角色回報不是宣告」——同一個理由在這裡沒有被守住。全 repo grep `isSidechain` **0 命中**，既有機制不做這件事，所以無法沿用 | 拿一份真的 jsonl 確認 subagent 訊息會不會出現在主 session 的 transcript 裡（`grep -c isSidechain` 在真檔上跑一次）。若會，`iter_turn_assistant_texts()` 要加過濾；若不會，把這個結論寫進 contract.py 的 docstring，免得下次有人再問一次 | 下次施工 | 閘門 | 中 |
| **Phase 3 只剩「cron 告警管道未定」一項**（**8/20 訂正：三項已完成兩項**） | 原記三項未動。8/20 實查：①`git push --no-verify` 的 deny **已補**（`~\.claude\settings.json:124`／`:137`，`d:\IT-department\.claude\settings.json:85`／`:94`），commit `cad20b81`（2026-07-30）②`styles.css` **已在** `auto_commit.ps1:9`／`:29` 兩側，commit `ee32b734`（同日）⇒ 「落在 detect_set 卻不在 verify_set」不再成立。**兩項在登記後第二天就做完了，登記簿掛了三週** | 只剩③：cron 無人看管情境的告警管道未定（全 harness grep 只命中 `PHASE3_PLAN.md:106` 與本表的複述，無任何實作）。先決定管道（Teams？便箋？）。順帶更新 `PHASE3_PLAN.md` §5 的 3、4 兩點（仍寫著「都沒有」） | 下次施工 | 流程 | 低 |
| 🔧 **`backup_global_config.py` 印的建議與契約方向相反 ⇒ 照做會吃掉剛寫的規則**（2026-08-25 差一步就踩到）| 契約（根 `CLAUDE.md`／`COLLAB_HANDOFF.md`）明訂 **`global/CLAUDE.md` 是單一真相、編輯在 repo 這一側**，再同步到 `~\.claude\CLAUDE.md`。但這支的**無旗標預設是備份方向（live → repo）**：`cmd_sync()` 跑 `shutil.copy(src, dst)`，`src=~\.claude`、`dst=global\`。於是 `--check` 偵測到差異後印的那句 **「跑一次不帶 `--check` 即可更新副本」**，在「剛編輯完 repo 側」這個最常見的情境下**會把新寫的規則整份蓋回舊版**——而且完全沒有提示：兩個檔都是合法 markdown、大小相近，蓋掉之後看不出來。正確的是 `--restore`（repo → live），但那個旗標名字看起來像災難復原專用、不像日常同步。⚠ 這次沒中的原因不是工具擋住了，是我上線「任務」欄之前**手動 `diff` 過兩份**才發現方向不對——**靠人多做一步才不出事的工具，遲早會出事**| 兩個方向都要有名字，且**預設不要猜**。做法二選一（先量再決定）：①把 `--check` 的建議句改成同時列出兩個方向與各自後果，並在 **repo 側 mtime 較新**時直接指向 `--restore`（比一次 mtime 就分得出來）；②把無旗標的預設從「直接同步」改成「先印方向、要求明確旗標」——這支不是高頻工具，多打一個字的成本可以接受。⚠ **別只改文案就收工**：`cmd_sync()` 是無旗標預設，最容易被順手跑到。**驗證（兩個方向都要驗，只驗一邊會把方向判反的 bug 留著）**：造一份 repo 側較新的情境 → 無旗標與 `--check` 的輸出都必須指向 `--restore`、不得再說「更新副本」；再造 live 側較新的情境 → 必須指向備份方向| 待排程| 備份| 高 |
| 🔧 **對抗式覆核的沙箱不是存取邊界（已處置·白名單判定不可行·工具已做）**（2026-08-27） | **四份平行敘述都寫著「`--workspace` 指到隔離沙箱」，而那不是隔離。** 當天四組實測：①絕對路徑讀得到沙箱外任何檔 ②沙箱內 junction 指向外部也讀得到 ③`--sandbox enabled` 在 Windows 直接 `exit 1`（原生沙箱限 macOS/Linux，且管的是 command execution 不是 Read 工具）④**找到可用的官方機制**：沙箱內放 `.cursor/cli.json` 的 `permissions.deny` 真的擋得住，**但路徑必須用反斜線** —— `Read(D:/...)`（官方範例寫法）讀得到且**不報錯**、`Read(D:\...)` 才回 `Permission denied`。⇒ 已改 `skills/adversarial-review/SKILL.md`／`reviewer/server.py`／`COLLAB_HANDOFF.md`／`dashboard/harness-dashboard.shell.html` 四處，並訂正 `.scratch/plaintext-credential-gate/PLAN.md` 那筆「junction 被沙箱擋掉」的誤歸因（真正在擋的是 `dispatch.py` fail-closed）與本表上一列「審查者那條路修不修都一樣」的射程 | ✅ **①白名單已驗，判定不可行**（2026-08-27 實測）：`beforeReadFile` 在 `cursor-agent` CLI **確實會觸發**——探針 log 收到帶 `conversation_id`／`generation_id` 的 agent payload，所以「官方文件只講 IDE 與 Cloud Agents、沒說 CLI 會不會觸發」這個未知有答案了。**但兩堵牆都繞不過**：⑴Cursor 把 hook 命令包成 PowerShell 再交給 **bash** 跑 ⇒ `eval: syntax error near unexpected token '&'` ⇒ CLI 判 block、**每個工具呼叫都停**（與 8/26 `dispatch.py` 全滅**同一個根因**，本表那列已記）⑵就算繞過執行方式，payload 仍解不開（`Expecting ',' delimiter: line 1 column 2055`／`column 213`——Cursor 對含中文的 payload 序列化出不合法 JSON，本表 Cursor payload 那列已記），**拿不到 `file_path` 就無從判斷白名單**。⚠ **兩者在畫面上長得一模一樣**（都顯示 `Hook blocked`），只有 hook 自己的 log 分得出「我 deny 了」與「hook 執行失敗被當成拒絕」——**少看 log 會把後者讀成前者，得出「白名單可行」的錯誤結論**。⇒ **deny 黑名單是目前唯一可用的機制**。解凍條件＝Cursor 同時修好 hook 命令的執行包裝與 payload 序列化，兩件都不在我們手上。⚠ 探針是全域 `~\.cursor\hooks.json`，**測完已還原並驗證 CLI 恢復正常**。✅ **②守門已做一半**：`tools/build_review_sandbox.py`（14 條回歸、6 條變異精準轉紅，已接進 `run_hook_tests.py`）——一支指令建沙箱、把三個坑一次寫對、檢查父鏈、印出可貼的命令。**仍沒有的**：派出前沒有自動閘門檢查「這次真的用了沙箱、deny 真的是反斜線」，工具做出來了但要人記得跑。 | 我 | 流程 | 中 |
| ⏳ **`超過 12 字截斷` 測試比對的是實作當下的輸出 —— 是鏡射不是守門** | 2026-08-27：`_clean()` 的截斷改成留一格給省略號（別條線的**未提交**在製品），該測試的 `want` 因此在工作區變紅（HEAD 仍綠，因為省略號還沒 commit）。把 `want` 改掉讓它綠很容易 —— 但那只是讓測試跟著實作跑，**實作再改一次它又要再改一次**，等於沒有守到 §2 的「≤12 字」。已在暫存實測過補法：加一條不隨實作字面走的斷言 `len(M._clean(...)) <= M._MAX_CHARS`，變異測試把實作改成 `name[:_MAX_CHARS + 2]` 時**兩條一起紅**（77/79），還原後 79/79 ⇒ 那條斷言真的在守。⚠ 該修法目前只存在於某個 session 的 scratchpad，**會被清掉**，所以登記在這裡。 | 等省略號實作進 HEAD（屆時 `超過 12 字截斷` 會在 HEAD 變紅），在 `tests/test_session_title.py` 的 `_clean` 區塊：① `want` 改成 `"一二三四五六七八九十十…"` ② 緊接著補一列 `case("截斷後總長仍守 ≤12 字", "…", len(M._clean("一二三四五六七八九十十一十二十三")) <= M._MAX_CHARS, True)`。**驗收＝把實作暫時改成 `name[:_MAX_CHARS + 2] + "…"`，兩條都要紅**，只紅一條代表新斷言沒接上。 | 下次施工 | eval | 中 |
| 🔧 **「commit 別夾帶別人的檔」目前只是規則，沒有閘門** | 2026-08-27 **一天之內同一條規則失效三次**：①stage 完跑去驗證，commit 被別條線收走 ②`git commit --amend` 前沒重查 HEAD，**改寫了別人的 commit**（已 `git reset --soft` 還原）③commit 夾帶別人兩個檔（253 行＋9 行）。⚠ 第三次最關鍵：`feedback-concurrent-sessions-same-repo` 8/25 第 7 條**早就寫了**「commit 前必看 staged 清單」，我也**確實看了**、檔數還印在畫面上，然後用 and-and 一路串到 commit ⇒ **「看」不是防線，因為看了可以不理**。規則沒失效，**強制力**失效。而 harness 的定位就是把這種規則變成執行期閘門（`UNIVERSAL_HARNESS_PLAN.md`），這一條還停在「叫模型記得」。第 ② 次若沒被 user 一句「你檢查一下」抓到，那顆被改寫的 commit 就留在歷史裡了。 | ⚠ **判準要先想清楚再寫碼**（同 eval L2 那列的教訓）：`PreToolUse` 攔 `Bash`／`PowerShell`，命令含 `git commit` 時比對 `git diff --cached --name-only` 與「本 session 這輪 Edit／Write 碰過的檔」，有多出來的就 block 並列出多的是哪幾個。**三個待決**：(a) 「本輪碰過的檔」從哪來（hook 自己維護 state？讀 transcript？）(b) 合法多檔 commit 怎麼豁免（收工封存本來就要 commit 一批）(c) `--amend` 要另外比對 HEAD 有沒有變。**先證明它會紅**：造一個 index 含非本輪檔的 fixture，改之前必須放行、改之後必須擋下。 | 下次施工 | 閘門 | 高 |


| ⏳ **Claude 端自動改名已整套退役（user 2026-08-28 決定）** | **退役理由三條，全是同日查證出來的事實**：①**官方早就有正式介面**——Agent SDK 的 `rename_session()`，官方文件逐字「Renames a session by appending a custom-title entry. Repeated calls are safe; the most recent title wins.」，與我方手寫的 `_append_title()` 完全同構；另有 `hookSpecificOutput.sessionTitle` 可讓 CLI 自己寫，「跟 client 快取搶」那整層在架構上就不存在 ②**從未穩定**——三天九版、最長活 12.9 小時、三次歸因被推翻（記憶體快取→沒人重讀／欄位所有權→覆蓋時序／格式判準→歸屬判準），而它做的事是「讓側邊欄名字好看一點」；規劃與覆核文件 162KB ③**`PreToolUse` 兩次咬到 Cursor CLI**（8/26 早上全滅、8/28 無 matcher 重現，A/B 三次實測），而 Cursor 是這台機器唯一跨模型族的審查者。**已做**：三個掛載全拆（備份 `settings.json.bak.20260828_retire_title`，殘留數 0）；`global/hub/20-modes.md` 撤回「每則對話都宣告」那條（那是為改名加的，看板合規統計要的是「有任務時宣告」）；`21-title-claude.md` 清空並記退役理由；`.cursor/rules/task-naming.mdc` 與 `22-title-cursor.md` 移除 `【待】` 格式並標明只對 Cursor 有效；`hooks/session_title.py` 加檔頭退役說明；`test_gen_rule_hub.py` 的「task-naming 指標只在 Claude 產出」改判準（前提隨退役消失）。**已驗**：全套 1459/1462（回到 3 條既有債）、`test_session_title` 128/128、`test_session_archive` 34/34、Cursor 實跑 exit 0 列出六個子目錄。⚠ **程式碼沒刪也不要刪**：`session_archive.py` 仍 import 它的 `compose_idle`／`project_name`／`cloud_request` 做封存時的雲端 idle 名（票 02 Q5），那條路與側邊欄命名無關、仍在跑。⚠ **副作用**：`_remember_last_task()` 只在 `session_title.main()` 裡呼叫 ⇒ 沒有人再寫 `state/lasttask.*.txt` ⇒ 封存推的「上一個任務」會永遠停在最後一次的值（不影響正確性，但會逐漸過期）。⚠ **未驗**：設定只在 session 啟動時載入 ⇒ **現行對話仍持有三個掛載、hook 還會跑到本則結束**；要開新 session 才看得到退役後的行為 | ①開一個新 session 做一輪工作 ⇒ 側邊欄應顯示**平台自產的標題**（多為英文），且 `state/session_title.log` **不再新增**；還在寫就是設定沒生效 ②想裝回去的話：`PreToolUse` **一律不可用無 matcher**，掛進 `dispatch.py` 那一組；三個事件的完整配置見 `settings.json.bak.20260828_wire_title3` ③若之後要改走官方路徑，先實測 `hookSpecificOutput.sessionTitle` 在哪些事件真的生效——官方文件只列 SessionStart，第三方說 UserPromptSubmit 也吃，兩邊說法沒對上 | user | hook | 中 |
egister_session_title_hook.py`（只掛 PreToolUse），另外兩個事件要手動加進 `~\.claude\settings.json` 的 `Stop` 與 `UserPromptSubmit`（都已有 `dispatch.py`，加成同一個 matcher 的第二支）②驗收：**開一個新 session**、第一句打一句有內容的話、不做自我宣告 ⇒ 側邊欄按「重新整理」鈕後應顯示 `【待】<那句話>｜<專案名>`；**還是「等待任務」或英文標題就是沒生效** ③不想掛的話這一列改記「已決定不裝」，別留著當未決 | user | hook | 高 |


## 全域·需求（待判斷）

> **與上面那張「待辦」表的差別**：待辦是**已經決定要做**、只差排期；
> 這裡是**還沒判斷要不要做**——只登記、不排期，你定期看一眼決定升級成待辦或直接刪掉。
> 分不清就丟這裡，判斷是你的事、不是提出者的事。
>
> **誰會往這裡寫**：
> - **角色**（`agents\*.md` 的「碰到能力邊界時」）在回報末尾寫 `【需要但沒有】<工具｜技能｜人力>——<說明>`，
>   主 session 收到就抄進這張表。⚠ 角色多半沒有 Write 權限（`locator` 只有 Read/Grep/Glob），
>   **它們寫不了這個檔，落檔一定是主 session 的責任**——這條漏掉的話整個機制等於沒有。
> - **主 session 自己**踩到同類情況時一樣登記。
>
> **兩種情況都收**：①做不到（被工具限制擋住）②做得到但明顯繞路多花時間。
> **②必須附實例**——哪一步繞了、繞法是什麼。沒有實例的不要登記，那是想像中的需求。
> 「繞路成功」才是常態：做不到會喊，繞過去了不會喊，而不喊的那些就是這張表要接住的。
>
> 做完或判定不做就把該列刪掉（同上表，不留歷史）。

| 項目 | 現況／為何還沒做 | 下一步（逐字指令或動作） | 誰 | 分類 | 優先 |
|---|---|---|---|---|---|
| 🔧 **`locator` 角色沒有執行權限，答不出執行期事實**（**第 3 個實例到齊，2026-08-25 —— 該判斷了**） | 2026-08-24 第 2 例：盤點「DEV 怎麼啟動」時查到靜態矛盾（`start_dev.bat` 寫 3000、文件普遍稱 8090），但**判不出哪個埠現在真的有 listener**。**2026-08-25 第 3 例**：盤點 reviewer 設定時要判「`C:\Users\<USER>\.claude\skills\adversarial-review` 與 `D:\Patrick-AI\.ai-harness\skills\adversarial-review` 哪個是 junction」，需要 `cmd /c dir /AL` 或 `Get-Item \| Select LinkType,Target`，`locator` 只有 Read/Grep/Glob ⇒ 只能從「內容逐字相同＋commit 訊息」做旁證，並自己標明「不是查詢結果」。⚠ **這一例的代價是實的**：主 session 接手後實測，發現 PowerShell 5.1 的 `Target` 回報 `C:\.ai-harness\…`（**該路徑根本不存在**），要再用 `fsutil file queryfileid` 才判得出來——唯讀角色連錯誤的旁證都給不出。**2026-08-27 第 4 例（這次代價最直接）**：收工派 `locator` 盤點兩個 `.scratch/` 的「逾期 >7 天」與「今天新增」，它跑了 **65 次工具呼叫／10 萬 token／6 分鐘**，回來的結論是「A、B、C 三項全部查不到」——`Glob` 號稱依 mtime 排序但**不吐出時間值**，`Read`／`Grep` 不含檔案系統中繼資料。主 session 用**一段 `os.walk` + `os.stat`、3 秒**就量完（209／156 檔、逾期各 0）。⚠ 它誠實喊了【需要但沒有】而且沒有拿檔名日期硬湊答案，**角色紀律是對的**——壞的是派工判斷：**「盤點檔案」聽起來像唯讀查詢，但需要 mtime／bytes 就已經是執行期事實。**派工前的判準應該是「答案要不要讀檔案系統中繼資料」，不是「聽起來唯不唯讀」 | **三個實例到齊，判斷點到了。** 二選一：①維持現狀（主 session 代跑執行期查詢）②給 `locator` 一個純查詢窄口白名單（`netstat`／`pip freeze`／`--version`／`fsutil file queryfileid`／`dir /AL` 類，**唯讀且無副作用**）。⚠ 選②要先確認這些命令在 hook 白名單裡放行，否則角色會拿到一個喊得出口卻跑不動的權限 | 待判斷 | 角色 | 中 |
| 🔧 **`CLAUDE.md` §9 的「DEV 平台開 `:8090`」與啟動腳本不符**（2026-08-24 locator 查到） | `SOP/scripts/start_dev.bat:6` 是 `set PORT=3000`，`SOP/05_UI_Demo/server.py:208` 的預設值也是 `3000`。`8090` 唯一寫死的位置是 `SOP_PROD/05_UI_Demo/.env.production:2`（**本機 PROD 副本**的埠，配 `restart_prod.bat`）。另有孤兒檔 `SOP/05_UI_Demo/.env.production` 寫 `PORT=8080` 但 grep 不到任何消費者。⇒ **§9 那句可能一直在指錯環境**，而 §9 是部署硬規則區 | 跑 `netstat -ano \| findstr LISTENING \| findstr \":3000 :8080 :8090\"` 看實際哪個埠活著；或直接跑一次 `SOP\scripts\start_dev.bat` 看它印什麼。確認後訂正 §9 那一句（**別反過來改腳本**——先查清楚「DEV 平台」這個口語到底指哪一個環境） | 待判斷 | 文件 | 中 |
| 🔧 **遠端桌面憑證以明文出現在 transcript 裡（316 次）** | 2026-08-24 跑 `/fewer-permission-prompts` 掃 572 個 transcript 時撞見：PowerShell 遠端腳本用 `PSCredential(<帳號>, (ConvertTo-SecureString <明文密碼>))` 的寫法，**密碼直接寫在命令列裡** ⇒ 每跑一次就在 `~/.claude/projects/*.jsonl` 多留一份。**已查證沒有外洩到版控（2026-08-24 全機掃過）**：掃 `d:/AI-Projects`、`d:/IT-department`、`d:/Patrick-AI/.ai-harness`、`~/.claude` 四個根目錄共 **1,260 個原始檔（.md/.ps1/.py/.json/.txt/.bat…，不含 .jsonl）→ 明文密碼 0 命中**；`git log -S` 在兩個 repo 各 0 個 commit。⇒ **密碼只活在 `~/.claude/projects/*.jsonl` 裡**，也就是每次被打進命令列、不是從任何檔案讀出來的。**帳號名**另有 21 個檔硬寫（`d:/AI-Projects` 的引擎 `.ps1` 與計畫書），但**那 21 個檔 0 個進版控**；進版控的只有 `.ai-harness/dashboard/harness-dashboard.html` 4 處帳號名、不含密碼 | 決定要不要處理，三個方向：①改用 `Get-Credential` 或 `cmdkey`（Windows 認證管理員），命令列不再帶明文 ②既有 transcript 要不要清（316 筆，刪檔會一併失去對話歷史，要權衡）③該帳號改密碼——⚠ 動它要一併改 `d:/AI-Projects` 那 21 個硬寫帳號名的檔（`grep -rl <ADMIN-ACCT> d:/AI-Projects` 列得出來）。⚠ **產生檔是活的風險**：`harness-dashboard.html` 會把工具呼叫原文擷取進去，今天剛好只截到使用者名——**擷取長度一改就會連密碼一起進版控** | 待判斷 | 流程 | 高 |
| 🔧 **escape 被中間層吃掉，需要 lint 而不是靠記得** | 同一天內同一個母題踩三次：抽區塊撞解構參數的 `{`／CRLF 下 `replace` 靜默沒命中／heredoc 非 raw 字串把 `\t` 吃成 tab（寫進記憶檔的路徑變成 `.ai-harness<TAB>ools`）。前兩個已做成 `tools\js_source_probe.js`，**第三個還沒有守門**。**2026-08-23 同一輪再兩次**（同母題第 4、5 例，都是 Bash heredoc → Python 字串兩層）：①`\\` 被吃成 `\`，寫出的 `print("D:\Patrick-AI\.ai-harness\...")` 觸發 SyntaxWarning（invalid escape sequence）——**語法檢查照過、只有開 `-W error::SyntaxWarning` 才叫**；②`\n` 被寫成字面兩字元，把 `if …:` 與它的 body 併成一行 → IndentationError 當場炸。可靠繞法已驗證兩種：`cat > frag.txt <<'EOF'` 純字面 heredoc 寫檔再 splice、或用 `chr(92)` 組字串。**2026-08-23 第 6 例**：往這張表登記下面那條 `peek_sessions` 時當場又踩，而且**踩的是跟第 3 例一模一樣的路徑**——`D:\Patrick-AI\.ai-harness\tools\` 的 `\t` 又被吃成 TAB（`.ai-harness<TAB>ools`），同時 `'\U0001f464'` 被解讀成 👤 把 traceback 原文引錯。⇒ **知道這個坑存在並不能避開它**，這條需要的是機器檢查不是記憶；順帶證實那兩種繞法可靠（改用純字面 heredoc 一次就對）。**2026-08-23 同一天再踩四次（第 7–10 例）**：⑦regex 的反斜線字元類 `[.\\/]` 落地成 `[.\/]`，**pattern 合法、compile 得過、只是永遠不命中 Windows 路徑**（是那支工具自己的 `--self-test` 抓到的）⑧就在用 heredoc **記錄第 7 例的那一行**，`\\` 又被吃一層、箭頭兩邊變成一模一樣的字串⑨`\\n` 被吃成真換行，把 Python 字串字面值攔腰打斷（`SyntaxError: unterminated string literal`）⑩同⑨，換一個檔又發生一次。**十例裡有八例的症狀是「安靜地不對」而不是報錯**；四例都靠改用 Edit 工具（無跳脫層）或 `chr(10).join([...])` 修掉。**2026-08-25 再踩兩次（第 11、12 例，兩例都是 Bash quoted heredoc `<<'JSON'` → 檔案內容）**：⑪寫 `.cursor\mcp.json` 時 `"C:\\Program Files\\nodejs\\node.exe"` 落地成單反斜線 `"C:\Program Files\..."` ⇒ **JSON 非法**（`Invalid \escape`），但 `cat` 出來肉眼幾乎看不出差別，是 `json.load` 才炸；⑫同一輪寫 DeskBus 任務 body 時 `\\round-1-ask.md` 落地成 `\round-1-ask.md`、Python 再把 `\r` 解讀成 **CR 字元** ⇒ 送出去的路徑變成 `…sg084-hydrate-deadlockound-1-ask.md`，**對方（Cursor）已經讀了那則訊息才被發現**——這一例的代價是外溢到另一個 agent 的。⚠ **`<<'QUOTED'` 理論上不做替換，實測仍吃掉一層**，所以「用 quoted heredoc 就安全」是錯的直覺。⑪⑫都改用 Python `json.dump`／`r'...'` 一次就對。**十二例裡十例是「安靜地不對」** | 想一個能擋「用會解讀跳脫的那一層去寫含跳脫字元的文字」的檢查；或至少在寫檔類 hook 加一條偵測（檔案內容出現裸 TAB 在路徑中段＝可疑；**JSON 檔寫入後自動 `json.load` 驗一次**＝第 11 例當場會叫）。**十二例之後可以確定：這條靠人記得是無效的**——每一次都是「知道有這個坑、下一句就踩進去」。**最省事的通則：凡是要寫含 Windows 路徑的內容，一律走 Edit 工具或 Python（`r'...'`／`json.dump`），不要走 heredoc**。⚠ **另一條線同日獨立踩到第 13–15 例並得出同一個通則**（2026-08-25 收工時寫 `PENDING_VERIFY` 與工作日誌：`\r` 落地成 CR、`\t` 落地成 TAB，路徑變 `.ai-harness<CR>eviewer`）——**其中第 15 例發生在「不需要更多實例」那一句裡**：知道坑在哪、正在寫它、正在說自己不會再踩，下一句照樣踩。⇒ **兩條線同日各自撞到、各自得出同一個結論＝證據已飽和，缺的是機器檢查不是實例**（寫檔前掃寫入內容有無裸 CR／TAB／單反斜線 Windows 路徑）。⚠ **2026-08-26 補一個新形狀，檢查範圍要跟著擴**：不只反斜線——**反引號在 Bash 雙引號字串裡是命令替換**，`py -3 -c "…\`refreshDraftBaseline\`…"` 會讓 shell 去執行那個字，寫進檔案的是**空字串**（症狀：規範文字變成「深層路由（）」「merge 後禁 （會…」，讀起來只是漏字、不像被吃掉）。同輪還連帶噴出 `command not found` 三行，但那看起來像雜訊、很容易略過。⇒ 掃描項要含**反引號與 `$(` 出現在寫入內容裡**，不只 CR／TAB／反斜線。 ⚠ **2026-08-26 再補一個新層：`subprocess.run(..., text=True)` 在 Windows 上會把 stdin 的換行從 LF 改寫成 CRLF**——餵給任何以換行分隔的工具都會靜默污染。實例：算 `git mktree` 時那個 CR 被當成**檔名的一部分**，同一份輸入用 shell `printf` 得到 `43fb277f…`、用 Python text mode 得到 `0e2f6725…`，**兩個都是合法的 tree hash、都不報錯**，害我一度判定「乾淨的 upstream 副本對不上」而差點推翻一份正確的研究結論。⇒ **餵 stdin 給外部工具一律送 bytes**（`input=payload.encode()`，不要 `text=True`）。這是第 17 例，也是第一次出現在「不是我寫檔、是我讀資料」的方向上。⚠⚠ **這條落檔本身第一次寫進來時就被它自己描述的坑咬了**：quoted heredoc 仍吃掉一層，字面的跳脫序列變成真的控制字元，把這一整列從中間切成三行。**教訓：寫「關於跳脫」的規則時，措辭裡一個跳脫序列都不要放**——用 CR／LF／CRLF 這種字面名稱講。 | 待判斷 | 流程 | **高（16 例·兩線獨立佐證·形狀已擴到反引號）** |
| 👤 **需求表的有用指標是「重複度」，不是筆數或年齡**（2026-08-23 實測改寫） | 原本登記的是「`gen_roles_topology` 的 `boundaryReport` 只驗角色檔有沒有寫那段話 ⇒ 永遠 6/6 綠，要不要加第二個指標（筆數＋最舊一筆年齡）」，並註明「先讓表跑一陣子有真實資料再說」。**資料現在有了，而它推翻了原本的提議**：`tools\esc1_corpus.py --dump` 實跑 342 份 subagent transcript → 含標記 57 份／78 行 → **真需求 42 行／39 份檔案**，而表上只有 8 列。但逐筆讀下去，**大宗是同一件事**（N1 的唯讀閘門白名單：`py -3`／`py_compile`／`node <script>`／`ls -la`／`git diff`），**而且其中好幾筆是角色檔誤述自己能力造成的假需求**——`harness-auditor.md` 原本寫著「`py -3` 不在白名單」，實際是放行的（2026-08-23 已修）。⇒ 筆數與年齡量的是積壓，落檔率量的是捕獲，**兩個都答不出「這 42 次在喊同一件事」** | 指標改成**重複度**：把 `esc1_corpus.py` 的真需求行做粗分群（關鍵字即可：白名單／執行／遠端／字數…），看板顯示「前三大需求各被喊幾次」。判準：**同一群 ≥5 次還沒動工就該紅**。⚠ 做之前先扣掉「角色檔誤述能力」造成的假需求，否則指標會把文件錯誤算成能力缺口 | 待判斷 | 角色 | 低 |
| 🔧 **唯讀角色驗不了「投遞鏈」，而回測的 ground truth 也還是啟發式判的** | 2026-08-22 對抗式覆核第 3 輪（`agent-a04587dd8c91c621a`）在回報末尾喊了三筆，**由 ESC-1 上線後第一次真實命中帶進來**——這一列本身就是那條規則的第一個閉環樣本。①**沒有能實際觸發一次 hook 的權限**：審查者是唯讀環境，`_queue_pending_warning` → `UserPromptSubmit` → 模型是否真的收到，以及模型看到 `agentId` 時會不會因為 tool_result 裡那句「never quote… including the agentId」而拒絕引用，**兩件事只能從 log 反推、沒有實跑**。②**回測母體沒有版本化**：第 3 輪重新量到 50 筆、與 v3 寫的 49 筆對不起來，沒有原始清單可逐筆對。③**「真需求 vs 無」的分類是啟發式判的**：兩輪覆核獨立量到 36/14 與 37/15、互相佐證，但**兩個都是自動判的**，而它是回測的 ground truth。 | ①要一次真實派工＋改 settings 才驗得到，屬「動 hook 佈線」等級，排進下一次動 harness 時一起做；②把 `tools\esc1_corpus.py --dump` 的輸出存成帶日期的檔，讓母體有可比對的版本；③人工掃一遍那 10 筆「無」與 4 筆「提及」（`py -3 -X utf8 tools\esc1_corpus.py --dump`），確認分類後把數字釘進 `HARNESS_ROLE_ARCH_PLAN.md` §9.5 ①。 | 待判斷 | 角色 | 低 |
| 🔧 **eval L2 的 `_resolve_path` 構不到兩層深的 skill 檔**（2026-08-23 閘門清理線發現·**2026-08-25 複核：根因未修，但當時未發作**） | `eval\check_contracts.py` 的 `SEARCH_BASES` 沒有「skill 自己的目錄」，且只寫檔名時的遞迴**限一層深度** ⇒ 構不到兩層深的 `skills\skill-watch\run.py`。**檔案真的在**（`git ls-files skills/skill-watch/` 列得出來），所以是假紅不是缺檔。今天才浮出來的原因：HEAD 版只掃專案層一層、看不到 harness 層的 skill，是**案 A 的 A-3「掃兩層」（工作區未 commit）**第一次把它納入檢查；而 8/16 施作 A-3 時 harness 層只有 `context-health`／`visual-check` 兩支、都沒有同目錄檔案引用，所以當時驗不出來。⚠ `SKILL_EVAL_PLAN.md` §9.6b「案 A 範圍外、已知但不做的」六條**沒有涵蓋這一條**。**2026-08-25 複核快照（實測，不是現況宣稱）**：`eval\run_all.py` L1–L4 **全 PASS**，且 `contract_allowlist.json` 只有 `domain-modeling`／`CONTEXT-MAP.md` 一筆 ⇒ **不是靠豁免壓下去的**（原記那條「不要豁免」的警告沒有被違反）。**2026-08-26 B-4 基準**：`adversarial-review` 的動態範本 `round-N-reply.md` 被 L2 報 MISSING，現行 L2 exit 1；這不是同一個兩層路徑案例，但證明前一天的全 PASS 不可當現況。兩層根因仍原封不動：`_resolve_path('run.py')` → `None`、`_resolve_path('skill-watch\run.py')` → `None`，只有 `_resolve_path('skills\skill-watch\run.py')` 找得到 —— **當時不紅只是因為那支 SKILL.md 剛好寫了完整相對路徑**（`skills/skill-watch/SKILL.md:26`／`:38`）。下一支寫 `${CLAUDE_SKILL_DIR}/run.py` 或裸檔名的 skill 會再中一次。⚠ **不發作 ≠ 修好了**，別看到 L2 全綠就把這列當已結案刪掉 | 完整根因（定位到行）·修法·四步驗證（含「先證明它會紅」的反向項：把 `run.py` 暫時改名必須重新變紅）·沒找到的 → `.scratch\room-gate-cleanup\FINDINGS.md` 發現 1。⚠ **不要放進 `contract_allowlist.json` 豁免**——檔案真的存在，豁免等於把檢查器的能力關掉。⚠ 它在別條線正在重構的同一區（U-1 硬編碼還債），**該由那條線順手做**，別兩邊各改一次 | 待判斷·歸 eval 線 | 流程 | 中 |
| 👤 **唯讀角色查得到「在哪裡」卻答不出「算出來一不一樣」** | 2026-08-23 盤點「維運腳本 N 支」時，`locator` 找齊了**三套各自獨立的 ops 計數邏輯**（`check_freshness.gather_current`／`gen_layers.count_ops`／`capability_checks._p_ops_scripts`），但答不出它們算出來的數字一不一致——那要實跑 Python，而它只有 Read/Grep/Glob。主 session 代跑三秒有答案（ops 66／hooks 19／總計 85，其中兩套範圍相同、`gen_layers.count_ops` 問的是另一個問題）。**與上面 ① 不同：這不是白名單太窄**，locator 的角色定義本來就是「不執行任何指令」，所以真正的問題是「唯讀盤點碰到需要算的問題時該由誰算」。這次機制運作正常（角色喊、主 session 接），登記是為了看它會不會反覆發生 | 判斷二選一：①**維持現狀**（傾向這個——這次繞路成本是三秒，而放寬唯讀角色的執行權限是永久的風險）②給 locator 一個純計算窄口。**先不要動**，等這一列累積到第三個實例再判斷 | 待判斷 | 角色 | 低 |
| 🔧 **Windows 本機 agent 拉不到 `origin.cursor.com` 的雲端 agent 產物**（2026-08-25 DeskBus 接 harness 撞到） | 來源 `https://origin.cursor.com/git/sars525200/tmp-e33b4a2339d80836`。已試：①`git clone` 無 helper → `could not read Username` ②開 helper → 卡 GUI 登入 ③瀏覽器開同一 URL → Cursor 登入牆 ④`origin` CLI：Windows 原生不支援、本機也沒裝 WSL。檔也不在 D:／`.cursor`。不做就接不進 harness；默默重寫 DeskBus 會跟雲端那份分叉 | 判斷三選一：①人把該 repo 放到本機路徑再叫這則繼續 ②人在 Git Credential Manager 登入 Origin 後再 clone ③授權雲端子代理把檔拷進 `D:\Patrick-AI\.ai-harness`。⚠ 不要在沒有來源檔的情況下重寫一份 | 待判斷 | 流程 | 中 |
| 🔧 **Workflow 類別沒有基準 ⇒ 官方新增一個 workflow 不會被報成變動**（2026-08-23 修一半） | 交叉驗證原本只比 `[Skill]` 那一類，而 `/deep-research` 被官方標成 `[Workflow]` ⇒ **它從來沒被端到人面前過**，儘管 `skill-watch` 的開場白自己就在講「我們正打算自己包一支功能更弱的」講的就是它。**已修一半**：現在會把 workflow 逐項印出來附說明（`skill_watch_run.py` 的 `[3/6]` 那段）。**沒修的一半**：workflow 不進 `baselines`，所以「官方**新增**了一個 workflow」這件事偵測不到——而那正是這支工具存在的理由 | 決定 workflow 要不要進基準。⚠ 它與 skill 的形狀不同（不進注入清單、比不出「本機有沒有」），所以不能直接套 `baselines[平台][模式]` 那個結構——可能要另開一個 `officialWorkflows` 快照，比對邏輯也另寫。做之前先想清楚「workflow 消失了」要不要報 | 待判斷 | 流程 | 高 |
| 🔧 **唯讀閘門擋掉 `git ls-tree`／`hash-object`／`check-attr`，B-4 的獨立審查者拿不到 subtree digest**（2026-08-26 `visual-check` 那列第 1 例） | `hooks\agent_readonly_gate.py:44` 的 `_GIT_READONLY` 只有 diff/status/show/log/ls-files/rev-parse/cat-file/describe/branch/remote/check-ignore。**實例**：B-4 §9.6a 步驟 1／4／7 明文要求記「before／candidate subtree SHA」與「bundle 完整檔案清單」，但獨立審查者（`harness-auditor`）三個純查詢命令全被擋 ⇒ 它只能用 `ls` 看工作區＋`git diff --stat` 只出一檔**推**得「HEAD 側也只有一檔」，並自己標明是推導不是證據；candidate 也只拿得到 `git diff --full-index` 的 blob，拿不到 subtree。B-4 還有 12 支要走，**每一支的審查者都會卡在同一處**。**同一輪還暴露一個相關缺口**：候選檔一旦被改寫覆蓋、而且從未 staged，前一版就**無法用任何唯讀手段取回**（`git cat-file -p <blob>` 回 `Not a valid object name`）⇒ §9.6a 步驟 4「digest 一變就回本步重驗」在機器層等於只能靠審查者的記憶執行。本列已當場繞過：主 session 用 `git hash-object -w` 把前後兩個候選都寫進 object DB，審查者才做得出機器 diff | 二選一：①`_GIT_READONLY` 補 `ls-tree`、`check-attr`（兩個都是純讀，位置參數是要查的路徑，比照 `check-ignore` 處理、**不得進 `_GIT_NO_POSITIONAL`**）②`write-tree` 同屬純讀但需要 index，優先度低於前兩個；③`hash-object` **不可照抄**——`-w` 會寫進 object DB、`--stdin-paths` 會讀 stdin，要進白名單得先加旗標黑名單擋掉這兩個，或乾脆不放、由主 session 代跑。（規範側的配套補句已由 user 決定「等 B-4 十四支全做完再一次性收」，記在 `SKILL_EVAL_PLAN.md` §9.6b 範圍外清單，本列不重複。）⚠ 改完要跑 `py -3 tests\run_hook_tests.py` 確認閘門測試沒破 | 待判斷 | 角色 | 中 |
| 🔧 **唯讀審查者算不出任意 git blob 的 token 數 ⇒ B-4 每一列的 before 值都只能採信被審方**（2026-08-26 提出·**同日第 4 次命中**） | `eval\check_structure.py` 的 `approx_tokens()` 只吃**檔案路徑**，要量 before 就得先把 blob 落到磁碟；而獨立審查者是唯讀角色，三條路徑全被擋：`git cat-file … \| py -3 …`（閘門擋 shell 元字元）、寫暫存檔（禁 Write）、`git hash-object`（不在 `_GIT_READONLY`）。**實例**：`skill-watch` 那列 before 的 2526 tok，審查者三種方式全試過都拿不到，只能標「未獨立復算」——而 token 前後值正是 §9.6a 要求逐列記錄的觀測值，**這是「獨立對帳」裡最不該採信被審方的地方**。after 值它跑得到（檔案在工作區），所以缺的恰好是對照組。**第 2 例（同日 `context-health` 那列）**：before 2406 tok 同樣三種方式全被擋，審查者只能背書 −51 中的 −32，並要求在隊列表標成「改寫者單方值、未經獨立復算」。**第 3 例（同日 `escalate` 那列）**：before 1174 tok 同樣復算不了，審查者只背書 +16 中的 +3。**第 4 例（同日 `design-spec` 那列）**：before 1805 tok 復算不了，只背書 +132 中的 +47。**第 5、6 例（外部批第 8、9 列）**：兩列的審查者都被 `git ls-tree`／`hash-object` 擋住，**做不出 §9.6a 步驟 4 要求的 candidate digest 對帳**，只能改用 `git diff` 的 index 行反推，或由**被審方**先把候選寫進 object DB 再餵給它。⚠ **同一個缺口第二次擋住覆核的獨立性 ⇒ 從「便利性問題」升級成「分工缺陷」**（前像與 digest 的存否掌握在被審方手上）。**具體工具形狀（第 10 列審查者提的）**：`tools/blob_probe.py <sha> --tokens --eol --wc`。**同一條閘門另擋掉 `git ls-tree`（純查詢）與 `find`、以及所有含 shell 元字元的複合指令**（`ls -la A && ls -la B` 也不行）⇒ 審查者盤 bundle 要拆成 N 次呼叫，且**驗不出「某路徑是 junction 還是實體資料夾」**——把任意 blob 落到唯讀可讀位置並量測。⚠ 現況閘門**擋得是對的**，代價才是問題：審查者無法復算任何 before 數字，第 3、4、5、6、10 列連五次同一句「改寫者單方值·審查者無法復算」（前像與 digest 的存否掌握在被審方手上）。⚠ 這張表自己的判準是「有用指標是重複度不是筆數」——**同一群 3 次了** | 三選一：①加一支 `tools\tokens_of.py <路徑或 blob-sha>`，內部重用 `approx_tokens()`，唯讀無副作用，加進閘門白名單 ②`eval\check_structure.py` 加 `--blob <sha>` 旗標 ③維持現狀，逐列在隊列表把 before token 標成「未獨立復算」。⚠ 與上面那條閘門白名單需求是**同一個根因的兩個面向**（審查者拿不到 git 物件層的事實），要動最好一起動 | 待判斷 | 角色 | 中 |
| 🔧 **缺一支「跨 skill 指標健檢」工具 ⇒ 壞指路只能靠人一支一支 grep**（2026-08-26 B-4 第 5～7 列連三次撞到） | 現有機器層只驗「檔案存不存在」（`eval\check_contracts.py`），**不驗指標指得準不準**。四類都已在 B-4 實際咬到：**(a) 裸 `CLAUDE.md §N`**——全域 §3 是「工作流」（有 L／S／M）、專案 §3 是「開工 SOP」（沒有），`escalate` 與 `design-spec:33`（§4：全域是「派工模型成本」、分流原則在專案）各中一次；**(b) 裸檔名的跨 repo 指標**——Cursor 端從別的專案根讀不到；**(c) 指到別支「步驟 N」的內容型引用**——`design-spec` 步驟 5 的「M 級」被第 6 列改成過期標籤、`session-workflow:31` 曾指向 callee 已明文拒收的情境，兩者都靜默；**(d) 反引號內帶磁碟機代號的絕對路徑逃出 `PATH_RE`**——`check_contracts.py:58` 字元類 `[A-Za-z0-9_./\-]` **沒有 `:`**，實測 `` `D:\Patrick-AI\.ai-harness\X.md` `` 抽出來是 `[]`，所以「補絕對路徑」這個做法會**靜默拿掉該指標的機器兜底**（第 6 列 `design-spec:78` 就是這樣種下的，第 7 列才發現）。⚠ 第 8～13 列（外部 6 支）還會再遇到 | 做一支唯讀掃描器（比照 `rulefile\check_bloat.py` 的形狀），四類各出一條 WARN：(a) `CLAUDE.md §N` 未標全域／專案或未帶節名 (b) 跨 repo 指標只有裸檔名 (c) 引用別支的 `步驟 N`／`§N` (d) 反引號絕對路徑 **(e) 同一節內多條路由條件互相重疊**。⚠ (e) 與 (a)–(d) 分析方式不同（前四類是「指標解析得到嗎」，(e) 是「條件集合有沒有交集」），但同屬「skill 正文語意檢查、L1／L2 完全無感」，做成一支比較省。**(e) 的實例**：第 7 列三輪裡有兩輪栽在同一類——R1 前的 `session-workflow:31`「M 但還沒 map → design-spec」撞 callee 拒收；R3 補「窮盡」時又因為「明確 M 幾乎必然同時是人要正式計畫書」把 M 送回同一條死路。**兩次 L1／L2 都是 0 FAIL 且該支不在 WARN 名單**。第 13 列 `wayfinder`（另一支編排器·129 行）會再遇到。⚠ **不要改 `PATH_RE` 去吃 `:`**——那會讓現有 12 支的契約數一次全變，且 B-4 期間 §9.6a 明文禁改 eval 工具。⚠ 與別條線剛加的 `rulefile\check_claims.py`（驗「宣稱改了什麼真的落在檔案裡」）**主題相鄰但不同**，動工前先看那支能不能擴，別做成第二支 | 待判斷 | 流程 | 中 |
| 🔧 **缺「做了的有沒有說」這個方向的檢查 ⇒ 未宣告的刪除只能靠人逐行讀 diff 抓**（2026-08-26 B-4 第 7 列**連兩輪**栽在同一模式） | repo 內已有 `rulefile\check_claims.py`（別條線今天新建），驗的是「**說改的有沒有做**」；缺的是**反方向**：「**做了的有沒有說**」。**實例（同一列、同一模式、兩次）**：R3 我宣告「改兩處」，實際 hunk 還刪掉了 R2 才加的仲裁句「明確 M 不走那支，見下一條」⇒ 上一輪剛修好的死路由從另一道門回來；R4 我宣告「改兩處」，實際又刪掉「，作法寫在對話裡就好——兩者都不開計畫書」⇒ 那 17 字是全檔**唯一**定義「不落檔」的地方，使同一輪新增的完成判準「S 已聲明不落檔」當場懸空、並與步驟 5 的 `PENDING_VERIFY.md` 字面衝突。**兩次 L1／L2 都是 0 FAIL 且該支不在 WARN 名單**——機器層對這類缺陷完全無感，兩次都是獨立審查者逐行對帳才抓到 | 做一支對帳器：吃「本輪宣告的改動清單」＋`git diff`，把 hunk 的每一段增刪逐一歸給某一條宣告，**歸不掉的就報**。純機械可驗（宣告條目 ↔ 增刪行對帳），不需要語意判斷。⚠ 先看 `rulefile\check_claims.py` 能不能加一個反向模式，別做成第二支。⚠ 這條的價值在於它**接得住 B-4 剩下 7 支**——第 8～13 列是外部 skill，未宣告的刪除會直接變成與 upstream 的無聲分歧 | 待判斷 | 流程 | **高（同列兩次·機器層零感知）** |
| 🔧 **新視窗佔位名只在測試裡驗過，真實側欄還沒看到**（2026-08-26 新做） | `session_title.py` 新增第四種命名 `專案名｜等待任務｜上一個任務`（沒有上一個任務就兩段），用途是新開的視窗在側欄看得出「哪個專案·還沒接任務·上一件做的是什麼」。**測試面已到位**：69/69 綠（含 4 條 e2e），12 個變異逐條精準轉紅，改動前後跑全套回歸網失敗集合完全相同（都是既有的 7 條）。**實機也已有兩項證據**：跨 session 紀錄真的會寫（`state\lasttask.d--IT-department.txt` ＝「索引健康工具」）；`cwd` 確實在真實 payload 裡（`dispatch.py:429` 一直靠它做 git 判定）。**沒驗到的只剩最後一哩**——側欄名稱是 `custom-title`，這支 hook 的既有限制是 **要 Reload Window 才會更新**，所以「真的開一個新視窗，那一列長什麼樣」我這邊看不到 | ①開新視窗（或 `/clear`）→ 送出第一則訊息 → `Developer: Reload Window` → 看 Recents 那一列是不是 `AI-Projects｜等待任務｜<上一個任務>` ②同時看 log 的兩個新欄位：`py -3 -c "print(*open(r'D:\Patrick-AI\.ai-harness\state\session_title.log',encoding='utf-8').read().splitlines()[-3:],sep=chr(10))"`。判讀：`idle=` 有值＝佔位名算出來了；`idle=-` 且 `prev=` 非空＝守門擋掉（**正常**，代表那個 session 早就被命名過）；`idle=-` 且 `prev=-` ＝專案名取不到，**那才是 bug** | user（要看畫面＋Reload Window） | 閘門 | 中 |
