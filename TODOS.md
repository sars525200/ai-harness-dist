# 待辦登記簿（TODOS）

> **這個檔存在的理由**：2026-08-06 前，全域待辦是**手寫在看板 HTML 裡**的 10 個
> `<div class="todo-item">`。手寫在顯示層的東西只有兩種下場——沒人改（靜默過期），
> 或改了顯示層卻沒人知道資料原本在哪。這裡是**唯一真相**，看板由
> `dashboard\gen_todos.py` 讀本檔產生。
>
> 規則：
> - **做完就把該列刪掉**，不留「已完成」歷史（那是 git log 的職責）。
> - 一列一件事。**「下一步」欄要寫得能直接照做**——寫「再看看」等於沒登記。
> - 屬於某個專案的待辦**不要寫在這裡**：待驗的寫該專案的 `PENDING_VERIFY.md`，
>   其餘寫該專案 `.claude\PROJECT_CONTEXT.md`「待辦來源」表指到的檔案。
>   這裡只放**跨專案／harness 本體**的事。
> - 欄位裡要出現半形 `|` 請寫成 `\|`（markdown 轉義；產生器認得）。
> - **每列都要有「分類」與「優先」**（2026-08-23 起）。這兩欄是**溝通的基準點**：
>   指一件事時說「閘門那三條高的」，不用貼標題。
>
>   **分類＝領域**（回答「要動哪一塊」，同一類可以一批做完）：
>
>   | 值 | 指什麼 |
>   |---|---|
>   | 閘門 | `hooks\` 底下的規則與白名單（會擋人／會判定的東西） |
>   | 看板 | `dashboard\` 的產生器與呈現 |
>   | 角色 | `agents\` 的角色檔與它們的能力邊界 |
>   | 流程 | skill、SOP、覆核、eval —— 人與模型怎麼走一遍 |
>   | 備份 | 版控、鏡像、異地、未 commit 的成果 |
>   | 文件 | 規範檔本身的正確性與長度 |
>
>   **優先＝不做會怎樣**（不是「有多想做」）：
>
>   | 值 | 判準 |
>   |---|---|
>   | 高 | ①不做會**失去東西**（不可逆）②或工具**正在讓人做錯事** ③或**每次都必然發生** |
>   | 中 | 每天／每次都在付摩擦，但繞得過去 |
>   | 低 | 改善。不做也不會壞，只是沒有變好 |
>
>   ⚠ **沒填的話產生器會自己推導優先度，而且畫面上標「（推導）」** —— 推導值是從
>   「誰」欄的措辭猜的，猜不準。**看到「（推導）」就代表這一列沒人真的排過。**
>   分類沒填則整個 chip 不出現（空 chip 比沒有 chip 更吵），該列掉進看板的
>   **「未分類」桶**。**`gen_todos.py` 會點名漏填的列**（2026-08-24 起）——
>   在此之前沒有任何東西會發現，實際上 8/23 標完 20 列後又靜靜多出 4 列沒填。
>
>   ⚠ **兩欄一律加在表尾**：產生器的前四欄是位置固定的（項目｜現況｜下一步｜誰），
>   插到前面會讓所有欄位錯位，而且**不會報錯**——它只會把「下一步」當成標題顯示。

【核心層】格式與解析規則跟被服務的專案無關；內容是本機 harness 的。

---

## 全域（harness 共用層）

| 項目 | 現況／為何還沒做 | 下一步（逐字指令或動作） | 誰 | 分類 | 優先 |
|---|---|---|---|---|---|
| 🔥 **回歸網依賴一支沒進版控的測試檔 ⇒ 換一台機器 clone 下來整套跑不起來**（2026-09-04 由另一則 session 提到 `test_doc_integrity` 時查出來的） | `tests/test_config_residue.py` **不在 git**（`git check-ignore` 無命中 ⇒ 不是被忽略，是單純沒 `git add`），而 `tests/run_hook_tests.py:289` 是**裸的 `import test_config_residue`，沒有 try**，:290／:492／:635 三處都用它。⇒ 新機器 clone 之後跑回歸網會直接 `ModuleNotFoundError` 死在載入階段，**1722 條一條都跑不到**。⚠ 諷刺的是這支檔的職責正是「回頭看現行 config 有沒有被跑壞」——2026-09-03 那起「1640/1640 全綠而環境被留在範本態 12 小時」的事故就是它補上的洞，結果它自己沒進版控。旁邊還有一支同型的：`tests/test_doc_integrity.py` 同樣不在 git、**且沒接進任何 runner**（全 repo 只有 `dashboard/sources_state.json:74` 知道它存在），自己跑 exit 1（現在就是紅的），另有一條變異驗不出來（「中文之間連續空格 → 應該紅」實際是綠）。⚠ **不要只把檔 `git add` 就當修好了**——真正的缺口是「回歸網對自己缺件毫無防禦」，而 `.gitignore` 或誰忘了 add 都會再造一次。⚠ **2026-09-04 06:53 起另一則 session 正在動手**（`tests/test_doc_integrity.py` 與 `tests/run_hook_tests.py` 都在被編輯中，回歸網總數已由 1723 升到 1783）⇒ **下面②多半已經被做掉，接手前先看現況**；③那條自檢是本票真正耐久的部分。 | ①先答一個問題再動手：**回歸網應該在缺件時硬紅還是拒跑**？現況是「死在 import」，看起來像硬紅但訊息是 traceback 不是判定 ②`git add` 兩支檔，並把 `test_doc_integrity` 接進 runner（接之前先看它為什麼 exit 1，別接一條長期紅進去——長期常駐的紅等於沒有紅）③補一條自檢：列出 runner 依賴的測試模組，逐支 `git ls-files --error-unmatch`，有未追蹤的就 FAIL 並點名 ④**先證明它會紅**——把任一支已追蹤的測試檔暫時 `git rm --cached` 必須被抓到 ⑤修 `test_doc_integrity` 那條驗不出來的變異 | 我 | 閘門 | 高 |
| 🔧 **U-1 `.py` 台帳落後兩筆，測試每次都印「已償還但台帳未更新」提醒**（2026-09-05 跑 `tests/test_harness_config.py` 時看到·**不是本輪造成的**） | `test_u1_debt_does_not_grow` 是「只准變少」的閘門，所以字面值已清掉但 `_KNOWN_U1_DEBT` 沒減仍算 PASS、只印一行 ※ 提醒：`dashboard/gen_cost_panel.py` 與 `dashboard/subagent_stats.py` 各有 1 筆 `d--IT-department` 已不存在。⚠ 不修的後果：那行提醒每次都印、沒人看；而台帳虛高等於**閘門的下限比實況鬆 2 筆**——有人再把同一個字面值寫回去，閘門會靜默放行 | ①把 `tests/test_harness_config.py` 的 `_KNOWN_U1_DEBT` 那兩筆刪掉 ②**先證明它會紅**：在其中一支暫時寫回 `d--IT-department` 字面值必須紅，還原後回綠 ③跑 `py -3 -X utf8 <harness>	ests	est_harness_config.py` 確認 ※ 提醒消失 | 我 | 閘門 | 低 |
| 🔧 **看板結構守門把「待辦文字裡提到某個路徑」誤判成「說明被塞進 HTML」**（2026-09-04 開工時撞到·**不是這輪造成的**） | `tests/test_dashboard_structure.py:78` 的判準是「某個美術庫目錄路徑不得出現在 html 裡」——**裸子字串比對**。⚠ **那個字串本列刻意不逐字寫出來**：寫了就會被產生器灌進 html、當場再觸發一次同一條守門（本列第一版就是這樣紅的）。而 `dashboard/harness-dashboard.html` 是產生器把 `TODOS.md` 的待辦文字**逐字**灌進去的產物，而第 60 行附近那條待辦正文就提到那個路徑 ⇒ 只要有人在待辦裡寫到它，守門就會紅。⚠ **今天 12:40 還是綠的、13:23 重產後變紅**，而那個 html 已 gitignore（`.gitignore:46`）⇒ **git 不會提醒、diff 也看不到**，只會看到回歸網無緣無故多一條紅。⚠ 這與另一條線同日踩到的是同一個母題：**把「引用／示範用的字面值」當成「真的壞掉的實例」**（對方的例子是 `skills/adversarial-review/SKILL.md:119-120` 兩行示範用的寫死路徑不能換成佔位符）。⚠ **不修的後果**：回歸網長期停在 1768/1770，而長期紅燈會被讀成「本來就這樣」，下一次真的把說明塞進看板時沒有人分得出來 | ①判準要能分辨「產生器灌進來的待辦文字」與「真的被內嵌的說明」——最省事的做法是只掃待辦區塊以外的 html，或改成比對 `visual-lib/README` 的實際內容片段而不是路徑字串 ②**先證明它會紅**：把 README 的一段真的貼進 shell html 必須紅，只在待辦文字裡提到路徑必須綠 ③順帶確認同一支測試裡還有幾條同形：`test_dashboard_structure.py:64` 那兩個「總覽舊區塊已刪」的判準也是裸子字串比對，**同樣不逐字寫在這裡** ④**判準要一般化，不要只治這一格**（2026-09-04 兩則 session 各自撞到一次後歸納）：凡是「清掉某種字面值」的守門，**它自己的說明文件必然是最大的偽陽性來源**——票的描述、規範的舉例、skill 裡示範「錯的長怎樣」的那兩行，都會逐字帶著那個值。當天的兩個實例：`skills/adversarial-review/SKILL.md:119-120` 的兩行寫死路徑**不能換成佔位符**（它們在示範「正斜線讀得到而且不報錯、反斜線才擋得住」，換掉之後兩行會長得一模一樣）；本表另一列的空全形括號**不能補**（那是該列在引號裡示範症狀）。⇒ 任何這類 lint 的第一條判準都是「**先排除引號、行內碼與圍籬區塊，再掃**」，而且要配一條反向斷言證明沒挖過頭——挖過頭之後它會安靜地永遠綠。`tests/test_doc_integrity.py:199-202` 是現成的形狀 | 我 | 閘門 | 中 |
| 🔧 **三組 skill description 互相吸走對方的觸發，L3 每次都會扣分**（2026-09-04 VB-6 量到後開票） | VB-6 重測（30 支 roster）：正例 30/32、反例 17/18，三個失分**全部**是描述重疊或過寬，**不是案 B 造成的**——那五支的 `description` 行拿 2026-08-16 的 `650542f4`（**IT repo 的 hash，不在 harness**）逐字比對雜湊相同。三組是：①`data-incident` 與 `platform-resource-rules` **都寫了「跑回舊值」**，「這個設定又跑回舊的值了」被後者吸走 ②`ui-rules` 的觸發清單列了「按鈕」，「這個按鈕點了沒反應」（可重現的互動 bug，該走 `diagnose-bug`）被它吸走 ③「這個 Excel 匯入系統」沒有動詞，`dry-run-migrate` 沒接到——這一項是題目歧義不是描述問題。⚠ **不修的後果**：L3 會長期停在 94%／94%，而**長期不會滿分的指標會被讀成「本來就這樣」**，下一次真的因為改壞而掉分時沒有人分得出來。 | ①先決定要不要修——改 `description` 會同時觸發 metadata probe 與 L3 題庫重測，是一個新的決定不是順手 ②真要修：`data-incident` 的「跑回舊值」加上「事件已發生在過去、無法重現」的限定；`ui-rules` 的元件清單把「按鈕」改成「按鈕**外觀**」之類的視覺限定 ③改完**必須重跑** `py -3 <harness>\eval\run_triggers.py --prompt` 交給不共用脈絡的 subagent 判題、再 `--score`，並把新數字寫回計畫書當基準 ④⚠ **不可拿 7/28 的「32/32、18/18」比**，那是 10 支 roster 的舊實驗 | 待判斷 | 流程 | 中 |
| ✅ **指令層 `.md` 的寫死路徑閘門已補上**（2026-09-04 開票·同日結案） | B-3 是一次性清帳，而 `.md` 這一維度原本無台帳無測試——`tests/test_harness_config.py` 的 U-1 台帳只掃 `.py` 與 `.json`。**已補第三個維度** `test_u1_debt_md_does_not_grow`：掃 harness 的 `global/hub`／`agents`／`cursor-agents`／`skills` 與各專案（從設定讀，不寫死）的 `.claude/skills`／`.claude/rules`／`PROJECT_CONTEXT.md`／`CLAUDE.md`／`.cursor/rules`，凍結現況、只准變少。⚠ **凍結的 19 處要看清楚是什麼**：①`global/hub/00-preamble.md` 1 處＝佔位符定義本身，**單一真相刻意保留**，修掉它其他 52 處就失去展開依據 ②`skills/` 四支共 18 處屬 B-4 範圍（`adversarial-review` 9／`context-health` 6／`design-spec` 2／`session-workflow` 1）——**B-4 已結案但這 18 處沒改**，且其中至少兩處是**內容本身不是指路**（`adversarial-review` 有兩行在示範「正斜線讀得到且不報錯、反斜線才擋得住」，那兩個字面值就是那一課的證據；`session-workflow` 那行是刻意的雙形並列）⇒ **要不要清是一個新的決定，不是順手**。⚠ 判準與另兩維不同：`.md` 只記處數不記值集合（字面值只有 harness 根一個），代價是**同檔案內搬位置而處數不變會看不見**。 | ①**已完成**，紅法實跑過：在 `skills/session-workflow/SKILL.md` 與 IT 的 `.claude/rules/html-structure.md` 各貼一條寫死路徑 → 兩條判準各自轉紅（前者「變多了」、後者「新增檔案」），還原後回綠；另有 4 條合成測試驗偵測器本身（抓得到 skill 正文／放過 `command:` 行／不掃計畫書／乾淨行不誤計）②回歸網 1722/1723（+6 條全是本次新增），唯一紅是既有的 git hash 誤判 ③**那 18 處已由 user 2026-09-04 裁決「逐支讀過、能改的改」**，逐處清單與紅法寫在 `.scratch/handoff/20260904-shared-skill-path-cleanup.md`（含已知不可換的兩行與理由）——改完要同步更新 `_KNOWN_U1_DEBT_MD` 的處數 ④✅**2026-09-05 已清（commit db1d7b3）**：18→10 處，只換「指路」不換「內容」——`adversarial-review` 9→2（7 處 ```text 圍欄指令示例換 `<harness>`）、`context-health` 6→5（:182 散文指路）；留下的 10 處各自被一個機器兜底逐字讀取（V-14 的 `_referenced_scripts()`、`eval/check_contracts.py` 的 `PATH_RE`、正反斜線那一課的證據），換掉會靜默失效，逐條理由與兩次實測踩雷寫在 `tests/test_harness_config.py` 的 `_KNOWN_U1_DEBT_MD` 註解。驗證：38/38、24/24、兩支 skill 契約數前後一致 7／13 | 我 | 閘門 | — |
| ⚠ **`harness.config.json` 被留在 `--init` 範本態約 12 小時，跑回歸網沒把它修好**（2026-09-03 開票·**根因未查到**） | 2026-09-03 00:12 到 12:00 之間，`harness.config.json` 的內容變成 `--init` 產的範本（`currentProject` 指向 `D:\你的專案`），真資料被留在 `harness.config.json.v15bak5`。後果：`rulefile/check_layers.py` 與 `dashboard` 的 `discover_projects()` **全部拒跑**（U-2 生效，行為正確，但沒有人會注意到——那個檔 gitignored，git 不提醒）。⚠ **這正是 D-1 在談的形狀**：裝了等於沒裝，而且靜默。⚠ **本輪跑了兩次回歸網都沒修好它**（跑完仍是範本、`v15bak5` 仍在）；user 拍板後手動用備份還原，**接著連跑三次回歸網都正常**（自癒訊息有印、跑完 config 完好、無殘留備份）⇒ **同樣的起始狀態、不同的結果，我重現不出來**。`tests/test_harness_config.py:54` 的 `swapped_config()` 註解自己寫過同型事故（2026-08-24 → 08-28 硬殺後停在中間態四天）。⚠ **回歸網 1640/1640 全綠與環境壞掉同時成立**——測試沒有一條會看現行 config 是不是範本⚠ **09-03 下午新證據：非決定性已重現，但不是併發**。同一份程式連跑兩次全套，兩次都是「1659/1662」卻**紅在完全不同的 3 條**（第一次 WARN 輸出通道：`state/pending_warn.*.tmp` 改名 PermissionError＋JSONDecodeError；第二次 /context-health 可用性：子進程讀到 **`schema 是 999`** 的假設定而拒跑）。**兩組紅單獨跑都全綠**（`test_warn_channel.py` 23/23、`test_context_health_skill.py` 連跑兩次各 24/24）。已排除跨 session 併發：查過父進程鏈，當時唯一的另一個 `run_hook_tests.py` 是我自己那一輪。⇒ **這是套內的順序／時序敏感**，而 `schema 999` 的來源可精確指到 `tests/test_harness_config.py:342`（設定檔借用區塊裡寫的假資料）——代表**借用視窗有洩漏到別支測試身上**，正是本票在找的形狀。⚠ 但我這一輪**沒有繼續追根因**（範圍是補偵測），而新補的收尾比對兩次都 PASS ⇒ 洩漏發生在套內、收尾前已還原，所以新偵測抓得到「跑完還壞著」，**抓不到「跑到一半壞過」**。 | ①✅**已做**（2026-09-03 下午）：`tests/test_config_residue.py` 補了四條判準（檔還在／`currentProject` 指向真目錄／沒有**新增**的 `harness.config.json.*` 備份／內容與開跑前逐位元組相同），接進回歸網開場拍照＋收尾比對。**判準刻意不是「不准有備份檔」**——目錄裡 6 個備份有 2 個是人手動留的證據，寫成絕對零會逼人刪不該刪的東西（票 10-5 同型）。**已證明會紅**：模組自檢 7/7（含對照組與四個變異，變異③④互不誤紅）；接線層兩個真實變異各自轉紅（洩漏一個備份檔 → 點名檔名；把 config 寫成範本態 → 同時報②④） ②要重現得先知道 `swapped_config` 的 finally 在什麼情況下不執行；**已排除**：併發（`peek_sessions.py` 顯示當時無其他活躍 session）③殘留檔清點（09-03 下午實測 **6 個**）：`.template-rescued-20260903`（122 bytes 零資訊範本，可刪）／`.v15bak2/3/5-rescued`（真資料，測試自癒留證）／`.bak_20260903_001234`、`.broken-20260828`（**人手動留的，別當殘留刪掉**）。⚠ **09-03 12:48 user 點頭後已刪 `.template-rescued-20260903`（零資訊範本），現存 5 個**。其餘 4 個（3 份自癒留證＋2 份人工，扣掉已刪那份）**刻意留著**：自癒留證正是上面那條「借用視窗洩漏」的現場，追根因時要用 | 我 | 閘門 | **高** |
| 🔧 **分層標註閘門掃不到 `tools/`**（2026-09-03 開票） | `rulefile/check_layers.py:42` 的 `SCAN_DIRS` 是 `hooks/rules`／`hooks`／`dashboard`／`eval`／`rulefile`／`reviewer`——**沒有 `tools/`**。那底下有數十支核心層腳本（`backup_global_config.py`、`skill_watch.py`、本輪新增的 `wiring_probe.py` …），一支都不在覆蓋率分母裡。⇒ 「分層標註 73／73、未標 0」這個綠燈**看起來像全庫覆蓋，實際上整個 `tools/` 在範圍外**。⚠ `tests/test_layer_marks.py` 有一條「掃描範圍沒縮水」的守門，但它守的是**不准變小**，守不到**一開始就沒涵蓋** | ①決定 `tools/` 要不要納入 `SCAN_DIRS`（納入的話會一次冒出數十支未標，要逐支補 docstring 首行）②**先證明它會紅**：納入後未標的支數必須 > 0 且測試轉紅，補完才轉綠③若決定不納入，要在 `check_layers.py` 檔頭**明寫為什麼 `tools/` 不算**——現在沒有這句話，看的人會以為它掃全庫 | 我 | 閘門 | 中 |
| ✅ **MIS-install 沒有逐條基準，`bloat_snapshot.json` 的 key 還是改名前的**（2026-09-03 開票·同日訂正·同日結案） | **已修**：user 跑 `--write-snapshot --project MIS-install` 寫進基準（逐條資料 20 筆），我接著刪掉兩把孤兒 `AI-Projects…` key（**只在對應的新 key 都已存在時才刪**，否則會把唯一的基準刪掉）。現在 5 把 key 全部對應現存專案。⚠ **開票時的宣稱是錯的、已訂正**（見 `419b96d`）：失明偵測不需要基準、沒基準的失敗方向是安全的，真正少掉的只有「既有條目被加長」這一種偵測。⚠ **驗收判準我也寫錯過一次**：原本寫「趨勢欄不再是『基準』」—— 那是歷史筆數機制、與快照無關，寫完基準它照樣印「基準」。正確的驗法是看快照裡有沒有逐條資料。 | ⏳ **還沒做的最後一步**：`--write-snapshot` 之後沒有實際跑過「加長既有條目 → 守門必須喊」的紅測（那要改 MIS-install 那個 repo 的 CLAUDE.md，跨 repo 動別人的活檔）。下次有人在那個 repo 開工時順手驗：加長某條既有規則 200 字，`py -3 rulefile/check_bloat.py` 必須報「加長」；不喊就是基準沒接上。⚠ **這個紅測必須從 MIS-install 那個目錄跑**——`diff()` 的 `only_project` 過濾（`check_bloat.py:1075`）會在 harness 目錄下把它整列濾掉，得到假陰性 | 我 | 閘門 | 低 |
| 🔧 **HND-1 的失效引用偵測看不到「巢狀 repo」，`commit d61c68f7` 是它唯一活著的誤報**（2026-09-04 開票） | `hooks/rules/hnd1_handoff_lifecycle.py` 的 `_sibling_repos()` **只掃父目錄的同一層**找鄰居 repo。`d61c68f7` 這顆 commit 在 `IT-department\SOP` 底下的**巢狀** repo 裡（SOP 是 IT-department 內部另一個 git repo）⇒ 掃不到 ⇒ 一筆完全正常的引用被報成「已經不存在」。⚠ **這一筆從 2026-09-03 就被判定是誤報並刻意留著不動**，理由是「沒有新資料前，動它的正則等於在猜」；2026-09-04 那份交接檔搬進歸檔區之後**它不再被報，但盲點本身一個字都沒修**——不要拿「清單清空了」當這件事解決了的證據 | ①`_sibling_repos()` 要能往下找一層巢狀 repo（掃到 `.git` 就算，設遞迴深度上限免得掃爆）②**先證明它會紅**：造一個巢狀 repo、在交接檔引用它的 commit，修之前必須被誤報、修之後必須不報 ③反向也要驗：真的不存在的 commit 仍然要報得出來（放寬判準是這條規則歷史上三次精準度崩掉的共同成因） | 我 | hook | 低 |
| ✅ **【已決 2026-09-04】看板派工頁的「白派／ROI」——user 裁定：拿掉這格，承認量不出來**。`dashboard/harness-dashboard.shell.html` 的 `白派／ROI 尚無獨立指標` 改成「只列量得出來的：派了幾次、去了哪裡」。理由：「白派」要判斷「派出去的結果有沒有被採用」，那是語意判斷，機械量不出來；留一格永遠寫著「尚無指標」，只是每次看到都提醒一次做不到。⚠ **`dashboard/visual-lib/` 只有 README 那半不在本決定內**：缺的是真的 IB 參考截圖，而原交接檔硬限制寫明不要生成假 IB UI、不要把截圖進 git ⇒ 那是人要做的取材，要做再另開。原記錄： | `dashboard/harness-dashboard.shell.html:2189` 至今寫著 `白派／ROI 尚無獨立指標`，五問模殼的其他格都有真數字、只有這格是說明文字。⚠ **卡住的不是實作**：要先有人定義「白派」怎麼算（派出去但結果沒被採用？回報「都正常」但問題還在？同一件事重派？）與 ROI 的分子分母（省下的主 session token vs subagent 成本？）。定義沒定之前寫任何公式，都會產出一個看起來合理、但沒人能反駁也沒人敢用的數字——正是本 repo 反覆記著的「看起來合理的錯誤」。⚠ 另一半 `dashboard/visual-lib/` 至今只有 `README.md`，缺的是**真的 IB 參考截圖**，而原交接檔的硬限制寫明**不要生成假的 IB UI、不要把截圖進 git** ⇒ 那一項本質上是人要做的取材，不是模型能補的 | ①先回答「白派」與「ROI」各自的算式與資料來源（我）②定義定了才動程式，且**只准 JS／產生器寫數字**，不要在 html 手填第二份（原交接檔硬限制）③visual-lib 的參考圖要不要留、留在哪（不進 git），一併決定 | 待判斷 | 看板 | 低 |
| ⏳ **HND-1 交接檔生命週期已轉正，但誤觸率從沒被判讀過**（2026-09-03 上線·同日 10:53 轉正） | 動機是實測：22 份交接檔只有 4 份帶結案標記，2 份是把狀態塞進檔名（`-done`／`-verified`）—— 人本來就在標，只是沒有欄位可標。**已做**：①`hooks/rules/hnd1_handoff_lifecycle.py` 掛 Stop，兩欄分開報（開著很久／引用已失效）②`tools/archive_handoff.py` 搬結案的進歸檔區，預設只印計畫、`--move` 才動檔、同名不覆蓋 ③寫交接檔那支技能加 `status:` frontmatter 與結案段。**精準度走過三版**：裸檔名也算 → 24 筆大半誤報；只認帶斜線 → 剩 2 筆**仍全是誤報**（指的是別的 repo）；加鄰居 repo 比對 → 0 筆誤報。**已驗**：35 條測試、七個變異各自精準轉紅（含兩個接線變異：從 REGISTRY 拿掉、applies 只讀 ctx.git）、全套 1559/1559、端到端實跑 dispatch 看到 `decision HND-1 WARN shadow:true`。⚠ **轉正前要看的是誤觸率不是有沒有用**（同 CTX-1）：這條每輪都跑，若提醒長期都是「對，我知道」，它會退化成噪音然後被整條無視 —— 而查到的先例正是「假陽性太多 → 大家把偵測關掉」。⚠ 現在的清單是 5 份「開著很久」、0 份「引用失效」；0 是誤報清零的結果，**不代表它抓不到東西**（正對照測試證明抓得到） | ①觀察一段時間後跑 `py -3 hooks/report.py` 看 HND-1 的 applies／WARN 比 ②決定轉正就在 `hooks/dispatch_config.json` 加 `"HND-1": {"shadow": false}` ③⚠ 轉正後第一次真的收到便箋時，記下「這次提醒有沒有讓我真的去結案」——那是門檻校準的唯一資料 ④那 18 份還開著的檔要不要逐份標狀態，是另一個決定，不要跟轉正混在一起 ⚠ **09-03 盤點：部分完成**。②**已做**——`dispatch_config.json` 有 `"HND-1": {"shadow": false}`，且 11:02:32 有一筆 `shadow:false` 的真實 WARN 佐證它生效（不是只改設定字）。①③**沒做**：誤觸率判讀在檔案上零痕跡，而且 **shadow 觀察窗只有 02:09–02:25 共 16 分鐘、14 次 applies 就轉正了**——「觀察一段時間」這個前提實際上沒發生。轉正後第一筆便箋**還躺在待投遞區沒送達**，被它指的死引用原封不動 ⇒ 票上說「門檻校準的唯一資料」目前是空的。④**做了，但正是票文交代不要混在一起的那件事**：同一顆 commit 同時轉正＋逐份標 status，還把 25 份檔的 mtime 全刷成 09-02/09-03 ⇒ **「開著很久」從 5 變 0 是分母被重置、不是存貨解決**（門檻 7 天 ⇒ 2026-09-10 之前結構性恆為 0；4 份 08-25/08-26 的檔至今仍 `status: open`）。⚠ **票上三個數字都過期**：22 份／18 開著 → 現況 **25 份／13 open**；「0 份引用失效」只對 harness 自己那個目錄成立（**02:10:17 就有一筆引用失效 WARN**，掃描目錄跟著 cwd 的 repo root 走，票文沒標是哪個 repo）；全套 1559 → 轉正 commit 自己寫 1577，**兩個數字都未經本輪重跑**。  ⚠ **09-03 傍晚：③要的那筆資料到手了，答案是誤報**。轉正後第一筆真的送達的便箋指 `20260903-d1-round5-dispositions.md` 引用 `state/skill_watch_baselines.json` 已不存在。**檔確實不存在（全庫也找不到同名檔），但它不是失效的引用**——那一行在該檔的「未完成／刻意沒做」節底下，寫的是「把 `baselines` 搬到 `state/skill_watch_baselines.json`」，是**還沒做的事的目標路徑**，而且 user 已拍板不在那條線做。⇒ 誤報率目前 **1/1**。⚠ 現有的兩道防護都擋不住這一型：父目錄 `state/` 存在、鄰居 repo 也沒有同名檔，兩條都通過。**這是結構性的**：交接檔的「未完成」節天生在講還不存在的東西。⚠ 判準要改的話只有一個安全方向：**掃描時跳過「未完成」「刻意沒做」「等人點頭」那幾節**，不要改成看檔名或放寬正則（票文自己記著三版精準度都栽在放寬上）。  ⚠ **09-03 夜間：三版精準度追加，兩版是這輪意外撞到的、一版是計畫內的**。①未完成節誤報已修（切節，不動正則）②**同一晚重新盤點又撞到第二個雙真相**：`merged-20260902-d-drive-p4-done.md` 檔名說結案、frontmatter 寫 `status: open`，首版先看檔名 ⇒ 誤判結案、`archive_handoff.py` 差點把一份還開著的檔搬走 —— 已改**欄位優先於檔名**③**同一晚第三個洞**：今天第一次真的跑 `--move`（20 份），還開著的檔引用另一份被搬進 `archive/` 的交接檔，舊路徑當然找不到 ⇒ 已加歸檔區豁免（只對 `.scratch/handoff/` 路徑生效，不是任意檔名撞名就放行）。**三版都雙向變異驗證過**（少防護會漏、防護過寬會誤放）。⇒ **真實語料重新盤點**：6 筆失效降到 2 筆，剩下 2 筆是**已知既有殘留**、不是這三版造成的新問題——`state/mirror_sync_failed.txt`（另一則 session 當晚已判過是誤報，兩份交接檔都寫著檔案消失是預期結果）、`commit d61c68f7`（引用的是巢狀在 `IT-department\SOP` 底下的另一個 repo，`_sibling_repos` 只看父目錄同層、找不到巢狀 repo）。**這兩筆留著不動**——沒有新資料前，動它們的正則等於在猜。 | 我 | hook | 中 |
| ✅ **User Rules 對帳表 2026-09-03 那列的雜湊是「組」出來的，不是量到的**（2026-09-03 開·同日 12:1x 結案） | **已結案**：09-03 12:1x 人實際貼上並倒出 live 正文（`.scratch/user-rules-live-20260903-v2.md`），與產出檔逐字元相同、正規化 SHA 皆 `764c3b8a…`；該列六欄已全部用量到的值填滿，`b72af56a…` 那個組出來的值已被取代並在「現況」節記下來由。附帶查到兩個欄位設計缺陷：SHA8 欄貼前貼後同值（`97eece54`）分不出成敗；產出檔雜湊欄會因重產而過期。兩點已寫進 `global/user-rules-reconcile.md`「三個踩到的坑」。 | ①腳本改成**收一個檔案路徑當參數**，不內建任何正文來源 ②驗改對了沒：**餵兩個不同的檔進去要得到不同的雜湊**，餵什麼都一樣就是還在讀內建來源 ③真值只能靠**新開一則 Cursor 對話**（它才載得到新正文）把自己的 `<user_rules>` 自訂規則正文**原樣寫成檔**，再對那個檔算 ④表上 2026-09-03 那列的「本次差異落點」欄補一句來源標註（**不要刪值，標註就好**） ⚠ **09-03 盤點：部分完成**。①②④**已做**——腳本現在只吃參數路徑、repo 內無任何程式碼再引用內建正文；實跑兩個不同檔得到兩組不同雜湊，`--self-test` 也過；表上來源標註在、原值未刪（但**該檔目前未提交**）。③**沒做**：表上「貼上前」那格仍等於磁碟組出檔的雜湊（實測逐字相同）⇒ **基準沒被真值取代，只是被加了註解**。⚠ **09-03 訂正**：①判準已改寫成「用完整 SHA256」（見左欄②）②對帳表被刪掉的守門句已補回檔頭——原句只管「貼上後」那三格，現在補的是**「貼上前」那格不得由模型代填、也不得由本機腳本拼正文再算**，那才是 ③ 真正缺的守門。 | 我＋Cursor | 工具 | 中 |
| ✅ **【已解決 2026-09-04】常駐層預算：係數已收斂成單一來源**：`hooks/rules/ctx1_resident_budget.py` 改成動態載入 `rulefile/resident_budget.py`、呼叫 `limit_for()`，不再自己存 `GROWTH_RATIO`／`GROWTH_FLOOR`（走的是原票②「讓兩處都 import」那個方向，沒有升格進 `hooks/contract.py`，因為那會讓產生器那條動態載入路徑多一個對閘門層的相依，票上原本就沒交代要怎麼處理這個副作用）。`eval/check_structure.py` 依原票判斷維持不收。`tests/test_resident_budget.py` 的跨副本檢查改成「CTX-1 有沒有偷偷加回副本」＋「真的呼叫 `_rb.limit_for()`」兩條斷言，`tests/mutations/mutate_budget_coeff_parity.py` 三個變異全部證實會紅、還原雜湊一致；`tests/test_ctx1.py` 27/27、`tests/test_checks_failopen.py` 25/25、全套 `run_hook_tests.py` 綠（唯一失敗是已知、與本次無關的舊誤報）。原記錄：常駐層預算：係數 2 份、棘輪形狀 3 份（2026-09-03 開票·同日訂正票名） | **⚠ 這一列 2026-09-03 訂正過票名。原本寫「係數有三份副本」是錯的**，兩件事要分開：**係數 `1.10`／`800` 只有兩份**——`rulefile/resident_budget.py:32-33`（新，產生器那條路徑用）與 `hooks/rules/ctx1_resident_budget.py:86-87`（CTX-1，掛改檔工具），這兩份管的是同一件事（常駐層 bytes），漂開的症狀是兩條路徑判準不一致而且都不報錯。**第三處 `eval/check_structure.py:311-320` 抄的只有棘輪形狀 `ref=max(prev, last_warned)`**——它的係數是自己的 `TOKEN_SURGE_RATIO = 1.50`（`:56`）、**沒有下限那一邊**，管的也是別的東西（skill description 的 token 突增，不是常駐層 bytes）⇒ **照舊票面「三處一起改係數」會把 1.50 綁成 1.10，改壞它的門檻語意**。本輪刻意不收斂：CTX-1 是活的閘門、有自己的回歸網，在補洞的同一輪重構它會讓「洞補好了沒」變得無法歸因 | ①**只收斂係數那兩處**：把 `1.10`／`800` 與棘輪升格到 `hooks/contract.py`（該檔檔頭 `:226-227` 明講共用邏輯要升格、不准互 import 私有函式），或讓那兩處都 import `rulefile/resident_budget.py`。⚠ **`eval/check_structure.py` 不要一起收**——只有形狀相同，值與語意都不同 ②**先證明它會紅**：改一處係數，三處的測試都要有反應 ③驗收：`py -3 tests/run_hook_tests.py` 全綠，且 `tests/test_resident_budget.py` 的變異測試仍轉紅 ⚠ **09-03 盤點：仍開著**。一處都沒收斂：`contract.py` 沒有係數，三處也沒有任何 import 關係；被指的那支自己的檔頭到現在還寫著「收斂那兩份登記在 TODOS.md」。⚠ **原本寫的「跨副本零偵測」已於 09-03 解掉**：`tests/mutations/mutate_budget_coeff_parity.py` 與 `tests/test_resident_budget.py` 已補上跨副本一致性測試，兩份係數漂開時會紅。**剩下的是收斂本身**——兩份仍各寫各的，測試只是讓漂開會被抓到。⚠ **09-03 訂正**：票名與左欄①已改寫成「係數 2 份、棘輪形狀 3 份」，本欄原本的訂正建議已套用。⚠ ①有個票上沒寫的副作用：被指的那支是產生器用動態載入方式吃進去的（刻意不進 `sys.path`），改成從 `hooks/` import 會讓產生器那條路徑多一個對閘門層的相依，票上沒交代這要怎麼處理。 | 我 | 閘門 | 中 |
| ✅ **【已解決 2026-09-04】** `tools/measure_skill_descriptions.py` 已補：唯讀，涵蓋 global／project／platform 三層，輸出 chars 與 bytes 兩欄。原記錄：沒有任何腳本能量 skill description 的長度（2026-09-03 稽核角色喊的） | `tools/skill_inventory.py` 只數支數不吐長度。⇒ 「描述總量超過預算會被靜默砍掉」這個機制**永遠只能靠手抄**，而 `TRIGGER_MECHANISMS_REFERENCE.md` 的整個立論就是「手抄的數字會過期兩個月沒人發現」。⚠ 現況已經因此出錯一次：文件原本只算自訂 30 支（合計 10,037 bytes），漏掉平台與外掛 **24 支**，把風險低估將近一半；而且「字元」與「bytes」混用（`ui-rules` 目測 182 字元、526 bytes） ⚠ **09-03 盤點實例**：這輪稽核就因為沒有這支腳本，判不出描述預算那列的兩個 bytes 數誰是現值——唯讀閘門不放行行內 python，harness 內又沒有量測腳本，只能把兩個數字標成互相矛盾。 | ①在 `tools/` 寫一支唯讀量測腳本，涵蓋 global＋project＋platform 三層，**輸出 bytes 與字元兩欄**（混用是這次出錯的成因）②要能被唯讀角色跑（放 harness 底下、不寫檔、不吃 `-c`）③驗收：拿它的輸出回填 `TRIGGER_MECHANISMS_REFERENCE.md` §1.1(a)，並與 `/context` 的 Skills 列對照 | 我 | 工具 | 中 |
| ✅ **【已解決 2026-09-04】** `tools/skill_inventory.py` 預設從「寫」改成「不寫」，要寫回需帶 `--write`（該旗標已在 `agent_readonly_gate.py` 既有的 `--write*` 黑名單裡，不用另外改閘門）；新增回歸測試 `tests/test_skill_inventory_write_gate.py`。原記錄：唯讀閘門放行了會寫檔的腳本（2026-09-03 實例） | `hooks/agent_readonly_gate.py` 的放行判準是「在 harness 底下的既有 `.py` ＋ 不帶已知寫入旗標」。`tools/skill_inventory.py` 兩條都過，**卻會自己回寫** `SkillViewer/platform_skills.json`——稽核角色這輪就意外寫了一次（+88/-38，內容是把 8/22 舊快照更新成 9/03 實況，**判定不需還原**，但那是運氣好）。⇒ 「唯讀角色」這個宣稱有洞，而洞的形狀是「腳本自己決定要不要寫」，旗標黑名單抓不到 | ①維護一張「已知會寫檔的 harness 腳本」名單，或讓這類腳本預設 dry-run、要寫檔得帶明確旗標 ②**先證明它會紅**：讓唯讀角色跑 `skill_inventory.py`，必須被擋或必須不寫檔 ③順手決定 `skill_inventory.py` 的回寫要不要改成 opt-in | 我 | 閘門 | 中 |
| ✅ **【已解決 2026-09-04】user 裁定「另寫唯讀查詢腳本」**：新增 `tools/file_facts.py`（唯讀，回 mtime／bytes／距今幾天，支援 `--glob`／`--older-than`／`--newer-than`／`--json`），`agents/locator.md` 規則 5 改成直接指向它。**不改任何權限設定**——唯讀閘門本來就放行 harness 底下不帶寫入旗標的 `.py`（實測 `_decide()` 回 None＝放行）。實跑當年查不到的那題（`.scratch` 逾期 >7 天）：**一次呼叫回 103 筆**，對照當年是 65 次工具呼叫／10 萬 token／6 分鐘後回報查不到。原記錄：`locator` 角色拿不到檔案的修改時間（2026-09-03 角色喊的） | `locator` 的工具只有 Read／Grep／Glob，**沒有 Bash 也沒有 stat**。派它去盤點「每份索引檔最後改於何時」時，它只能退而用檔案內容裡自述的日期（例如開頭寫「建立：2026-07-28」）——那是**寫的人手打的**，與檔案系統時間無關，而且正是會過期的那種東西。這輪它明講了這個限制，沒有假裝查到 | ①決定要不要給 `locator` 一個受限的時間查詢能力（例如允許 `Bash(stat:*)`、或另寫一支唯讀的 mtime 查詢腳本讓它跑）②或者接受這個限制，但在 `agents/locator.md` 的邊界一節明寫「查不到 mtime，要問就派別的角色」——**現在兩者都沒有，所以每次派它都會重撞一次** | 我 | 角色 | 中 |
| ⏳ **`~\.claude\skills` 整個目錄做成 Junction，不在官方保證範圍**（2026-09-03 查證） | 官方文件只承諾「`skills\` 是真目錄、底下**單一 skill 資料夾**可以是 symlink」，**從未承諾容器層**。硬證據：`claude plugin validate ~\.claude\skills` 直接拒讀（回「This path is a symlink. Component directories are read without following symlinks, so nothing here was validated」），傳真實路徑 `D:\Patrick-AI\.ai-harness\skills` 才 pass。官方 issue anthropics/claude-code#38051：v2.1.69 修 symlink 安全漏洞後，這種形態的 user-level skill **整批靜默消失**，回報者的 workaround 正是改成容器真目錄＋各支 symlink。⚠ 現在能用（15 支實測全載入、實跑呼叫 `explainer-style` 成功），但沒人承諾下一版還能用，而且壞掉的症狀是**靜默**的 | ①先備份 `D:\Patrick-AI\.ai-harness\skills` ②`cmd /c rmdir C:\Users\<USER>\.claude\skills` 拆掉容器 junction ③`mkdir` 成真目錄，逐支 `cmd /c mklink /J "C:\Users\<USER>\.claude\skills\<name>" "D:\Patrick-AI\.ai-harness\skills\<name>"` ④驗收兩關缺一不可：`claude plugin validate C:\Users\<USER>\.claude\skills` 要 exit 0，**且**開一則新對話數 skill 是否 15 支全在——只驗前者會漏掉「驗證過但載不到」 | 我 | 流程 | 中 |
| ⏳ **skill 描述常駐約 8,960 bytes，溢出時官方會靜默砍掉最少用的那幾支**（2026-09-03 查證＋實測；**已量到未溢出，風險改為「換小視窗模型」**） | 官方機制：skill listing 有預算上限＝模型 context window 的 **1%**，超過時「drops descriptions starting with the skills you invoke least」，**不報錯**。被砍掉描述的 skill 從此不會被自動觸發——**惡性循環：越少用越先被砍，砍了就更不會被用**。自訂 30 支 description 重量（2026-09-03）＝**4,357 字元／8,960 bytes**（全域 15 支 2,516 字元／4,500 bytes ＋ IT 專案 15 支 1,841 字元／4,460 bytes）。⚠ 先前寫的「10,037 字元」是把 bytes 當字元，**單位混用又錯了一次**——最長是 `to-tickets` 247 字元／247 bytes、`audit` 240 字元／636 bytes，純中文的 bytes 約為字元的 2.6 倍，兩者不可互換。本則對話只載全域 15 支（合計 5,323 字元）**沒有溢出**，15 支描述肉眼確認完整；IT-department 側**已實測**：`/context` 的 Skills 列 **5.7k tokens**，該對話視窗 1M ⇒ 預算約 10k ⇒ **沒有溢出，餘裕約 4.3k**（同畫面 Memory files 18.9k／3 檔，是 Skills 的 3.3 倍，常駐層真正的大頭在那裡不在 skill）。⚠ 預算＝視窗的 1%，**換到 200k 視窗的模型預算只剩 2k，5.7k 會被砍掉約三分之二且不報錯**——風險在換模型，不在再加 skill | ①**已做**（2026-09-03：Skills 5.7k／1M 視窗，未溢出）——複驗一律用 `/context` 的數字 ②真的溢出：調 `skillListingBudgetFraction`（例 `0.02`）或把最長那幾支的 description 砍短 ③長期解：極少被自動觸發、只靠打斜線叫的 skill 加 `disable-model-invocation: true`，它的 description 就**完全不進 context** ④⚠ 別靠感覺判斷有沒有溢出，要拿 `/context` 的數字 ⚠ **09-03 盤點：還差** 兩個 bytes 數誰是現值仍判不了：沒有量測腳本（＝本表「沒有任何腳本能量 skill description 的長度」那列），唯讀角色也算不出來。 | 我 | 流程 | 中 |
| ⏳ **規則檔被別的 session 改過，正在跑的對話不會知道**（2026-09-03 親身踩到） | `CLAUDE.md` 在對話開始時載入一次，之後檔案改了**不重載、不提醒**。實例：本則對話載到的是舊版（寫 `D:\.ai-harness\tools\push_cloud_title.py`），而磁碟上 00:08 已被另一 session 修成 `D:\Patrick-AI\.ai-harness\...`；照舊指令跑才發現檔案不存在。⚠ 而且這種失敗**很容易被吞**：管線接 tail 之後取 `$?` 拿到的是 tail 的 exit code，要寫 `${PIPESTATUS[0]}` 才是腳本的。⚠ **skill 檔案不受影響**——官方有 live change detection，skill 改動當場生效。⚠ user 原本選「開場比對」，但開場當下規則是對的、抓不到，掛點必須在**每次動工前** | ①在 `hooks/dispatch.py` 加一條規則：SessionStart 記下 `~\.claude\CLAUDE.md` 的 hash 到 `state/`，PreToolUse 每次比對，不一致就提醒重讀 ②**先證明它會紅**：手動改規則檔一個字，下一次工具呼叫必須出現提醒；不會紅就是規則沒掛上 ③fail-open：比對本身失敗一律放行 exit 0，擋住工具呼叫比漏報嚴重得多 | 我 | 閘門 | 中 |
| ⏳ **Cursor 角色副本落後 9 天，而且這一縫沒有任何守門**（2026-09-03 盤點發現） | Claude 的 `agents/`、`skills/` 是 junction，改 repo 等於改執行期；**Cursor 沒有這條線**——`cursor-agents/*.md` 要人工複製到 `~\.cursor\agents\`，而每個檔自己的第一句就寫著「那不是 junction」。**實跣不是推測**：`harness-auditor.md` 與 `visual-designer.md` 的 repo 版本 09-02 更新，live 停在 08-25 ⇒ Cursor 這九天派出去的稼核員與美編讀的是舊規格。⚠ **`sync-checker` 不查這一組**（它查的是專案前端雙目錄），沒有工具、沒有測試、沒有閘門。⚠ 別拿 `agents/` vs `cursor-agents/` 的 diff 當漂移，**那兩份的差異是刻意的**（frontmatter 與平台專屬段落）；要比的是 `cursor-agents/` ↔ `~\.cursor\agents\`。斷點已寫進 `COLLAB_HANDOFF.md`「共用層現在有哪些斷點」一節 | ①先重量：`for f in cursor-agents/*.md; do n=$(basename "$f"); diff -q <(sed 's/\r$//' "$f") <(sed 's/\r$//' "$HOME/.cursor/agents/$n") >/dev/null 2>&1 &#124;&#124; echo "DRIFT or MISSING: $n"; done` ②有輸出就把那幾個檔複製過去，先看一眼 diff 確認不是 Cursor 自己改的 ③要真的陣門就寫一支檢查工具並挂進回歸網；**先證明它會紅**（改一個 live 副本的一個字，它必須轉紅） ④順手決定 `cursor-agents/` 要不要補 `locator`，或在那側寫一行「為什麼沒有」——現状是讀的人只能猜是設計還是漏掉 ⚠ **09-03 盤點：還差** `cursor-agents/` 要嘛補查詢員、要嘛寫明「刻意不放」——現況讀的人分不出那是設計還是漏掉。 | 我 | 工具 | 中 |
| ✅ **對話改名規則已改，Cursor 雲端那份已貼、reconcile 已回寫**（2026-09-02 開·2026-09-03 12:1x 結案） | **已結案**：貼上成功並逐字元驗過（commit `c03e983`）。⚠ **前三次貼上失敗的原因仍未查明**，只排除了長度——先貼 54 字哨兵，新對話讀得到 ⇒ 儲存管道正常；第四次照逐步指示貼完整版即成功。推測是前三次沒貼到 User Rules 那個欄位，**未證實**，不要當事實往下傳。 | ①user 把 `global/CURSOR_USER_RULES.md` 去掉 GENERATED 檔頭後的正文貼進 Cursor User Rules ②貼完把同一份正文回寫進 `global/user-rules-inventory.md` 並 `git add` ③在 `global/user-rules-reconcile.md` 補一列，產出檔正規化雜湊欄填 `7cc03dee3e0a`（本次已量）④驗生效：Cursor 開新對話，看它是否主動呼叫 `rename_chat`——**沒叫就是沒吃到**。⚠ Claude 側同樣未驗：要開**新** session 才會載入改過的規則，現行對話載入的是舊的 ⚠ **09-03 盤點：還差** 四件：貼進雲端、回寫快照、補對帳表兩欄、開新對話看它會不會主動改名。⚠ 票上要照抄的那個雜湊值**已經過期**（現行是另一組），照貼會填錯。 | user＋我 | 規則 | 中 |
| ⏳ **介面注入的禁派指令：規則已交人手貼，未驗生效**（2026-09-02） | 桌面版在 system 層注入 `Do not call the AgentTool unless the user requested it`＋禁 workflow，直接覆蓋全域 §4.1「唯讀盤點一律派、不先問」。**實測不是推測**：user 一句「先實測」後派 locator 成功（47k token／13 次工具／52 秒／回報含「沒找到的」）⇒ 該條要的是 user 開口、不是硬性禁令。⚠ 要補進 §4.1 的那四行**被 auto mode 分類器擋兩次**（Edit 與 Bash 寫入都擋，理由都是修改全域設定），已備份 `CLAUDE.md.bak.20260902_dispatch_auth`、檔案本身沒動 ⇒ 2026-09-02 交由 user 自己貼。⚠ **補了也不會讓我自動派**——注入指令仍在，補的價值只在「user 開口後我不再回頭確認一次」 | ①先確認 user 貼了沒：`grep -n "介面注入" C:/Users/<USER>/.claude/CLAUDE.md`，沒有命中＝還沒貼 ②驗生效：下一則對話由 user 說一次「派角色去查」，看模型是否直接派而不再反問——**反問了就是沒吃到** ③⚠ 若手貼後仍每次反問，這條規則等於裝飾，改走「每次口頭講」那條路 | user＋我 | 規則 | 中 |
| 🔧 **`tools/link_memory.py` 沒有回歸網** | 2026-08-28 上線並實際套用在 AI-Projects（16 份記憶檔接進版控）＋ `IT-department` 的 `bootstrap.ps1` 已改成呼叫它。**四個守門是手動驗的**（沒給 `--repo`／路徑不存在／不是 git repo／已經是 junction 回 0），沒有自動測試釘住。⚠ **它會刪目錄**——回歸價值比一般工具高，而且這種東西壞掉的症狀是「某台機器的記憶沒接上」，靜默。⚠ 上線當天實跑就炸過一次：PowerShell 下印中文 `UnicodeEncodeError`（已修＋雙向驗過），**語法檢查、dry-run、單獨跑三種都照不到那一條**——回歸網要涵蓋「從 PowerShell 呼叫」這條路徑，不能只測 python 內部行為 | 造暫存 repo（`git init` ＋假的 `~\.claude\projects\<enc>\memory`）跑四個守門＋一次真的接線＋冪等重跑；再加一條「用 `powershell -Command "& py -3 <tool>"` 呼叫，stdout 要有內容且 exit 0」的端到端。**先證明它會紅**：把 reconfigure 那段拿掉，那條端到端必須轉紅 | 我 | 工具 | 中 |
| 🔧 **CTX-1 已轉正，但仍抓不到產生器那條寫入路徑** | 2026-08-28 上線（`hooks/rules/ctx1_resident_budget.py`，掛 PostToolUse 改檔工具，與 ENC-1／HTML-1／UI-1 同槽位）。**動機是實測數字**：當天一輪人工瘦身砍 653 bytes，同期另一條線加 768 ⇒ 淨 +39，趨勢線上完全看不出成效 ⇒ 手動清理追不上增長速度，要改斜率只能在寫入的當下叫。門檻 `max(基準×1.10, 基準+800)`，基準取 `rulefile/bloat_snapshot.json`（不另存一份）。已驗：回歸網 20/20＋四個變異精準轉紅（拿掉小檔底線 19/20、門檻放到不會越線 14/20、對不上基準改硬猜 18/20、拿掉全域路徑辨認 19/20）；端到端三態實跑（shadow 靜默但 event log 有 `decision WARN shadow:true`／暫時轉正輸出 646 bytes 的 `additionalContext`／還原後設定乾淨）。⚠ **已知不涵蓋**：`tools/gen_rule_hub.py` 走 Bash 寫檔、不經過改檔工具 ⇒ 產生器改大常駐層時這條不會叫，而那正是今天實際發生的路徑之一。⚠ **轉正前要看的是誤觸率不是有沒有用**（同 IDX-1）：常駐層本來就會長，若警告長期都是「這次成長是必要的」，它會退化成每次改規則都出現的噪音然後被整條無視 | ①**轉正已完成**（`hooks/dispatch_config.json` → `rules.CTX-1.shadow: false`，2026-09-03 實查），此項不必再做 ②產生器那條路徑要不要補：補的話得掛 PreToolUse Bash 並辨認產生器命令，**誤判面大**（任何 `py -3 ...` 都要判一次），先評估值不值得 ③⚠ 驗收要先造一個超標寫入再跑，**沒叫就是沒生效** ⚠ **09-03 盤點：②的前提已消失**——產生器那條寫入路徑**已經補了而且接線在**（`tools/gen_rule_hub.py:291` 定義、`:337` 在產生流程內被呼叫），不必再「評估值不值得」。這一項可回收。 | 我 | hook | 中 |
| 🔧 **WIN-1 門檻按 20 萬視窗訂，實際視窗 100 萬 ⇒ 63% 的發言都會收到便箋**（2026-08-28 轉正當天回放量到） | **實測不是推測**：單次請求合計 input 最高量到 999,128（session id `bb8d3376`，不是 git hash）後自動壓縮回 172,429 ⇒ 單次 input 不可能超過視窗 ⇒ 視窗至少 100 萬，140K＝14%。把 WIN-1 的判斷回放到 `d--IT-department` 最近 12 則對話：**84 次真人發言中 53 次會收到便箋＝63%**，最長那則 40 次發言中 37 次（92%）。⚠ 規則自己的註解寫著「140／160 每輪都講會被無視（BUDGET-1 同一病）」——140／160 有「同一則一次」防護，但 **180K 是設計上每輪再講**，在 20 萬視窗裡撐到 180K 的對話本來就快結束，在 100 萬視窗裡才剛熱身，後面八成路程一路被念。⚠ 另一半是文案：「較早的決定與檔案內容更容易被擠出視窗」在 140K／100 萬的當下**不成立**，講當下不成立的事會被當背景音，等真的 800K 危險時已經沒有可信度。**user 2026-08-28 決定維持原樣、先實地被念幾次再判斷**（不是沒發現，是刻意緩） | ①累積幾次真實觸發後跑 `py -3 hooks/report.py` 看 WIN-1 的 applies／WARN 比，並自問「這幾次有沒有一次讓我真的去交接」②要改就三處一起：`hooks/rules/win1_total_input.py` 的 `TIER_*`、`tests/test_win1.py` 的門檻常數、便箋文案那句因果（改成重送成本、或搬到真的會被擠出去的位置）③⚠ 驗收方式是回放不是感覺：拿 `C:\Users\<USER>\.claude\projects\d--IT-department\*.jsonl` 重算「真人發言中會收到便箋的占比」，目標降到個位數 ⚠ **09-03 盤點：還差** 便箋那句因果文案要改（或搬到真的會被擠出去的檔位）；回放占比要重跑——**這個數字目前不存在於任何檔案**。已量到的 87／219 是規則命中率，分母與「真人發言」不同，不能當同一件事引用。 | 我 | hook | 中 |
| ⏳ **AWC-1 裝了臨時探針，答完就要拆**（2026-08-28） | 防迴圈 A（平台重跑時帶不帶 stop_hook_active）與防迴圈 B 互相遮蔽：旗標若有帶 A 先回、B 永不觸發；旗標若沒帶則 B 觸發而 A 看不到。探針在兩個分支各記一筆到 `state/awc1_probe.ndjson`，哪個 branch 記到就知道答案，**不必拆掉任何一道防護**。目前 0 筆——要等下一次自然被擋才會有資料。 | 下次被擋後看 `type D:/Patrick-AI/.ai-harness/state/awc1_probe.ndjson`：有 branch=A ⇒ 平台有帶旗標；只有 branch=B ⇒ 沒帶、A 是死碼。得到答案後刪掉 `_probe` helper 與兩處呼叫，並更新待驗清單那一列 ⚠ **09-03 盤點：還差** 探針結論（44 筆 A／15 筆 B ⇒ 平台**會**帶旗標）目前**沒有任何落點**，先把它寫下來再拆探針；helper 與兩處呼叫都還沒拆。 | Claude | 閘門 | 中 |
| 🔧 **閘門總開關已搬到全域，但「在 harness／AI-Projects 內真的生效」沒驗到** | 2026-08-28 搬遷。原本 `dispatch.py` 只掛在 `d:\IT-department\.claude\settings.local.json`（而且 `.local` 通常不進 git ⇒ 換機器整套閘門消失且無提示），已改掛 `~\.claude\settings.json` 的五個事件、並從專案層移除同名重複（`session_title.py` 原封不動）。**已驗**：三種 payload 在 `D:\Patrick-AI\.ai-harness`／`D:\AI-Projects` cwd 下跑 `dispatch.py` 皆 exit 0 無誤報（四支【專案層】規則都有 `applies()` fail-open 第一道，且靠 `resolve_remote_ref("vm","master")` 解不出就放行）；反向測試 R4 在 IT cwd 下 exit 2 並印完整訊息 ⇒ 搬遷沒有改壞既有行為。⚠ **沒驗到的是「新覆蓋範圍真的會擋」**——那要在 harness 或 AI-Projects 開一個**新** session（現行 session 啟動時載入的是舊設定），而且要真的踩一條核心層規則。⚠ 順帶一提本次也證實 `~\.claude\CLAUDE.md`、`global\CLAUDE.md`、`global\hub\10-comm.md` 是三份各自獨立的檔，改一份不會傳到另外兩份 | 在 `D:\Patrick-AI\.ai-harness` 開新 session，故意用開放式問句結尾（不呼叫 AskUserQuestion）⇒ **AWC-1 必須擋下**；再於 `D:\AI-Projects` 開新 session 寫一支含 `import server` 而無 `server.DB_PATH =` 的 `.py` ⇒ **R4 必須放行**（專案層規則不該跨部門開火）。兩邊都要跑到，只跑一邊分不出是「生效」還是「都沒載入」 ⚠ **09-03 盤點：還差** 那兩條端到端要在**新** session 真的踩一次規則才算數。已量到的是「規則會被評估」，不是「越線會被擋」——兩者不是同一件事。 | 我 | 閘門 | 高 |
| 🔧 **收工的「逐檔驗證」全部答對，孤兒仍然掉出去** | 2026-08-27 實證。`hooks/session_title.py` 有 38 行未提交躺了兩小時（`_repo_root`＋`project_name` 改用 repo 根），期間**兩則收工各自逐檔驗證後都宣告「未提交的 harness 檔沒有一筆是我的」**（`13409e48` 說 30 檔、`1bac1ea4` 說 27 檔），**而且兩份判定都是對的** —— 它們用內容特徵比對（`git show HEAD:"$1" \| grep -c 我的特徵` ＋ `git diff -- "$1" \| grep 我的`），問的是「這截未提交的東西裡有沒有我的字」，答案確實是沒有。**沒有人問下一個問題：那這是誰的、還有沒有人要回來收？** ⇒ 每個人的檢查各自正確，孤兒正好從縫裡掉下去。⚠ **留著不是中性選項**：`1bac1ea4` 同日回報主 repo 的 auto-commit 把它做到一半的東西**收走了兩次**，還讓它 stage 的快照過期、差點回退掉別人 59 行 ⇒ 孤兒等的就是被掃進不相干的 commit，untested 且 unattributed。⚠ **結局訂正（22:5x）**：我原本判定「作者已走、沒人會回來收」——**錯了**。孤兒躺了約 2.5 小時後，另一條線用 `9ca8036`「佔位名用 repo 根、超長截斷補省略號」**刻意收走了它**（`session_title.py` 差異 38+/8- 與孤兒完全一致）。⇒ 缺口比我原本寫的小：孤兒最後有人認領，但**那 2.5 小時裡沒有任何訊號說它無主**，而且期間有兩則收工從它旁邊走過去。⚠ **同一顆 commit 順手把我的測試檔整檔帶走**（我補的 5 條 `_repo_root` 案例，`_repo_root` 原本**零覆蓋** —— 既有兩條 `project_name` 案例只斷言 fallback，整段拿掉也不會紅；我補完後三個變異精準轉紅）。測試在 HEAD 存活 5/5、87/87 綠，內容沒少，但**掛在別人的 commit 下** ⇒ 這是同日**第三例**跨線收走（前兩例是 `1bac1ea4` 回報的），這次輪到我。⚠ **同日第四例（8/28 收工時），形態比前三例更精確**：不是孤兒被掃走，是**我 `git add` 完、`git commit` 之前被插隊**。序列可重現：`git add` 兩個檔 → `git diff --cached --name-only` 印出來的卻是**別人的檔**（`SOP_PROD/…/test_issue_skip_menu.js`）→ `git commit -F` 回「no changes added to commit」→ `git show HEAD` 是別人的 commit。事後查證我的內容**沒丟**（日誌 235 行工作區與 HEAD 一致、未驗兩列都在），但分別掛在 `149734d1`（別人的收工）與 `104358e7`（別人的 pending-verify）底下。⇒ **「只 stage 自己的 hunk」這條規則擋不住這個形態**——它管的是我 stage 什麼，管不了 stage 完到 commit 之間有誰把 index 清空。真正的判準是**`git add` 與 `git commit` 之間不要插入任何指令**（我中間跑了一個 `git diff --cached --name-only` 想對帳，那一秒就被插隊了），或者改用 `git commit -o <路徑…>` 一次完成 stage+commit。 | 在收工 SOP 步驟 1 之後加一問：**逐檔驗證判定「不是我的」的檔，要再判一次「有沒有人會回來收」**。判準用現成訊號即可 —— `tools/check_before_start.py` 已經印「殘留·N 小時前寫過（沒人在動）」，只是沒有人拿它問這一題。落地形態二選一：①`check_before_start.py` 對「殘留＋無人在動＋超過 N 分鐘」的檔主動提示「這是孤兒，要認領還是登記」②`/shougong` 步驟 1 加一行檢查。⚠ 驗收要先造一個孤兒（改一個檔不 commit、關掉那個 session）再跑，**沒提示就是沒生效** | 我 | 流程 | 中 |
| 🔥 **`backup_global_config.py --restore` 無視自己 `--check` 印的逐檔方向，覆蓋較新的 live** | 2026-08-27 實地咬到。`--check` 明明逐檔印了「`CLAUDE.md` 建議 `--restore`／`settings.json` 建議 `--backup`」，但 `--restore` 把**兩個檔都**往 live 寫 ⇒ live `settings.json` 5,637→5,393，**吃掉 `outputStyle: PM-Challenger` 與另一條線剛裝的 `SessionStart` 探針 hook**。⚠ 更壞的是**它是靜默的**：輸出寫「已 --restore 到 live（原狀態：有差異）」，兩個檔長得一模一樣，看不出其中一個是逆向覆蓋。救回靠的是另一條線碰巧留的 `~\.claude\settings.json.bak.20260827_210404_teardown_probe`，**不是這支工具留的**——它自己不備份。票 04 說「方向 bug 這張票修」，修掉的只有「無旗標不寫」那一半，~~逐檔方向這一半沒修~~ → **同日 22:0x 另一條線已修**（`3aa1fde` 「備份工具加兩道閘門——方向不一致與來源較短一律拒跑」，`backup_global_config.py:138` `跨檔方向不一致時拒跑整批`，附 48 行新測試）。⚠ **我沒有實跑驗過那兩道閘門**——驗它要對 live 設定檔下寫入命令，而它正是今天被吃掉一次的那個檔。驗法：`py -3 tools/backup_global_config.py --restore` 應**拒跑並非零 exit**（現況 repo `settings.json` 比 live 少一整段 `SessionStart`），且跑完 `grep -c SessionStart ~/.claude/settings.json` 必須仍是 1。⚠ `--check` 的**建議文字**似乎沒跟著改：22:0x 實跑仍印「建議 `--restore`」，閘門擋的是寫入不是建議 ⇒ 人照著建議打下去才會知道被擋，**收工 SOP 步驟 4.0 每次都會遞這個反向建議**。⚠ **同日 21:37 第二次踩到，這次更糟——工具的「建議」本身是反的**：repo `global/settings.json` mtime 較新（21:37:33 vs live 21:34:10）所以印「建議 `--restore`」，但 repo 那份**少了 live 才有的 `SessionStart` hook**（`state\sessionstart_probe.py`）⇒ 照建議做會第二次刪掉同一個 hook。**根因：拿 mtime 當「較新」的代理，而 mtime 只說「誰後被寫」不說「誰內容較全」。**收工 SOP 步驟 4.0 每次都會叫人跑這支，等於每次收工都遞一個反向建議。 | 兩件事：①`--restore`／`--backup` 對「方向與該檔建議相反」的檔要先擋下（印出來要人加 `--force` 或指名單檔），不得預設整批同向；②寫入前一律先備份被覆蓋的那一側（`<檔>.bak.<時間戳>`），現在完全沒有；③**JSON 類的檔不要只比 mtime**——至少要偵測「一邊有、另一邊整段沒有的 top-level key」並改印「兩邊各有對方沒有的內容，需人合併」而不是指一個方向。驗收：造一個 live 較新的 `settings.json` ＋ repo 較新的 `CLAUDE.md`，跑 `--restore` 必須拒絕動 `settings.json`，且 `CLAUDE.md` 的舊 live 有 `.bak` 落地 ⚠ **09-03 盤點：還差** 寫入前備份與 JSON 逐 key 比對都沒做。⚠ 票上的驗收在備份缺席下**必然過不了**。 | 我 | 工具 | 高 |
| 🔧 **IDX-1 還在 shadow，要看資料才知道該不該轉正** | 2026-08-27 上線。`hooks/dispatch.py:350` 明訂「設定檔缺該規則 ID → 預設 shadow」，那是刻意的畢業儀式。端到端已驗：enforce 下走 `hookSpecificOutput.additionalContext` 吐完整 staged 清單（368 bytes），shadow 下靜默。⚠ **轉正前要回答的是「誤觸率」而不是「有沒有用」**：這條每次 `git commit` 都發動，而 commit 很頻繁 ⇒ 若清單長期沒有 ⚠ 標記，它就退化成每次都出現的噪音，然後被整條無視（`check_bloat` 的 WARN 疲勞同型）。判準應該是「⚠ 標記的出現率」而不是「applies 次數」。 | 觀察一段時間後跑 `py -3 hooks/report.py` 看 IDX-1 的 applies／would-block 比，並抽樣看幾則訊息裡有沒有 ⚠ 標記；決定轉正就在 `hooks/dispatch_config.json` 加 `"IDX-1": {"shadow": false}` | 我 | hook | 中 |
| 🔧 **eval L2 分不出「必須存在的依賴」與「該被產出的檔名／歷史文物」** | B-4 收尾（2026-08-26）全量 eval，L2 唯二兩條紅**都是偽陽性**：①`skills/adversarial-review/SKILL.md:98` 的 `round-N-reply.md` 是**審查者該產出**的檔名，不是既有依賴 ②`.claude/skills/data-incident/SKILL.md:99` 的 `restore_cjf.py` 寫著「實作參考**本次的**…」＝過去事故的一次性腳本，全 repo 已無此檔。⇒ 檢查器把「所有反引號裡像檔名的東西」都當成必須存在的依賴。**代價是紅燈失去意義**：真的斷線會混在偽陽性裡。 | 在 `eval/check_contracts.py` 加豁免判準（先想清楚判準本身——「上下文有『產出／參考／本次的』就豁免」可能過寬），或改成把這兩類標成第三種狀態而非 ❌ ⚠ **09-03 盤點：還差** 兩個個案各自消音解掉了，但**沒有第三種狀態**——下一支寫條件句的 skill 會再中一次。 | 我 | eval | 中 |
| **`dashboard-generators.md` 的「驗不了冪等」例外清單漏了兩個資料源**（2026-08-22 群 C 實測） | 該規則只把成本／mix 那頁列為「上游每回合都在長、驗不了連跑兩次雜湊相同」。實測連跑兩次 `refresh_dashboard.py --force`，差異落在 **`ROLES_TOPOLOGY`（2 處）＋時間戳徽章（1 處）**，**不是**成本面板——`gen_roles_topology` 從 event log 反推角色實派次數，而 event log 每跑一個工具就在長，是同一類但規則沒寫到。後果：照規則字面驗冪等會得到「不冪等＝壞了」的假結論，而真正該驗的是「**扣掉這幾個資料源之後**冪等」 | 在 `dashboard-generators.md` 的那一條補上 `gen_roles_topology` 與時間戳／session 徽章；或更好——把「哪些區塊驗不了冪等」做成一份機器可讀清單，讓冪等測試自己排除它們，不要靠人記得規則裡那句話 | 下次施工 | 看板 | 中 |
| ⏳ **`check_bloat` 對表格列把「記憶檔」指標欄算進 120 字**（8/13 量到·**8/20 複核仍成立**） | `parse_entries` 對表格列走的是整個 row 節點（全欄）：`check_bloat.py:715` 的索引列特例寫死 `u["kind"] == "entry"`，`table` 走不進去。實測合成 fixture：規則欄 30 字＋指標欄 60 字 → `chars=91`。照這個數字壓等於**刪真規則來替檔名騰位置**——正是這支工具已經為 MEMORY.md 索引列修掉的同一個病（link 前綴 76 字佔上限 63%），表格列沒比照辦理 | 比照索引列：表格列只計**第一欄**。⚠ **原本寫的驗收句已經過期**（8/20 發現）：它說「改完 §8 超標應從 4 降到 1」，但 commit `0612f9a` 把 IT-dept §8 壓到 `over_limit_count: 0`，那 4 條樣本不存在了。**新驗收要自己造 fixture**：一條「規則欄 100 字＋指標欄 60 字」的表格列，改前判超標、改後不判，且「規則欄本身 130 字」那條**仍要被抓到** | 下次施工 | 文件 | 高 |
| **R1 的 `.py` 側從未取樣過**（8/23 清帳時從已完成列救回來的殘留項） | R1 判準 8/20 改成比對整個賦值區塊後，在 `app.js` 的歷史上驗到 4/4（四筆真實事故全中）、400 個 commit 只有 4 個會出聲。但**那批樣本全是 `.js`**；新判準對 `.py` 的 `DEFAULT_*` 同樣適用、**卻一個樣本都沒驗過**。⚠ 這一項差點隨「已完成」整列被刪掉 —— 刪列前要看「誰」欄有沒有寫殘留 | 從 `db/**/*.py`、`server.py` 的歷史裡撈幾個動過 `DEFAULT_*` 的 commit，跑 R1 看會不會出聲；**先找一個「已知該被抓到」的樣本**（`.js` 那四筆就是這樣驗的），不要只跑「不會誤報」那一半 ⚠ **09-03 盤點：還差** 一個「已知該被抓到」的正樣本（本 repo 內找不到），以及**接進總表**。不接就等於沒裝——該規則至今 0 findings。 | 下次施工 | 閘門 | 中 |
| 🔧 **PR-1 在長輪次裡形同虛設 —— 2MB 尾窗找不到輪次起點**（2026-09-01 稽核抓到＋量到數字） | `hooks/rules/pr1_plan_review_marker.py:833` 是**唯一**呼叫 `note_failopen("讀不到 transcript 或找不到輪次起點（尾窗 2MB）", …)` 的規則，而 PR-1 是「唯一會擋住對話結束的閘門」。`contract.py:25` 的 `_TRANSCRIPT_TAIL_BYTES` 寫死 2,000,000——只讀 transcript 檔尾這段位元組去找「這一輪」真人訊息的起點，找不到就回 `None`，PR-1 選擇 fail-open（不擋）。實跑 `hooks/report.py` 量到：真 session 這個原因累計 66 次，且**2026-08-30／08-31 兩天真 session 的 Stop 事件 100% 命中**（10/10、2/2，分母小但比率是滿的），8/28 也到 34.7%（25/72）。⚠ 這不是雜訊——`_load_failopen_rows()` 的 `source` 欄已能把單元測試自檢（`source=test`）濾掉，這 66 次全是真 session。代價：**輪次裡工具輸出量大時，PR-1 對「這輪有沒有寫 plan.md 卻沒跑審查」直接放棄判斷，且不留使用者可見訊號** | 先想清楚再動，不要倉促調大數字治標：①先確認觸發原因是不是輪次工具輸出量真的變大（例如多派 subagent、報告貼回 transcript），跑 `py -3 hooks/report.py` 對照近期分數 ②要根治得想「有沒有辦法不用線性掃 tail 也能可靠定位輪次起點」（例如另外記錄輪次邊界的 offset，而不是每次從檔尾往回猜）③改完要能重現「觸發→修好」：造一個輪次工具輸出 >2MB 的 fixture，改前 `iter_turn_tool_uses` 回 None、改後回得出正確輪次 | 我 | 閘門 | 中 |
| **PR-1 只剩兩段未驗**（7/28 登記·**8/20 大幅縮小**） | 原本記「整條鏈未驗」，8/20 稽核在**生產設定**（`shadow:false`）的 event log 找到三段的證據：真擋（`state\events.0c9e00f3-….ndjson:74`、`:181`、`:203`）／蓋 marker 放行（同檔 `:83`／`:149`／`:186`，`bypassed:true`）／改一個字 hash 失效再擋（同一批 BLOCK 訊息就是 hash 對不上）。**仍沒證到的只剩兩段** | ①`/design-spec` 步驟 5 會不會真的產出標記——`SKILL_EVAL_PLAN.md:701` 有 PASSED marker，但查不出是誰蓋的，要走一次 M 級流程才知道 ②`tests\pr1_e2e\SAMPLE_PLAN.md` 那條路徑至今仍 `shadow:true`（`events.6de0ac63-….ndjson:32`），要在**非 `tests\`** 的 cwd 跑一輪才算生產設定 | 下次施工 | 閘門 | 低 |
| **Skill Eval L4：共用 Skill 逐支驗收**（進度以 `SKILL_EVAL_PLAN.md` §9.6b 的 B-4 隊列為準） | user 已終止規範覆核並授權施工。`adversarial-review`＝`skipped:user`；**第 1～9 列全數 `verified`**（本地批 7 支＋外部批 `grilling`·`research`）。外部批強度已調低（只做必留契約缺口＋真缺陷），四條操作定案見 §9.6b 校準節。⚠ **第 13 列的前置發現已到手**：`~\.agents\skills\wayfinder` 經獨立復算確認是**乾淨的 upstream 副本**（不抓網路即閉環），該列有 byte-exact 基準。**目前沒有 active 列** | 下一支是 `prototype`（第 10 列·bundle 有 4 檔·`SKILL.md`／`LOGIC.md`／`UI.md` 各帶一個 LOCAL EDIT）。⚠ 它與 `to-tickets` 是**僅存兩支既無免費基準、亦無 vendored 副本**的，依校準第 3 條走匯入 commit 並明記缺口。**要不要開由 user 說**；開列前先跑 `git status` 與 `tools\peek_sessions.py`（**這個 repo 現在很活躍**） ⚠ **09-03 盤點：還差** 逐支階段跑完了，卡在收尾的規範互斥。⚠ 計畫書自己有**三個並存的下一步**，該一併清掉。 | 等 user 指示 | 流程 | 高 |
| **R2（平台資源 key ＋ dump 同步守門）未動工** | 需先盤點 key 清單才能定判準；I1–I2 優先度已下修 | 先 `grep -n "ALLOWED_KEYS" -A 40` 盤出 key 清單，貼進 `HARNESS_PLAN.md` 再定守門判準 | 下次施工 | 閘門 | 低 |
| **claude.ai／Google Drive 兩個 MCP 尚未授權**（**8/20 訂正：比登記的更前面一步**） | 原記「需互動式 session 跑 OAuth」。8/20 實查 `C:\Users\<USER>\.claude.json`：`mcpServers` 只出現在 `projects.*` 底下且都是 `{}`（L870、L909），**無 top-level `mcpServers`**；`d:\IT-department` 下也沒有 `.mcp.json` ⇒ **連 server 都還沒設定**，不只是沒授權 | ①先設定 server（`claude mcp add …` 或寫 `.mcp.json`）②才在互動式 session 開 `/mcp` 跑 OAuth，或到 claude.ai 連結器設定授權。⚠ 少了第①步，第②步會找不到東西可授權 | 待你 | 流程 | 低 |
| 🔥 **harness 跨機同步仍未解，而且唯一的鏡像已靜默過期六天**（2026-09-02 量到） | 原記「已有跨實體磁碟鏡像（C 槽 NVMe · post-commit 自動同步）」——**「自動同步」這四個字在 8/27 20:47 之後就不成立了**。實測：本機 HEAD `d1a51e5` vs 鏡像 `81896ea`，**本機領先 73 顆、鏡像獨有 1 顆＝已分叉**。成因是 8/27 那顆之後本機改寫過歷史，鏡像那顆變孤兒 ⇒ 之後每次 `git push` 都被 non-fast-forward 拒絕，被 `2>/dev/null \| true` 吞掉。舊 hook 的註解自己寫著補償措施是「收工時順手看一眼」——**六天沒有人看，因為沒有東西提醒他要看**。⚠ 孤兒那顆已逐檔驗過**沒有獨有內容**：`user-rules-inventory.md`／`user-rules-reconcile.md` 與 HEAD 逐行一致，另 3 列 TODO 在本機歷史的 `9f90ba7`、`bf6e964` 裡 ⇒ `--force` 覆蓋不會失去東西。⚠ **已補上偵測（2026-09-02）**：①`tools/check_before_start.py` 新增 [4] 備份鏡像新鮮度，逐 remote 比 tip、分「落後 N 顆／已分叉／連不上／沒有 remote」四態，實跑已正確報出這次的分叉 ②`post-commit` 改成失敗時 stderr 印警告＋寫 `state/mirror_sync_failed.txt`（成功則刪），仍 fail-open 不擋 commit，實跑驗過（exit 0、標記檔內容正確）。⚠ **偵測補了，斷掉的鏡像本身還沒接回去**——force push 被權限層擋下，要人自己下 | ①**人自己跑**：`git -C D:/Patrick-AI/.ai-harness push --force backup main`，跑完 `py -3 tools/check_before_start.py --no-vm` 的 [4] 要變 `[OK]` ②`.git/hooks/` 不進版控 ⇒ 換機器或重新 clone 這個 hook 不會跟著走，正本放在 `tools/githooks/post-commit`（`18aa5aa`），**安裝這一步目前沒有任何東西會提醒**（要不要掛進 `bootstrap` 之類的入口待決）。⚠ 同一支檔還有第二個洞：**它沒有副檔名**，所以 `rulefile/check_layers.py` 與 U-1 硬編路徑掃描**兩支都掃不到它**，而它本文就帶 `D:\Patrick-AI\.ai-harness` 與 `C:\Users\<USER>\git-mirrors\...` 兩個絕對路徑。行尾那一類同病已於 `af53b20` 修掉（`.gitattributes` 改用路徑比對），但**掃描器那兩支仍是靠副檔名收檔**——驗收＝在 `tools/githooks/` 放一支帶絕對路徑的無副檔名檔，兩支掃描必須點名它③✅ **異地落點已解（2026-09-04）**：GitHub private repo `sars525200/ai-harness`，tip `08d06eb2`。⛔ user 2026-09-02 否決的 IT VM 仍在排除之列，**不要再提**。⚠ **雲端那份是清洗過的複製品，不是本機這份**——本機帶著公司內部 IP、正式網域、管理帳號名、機器名，以及 500 多筆 commit metadata 裡的公司信箱，不能交給第三方，而且推上去刪不乾淨。user 明確裁定「雲端的才清洗，本地的不動」⇒ **兩份歷史分叉、識別碼完全不同，它是備份不是可推拉的 remote**；本機全程未被改寫（實測 HEAD 不變）。更新走 `py -3 tools/push_cloud_backup.py --push`（URL 已寫進 `harness.config.json` 的 `cloudBackupRemote`），九項驗證任一項不過就中止不推。⚠ **兩層判準各有盲區**：清單判準掃規則檔列出的字串——但規則漏一條時它也跟著不掃（變異測試實證：拿掉 `<ADMIN-ACCT>==>` 八項照樣全過）；形狀判準掃私有網段 IP／email／Windows 使用者路徑、**不看規則檔**，上線後立刻抓到看板 HTML 裡顯示截斷的使用者路徑（完整字串的規則匹配不到）。⇒ **沒有形狀的字串（帳號名／機器名／公司名）漏了規則就沒有東西會叫，那一層只能靠人**。⚠ 規則檔與白名單刻意不進版控（`.scratch/cloud-export/`）：規則檔左半邊就是要清掉的那些字串，commit 進來等於把清單公開列出；**檔案不在就拒跑，不允許 fallback**。⚠ **86 個文件裡的 commit hash 引用在雲端那份是死的**——改文件會再改變 hash，循環依賴無解；新舊對照表在 `.scratch/cloud-export/commit-map-20260904.txt`。細節見 `CLOUD_BACKUP_PLAN.md`。⚠ **09-04 盤點：還差** 安裝入口 ＋ 兩支掃描器改成不靠副檔名收檔（`rulefile/check_layers.py:66` 仍靠副檔名）。鏡像已追平（`869512c`）。 ⚠ **09-04 晚：③的自動更新已接上**（commit `41c0439`）——上午推完後本機又進 13 顆、雲端那份沒有任何東西會自動推，跟鏡像分叉六天同型。現在 post-commit 呼叫 `tools/cloud_backup_hook.py --spawn` 背景推，結果落 `state/cloud_backup_last.json`、失敗留 `state/cloud_backup_failed.txt`，開工檢查 [4] 讀結果檔算落後幾顆並 `ls-remote` 核對雲端 tip。⚠ **第一次 commit 沒觸發**：另一則對話同時在做 `git stash`／`pop`，25 秒空窗內 `.git/hooks/post-commit` 被舊版蓋掉，已重裝、下一顆 commit 才是真正的端到端驗證。⚠ 規則檔另存一處：user 2026-09-05 已答「要」，副本過期無提醒 ⇒ 另開下一列。換機能用那條線（6 條 hook＋6 條角色 command＋5 處 STATE_DIR 常數）**不在這列**，走 `UNIVERSAL_HARNESS_PLAN.md` 接線器。 | 我 | 備份 | 高 |
| 🔧 **清洗規則檔的副本沒有過期提醒**（2026-09-05 user 決定另存後開票） | 雲端備份的清洗工具讀 `.scratch/cloud-export/` 四個檔：`replace-rules.txt`（不在就拒跑）、`mailmap.txt`（不在則公司信箱不換、第 5 項驗證紅）、`shape-allowlist.txt`（不在則 8 個白名單值失效、形狀判準紅）、`commit-map-20260904.txt`（工具不讀，只供災難還原查舊 hash）。這四個刻意不進版控、也不在任何備份裡 ⇒ 這台壞了，雲端那份能還原內容但**再也推不出下一版**。user 2026-09-05 決定自己另存一處（存放處：**待 user 填**，只寫地點名稱、不寫內容）。⚠ 存了之後**沒有任何程式會比對副本與正本**——規則檔每次新增樣式（09-04 一天就補過兩條）副本就靜默過期，跟鏡像分叉六天同型。 | ①user 存好後把存放處名稱填進本列 ②要不要加提醒二選一：(a) `tools/check_before_start.py` [4] 加一行——正本四檔任一 mtime 晚於 `state/cloud_export_copied_at.txt` 就 WARN「規則檔副本可能過期」，user 更新副本後跑 `py -3 tools/cloud_backup_hook.py --mark-copied` 蓋時間戳（一支檔、一個旗標、fail-open）；(b) 不加、靠人記。**先證明它會紅**：touch 正本一個檔，[4] 必須出現 WARN ③還原路徑要放回 harness 根目錄的 `.scratch/cloud-export/`，工具寫死讀那裡 | user／我 | 備份 | 中 |
| **看板新元件 `.rt-sum` 卡片列沒有人用眼睛看過**（8/08·未驗） | 交接契約那節新加的四張統計卡是**伺服端第一個發 `.rt-sum` 這個 markup 的地方**（在此之前只有角色彈窗的 client JS 在用）。CSS 是 `auto-fit minmax(118px,1fr)`，四張卡在窄欄會擠成兩行；`.rt-sum .k` 還帶 `text-transform:uppercase`（標籤全中文所以應該安全，但沒驗過）。結構測試只驗標籤平衡，量不到「看起來對不對」 | 走 `/visual-check`：開 `http://127.0.0.1:8099/` →「工作流程」→「遵循度」，截淺色與深色兩張，逐張用 Read 打開看四張卡有沒有擠行、標籤有沒有被畫成全大寫 | 下次施工 | 看板 | 低 |
| **規模欄的分母已經有了，問題換成「大量缺欄」**（8/08 登記·**8/20 推翻並改寫**） | 原記「分母還是 0，等自然樣本」。8/20 實查：`規模 S｜`／`規模 L｜` 這個 pattern 在 `~\.claude\projects\d--IT-department\*.jsonl` 有 **232 命中／49 檔**，而 `gen_workflow_compliance.py:612` 的 `scale_cutoff()` 只要有一筆帶 scale 就綁真實第一筆 ⇒ **分母早就 >0**。真正的問題是**遵循度**：473 段對帳裡「缺規模欄」54 段、「缺修改檔案欄＋缺規模欄」再 11 段 | ⚠ **原本的下一步照著做會撲空**：它說「跑 `--check` 看【規模分布】」，但那支的 `main()` 只印【專案】【宣告對帳】【階段軌跡】【覆蓋率】【交接契約】五節，**沒有【規模分布】**。要嘛補那一節，要嘛改成讀【宣告對帳】的旗標統計。這是行為問題不是程式問題，先決定「要不要把它做成看得見的指標」 | 下次施工 | 看板 | 中 |
| **DECL-1 掃整輪之後可能掃到 subagent 的訊息**（8/08·未驗） | 改成掃整輪的副作用：若 subagent 的 assistant 訊息會落進主 session 的 transcript，DECL-1 會把角色回報也當成宣告來源。`dispatch.py` 刻意不把 DECL-1 掛 SubagentStop，理由正是「角色回報不是宣告」——同一個理由在這裡沒有被守住。全 repo grep `isSidechain` **0 命中**，既有機制不做這件事，所以無法沿用 | 拿一份真的 jsonl 確認 subagent 訊息會不會出現在主 session 的 transcript 裡（`grep -c isSidechain` 在真檔上跑一次）。若會，`iter_turn_assistant_texts()` 要加過濾；若不會，把這個結論寫進 contract.py 的 docstring，免得下次有人再問一次 | 下次施工 | 閘門 | 中 |
| **Phase 3 只剩「cron 告警管道未定」一項**（**8/20 訂正：三項已完成兩項**） | 原記三項未動。8/20 實查：①`git push --no-verify` 的 deny **已補**（`~\.claude\settings.json:124`／`:137`，`d:\IT-department\.claude\settings.json:85`／`:94`），commit `cad20b81`（2026-07-30）②`styles.css` **已在** `auto_commit.ps1:9`／`:29` 兩側，commit `ee32b734`（同日）⇒ 「落在 detect_set 卻不在 verify_set」不再成立。**兩項在登記後第二天就做完了，登記簿掛了三週** | 只剩③：cron 無人看管情境的告警管道未定（全 harness grep 只命中 `PHASE3_PLAN.md:106` 與本表的複述，無任何實作）。先決定管道（Teams？便箋？）。順帶更新 `PHASE3_PLAN.md` §5 的 3、4 兩點（仍寫著「都沒有」） | 下次施工 | 流程 | 低 |
| 🔧 **對抗式覆核的沙箱不是存取邊界（已處置·白名單判定不可行·工具已做）**（2026-08-27） | **四份平行敘述都寫著「`--workspace` 指到隔離沙箱」，而那不是隔離。** 當天四組實測：①絕對路徑讀得到沙箱外任何檔 ②沙箱內 junction 指向外部也讀得到 ③`--sandbox enabled` 在 Windows 直接 `exit 1`（原生沙箱限 macOS/Linux，且管的是 command execution 不是 Read 工具）④**找到可用的官方機制**：沙箱內放 `.cursor/cli.json` 的 `permissions.deny` 真的擋得住，**但路徑必須用反斜線** —— `Read(D:/...)`（官方範例寫法）讀得到且**不報錯**、`Read(D:\...)` 才回 `Permission denied`。⇒ 已改 `skills/adversarial-review/SKILL.md`／`reviewer/server.py`／`COLLAB_HANDOFF.md`／`dashboard/harness-dashboard.shell.html` 四處，並訂正 `.scratch/plaintext-credential-gate/PLAN.md` 那筆「junction 被沙箱擋掉」的誤歸因（真正在擋的是 `dispatch.py` fail-closed）與本表上一列「審查者那條路修不修都一樣」的射程 | ✅ **①白名單已驗，判定不可行**（2026-08-27 實測）：`beforeReadFile` 在 `cursor-agent` CLI **確實會觸發**——探針 log 收到帶 `conversation_id`／`generation_id` 的 agent payload，所以「官方文件只講 IDE 與 Cloud Agents、沒說 CLI 會不會觸發」這個未知有答案了。**但兩堵牆都繞不過**：⑴Cursor 把 hook 命令包成 PowerShell 再交給 **bash** 跑 ⇒ `eval: syntax error near unexpected token '&'` ⇒ CLI 判 block、**每個工具呼叫都停**（與 8/26 `dispatch.py` 全滅**同一個根因**，本表那列已記）⑵就算繞過執行方式，payload 仍解不開（`Expecting ',' delimiter: line 1 column 2055`／`column 213`——Cursor 對含中文的 payload 序列化出不合法 JSON，本表 Cursor payload 那列已記），**拿不到 `file_path` 就無從判斷白名單**。⚠ **兩者在畫面上長得一模一樣**（都顯示 `Hook blocked`），只有 hook 自己的 log 分得出「我 deny 了」與「hook 執行失敗被當成拒絕」——**少看 log 會把後者讀成前者，得出「白名單可行」的錯誤結論**。⇒ **deny 黑名單是目前唯一可用的機制**。解凍條件＝Cursor 同時修好 hook 命令的執行包裝與 payload 序列化，兩件都不在我們手上。⚠ 探針是全域 `~\.cursor\hooks.json`，**測完已還原並驗證 CLI 恢復正常**。✅ **②守門已做一半**：`tools/build_review_sandbox.py`（14 條回歸、6 條變異精準轉紅，已接進 `run_hook_tests.py`）——一支指令建沙箱、把三個坑一次寫對、檢查父鏈、印出可貼的命令。**仍沒有的**：派出前沒有自動閘門檢查「這次真的用了沙箱、deny 真的是反斜線」，工具做出來了但要人記得跑。 ⚠ **09-03 盤點：還差** 把沙箱形狀檢查接進**派出前**的閘門。工具在、白名單已判不可行，目前仍靠人記得跑工具。 | 我 | 流程 | 中 |
| ⏳ **`超過 12 字截斷` 測試比對的是實作當下的輸出 —— 是鏡射不是守門** | 2026-08-27：`_clean()` 的截斷改成留一格給省略號（別條線的**未提交**在製品），該測試的 `want` 因此在工作區變紅（HEAD 仍綠，因為省略號還沒 commit）。把 `want` 改掉讓它綠很容易 —— 但那只是讓測試跟著實作跑，**實作再改一次它又要再改一次**，等於沒有守到 §2 的「≤12 字」。已在暫存實測過補法：加一條不隨實作字面走的斷言 `len(M._clean(...)) <= M._MAX_CHARS`，變異測試把實作改成 `name[:_MAX_CHARS + 2]` 時**兩條一起紅**（77/79），還原後 79/79 ⇒ 那條斷言真的在守。⚠ 該修法目前只存在於某個 session 的 scratchpad，**會被清掉**，所以登記在這裡。 | 等省略號實作進 HEAD（屆時 `超過 12 字截斷` 會在 HEAD 變紅），在 `tests/test_session_title.py` 的 `_clean` 區塊：① `want` 改成 `"一二三四五六七八九十十…"` ② 緊接著補一列 `case("截斷後總長仍守 ≤12 字", "…", len(M._clean("一二三四五六七八九十十一十二十三")) <= M._MAX_CHARS, True)`。**驗收＝把實作暫時改成 `name[:_MAX_CHARS + 2] + "…"`，兩條都要紅**，只紅一條代表新斷言沒接上。 ⚠ **09-03 盤點：還差** 那條不隨實作字面走的斷言。⚠ **現在的測試仍然是鏡射**——實作改了測試跟著改，實作再改一次它又要再改一次。 | 下次施工 | eval | 中 |
| 🔧 **「commit 別夾帶別人的檔」目前只是規則，沒有閘門** | 2026-08-27 **一天之內同一條規則失效三次**：①stage 完跑去驗證，commit 被別條線收走 ②`git commit --amend` 前沒重查 HEAD，**改寫了別人的 commit**（已 `git reset --soft` 還原）③commit 夾帶別人兩個檔（253 行＋9 行）。⚠ 第三次最關鍵：`feedback-concurrent-sessions-same-repo` 8/25 第 7 條**早就寫了**「commit 前必看 staged 清單」，我也**確實看了**、檔數還印在畫面上，然後用 and-and 一路串到 commit ⇒ **「看」不是防線，因為看了可以不理**。規則沒失效，**強制力**失效。而 harness 的定位就是把這種規則變成執行期閘門（`UNIVERSAL_HARNESS_PLAN.md`），這一條還停在「叫模型記得」。第 ② 次若沒被 user 一句「你檢查一下」抓到，那顆被改寫的 commit 就留在歷史裡了。 | ⚠ **判準要先想清楚再寫碼**（同 eval L2 那列的教訓）：`PreToolUse` 攔 `Bash`／`PowerShell`，命令含 `git commit` 時比對 `git diff --cached --name-only` 與「本 session 這輪 Edit／Write 碰過的檔」，有多出來的就 block 並列出多的是哪幾個。**三個待決**：(a) 「本輪碰過的檔」從哪來（hook 自己維護 state？讀 transcript？）(b) 合法多檔 commit 怎麼豁免（收工封存本來就要 commit 一批）(c) `--amend` 要另外比對 HEAD 有沒有變。**先證明它會紅**：造一個 index 含非本輪檔的 fixture，改之前必須放行、改之後必須擋下。 ⚠ **09-03 盤點：還差** 順序上要先讓它從 shadow 畢業（＝本表誤觸率那列），否則做了也不生效。 | 下次施工 | 閘門 | 高 |
| ⏳ **改過路徑的兩個排程還沒有 post-change 執行結果**（2026-09-03 D 槽重組收尾） | 2026-09-03 凌晨把五個 Windows 排程工作的舊路徑（`D:\IT-department`、`D:\.ai-harness`）改到 `D:\Patrick-AI\` 底下，XML 備份在 `.scratch\schtask-backup-20260903\`（**那五份是唯一能還原排程設定的東西，不要跟其他備份一起清**）。01:28 實測：兩個 Ready 的排程**最後一次執行都在改路徑之前**（`ClaudeCode-CleanFileHistory` 09-02 04:00、`ITAssetPlatform_NightlySemverBump` 09-02 23:00，`LastTaskResult` 都是 0）⇒ **那兩個 0 不能當成「改完還跑得起來」的證據**。下次執行是 09-03 04:00 與 09-03 23:00。⚠ `ClaudeCode-CheckControlChars` 的 `LastTaskResult` **遷移前就是 1**，既有故障、不是搬家造成的，別誤判成回火⚠ **09-03 12:39 實測**：①**已過**——`ClaudeCode-CleanFileHistory` `LastRunTime=2026/9/3 04:00:01`（落在改路徑之後）、`LastTaskResult=0`，兩條判準都成立 ⇒ **改完的路徑真的跑得起來**，不再是遷移前那個 0。②**還沒到**：`ITAssetPlatform_NightlySemverBump` 目前 `LastRunTime=2026/9/2 23:00:01`，`NextRunTime=2026/9/3 23:00`，今晚之後才驗得到。③`ClaudeCode-CheckControlChars` 今早 04:30 跑過、`LastTaskResult` 仍是 **1** —— 與遷移前同狀態，佐證它是既有故障、不是回火（**但它一直在紅，這件事本身沒人處理**） | ①✅**已驗（09-03 12:39）**，原指令： `powershell -NoProfile -Command "Get-ScheduledTaskInfo -TaskName ClaudeCode-CleanFileHistory \| Select-Object LastRunTime,LastTaskResult"` ②09-03 23:00 之後跑 `powershell -NoProfile -Command "Get-ScheduledTaskInfo -TaskName ITAssetPlatform_NightlySemverBump \| Select-Object LastRunTime,LastTaskResult"` ③**判準兩條都要成立**：`LastRunTime` 落在該次排程時間之後、且 `LastTaskResult` 為 0 ④不是 0 就拿 `.scratch\schtask-backup-20260903\` 的 XML 逐段比對哪個路徑改壞了 ⑤**只剩②，且已掛自動查（09-03 12:49）**：一次性排程 `ClaudeCode-VerifySemverBump-once` 今晚 23:10 會自己跑判準並把結果寫進 `.scratch\schtask-verify-20260903\result.txt`（**已乾跑驗證它會分辨**：現在跑會印「判準A False／判定：未通過」，不是橡皮圖章）。明天讀那個檔 → 通過就 (a) `schtasks /Delete /TN "ClaudeCode-VerifySemverBump-once" /F` (b) 刪掉本列；不過就走④。⚠ **那個一次性排程沒拆掉的話會一直留在系統裡**，拆除指令也寫在結果檔尾端 | 我 | 流程 | 高 |
| ✅ **桌面版在新路徑會不會跳信任對話框 —— 已繞開**（2026-09-03 D 槽重組收尾） | `.claude.json` 的 `projects` 底下，同一個目錄有正斜線與反斜線兩把鍵並存。**npm CLI 讀正斜線那把**，三個新路徑的 `hasTrustDialogAccepted` 已補成 `true`，headless 重測確認「95 條 allow 被忽略」那兩行警告消失。**但日常用的是桌面版，桌面版讀哪一把沒查證**。若它讀到 `false` 的那把，在新路徑開對話會跳一次信任框，而**在按下同意之前，專案自己的 95 條 allow 與 2 條 `additionalDirectories` 全部被靜默忽略**——不報錯，只是每一步都要人按同意。四個舊路徑 junction 已退役（`Test-Path` 全 False），沒有退路 ⚠ **09-03 17:50 已結案，但不是用票上那個方法**：票文寫「只能由人各開一則看」是假限制 —— **兩把鍵都補成 true，就不必知道桌面版讀哪一把**。⚠ **票文舉錯例子**：實量結果是`D:\Patrick-AI\IT-department` 兩把鍵都在且都 true，真正缺反斜線鍵的是 `D:\Patrick-AI\.ai-harness` 與 `D:\Patrick-AI\MIS-install`。已以各自的正斜線鍵為本補建（拿掉 `last*` 那組單次計量欄位）。實作帶樂觀鎖：讀取、備份、寫入前、落地前各量一次 sha256，不同就中止（這台機器有多個 session 同時在寫這個檔）。回讀比對：新增 2 把鍵、**既有 projects 鍵零變動、projects 以外的頂層鍵零變動**。備份：`~\.claude.json.bak_r48_20260903_175027`。⚠ **仍沒驗到的**：桌面版實際讀哪一把鍵——但兩把都 true 之後這個問題沒有後果了。⚠ **刻意沒動的**：`D:/Patrick-AI`（父目錄、不是專案）與小寫 `d:/IT-department`（殘留）兩把鍵仍是 false，它們跳框才是對的。 | **不必做**。下次建新專案目錄時順手確認兩把鍵都在：`py -3 -X utf8 -c "import json;d=json.load(open(r'C:\Users\<USER>\.claude.json',encoding='utf-8'));print(sorted((k,v.get('hasTrustDialogAccepted')) for k,v in d['projects'].items()))"` —— 同一個目錄只出現一次（只有正斜線或只有反斜線）就是風險點。⚠ 不要把這件做成自動補鍵的 hook：信任框存在是有理由的，自動幫所有路徑按同意等於把它關掉。 | 使用者 | 流程 | 低 |


| ⏳ **Claude 端自動改名已整套退役（user 2026-08-28 決定）** | **退役理由三條，全是同日查證出來的事實**：①**官方早就有正式介面**——Agent SDK 的 `rename_session()`，官方文件逐字「Renames a session by appending a custom-title entry. Repeated calls are safe; the most recent title wins.」，與我方手寫的 `_append_title()` 完全同構；另有 `hookSpecificOutput.sessionTitle` 可讓 CLI 自己寫，「跟 client 快取搶」那整層在架構上就不存在 ②**從未穩定**——三天九版、最長活 12.9 小時、三次歸因被推翻（記憶體快取→沒人重讀／欄位所有權→覆蓋時序／格式判準→歸屬判準），而它做的事是「讓側邊欄名字好看一點」；規劃與覆核文件 162KB ③**`PreToolUse` 兩次咬到 Cursor CLI**（8/26 早上全滅、8/28 無 matcher 重現，A/B 三次實測），而 Cursor 是這台機器唯一跨模型族的審查者。**已做**：三個掛載全拆（備份 `settings.json.bak.20260828_retire_title`，殘留數 0）⚠ **2026-09-03 訂正：那個 0 只對全域成立** —— 專案層兩個都漏了：IT-dept 撐到 09-02 09:36 才拆、MIS-install 到 09-03 還掛著兩發（且指向的路徑當天已退役 ⇒ 每次對話結束與每次送出訊息都在執行一個不存在的檔），已於本次盤點當場拆除；`global/hub/20-modes.md` 撤回「每則對話都宣告」那條（那是為改名加的，看板合規統計要的是「有任務時宣告」）；`21-title-claude.md` 清空並記退役理由；`.cursor/rules/task-naming.mdc` 與 `22-title-cursor.md` 移除 `【待】` 格式並標明只對 Cursor 有效；`hooks/session_title.py` 加檔頭退役說明；`test_gen_rule_hub.py` 的「task-naming 指標只在 Claude 產出」改判準（前提隨退役消失）。**已驗**：全套 1459/1462（回到 3 條既有債）、`test_session_title` 128/128、`test_session_archive` 34/34、Cursor 實跑 exit 0 列出六個子目錄。⚠ **程式碼沒刪也不要刪**：`session_archive.py` 仍 import 它的 `compose_idle`／`project_name`／`cloud_request` 做封存時的雲端 idle 名（票 02 Q5），那條路與側邊欄命名無關、仍在跑。⚠ **副作用**：`_remember_last_task()` 只在 `session_title.main()` 裡呼叫 ⇒ 沒有人再寫 `state/lasttask.*.txt`（⚠ **2026-09-03 訂正：生效日不是 8/28**——該檔一路寫到 09-02 23:38 都還有新行，09-03 起停掉的成因是**指令路徑死了**、不是掛載拆乾淨。⚠ 連帶：**不能拿「紀錄檔有沒有長」判斷掛載拆了沒**，推雲端那支工具寫的是同一份檔） ⇒ 封存推的「上一個任務」會永遠停在最後一次的值（不影響正確性，但會逐漸過期）。⚠ **未驗**：設定只在 session 啟動時載入 ⇒ **現行對話仍持有三個掛載、hook 還會跑到本則結束**；要開新 session 才看得到退役後的行為 | ①開一個新 session 做一輪工作 ⇒ 側邊欄應顯示**平台自產的標題**（多為英文），且 `state/session_title.log` **不再新增**；還在寫就是設定沒生效 ②想裝回去的話：`PreToolUse` **一律不可用無 matcher**，掛進 `dispatch.py` 那一組；三個事件的完整配置見 `settings.json.bak.20260828_wire_title3` ③若之後要改走官方路徑，先實測 `hookSpecificOutput.sessionTitle` 在哪些事件真的生效——官方文件只列 SessionStart，第三方說 UserPromptSubmit 也吃，兩邊說法沒對上 ⚠ **09-03 盤點**：MIS-install 那份殘留的掛載已當場拆除（整個 `hooks` 區塊移除，不是改路徑；備份 `settings.local.json.bak.20260903_retire_title`；已驗 JSON 合法、允許清單逐字未變；全機掃過所有 `settings*.json` 沒有第二處）。現況欄的兩處錯誤也已就地訂正。⚠ **未驗**：設定只在 session 啟動時載入 ⇒ 該專案**現行對話仍持有舊掛載**，要開新對話才看得到效果；失敗形狀本來就觀察不到（錯誤紀錄檔接不到「指令檔不存在」），所以驗收只能是「新對話不再有 hook 錯誤」這種反面證據。 | user | hook | 中 |


| 🔧 **登記簿有一列被半形 \| 切成 8 格，產生器取到敘述片段當分類／優先**（2026-09-03 盤點時記票） | 全域表「收工的『逐檔驗證』全部答對，孤兒仍然掉出去」那列切出 **8 格**（表頭 6 格），成因是敘述裡的半形管線字元沒有寫成轉義形式。**既有問題，不是 09-03 那輪造成的**。⚠ 產生器用**欄位位置**取「分類」與「優先」⇒ 這一列取到的是敘述片段；分類值域刻意不做白名單 ⇒ **不會報錯**，只會在看板上顯示一段怪字或掉進「未分類」桶。 | ①把那列敘述裡的半形管線逐個轉義，讓整列回到 6 格 ②加一條檢查（併進既有測試即可）：登記簿每一張表的每一列切出來的欄數必須等於表頭欄數，不等就 FAIL ③**先證明它會紅**——故意留一列 8 格必須被抓到 | 我 | 看板 | 中 |
| 🔧 **需求表兩列的「優先」被裝飾成粗體加括號，產生器完全比對查不到 ⇒ 靜靜落回「推導」**（2026-09-03 記票） | 兩列的優先欄寫成「**高（16 例·兩線獨立佐證·形狀已擴到反引號）**」與「**高（同列兩次·機器層零感知）**」。產生器用完全比對查 `_PRIO_WORDS`，帶粗體與括號註解就查不到。⚠ 畫面標「（推導）」的意思是「這一列沒人真的排過」，但實際上人排過而且寫了理由 ⇒ **填了跟沒填在畫面上長得一樣，而且不會報錯**。 | ①把括號裡的理由搬到「現況」欄，優先欄只留「高」 ②改 `dashboard/gen_todos.py`：優先欄查不到對應值時要**點名這一列**，不要靜靜落回推導（分類欄已經有同樣的點名機制，照那個做） ③**先證明它會紅**——故意把一列的優先寫成「**高（測試）**」必須被點名 | 我 | 看板 | 中 |
| 📄 **進度文件裡兩句話互相矛盾：`HARNESS_PROGRESS.md:47` 寫「19 條全數 enforce（0 shadow）」，同檔 `:148` 寫另有一條刻意留 shadow**（2026-09-03 記票） | 實況站在 `:148` 這邊：IDX-1 不在 `hooks/dispatch_config.json` 裡，而 `hooks/dispatch.py:441` 對缺席規則**預設 shadow=true**，報表也量到它有 285 applies／75 findings。⚠ 「0 shadow」是讀進度文件的人最會直接引用的一句話，引用它就會漏掉一條**正在跑但不會擋人**的規則。 | ①把 `:47` 改成與 `:148` 一致的實況（哪幾條 enforce、哪一條刻意留 shadow、為什麼） ②加一條檢查：`dispatch_config.json` 的內容與進度文件宣稱的 shadow 條數要對得上，不等就 FAIL ③**先證明它會紅**——故意把文件寫成 0 shadow 必須被抓到 | 我 | 文件 | 中 |
| 🔧 **CTX-1 在快照沒有這個專案時靜默放行，而且沒有失明偵測那層保護**（2026-09-03 修 MIS-install 那列時查到） | `hooks/rules/ctx1_resident_budget.py` 的 `_baseline()` 對不上 key 回 `(None, None)`（`:168-178`），`check()` 隨即 `return allow()`（`:195-197`），註解寫「沒有可比的東西，閉嘴」。⚠ 單一專案上這是對的，但**它沒有 `check_bloat.py:1058` 那條 `if m["blind"]` 的失明偵測**——盤點腳本抓得到「這個檔沒被掃到」，活閘門抓不到「這個專案沒有基準」。⇒ 專案改名、或新專案還沒立基準時，這條**每次改檔都跑**的閘門對它完全不出聲，而且沒有任何地方會說它沒在管。09-03 MIS-install 就是這樣過了一整天；那一列已結案，但**成因沒消失，下一次改名會再中一次**。 | ①決定要不要讓 CTX-1 在「檔名對得上常駐層、但快照沒有這個專案」時出一次**低音量提示**（不是擋） ②或改由盤點腳本負責：`check_bloat.py` 每次跑時比對「快照涵蓋的專案集合」vs「設定裡的專案集合」，缺的要點名 ③**先證明它會紅**——把快照裡某個專案的 key 改名，指定的偵測必須喊 ⚠ 看板那個 `antibloat` 探針只驗「腳本在＋快照在＋skill 有提到」，**不驗快照涵蓋哪些專案** ⇒ 這個洞在看板上是綠的 | 我 | 閘門 | 中 |
| 🔧 **在 git worktree 裡跑全套測試必定假失敗，而且訊息看不出來是環境問題**（2026-09-03 實測） | `harness.config.json` 已 gitignore（F-8 刻意的）⇒ **只存在主目錄**。在 `.claude/worktrees/*` 裡跑 `tests/run_hook_tests.py` 會紅六處：P-12 去專案化 5 條（20/25）、常駐層健檢 1 條（115/116）、`test_todos.py` 3 條。⚠ 失敗訊息長得跟真的壞掉一樣（`[Errno 2] No such file or directory`、「找不到 harness 設定 …拒跑，不猜要掃哪裡（U-2）」），**沒有任何一行說「你在 worktree 裡，這是環境問題」**。已實測：把主目錄那份設定檔複製進 worktree，這六處全綠。⚠ 兩個方向都會出錯：在 worktree 工作的人以為是自己改壞的；或反過來**把真的失敗當成「又是那個環境問題」而忽略掉**。 | ①測試啟動時判斷 cwd 是不是 worktree（`git rev-parse --git-common-dir` 與 `--git-dir` 不同即是），是的話往主目錄找設定檔，或**至少把訊息改成點名 worktree** ②或在建 worktree 時自動連結一份 ③**先證明它會紅**——在 worktree 裡不放設定檔跑一次，訊息必須提到 worktree ⚠ 跟本表「被留在 --init 範本態」那列是**同一個檔、不同的病**：那列是內容被改壞，這列是它在 worktree 裡根本不存在（gitignore 刻意的）。兩列一起看才完整 | 我 | eval | 中 |
| 🔥 **登記簿有一列被攔腰切成兩段，後半段開頭連管線都沒了 ⇒ 產生器整列讀不到**（2026-09-03 要加新列時撞到） | 全域表末尾有一段孤兒文字，開頭是「egister_session_title_hook.py」——那是某列裡 `hooks\register_session_title_hook.py` 這個路徑的後半。⚠ **成因幾乎確定是路徑裡的 `\r` 被當成歸位字元**：某支處理腳本用 `open()` 預設模式讀寫，把單獨的 CR 翻成換行，整列從那裡斷開。⚠ 後果：**那半列不以管線開頭 ⇒ 完全不是表格列**，產生器連「欄位數不對」都不會抱怨，直接當散文跳過。被吃掉的那半列自己標的優先是「高」。⚠ 這跟本表另外兩件（欄位被切成 8 格、優先值被裝飾）是**同一個家族**：登記簿被靜默吃掉東西，而且畫面上看不出來。⚠ **這一列是證物，修之前不要動它**。 | ①先判斷那半列的內容是不是已隨改名退役而作廢——是就整段刪掉、不是就併回原列 ②檢查還有沒有第二處：掃全檔找「不以管線開頭、卻夾在表格列之間」的行 ③加進 T1 那條欄數檢查裡：表格區塊內出現非表格行就 FAIL ④**先證明它會紅**——故意插一行散文進表格中間必須被抓到 ⑤處理讀寫 markdown 的腳本一律 `newline=""`，別讓 `open()` 再翻一次 CR | 我 | 看板 | 高 |
| 🔧 **交接檔改成開工就建，HND-1「開著很久」那欄的分母會被灌大**（2026-09-03 開票·本輪改動的已知副作用） | 本輪把任務檔的建立時機從「換則前」提前到「S／M 開工當下」（`skills/chat-handoff/SKILL.md` §0、`skills/session-workflow/SKILL.md` 步驟 1）。後果：每個 S／M 任務都會在 `.scratch/handoff/` 留下一份 `status: open` 的檔，而 HND-1 的 `_STALE_DAYS` 是 7 ⇒ **2026-09-10 之後，凡是超過一週沒結案的任務檔都會進「開著很久」清單**。票 57 已經在擔心「提醒長期都是『對，我知道』就會退化成噪音」，這個改動正是把那個分母做大的來源。⚠ 目前**沒有量測**：改動今天才落地，還沒有任何一份用新格式建的檔 | ①2026-09-10 之後跑 `py -3 hooks/report.py` 看 HND-1 的 WARN 比與清單長度 ②若「開著很久」常態 > 5 份，兩條路擇一：把 `_STALE_DAYS` 拉長，或改判準為「進度日誌最後一段的時間」而不是檔案 mtime——後者才分得出「擱置」與「還在做」 ③**不要先改**：先讓它真的吵起來再改，否則調的是想像中的數字 | 我 | hook | 中 |
| 🔧 **「兩邊各存全文、改一邊要同步另一邊」的那一對，實際上早就不逐字相同**（2026-09-04 開票·做 B-1 時順手量到，**不是本輪造成的**） | IT 專案的 `/verify-rules`「靜態重算算出的『命中率』不等於可達性」節與 `.aimemory/feedback-execution-test-before-deploy.md` 互相聲明「兩邊各存全文」「改任一邊必須同步另一邊」。**實測 4 條完成判準只有 2 條逐字相同**，另 2 條是同義改寫（「一律寫成」vs「一律標成」、「必須逐條接到」vs「要逐條接到」）。⚠ 本輪 B-1 是**逐字搬移**，改寫前的 SKILL.md 就已經和記憶檔對不上 ⇒ 漂移早於本次改動。⚠ 危險不在字面，在**沒有任何東西在檢查這個聲明**：兩份檔互相宣稱同步，而同步與否無人驗證——正是那節自己在講的「規則之間的關係沒人斷言」。 | ①先判斷這對「各存全文」還值不值得維持（一份在 skill、一份在記憶檔，兩邊觸發時機不同，當初是刻意不收斂）②要維持就補一條機械檢查：抽兩側該節的 4 條判準做正規化比對，不同就 FAIL；**先證明它會紅**——改一側一個字必須被抓到 ③不維持就把其中一側改成指標，並拿掉兩邊的「各存全文」宣告，別留一個沒人守的承諾 | 我 | 規則 | 中 |
| 🔧 **階段轉換要追寫進度日誌，但零強制**（2026-09-03 開票·同上改動） | 新規則只寫在兩支 skill 的完成判準裡，是文字約束、沒有任何閘門會檢查。`skills/session-workflow/SKILL.md` 的「邊界」明文寫著**不建階段順序 hook**（2026-08-25 W-9：先量測）⇒ 這個無強制狀態是**刻意的**，不是漏掉。風險：追寫這件事跟自我宣告不同，宣告有 DECL-1 在看，追寫沒有人在看，忘了不會有任何徵兆。⚠ 而「忘了」正是這條規則存在的原因——事後回憶寫出來的交接檔會過時 | ①先量測，不要先建閘門：接下來幾則 S／M 任務結束後，數 `.scratch/handoff/` 那份檔的進度日誌段數 vs 實際走過的階段數 ②追寫率若明顯低於 1，才討論要不要在 DECL-1 那條既有的宣告檢查上**加掛**一句（不新建規則——W-9 擋的是新的階段順序 hook，不是既有規則多看一欄）③若追寫率接近 1，這張票直接刪掉，別留著養一條沒必要的閘門 | 我 | 流程 | 中 |



## 全域·需求（待判斷）

> **與上面那張「待辦」表的差別**：待辦是**已經決定要做**、只差排期；
> 這裡是**還沒判斷要不要做**——只登記、不排期，你定期看一眼決定升級成待辦或直接刪掉。
> 分不清就丟這裡，判斷是你的事、不是提出者的事。
>
> **誰會往這裡寫**：
> - **角色**（`agents\*.md` 的「碰到能力邊界時」）在回報末尾寫 `【需要但沒有】<工具｜技能｜人力>——<說明>`，
>   主 session 收到就抄進這張表。⚠ 角色多半沒有 Write 權限（`locator` 只有 Read/Grep/Glob），
>   **它們寫不了這個檔，落檔一定是主 session 的責任**——這條漏掉的話整個機制等於沒有。
> - **主 session 自己**踩到同類情況時一樣登記。
>
> **兩種情況都收**：①做不到（被工具限制擋住）②做得到但明顯繞路多花時間。
> **②必須附實例**——哪一步繞了、繞法是什麼。沒有實例的不要登記，那是想像中的需求。
> 「繞路成功」才是常態：做不到會喊，繞過去了不會喊，而不喊的那些就是這張表要接住的。
>
> 做完或判定不做就把該列刪掉（同上表，不留歷史）。
>
> 🔧 **`D:\AI-Projects` 沒有任何 git remote，12.6 GB 只有本機一份**（2026-09-01 D 槽重整盤點時量到，user 當下決定「先記票、不在這次做」）。
> 實測：`git -C D:\AI-Projects remote -v` 零筆；同日 `main` 分支仍有 commit（`docs: 補齊 0830.6 發版紀錄`），是活躍專案。
> 對照組：`.ai-harness` 有 `backup` → `C:\Users\<USER>\git-mirrors\JEFF-Harness.git`；`IT-department` 有 `vm` → `<VM-HOST>:/srv/git/<ORG>-IT-Portal.git`。**三個 repo 只有 AI-Projects 裸奔。**
> 下一步：照 harness 做法建本機鏡像（`git init --bare C:\Users\<USER>\git-mirrors\AI-Projects.git` 再 `git remote add backup`，約 10 分鐘，但同一台機器不算異地）。
> ⛔ **原本的選項②「推到 IT 那台 VM」已由 user 於 2026-09-02 否決**：**那台 VM 只放 IT-portal 平台，不要拿來放別的東西**。
> 這條界線對三個 repo 都成立（見下方「harness 跨機同步」那列的同一條註記）——**不要再把 VM 當成異地備份的候選**。

| 項目 | 現況／為何還沒做 | 下一步（逐字指令或動作） | 誰 | 分類 | 優先 |
|---|---|---|---|---|---|
| 🔧 **`locator` 角色沒有執行權限，答不出執行期事實**（**第 3 個實例到齊，2026-08-25 —— 該判斷了**） | 2026-08-24 第 2 例：盤點「DEV 怎麼啟動」時查到靜態矛盾（`start_dev.bat` 寫 3000、文件普遍稱 8090），但**判不出哪個埠現在真的有 listener**。**2026-08-25 第 3 例**：盤點 reviewer 設定時要判「`C:\Users\<USER>\.claude\skills\adversarial-review` 與 `D:\Patrick-AI\.ai-harness\skills\adversarial-review` 哪個是 junction」，需要 `cmd /c dir /AL` 或 `Get-Item \| Select LinkType,Target`，`locator` 只有 Read/Grep/Glob ⇒ 只能從「內容逐字相同＋commit 訊息」做旁證，並自己標明「不是查詢結果」。⚠ **這一例的代價是實的**：主 session 接手後實測，發現 PowerShell 5.1 的 `Target` 回報 `C:\.ai-harness\…`（**該路徑根本不存在**），要再用 `fsutil file queryfileid` 才判得出來——唯讀角色連錯誤的旁證都給不出。**2026-08-27 第 4 例（這次代價最直接）**：收工派 `locator` 盤點兩個 `.scratch/` 的「逾期 >7 天」與「今天新增」，它跑了 **65 次工具呼叫／10 萬 token／6 分鐘**，回來的結論是「A、B、C 三項全部查不到」——`Glob` 號稱依 mtime 排序但**不吐出時間值**，`Read`／`Grep` 不含檔案系統中繼資料。主 session 用**一段 `os.walk` + `os.stat`、3 秒**就量完（209／156 檔、逾期各 0）。⚠ 它誠實喊了【需要但沒有】而且沒有拿檔名日期硬湊答案，**角色紀律是對的**——壞的是派工判斷：**「盤點檔案」聽起來像唯讀查詢，但需要 mtime／bytes 就已經是執行期事實。**派工前的判準應該是「答案要不要讀檔案系統中繼資料」，不是「聽起來唯不唯讀」 **2026-09-03 第 5 例（這次是 DB）**：盤點「建議信箱要發給誰、平台有沒有姓名→帳號對照」時，`locator` 查得到 JSON dump 裡的人員名冊（85 筆），但 `accounts` 表在 SQLite 二進位檔裡 ⇒ `Grep` 視為 binary 直接略過、回 0 命中，**它自己標明「非真的無資料」而不是硬猜**，並喊了【需要但沒有】SQL 查詢工具／DB 讀取權限。主 session 接手才量到 81 筆、email 全滿、`employee_no` 81 筆全空——**而「有幾個帳號、欄位填了沒」正是這次評估要不要做 Teams 個人通知的關鍵輸入**。⇒ 同一個母題第 5 例，這次的形狀是**唯讀角色讀不了任何二進位資料檔**（SQLite、xlsx、圖），不只是執行期事實。 | **三個實例到齊，判斷點到了。** 二選一：①維持現狀（主 session 代跑執行期查詢）②給 `locator` 一個純查詢窄口白名單（`netstat`／`pip freeze`／`--version`／`fsutil file queryfileid`／`dir /AL` 類，**唯讀且無副作用**）。⚠ 選②要先確認這些命令在 hook 白名單裡放行，否則角色會拿到一個喊得出口卻跑不動的權限 | 待判斷 | 角色 | 中 |
| 🔧 **`CLAUDE.md` §9 的「DEV 平台開 `:8090`」與啟動腳本不符**（2026-08-24 locator 查到） | `SOP/scripts/start_dev.bat:6` 是 `set PORT=3000`，`SOP/05_UI_Demo/server.py:208` 的預設值也是 `3000`。`8090` 唯一寫死的位置是 `SOP_PROD/05_UI_Demo/.env.production:2`（**本機 PROD 副本**的埠，配 `restart_prod.bat`）。另有孤兒檔 `SOP/05_UI_Demo/.env.production` 寫 `PORT=8080` 但 grep 不到任何消費者。⇒ **§9 那句可能一直在指錯環境**，而 §9 是部署硬規則區 | 跑 `netstat -ano \| findstr LISTENING \| findstr \":3000 :8080 :8090\"` 看實際哪個埠活著；或直接跑一次 `SOP\scripts\start_dev.bat` 看它印什麼。確認後訂正 §9 那一句（**別反過來改腳本**——先查清楚「DEV 平台」這個口語到底指哪一個環境） ⚠ **09-03 盤點：這一列指錯位置了**。要訂正的那句話已經不在那份規則檔，搬到 `IT-department/.aimemory/project-it-asset-platform.md:32`——票文要跟著改，否則下一棒會去改一個已經沒有那句話的檔。另外腳本側原封不動（一支寫 3000、一支寫 8080），到底哪個埠有 listener 從未對帳。 | 待判斷 | 文件 | 中 |
| 🔧 **遠端桌面憑證以明文出現在 transcript 裡（1,278 處·已遮蔽·帳號待 RD 刪）** | 2026-08-24 跑 `/fewer-permission-prompts` 掃 572 個 transcript 時撞見：PowerShell 遠端腳本用 `PSCredential(<帳號>, (ConvertTo-SecureString <明文密碼>))` 的寫法，**密碼直接寫在命令列裡** ⇒ 每跑一次就在 `~/.claude/projects/*.jsonl` 多留一份。**已查證沒有外洩到版控（2026-08-24 全機掃過）**：掃 `d:/AI-Projects`、`d:/IT-department`、`d:/Patrick-AI/.ai-harness`、`~/.claude` 四個根目錄共 **1,260 個原始檔（.md/.ps1/.py/.json/.txt/.bat…，不含 .jsonl）→ 明文密碼 0 命中**；`git log -S` 在兩個 repo 各 0 個 commit。⇒ **密碼只活在 `~/.claude/projects/*.jsonl` 裡**，也就是每次被打進命令列、不是從任何檔案讀出來的。**帳號名**另有 21 個檔硬寫（`d:/AI-Projects` 的引擎 `.ps1` 與計畫書），但**那 21 個檔 0 個進版控**；進版控的只有 `.ai-harness/dashboard/harness-dashboard.html` 4 處帳號名、不含密碼 ⚠ **09-03 重盤，票上四個數字全部過期**：命中 **316 → 1,278 處**；檔數 **→ 28 個**（封存區 25、現役區 3）；帳號名硬寫檔 **21 → 25 個**且磁碟路徑已從 `d:/AI-Projects` 搬到 `D:\Patrick-AI\MIS-install`；「三個方向」的③實際上做不到。⚠ **帳號是 `<ADMIN-ACCT>`，在一台已經不歸我們的機器上**：那是部署工具自己建的本機管理員，用途只有「讓管理機 `10.0.0.1` 遠端跑安裝腳本」。`<HOST-A>`／`10.0.0.2` 於 08-11 依 user 裁定**帶著該帳號交還 RD**（`155還機清除計劃書.md` R13），現在連不上——**但關掉的是門，鑰匙還是好的**。⚠ **工具本身是乾淨的**：`_enable_remote_run.ps1` 沒有內建預設密碼（讀 secrets 檔或當場問），而 secrets 檔在端點上實測不存在 ⇒ 漏的不是設計，是「把密碼打進命令列」這個動作。✅ **已做：遮蔽 1,226 處**（`tools/redact_credentials.py`）——對話內容一字不動，只把憑證換成 `<REDACTED>`。驗證：殘留 0、JSON 壞行 0、與備份重放位元組相同。⚠ **第一版過度遮蔽、已修**：`$Pass`／`$pass` 是變數名不是憑證，被一起遮掉 52 處，已從備份重放還原。連帶推翻一個誤判：**`.aimemory` 那筆不是密碼而是 `$pass`** ⇒ 票上「版控內0 命中」仍然成立，那個檔已還原成一字未動。 | ① **已做**：遮蔽工具 `tools/redact_credentials.py`，備份在 `~\.claude-credential-redact-backup`（⚠ **備份裡仍有明文**，確認無誤後要自己刪）。② **待驗：收工後要再跑一次** `py -3 -X utf8 tools/redact_credentials.py` ——工具會跳過6 小時內被寫過的檔，**所以做這件事那一則對話自己的紀錄一定漏掉**（這次漏 26 處）；驗收跑 `--verify`，「真正殘留的明文」要是 0。③ **待 RD（我做不到）**：請他們在 `10.0.0.2` 上刪掉 `<ADMIN-ACCT>` 並關掉 WinRM。要給的說明已寫好（四行指令＋為什麼要刪＋不會影響他們哪些東西），**不含密碼**。帳號一刪，備份裡那些明文也自動作廢。④ 尚未辨識的 11 組短字串維持遮蔽（安全優於還原度）；若日後發現其中哪一組也是變數名，把它的 sha8 加進工具的 `BENIGN` 再從備份重放。 | 我＋RD | 流程 | 高 |
| 🔧 **escape 被中間層吃掉，需要 lint 而不是靠記得** | 同一天內同一個母題踩三次：抽區塊撞解構參數的 `{`／CRLF 下 `replace` 靜默沒命中／heredoc 非 raw 字串把 `\t` 吃成 tab（寫進記憶檔的路徑變成 `.ai-harness<TAB>ools`）。前兩個已做成 `tools\js_source_probe.js`，**第三個還沒有守門**。**2026-08-23 同一輪再兩次**（同母題第 4、5 例，都是 Bash heredoc → Python 字串兩層）：①`\\` 被吃成 `\`，寫出的 `print("D:\Patrick-AI\.ai-harness\...")` 觸發 SyntaxWarning（invalid escape sequence）——**語法檢查照過、只有開 `-W error::SyntaxWarning` 才叫**；②`\n` 被寫成字面兩字元，把 `if …:` 與它的 body 併成一行 → IndentationError 當場炸。可靠繞法已驗證兩種：`cat > frag.txt <<'EOF'` 純字面 heredoc 寫檔再 splice、或用 `chr(92)` 組字串。**2026-08-23 第 6 例**：往這張表登記下面那條 `peek_sessions` 時當場又踩，而且**踩的是跟第 3 例一模一樣的路徑**——`D:\Patrick-AI\.ai-harness\tools\` 的 `\t` 又被吃成 TAB（`.ai-harness<TAB>ools`），同時 `'\U0001f464'` 被解讀成 👤 把 traceback 原文引錯。⇒ **知道這個坑存在並不能避開它**，這條需要的是機器檢查不是記憶；順帶證實那兩種繞法可靠（改用純字面 heredoc 一次就對）。**2026-08-23 同一天再踩四次（第 7–10 例）**：⑦regex 的反斜線字元類 `[.\\/]` 落地成 `[.\/]`，**pattern 合法、compile 得過、只是永遠不命中 Windows 路徑**（是那支工具自己的 `--self-test` 抓到的）⑧就在用 heredoc **記錄第 7 例的那一行**，`\\` 又被吃一層、箭頭兩邊變成一模一樣的字串⑨`\\n` 被吃成真換行，把 Python 字串字面值攔腰打斷（`SyntaxError: unterminated string literal`）⑩同⑨，換一個檔又發生一次。**十例裡有八例的症狀是「安靜地不對」而不是報錯**；四例都靠改用 Edit 工具（無跳脫層）或 `chr(10).join([...])` 修掉。**2026-08-25 再踩兩次（第 11、12 例，兩例都是 Bash quoted heredoc `<<'JSON'` → 檔案內容）**：⑪寫 `.cursor\mcp.json` 時 `"C:\\Program Files\\nodejs\\node.exe"` 落地成單反斜線 `"C:\Program Files\..."` ⇒ **JSON 非法**（`Invalid \escape`），但 `cat` 出來肉眼幾乎看不出差別，是 `json.load` 才炸；⑫同一輪寫 DeskBus 任務 body 時 `\\round-1-ask.md` 落地成 `\round-1-ask.md`、Python 再把 `\r` 解讀成 **CR 字元** ⇒ 送出去的路徑變成 `…sg084-hydrate-deadlockound-1-ask.md`，**對方（Cursor）已經讀了那則訊息才被發現**——這一例的代價是外溢到另一個 agent 的。⚠ **`<<'QUOTED'` 理論上不做替換，實測仍吃掉一層**，所以「用 quoted heredoc 就安全」是錯的直覺。⑪⑫都改用 Python `json.dump`／`r'...'` 一次就對。**十二例裡十例是「安靜地不對」**　⚠ **排這個優先的理由**：16 例·兩線獨立佐證·形狀已擴到反引號。⚠ **09-03 12:52 同日連兩例，形狀各自不同（第 17、18 例）**。第 17 例（跳脫被**執行**，不是被吃掉）：用 heredoc 餵 python 改本檔時寫雙反斜線路徑，中間層吃掉一層後 python 把 `\r` 解讀成**換行字元**寫進表格，把一列切成兩列（欄數 5→2）。第 18 例（**修第 17 例的那一則指令自己又踩**）：改用 `py -3 -c "…"` 時，訊息裡的反引號被 bash 當成**命令替換執行掉**，於是登記文字寫進去時是**有洞的**（`\r`、檔名、跳脫清單全部變空白），而落檔本身成功、驗證器全綠、**沒有任何一層會發現內容缺字**。⚠ **兩層檢查都沒抓到第 17 例**：待辦驗證器 36 條全綠、產生器照跑 —— 因為沒有一條在看「每列欄數是不是一致」。是我自己另外數欄數才發現。⇒ lint 要抓的不只是 `\|`，還有**任何會被中間層解讀的字元**：python 側的 `\r \n \t \b`，以及 shell 側的反引號與 `$(…)`。⚠ **最小守門（兩件現在零偵測）**：①待辦驗證器加「表格每列欄數一致 ＋ 檔內不得有裸 CR」②任何寫檔後回讀比對「寫進去的字數 = 打算寫的字數」——第 18 例的洞用這一條就會當場叫。⚠ **票上原本就寫著通則「不要走 heredoc、改走 Edit 或 Python 落檔」，我同一天違反兩次**⇒ 再次佐證這條靠人記得無效，必須有機器擋。 | 想一個能擋「用會解讀跳脫的那一層去寫含跳脫字元的文字」的檢查；或至少在寫檔類 hook 加一條偵測（檔案內容出現裸 TAB 在路徑中段＝可疑；**JSON 檔寫入後自動 `json.load` 驗一次**＝第 11 例當場會叫）。**十二例之後可以確定：這條靠人記得是無效的**——每一次都是「知道有這個坑、下一句就踩進去」。**最省事的通則：凡是要寫含 Windows 路徑的內容，一律走 Edit 工具或 Python（`r'...'`／`json.dump`），不要走 heredoc**。⚠ **另一條線同日獨立踩到第 13–15 例並得出同一個通則**（2026-08-25 收工時寫 `PENDING_VERIFY` 與工作日誌：`\r` 落地成 CR、`\t` 落地成 TAB，路徑變 `.ai-harness<CR>eviewer`）——**其中第 15 例發生在「不需要更多實例」那一句裡**：知道坑在哪、正在寫它、正在說自己不會再踩，下一句照樣踩。⇒ **兩條線同日各自撞到、各自得出同一個結論＝證據已飽和，缺的是機器檢查不是實例**（寫檔前掃寫入內容有無裸 CR／TAB／單反斜線 Windows 路徑）。⚠ **2026-08-26 補一個新形狀，檢查範圍要跟著擴**：不只反斜線——**反引號在 Bash 雙引號字串裡是命令替換**，`py -3 -c "…\`refreshDraftBaseline\`…"` 會讓 shell 去執行那個字，寫進檔案的是**空字串**（症狀：規範文字變成「深層路由（）」「merge 後禁 （會…」，讀起來只是漏字、不像被吃掉）。同輪還連帶噴出 `command not found` 三行，但那看起來像雜訊、很容易略過。⇒ 掃描項要含**反引號與 `$(` 出現在寫入內容裡**，不只 CR／TAB／反斜線。 ⚠ **2026-08-26 再補一個新層：`subprocess.run(..., text=True)` 在 Windows 上會把 stdin 的換行從 LF 改寫成 CRLF**——餵給任何以換行分隔的工具都會靜默污染。實例：算 `git mktree` 時那個 CR 被當成**檔名的一部分**，同一份輸入用 shell `printf` 得到 `43fb277f…`、用 Python text mode 得到 `0e2f6725…`，**兩個都是合法的 tree hash、都不報錯**，害我一度判定「乾淨的 upstream 副本對不上」而差點推翻一份正確的研究結論。⇒ **餵 stdin 給外部工具一律送 bytes**（`input=payload.encode()`，不要 `text=True`）。這是第 17 例，也是第一次出現在「不是我寫檔、是我讀資料」的方向上。⚠⚠ **這條落檔本身第一次寫進來時就被它自己描述的坑咬了**：quoted heredoc 仍吃掉一層，字面的跳脫序列變成真的控制字元，把這一整列從中間切成三行。**教訓：寫「關於跳脫」的規則時，措辭裡一個跳脫序列都不要放**——用 CR／LF／CRLF 這種字面名稱講。 ⚠ **09-03 盤點：還差** 三類檢查（反引號、`$(`、寫 JSON 後驗一次）。而且它**只報不擋**——沒有任何守門或測試引用它，只有日排程一條腿。 | 待判斷 | 流程 | 高 |
| 👤 **需求表的有用指標是「重複度」，不是筆數或年齡**（2026-08-23 實測改寫） | 原本登記的是「`gen_roles_topology` 的 `boundaryReport` 只驗角色檔有沒有寫那段話 ⇒ 永遠 6/6 綠，要不要加第二個指標（筆數＋最舊一筆年齡）」，並註明「先讓表跑一陣子有真實資料再說」。**資料現在有了，而它推翻了原本的提議**：`tools\esc1_corpus.py --dump` 實跑 342 份 subagent transcript → 含標記 57 份／78 行 → **真需求 42 行／39 份檔案**，而表上只有 8 列。但逐筆讀下去，**大宗是同一件事**（N1 的唯讀閘門白名單：`py -3`／`py_compile`／`node <script>`／`ls -la`／`git diff`），**而且其中好幾筆是角色檔誤述自己能力造成的假需求**——`harness-auditor.md` 原本寫著「`py -3` 不在白名單」，實際是放行的（2026-08-23 已修）。⇒ 筆數與年齡量的是積壓，落檔率量的是捕獲，**兩個都答不出「這 42 次在喊同一件事」** | 指標改成**重複度**：把 `esc1_corpus.py` 的真需求行做粗分群（關鍵字即可：白名單／執行／遠端／字數…），看板顯示「前三大需求各被喊幾次」。判準：**同一群 ≥5 次還沒動工就該紅**。⚠ 做之前先扣掉「角色檔誤述能力」造成的假需求，否則指標會把文件錯誤算成能力缺口 | 待判斷 | 角色 | 低 |
| 🔧 **唯讀角色驗不了「投遞鏈」，而回測的 ground truth 也還是啟發式判的** | 2026-08-22 對抗式覆核第 3 輪（`agent-a04587dd8c91c621a`）在回報末尾喊了三筆，**由 ESC-1 上線後第一次真實命中帶進來**——這一列本身就是那條規則的第一個閉環樣本。①**沒有能實際觸發一次 hook 的權限**：審查者是唯讀環境，`_queue_pending_warning` → `UserPromptSubmit` → 模型是否真的收到，以及模型看到 `agentId` 時會不會因為 tool_result 裡那句「never quote… including the agentId」而拒絕引用，**兩件事只能從 log 反推、沒有實跑**。②**回測母體沒有版本化**：第 3 輪重新量到 50 筆、與 v3 寫的 49 筆對不起來，沒有原始清單可逐筆對。③**「真需求 vs 無」的分類是啟發式判的**：兩輪覆核獨立量到 36/14 與 37/15、互相佐證，但**兩個都是自動判的**，而它是回測的 ground truth。 | ①要一次真實派工＋改 settings 才驗得到，屬「動 hook 佈線」等級，排進下一次動 harness 時一起做；②把 `tools\esc1_corpus.py --dump` 的輸出存成帶日期的檔，讓母體有可比對的版本；③人工掃一遍那 10 筆「無」與 4 筆「提及」（`py -3 -X utf8 tools\esc1_corpus.py --dump`），確認分類後把數字釘進 `HARNESS_ROLE_ARCH_PLAN.md` §9.5 ①。 | 待判斷 | 角色 | 低 |
| 🔧 **eval L2 的 `_resolve_path` 構不到兩層深的 skill 檔**（2026-08-23 閘門清理線發現·**2026-08-25 複核：根因未修，但當時未發作**） | `eval\check_contracts.py` 的 `SEARCH_BASES` 沒有「skill 自己的目錄」，且只寫檔名時的遞迴**限一層深度** ⇒ 構不到兩層深的 `skills\skill-watch\run.py`。**檔案真的在**（`git ls-files skills/skill-watch/` 列得出來），所以是假紅不是缺檔。今天才浮出來的原因：HEAD 版只掃專案層一層、看不到 harness 層的 skill，是**案 A 的 A-3「掃兩層」（工作區未 commit）**第一次把它納入檢查；而 8/16 施作 A-3 時 harness 層只有 `context-health`／`visual-check` 兩支、都沒有同目錄檔案引用，所以當時驗不出來。⚠ `SKILL_EVAL_PLAN.md` §9.6b「案 A 範圍外、已知但不做的」六條**沒有涵蓋這一條**。**2026-08-25 複核快照（實測，不是現況宣稱）**：`eval\run_all.py` L1–L4 **全 PASS**，且 `contract_allowlist.json` 只有 `domain-modeling`／`CONTEXT-MAP.md` 一筆 ⇒ **不是靠豁免壓下去的**（原記那條「不要豁免」的警告沒有被違反）。**2026-08-26 B-4 基準**：`adversarial-review` 的動態範本 `round-N-reply.md` 被 L2 報 MISSING，現行 L2 exit 1；這不是同一個兩層路徑案例，但證明前一天的全 PASS 不可當現況。兩層根因仍原封不動：`_resolve_path('run.py')` → `None`、`_resolve_path('skill-watch\run.py')` → `None`，只有 `_resolve_path('skills\skill-watch\run.py')` 找得到 —— **當時不紅只是因為那支 SKILL.md 剛好寫了完整相對路徑**（`skills/skill-watch/SKILL.md:26`／`:38`）。下一支寫 `${CLAUDE_SKILL_DIR}/run.py` 或裸檔名的 skill 會再中一次。⚠ **不發作 ≠ 修好了**，別看到 L2 全綠就把這列當已結案刪掉 | 完整根因（定位到行）·修法·四步驗證（含「先證明它會紅」的反向項：把 `run.py` 暫時改名必須重新變紅）·沒找到的 → `.scratch\room-gate-cleanup\FINDINGS.md` 發現 1。⚠ **不要放進 `contract_allowlist.json` 豁免**——檔案真的存在，豁免等於把檢查器的能力關掉。⚠ 它在別條線正在重構的同一區（U-1 硬編碼還債），**該由那條線順手做**，別兩邊各改一次 ⚠ **09-03 盤點：現在跑出來的綠燈是假的**——真正被引用的那個執行形式躺在程式碼區塊裡，根本沒被掃到。還差兩件：搜尋基準要補 skill 根，以及票上要求的反向驗證（把檔案改名必須重新變紅）。 | 待判斷·歸 eval 線 | 流程 | 中 |
| 📌 **快取花費：壓縮舊內容可能反而讓帳單上升，且工具呼叫密集的單輪會悄悄跳出快取回溯視窗**（2026-09-04 背景研究登記，先落檔不排期） | 官方 `/claude-api` 文件確認：快取讀取只往回找最近 **20 個內容區塊**，超過就找不到前一筆快取、整段變回全額計價。另有社群案例回報：**回頭裁剪/壓縮已經寫入快取的舊內容**會讓帳單不降反升（一例：output 縮 38%、總花費卻漲 6.8%），因為動到了已快取的位元組、把讀取折扣洗掉了——跟直覺「壓縮＝省錢」相反。目前 harness 沒有任何自建的 context 壓縮/裁剪邏輯，純粹先登記避免以後重踩。 | 若之後有人要寫自動壓縮/裁剪 context 的邏輯，動手前先讀這條：①只能往前壓縮新內容，不要回頭改已經快取住的舊內容 ②工具呼叫密集的單輪每 ~15 個區塊補一個快取斷點，避免超過 20 區塊回溯視窗而悄悄變全額 | 待判斷 | 參考 | 低 |
| 👤 **唯讀角色查得到「在哪裡」卻答不出「算出來一不一樣」** | 2026-08-23 盤點「維運腳本 N 支」時，`locator` 找齊了**三套各自獨立的 ops 計數邏輯**（`check_freshness.gather_current`／`gen_layers.count_ops`／`capability_checks._p_ops_scripts`），但答不出它們算出來的數字一不一致——那要實跑 Python，而它只有 Read/Grep/Glob。主 session 代跑三秒有答案（ops 66／hooks 19／總計 85，其中兩套範圍相同、`gen_layers.count_ops` 問的是另一個問題）。**與上面 ① 不同：這不是白名單太窄**，locator 的角色定義本來就是「不執行任何指令」，所以真正的問題是「唯讀盤點碰到需要算的問題時該由誰算」。這次機制運作正常（角色喊、主 session 接），登記是為了看它會不會反覆發生 | 判斷二選一：①**維持現狀**（傾向這個——這次繞路成本是三秒，而放寬唯讀角色的執行權限是永久的風險）②給 locator 一個純計算窄口。**先不要動**，等這一列累積到第三個實例再判斷 | 待判斷 | 角色 | 低 |
| ✅ **Windows 本機 agent 拉不到 `origin.cursor.com` 的雲端 agent 產物 —— 已失效**（2026-08-25 開·2026-09-03 判失效） | 來源 `https://origin.cursor.com/git/sars525200/tmp-e33b4a2339d80836`。已試：①`git clone` 無 helper → `could not read Username` ②開 helper → 卡 GUI 登入 ③瀏覽器開同一 URL → Cursor 登入牙 ④`origin` CLI：Windows 原生不支援、本機也沒裝 WSL。檔也不在 D:／`.cursor`。不做就接不進 harness；默默重寫 DeskBus 會跟雲端那份分叉 ⚠ **09-03 判已失效**：這張票的唯一用途是把那份雲端產物接進 DeskBus，而 **DeskBus 已於 2026-08-26 整組移除**（`COLLAB_HANDOFF.md`「DeskBus 已移除」節）。三個接點 09-03 實測都不存在了：`.cursor/mcp.json`、`tests/test_deskbus.py`、看板的 `#panel-deskbus` ⇒ 票文寫的「不做就接不進 harness」那個接點沒有了、「會跟雲端那份分叉」的分叉對象也沒有了。⚠ 移除當時刻意留下這張票，理由是「它講的不是 DeskBus 本身」——**但全檔搜過，那個 URL（`tmp-e33b4a2339d80836`）除了本列與看板產出，沒有第二個使用者**，所以留下來的其實是一個沒有需求的能力題。⚠ **順手訂正一筆死引用**：`COLLAB_HANDOFF.md` 寫封存在 `D:\AI-Projects\_archive\deskbus-20260826`，但 `D:\AI-Projects` 已於 09-03 退役（現為 `AI-Projects_junction_retired_20260903`），實際位置是 `D:\Patrick-AI\MIS-install\_archive\deskbus-20260826`（已驗存在），該句已改。 | **不必做**。解凍條件：真的又出現「需要把某個 Origin 雲端 repo 拉到本機」的需求時再開新票。⚠ 屆時**不要沿用本列的三選一**，那三個選項是為 DeskBus 那次情境寫的。當時查到的事實仍成立、可當起點：Windows 原生不支援 `origin` CLI、本機無 WSL、瀏覽器直開會撞 Cursor 登入牙 ⇒ 現實可行的只有「人在 Git Credential Manager 登入後 clone」這一條。 | 我 | 流程 | 低 |
| 🔧 **Workflow 類別沒有基準 ⇒ 官方新增一個 workflow 不會被報成變動**（2026-08-23 修一半） | 交叉驗證原本只比 `[Skill]` 那一類，而 `/deep-research` 被官方標成 `[Workflow]` ⇒ **它從來沒被端到人面前過**，儘管 `skill-watch` 的開場白自己就在講「我們正打算自己包一支功能更弱的」講的就是它。**已修一半**：現在會把 workflow 逐項印出來附說明（`skill_watch_run.py` 的 `[3/6]` 那段）。**沒修的一半**：workflow 不進 `baselines`，所以「官方**新增**了一個 workflow」這件事偵測不到——而那正是這支工具存在的理由 | 決定 workflow 要不要進基準。⚠ 它與 skill 的形狀不同（不進注入清單、比不出「本機有沒有」），所以不能直接套 `baselines[平台][模式]` 那個結構——可能要另開一個 `officialWorkflows` 快照，比對邏輯也另寫。做之前先想清楚「workflow 消失了」要不要報 ⚠ **09-03 盤點：還差** 一個設計決定（消失了要不要報），以及基準結構本身。現況只印不進基準，程式碼自己逐字寫著「仍未做」。 | 待判斷 | 流程 | 高 |
| 🔧 **唯讀閘門擋掉 `git ls-tree`／`hash-object`／`check-attr`，B-4 的獨立審查者拿不到 subtree digest**（2026-08-26 `visual-check` 那列第 1 例） | `hooks\agent_readonly_gate.py:44` 的 `_GIT_READONLY` 只有 diff/status/show/log/ls-files/rev-parse/cat-file/describe/branch/remote/check-ignore。**實例**：B-4 §9.6a 步驟 1／4／7 明文要求記「before／candidate subtree SHA」與「bundle 完整檔案清單」，但獨立審查者（`harness-auditor`）三個純查詢命令全被擋 ⇒ 它只能用 `ls` 看工作區＋`git diff --stat` 只出一檔**推**得「HEAD 側也只有一檔」，並自己標明是推導不是證據；candidate 也只拿得到 `git diff --full-index` 的 blob，拿不到 subtree。B-4 還有 12 支要走，**每一支的審查者都會卡在同一處**。**同一輪還暴露一個相關缺口**：候選檔一旦被改寫覆蓋、而且從未 staged，前一版就**無法用任何唯讀手段取回**（`git cat-file -p <blob>` 回 `Not a valid object name`）⇒ §9.6a 步驟 4「digest 一變就回本步重驗」在機器層等於只能靠審查者的記憶執行。本列已當場繞過：主 session 用 `git hash-object -w` 把前後兩個候選都寫進 object DB，審查者才做得出機器 diff | 二選一：①`_GIT_READONLY` 補 `ls-tree`、`check-attr`（兩個都是純讀，位置參數是要查的路徑，比照 `check-ignore` 處理、**不得進 `_GIT_NO_POSITIONAL`**）②`write-tree` 同屬純讀但需要 index，優先度低於前兩個；③`hash-object` **不可照抄**——`-w` 會寫進 object DB、`--stdin-paths` 會讀 stdin，要進白名單得先加旗標黑名單擋掉這兩個，或乾脆不放、由主 session 代跑。（規範側的配套補句已由 user 決定「等 B-4 十四支全做完再一次性收」，記在 `SKILL_EVAL_PLAN.md` §9.6b 範圍外清單，本列不重複。）⚠ 改完要跑 `py -3 tests\run_hook_tests.py` 確認閘門測試沒破 | 待判斷 | 角色 | 中 |
| 🔧 **唯讀審查者算不出任意 git blob 的 token 數 ⇒ B-4 每一列的 before 值都只能採信被審方**（2026-08-26 提出·**同日第 4 次命中**） | `eval\check_structure.py` 的 `approx_tokens()` 只吃**檔案路徑**，要量 before 就得先把 blob 落到磁碟；而獨立審查者是唯讀角色，三條路徑全被擋：`git cat-file … \| py -3 …`（閘門擋 shell 元字元）、寫暫存檔（禁 Write）、`git hash-object`（不在 `_GIT_READONLY`）。**實例**：`skill-watch` 那列 before 的 2526 tok，審查者三種方式全試過都拿不到，只能標「未獨立復算」——而 token 前後值正是 §9.6a 要求逐列記錄的觀測值，**這是「獨立對帳」裡最不該採信被審方的地方**。after 值它跑得到（檔案在工作區），所以缺的恰好是對照組。**第 2 例（同日 `context-health` 那列）**：before 2406 tok 同樣三種方式全被擋，審查者只能背書 −51 中的 −32，並要求在隊列表標成「改寫者單方值、未經獨立復算」。**第 3 例（同日 `escalate` 那列）**：before 1174 tok 同樣復算不了，審查者只背書 +16 中的 +3。**第 4 例（同日 `design-spec` 那列）**：before 1805 tok 復算不了，只背書 +132 中的 +47。**第 5、6 例（外部批第 8、9 列）**：兩列的審查者都被 `git ls-tree`／`hash-object` 擋住，**做不出 §9.6a 步驟 4 要求的 candidate digest 對帳**，只能改用 `git diff` 的 index 行反推，或由**被審方**先把候選寫進 object DB 再餵給它。⚠ **同一個缺口第二次擋住覆核的獨立性 ⇒ 從「便利性問題」升級成「分工缺陷」**（前像與 digest 的存否掌握在被審方手上）。**具體工具形狀（第 10 列審查者提的）**：`tools/blob_probe.py <sha> --tokens --eol --wc`。**同一條閘門另擋掉 `git ls-tree`（純查詢）與 `find`、以及所有含 shell 元字元的複合指令**（`ls -la A && ls -la B` 也不行）⇒ 審查者盤 bundle 要拆成 N 次呼叫，且**驗不出「某路徑是 junction 還是實體資料夾」**——把任意 blob 落到唯讀可讀位置並量測。⚠ 現況閘門**擋得是對的**，代價才是問題：審查者無法復算任何 before 數字，第 3、4、5、6、10 列連五次同一句「改寫者單方值·審查者無法復算」（前像與 digest 的存否掌握在被審方手上）。⚠ 這張表自己的判準是「有用指標是重複度不是筆數」——**同一群 3 次了** | 三選一：①加一支 `tools\tokens_of.py <路徑或 blob-sha>`，內部重用 `approx_tokens()`，唯讀無副作用，加進閘門白名單 ②`eval\check_structure.py` 加 `--blob <sha>` 旗標 ③維持現狀，逐列在隊列表把 before token 標成「未獨立復算」。⚠ 與上面那條閘門白名單需求是**同一個根因的兩個面向**（審查者拿不到 git 物件層的事實），要動最好一起動 | 待判斷 | 角色 | 中 |
| 🔧 **缺一支「跨 skill 指標健檢」工具 ⇒ 壞指路只能靠人一支一支 grep**（2026-08-26 B-4 第 5～7 列連三次撞到） | 現有機器層只驗「檔案存不存在」（`eval\check_contracts.py`），**不驗指標指得準不準**。四類都已在 B-4 實際咬到：**(a) 裸 `CLAUDE.md §N`**——全域 §3 是「工作流」（有 L／S／M）、專案 §3 是「開工 SOP」（沒有），`escalate` 與 `design-spec:33`（§4：全域是「派工模型成本」、分流原則在專案）各中一次；**(b) 裸檔名的跨 repo 指標**——Cursor 端從別的專案根讀不到；**(c) 指到別支「步驟 N」的內容型引用**——`design-spec` 步驟 5 的「M 級」被第 6 列改成過期標籤、`session-workflow:31` 曾指向 callee 已明文拒收的情境，兩者都靜默；**(d) 反引號內帶磁碟機代號的絕對路徑逃出 `PATH_RE`**——`check_contracts.py:58` 字元類 `[A-Za-z0-9_./\-]` **沒有 `:`**，實測 `` `D:\Patrick-AI\.ai-harness\X.md` `` 抽出來是 `[]`，所以「補絕對路徑」這個做法會**靜默拿掉該指標的機器兜底**（第 6 列 `design-spec:78` 就是這樣種下的，第 7 列才發現）。⚠ 第 8～13 列（外部 6 支）還會再遇到 | 做一支唯讀掃描器（比照 `rulefile\check_bloat.py` 的形狀），四類各出一條 WARN：(a) `CLAUDE.md §N` 未標全域／專案或未帶節名 (b) 跨 repo 指標只有裸檔名 (c) 引用別支的 `步驟 N`／`§N` (d) 反引號絕對路徑 **(e) 同一節內多條路由條件互相重疊**。⚠ (e) 與 (a)–(d) 分析方式不同（前四類是「指標解析得到嗎」，(e) 是「條件集合有沒有交集」），但同屬「skill 正文語意檢查、L1／L2 完全無感」，做成一支比較省。**(e) 的實例**：第 7 列三輪裡有兩輪栽在同一類——R1 前的 `session-workflow:31`「M 但還沒 map → design-spec」撞 callee 拒收；R3 補「窮盡」時又因為「明確 M 幾乎必然同時是人要正式計畫書」把 M 送回同一條死路。**兩次 L1／L2 都是 0 FAIL 且該支不在 WARN 名單**。第 13 列 `wayfinder`（另一支編排器·129 行）會再遇到。⚠ **不要改 `PATH_RE` 去吃 `:`**——那會讓現有 12 支的契約數一次全變，且 B-4 期間 §9.6a 明文禁改 eval 工具。⚠ 與別條線剛加的 `rulefile\check_claims.py`（驗「宣稱改了什麼真的落在檔案裡」）**主題相鄰但不同**，動工前先看那支能不能擴，別做成第二支 ⚠ **09-03 盤點：票上那條「不要改某個規則式」的禁令已經被推翻**——做法正好相反，而且已驗過是對的。**該把那個警告從票上撤掉**，否則下一棒會照著一條已失效的禁令做設計。五類目前修掉一類。 | 待判斷 | 流程 | 中 |
| 🔧 **缺「做了的有沒有說」這個方向的檢查 ⇒ 未宣告的刪除只能靠人逐行讀 diff 抓**（2026-08-26 B-4 第 7 列**連兩輪**栽在同一模式） | repo 內已有 `rulefile\check_claims.py`（別條線今天新建），驗的是「**說改的有沒有做**」；缺的是**反方向**：「**做了的有沒有說**」。**實例（同一列、同一模式、兩次）**：R3 我宣告「改兩處」，實際 hunk 還刪掉了 R2 才加的仲裁句「明確 M 不走那支，見下一條」⇒ 上一輪剛修好的死路由從另一道門回來；R4 我宣告「改兩處」，實際又刪掉「，作法寫在對話裡就好——兩者都不開計畫書」⇒ 那 17 字是全檔**唯一**定義「不落檔」的地方，使同一輪新增的完成判準「S 已聲明不落檔」當場懸空、並與步驟 5 的 `PENDING_VERIFY.md` 字面衝突。**兩次 L1／L2 都是 0 FAIL 且該支不在 WARN 名單**——機器層對這類缺陷完全無感，兩次都是獨立審查者逐行對帳才抓到　⚠ **排這個優先的理由**：同列兩次·機器層零感知。 | 做一支對帳器：吃「本輪宣告的改動清單」＋`git diff`，把 hunk 的每一段增刪逐一歸給某一條宣告，**歸不掉的就報**。純機械可驗（宣告條目 ↔ 增刪行對帳），不需要語意判斷。⚠ 先看 `rulefile\check_claims.py` 能不能加一個反向模式，別做成第二支。⚠ 這條的價值在於它**接得住 B-4 剩下 7 支**——第 8～13 列是外部 skill，未宣告的刪除會直接變成與 upstream 的無聲分歧 | 待判斷 | 流程 | 高 |
