# 常駐層健檢：量測紀錄

> **這個檔為什麼不在 `CONTEXT_HEALTH_PLAN.md` 裡**：那份是 `> 狀態：待審核` 的計畫書，
> 受 PR-1 對抗式覆核閘門管轄。**量測紀錄不是計畫內容** —— 每量一次就改一次計畫書，
> 會讓「重算 hash」變成反射動作，而那正是偽造憑證的唯一動作
> （見 `hooks/rules/pr1_plan_review_marker.py` 檔頭 2026-07-29 第 4 點）。
> 所以量測與外部證據落在這裡，計畫書只放判準、分岔與做法。
>
> **判準／分岔／踩雷史** → `CONTEXT_HEALTH_PLAN.md`
> **怎麼跑** → `skills/context-health/SKILL.md`（或打 `/context-health`）
>
> 一次量測一節，新的加在最後。每節必須有「量到什麼」與「沒查到的」。

---

## 2026-08-26：去重的真實上限與外部證據

**觸發**：user 說「常駐層可以瘦身」。走完整條 `/context-health`（量 → 查去處 → 逐項讓人決定 → 動手 → 驗）。

### 1 量到什麼

| | 前 | 後 |
|---|---|---|
| IT-department `CLAUDE.md` | 22,771 B ／ 72 條 | **21,394 B ／ 57 條** |
| 其中 §8 關鍵硬規則速查 | 14,336 B（占該檔 **63%**） | 約 12,000 B |
| `MEMORY.md` | 16,291 B ／ 91 條 | 未動 |

兩輪我實際減 **1,645 B**（中間別的 session 加回約 268 B，故淨值 -1,377）。

⚠ **兩支偵測器一開始全報 exit 0、0 超標** —— 問題不在單條超過 120 字，在**條數**：
202 條常駐層條目，每一條都合規。**這是 skill 檔頭那個盲區的第二種形狀**：
第一種是「不長成條目所以看不見」（2026-08-13 的 6 段散文），
這一種是「每條都合規、但總量很大」。**兩支工具都不會叫，而報告很乾淨。**

### 2 檔名比對兩個方向都不可靠 —— 「不要期待自動化這一步」的第二個實例

用「§8 表格列引用的記憶檔名有沒有出現在參考型 skill 裡」自動分類 49 條，命中 13 條。
**逐條讀兩邊之後只剩 6 條成立**：

- **高估（7 條）**：skill 那邊只是**指路句不是完整版**。例：`/verify-rules` 只有
  「- 視覺爭議先量再改 →」一句，而 CLAUDE.md 那條帶著四個判準（rect 只量版面／
  只在特定瀏覽器縮放歪＝繪製問題／同元件禁 CSS 盒＋SVG 各畫一半／量 0 且不隨縮放變才是視錯覺）。
- **低估（2 條）**：規則**內容**在 skill 裡但 skill 沒引用該記憶檔名
  （`feedback-dynamic-render-ui-helpers` 的三件事在 `ui-rules` L66–68，檔名不在）。

⇒ **檔名是引用關係，不是內容關係。** 判「有沒有完整版」只能人讀兩邊。
這是步驟 2 那條警告的第二個實例（第一個是 `find_duplicates.py` 對 6 段散文 0 命中）。

### 3 真正的槓桿不是去重，是「還沒搬的模組」

§8 的 10 個模組區塊裡 **7 個早就壓成一行索引**（各 117–228 B），
剩 3 個還留著多行明細：公司平面圖 722 B/6 行、登入認證 399 B/4 行、保管人 305 B/3 行。
壓成索引句共省 **821 B** —— 與去重那一半（894 B）同量級，
但**不需要判斷副本在不在**：它們的完整版本來就在自己的記憶檔裡（11/11 逐句查證）。

⚠ 其中一句差點被誤判成「常駐層獨有」：CLAUDE.md 寫「**維修**/報廢維持原」、
記憶檔寫「**送修**/報廢」。**同義詞讓 grep 落空。**
逐句查證時要換詞再查，不能因為一次 grep 沒中就判定沒有副本。

### 4 外部證據（查詢員 2026-08-25·全部附原始出處）

**最重要的一條是反面的**：

- **arXiv 2605.10039**（1,650 個 Claude Code session、16,050 個函式層觀測）：
  CLAUDE.md 在 **25–500 行**區間，**檔案大小對指令遵循率測不到差異**，
  Bayes factor **0.05–0.10**（＝支持虛無假設，不是證據不足）。
  唯一測到的效應是**任務長度**：每多產生一個函式，遵循 odds 降 **5.6%**。
- 官方「target under 200 lines」在**五處文件重複，查不到任何實驗依據**。
  引用它當 Context Bloat 門檻的 Configuration Smells 論文也沒獨立驗證。

**有真實後果的硬數字只有一個**：

- `MEMORY.md` **前 200 行或 25KB（先到者為準）**，超出的部分**不載入且不報錯**。
  現況 16.5KB ＝ **66%**。這是唯一「超過就真的壞掉」的限制，且**壞得沒有徵兆**。

**仍然成立的瘦身理由（論據換了）**：

- **subagent 乘數**：subagent 啟動**仍載入 CLAUDE.md**（只有內建 Explore／Plan 不載），
  常駐層肥度會**乘以派工次數**。對「預設派出去」的工作流這是實成本。
- 指令密度：IFScale（20 模型）在 **100–150 條**開始明顯下滑、500 條時最佳模型 **68.9%**。
  ⚠ 那是**同質 keyword 指令**，外推到「異質規則條目」要打折，不可當結論。

**兩個容易做錯的**：

- `@import` **不減 context**（imported files load at launch）—— 以為拆檔就省了是錯的。
- 必須 100% 生效的規則，官方明說改 **hook** 而非 prompt（"a request, not a guarantee"）。

**支持硬規則 1「禁止機械壓縮」的外部證據**：Configuration Smells 的 **Blind References**
（16% 盛行）原文：「If you just mention the path, Claude will often ignore it.
**You have to pitch the agent on why and when to read**」。
本次刪 6 行時把消失的症狀端觸發詞（列裂開／捲不動／ESC 一次關全部／想自造下拉或日期欄）
補回索引句，就是照這條做的 —— **淨省的位元組因此變少，但那是正確的取捨。**

### 5 結論與下一步

**去重在 IT-department 上已經收斂**：剩 36 條是常駐層獨有、沒有 on-demand 副本，
照硬規則 1 不能動。**再壓是負期望值**（見 9.4 第一條）。

下一個該量的**不是 CLAUDE.md 而是 `MEMORY.md` 對 25KB 的用量**（現 66%），
理由是它是唯一超過就真的壞掉、而且靜默的限制。

**沒查到的**（不得當成已窮盡）：
①**中文常駐層**的 token 效率與遵循率**完全沒有研究涵蓋**（所有實驗都是英文語料），
而本專案常駐層是繁體中文；②「常駐層瘦身 → 任務成功率提升」的直接 A/B 查不到；
③「幾條規則算多」對**設定檔**沒有研究 —— IFScale 的 N=80／150 是同質 prompt 指令，
不可直接套到 CLAUDE.md 的條目數。
