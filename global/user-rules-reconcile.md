# Cursor User Rules 對帳

比對粒度＝`global/CURSOR_USER_RULES.md` 去掉 GENERATED 檔頭後、票 03 正規化。禁止模型自算 SHA。

> **守門（2026-09-03 補回）**：「貼上前雲端回報正文雜湊」那一格**只有實際開一則新 Cursor 對話的人量得到**——把那則對話自己的 `<user_rules>` 自訂規則正文**原樣落檔**再對該檔算。
> **不得由模型代填，也不得由本機腳本拼出一份「推測的正文」再算**。2026-09-03 就發生過一次：腳本讀磁碟上的舊正文＋寫死標題塊拼起來算，呼叫的是既有雜湊函式所以**形式完全合規、來源不實**，比手算更難發現。
> 判「貼成功了沒」**一律比完整 SHA256**。40 字 SHA8 只是人眼樣本——實測兩份內容不同的檔 SHA8 都是 `97eece54`（開頭 40 字是同一段標題塊），拿它當判準會漏判。

| 日期 | 產出檔雜湊 | 貼上前雲端回報正文雜湊 | 貼上前 40 字 SHA8 | 貼上後 40 字 SHA8 | 重抄前 inventory 雜湊 | 本次差異落點 |
|---|---|---|---|---|---|---|
| 2026-08-27 | e0dcb9f585a0683f1ee8a60d4923e90c3ab2a30f2d2b5fe406317708b5dbcf21 | 83f72e2660d4467f44aecf425413696d06d13be7b412d88e99d253e157512d64 | 5217f6f7 | e97871aa | dfcd2195b017f1f6dc8d0d08e42a2757561c6bc63d3f13d65954039e1a19a58b | 捨棄：Settings API 貼上前只有 AskQuestion 與工程師偏好兩條；舊 inventory 含產品內建 git／PR／瀏覽器驗，不在 API 清單 |
| 2026-09-03 | 764c3b8a0515fdcc07d8e3196a04a6e985c7f3861df521cde0022556495caeca | 7b504af661168c00019b257a8325b954ec38c9bbaa682cbabbaee3dbe45444a4 | 97eece54 | 97eece54 ⚠同值 | c4de800026f588d3f4bf18ce81fe99c0b8e26bef26f2fed2efab4c5dcef1148a | **已轉綠**（12:1x 實測）。貼上後 live 正文與產出檔逐字元相同（正規化 SHA 皆 `764c3b8a…`）。原填的產出檔雜湊 `b72af56a…` 是 commit `2602430` 那版，`318a709` 重產後已失效——**不是貼錯，是欄位過期**。 |

人眼樣本（正規化後前 40 字，LF→空白；樣本對不上不算紅）：2026-09-03 貼上前 `# 工作方式（跨專案通用·全域層）  > 所有專案都會載入這份。**判準是一句話`；貼上後尚未貼（欄位留空）。2026-08-27 貼上前 `需要使用者拍板或釐清時，立刻呼叫 AskQuestion（2–4 項、第一項標「`；貼上後 `# 工作方式（跨專案通用·全域層）  > 所有專案都會載入這份。**判準是一句話`

雜湊用法：把 `<user_rules>` 自訂規則正文原樣寫成檔，再 `py -3 .scratch/hash_live_user_rules.py <該檔>`（呼叫 `gen_rule_hub.sha256_norm`／`normalize`，禁止手算）。`--self-test` 用 2026-08-27 已知值 `5217f6f7` 驗 40 字 SHA8 演算法。2026-09-03 貼上前雜湊來源見該列「本次差異落點」。

---

## 現況（2026-09-03 12:1x · 已轉綠）

**這張表現在是綠的。** 人於 2026-09-03 把 `global/CURSOR_USER_RULES.md` 去檔頭後的正文
貼進 Cursor Settings → Customize → Rules → User Rules；新開一則 Cursor 對話倒出
`<user_rules>` 正文（落檔 `.scratch/user-rules-live-20260903-v2.md`），與產出檔
**逐字元相同**、正規化 SHA 皆 `764c3b8a…`。`user-rules-inventory.md` 已以該正文覆寫。

### 三個踩到的坑（下一次貼之前先看）

1. **SHA8 這一欄分不出貼成功沒有。** 貼前貼後都是 `97eece54`——前 40 字是標題塊，
   改動全在後面。**判準只能是整份 SHA**，該欄已標「⚠同值」。
2. **「產出檔雜湊」欄會過期。** 原填 `b72af56a…` 是 commit `2602430` 那版；
   `318a709` 重產後產出檔變成 `764c3b8a…`。當時看起來像「貼錯了」，其實是**欄位過期**。
   ⇒ 這欄要在**貼的當下**重量，不能沿用寫票時的值。
3. **前三次失敗的原因仍未知，但已排除長度。** 2026-09-03 先貼 54 字哨兵測試，
   新對話讀得到 ⇒ 儲存管道正常。第四次用同一組逐步指示貼完整版即成功
   ⇒ **推測是前三次沒貼到 User Rules 這個欄位**，未證實，不要當事實往下傳。

移除的三塊原文備份在 `.scratch/rule-hub-removed-20260827/`。移除理由見
`RULE_HUB_PLAN.md`：那三塊是 Cursor 產品自己注入的英文指示，與本機既有硬規則
五處正面衝突（`git add` 全部 vs 只 stage 自己的 hunk、`origin` vs `backup`、
禁 Task 工具 vs 常設派工授權、Shell tool 不存在、HEREDOC vs `-F <檔>`）。
