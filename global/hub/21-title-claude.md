---
audience: claude
# 2026-08-28 user 決定：Claude 端的對話自動改名**整套退役**——退役的是三個 hook 掛載。
# 2026-09-02 補回 body：退役後沒有任何東西改標題，而 `20-modes.md` 舊句還寫著
#   「對話名稱自己從宣告組（不必手動改名）」⇒ 每一則對話都以為有程式在做、於是都不改。
#   實測 `D:\AI-Projects` 兩則對話從昨晚開到今天都沒有名字。改法不是把 hook 裝回去，
#   是讓模型自己呼叫官方介面（2026-09-01 在本機實測成功）。body 見下。
# 2026-09-02 更正上一句「都沒有名字」：桌面版會自動塞名字（第一句 user 訊息，
#   或 `pc-<host>-<形容詞>-<名詞>` 代號），不是留白。看起來像名字 ⇒ 漏命名更難發現。
#
# 退役理由（三個，全部是同日查證出來的）：
#   1. 官方 Agent SDK 早就有 `rename_session()`，文件明說它就是 append 一筆 `custom-title`
#      —— 與我方手寫的完全同構。我們自幹了一套沒人維護的複本。
#   2. 三天九個版本、最長活 12.9 小時、三次歸因被推翻，從未穩定；
#      而它做的事是「讓側邊欄的名字好看一點」。
#   3. `PreToolUse` 掛載兩次咬到 Cursor CLI（8/26 全滅、8/28 A/B 重現），
#      而 Cursor 是這台機器唯一跨模型族的審查者。
#
# ⚠ 程式碼 `hooks/session_title.py` **沒有刪**：`session_archive.py` 仍 import 它的
#   `compose_closed`／`compose_idle`／`project_name`／`cloud_request` 等函式，
#   做 `/clear` 之後的雲端閒置名。
#   退役的是**三個 hook 掛載**，不是那支檔案。細節見它自己的檔頭。
# ⚠ Cursor 端（`22-title-cursor.md`）**不退役**：那邊是模型主動呼叫 `rename_chat`，
#   沒有 hook 對抗問題，而且 Cursor 平台端不提供程式化改名，那是唯一的路。
# 2026-09-06 標題格式改版（分類＋短 id＋階段中文顯示）——為什麼放在這裡不放常駐層：
#   - **短 id** 取值：`CLAUDE_CODE_SESSION_ID` 前 8 碼，複用既有值、不新建計數器
#     （曾誤解成「同任務第幾次開對話」的計數器，需要新狀態與撞號處理，已排除——
#     user 要的其實是像 `87c830f8` 這種既有 id，方便跨對話／跨 AI 互指）。
# 2026-09-08 查證：標題短 id 跟 `ListAgents`／`SendMessage` 那套短碼**不是同一套**——
#   拿 10 則對話逐一核對，標題短 id 跟 `list_sessions` 回的 `sessionId` 前 8 碼全部相符
#   （本來就是同一個值截出來的），但跟 `ListAgents` 顯示的六碼亂碼型短碼**沒有一個對得上**，
#   代表那是另一個子系統獨立配發的碼，沒有推導關係、也沒有工具能改。**不必再驗一次**——
#   結論是兩套 id 命名空間本來就分開，標題短 id 只能拿去對 `list_sessions`，
#   要對 `ListAgents`／`SendMessage` 一律另外查那邊自己的清單，不要拿標題短 id 手動比對或手抄。
#   - **階段只在標題層轉中文**，自我宣告本文仍寫英文五詞：`hooks/rules/decl1_stage_files.py`
#     的 regex 寫死只認 `Research|Design|Execute|Review|Fix`，宣告本文若跟著改中文，
#     這條對帳守門會悄悄配不到、無聲失效——所以中文對照表只用在組標題字串這一層。
#   - **提案 B（本機↔雲端自動同步標題）與 TITLE-2 判準擴大到抓「過期未更新」**都不在這次
#     範圍內：查證 TITLE-2 唯一一次真實 WARN（19:18:53, session `87c830f8`）0 次接著
#     改名，證據還不足以支撐自動化的必要性，兩者都留到 2026-09-13 驗證窗後再議
#     （見 `SESSION_TITLE_HOOK_PLAN.md` 提案 D／TODOS.md 同一張票）。
# 2026-09-03 常駐層瘦身：8 條合併成 6 條。拿掉的理由與細節在這裡（本區不進常駐層），
#   每一項都查證過完整版的落點：
#   - 「hook 2026-08-28 已退役、settings 未掛載」→ 本檔上方退役理由三點。
#   - 「憑證過期會自己叫一次官方 CLI 換發，換不到才報錯／exit 1 印原因」
#     → `tools/push_cloud_title.py:128`（失敗訊息）與 `:131`（換發成功訊息），檔頭 L19-26 有動機。
#   - 「自我宣告只餵成本歸因看板」的沿革 → `WORKFLOW_5STAGE_PLAN.md:586`（§12 宣告欄位為什麼是這個形狀）。
#   - 「【閒置】名字取該對話檔尾、抽不到才退回 `專案名｜等待任務`」
#     → `hooks/session_archive.py:339-340`；為什麼 `session_title.py` 沒刪見該檔 `:24`。
#   - 「收尾抽不到原名才退回宣告字面」→ 同 `session_archive.py` 的 compose_closed。
#   ⚠ 常駐層只留「做什麼」與「不做會怎樣」；**為什麼是這個設計**一律留在這裡，不要再抄回去。
---
- **`TITLE-1`**（文件內部代號，**非 hook 硬擋**——`dispatch_config.json` 沒有這條，沒有程式在執行期檢查）：以下對話命名規則。
- **對話名稱**：任務一確定就呼叫 `set_session_title`（`session_id: "self"`）；轉向再叫一次。user 給了名字就用他的，**不必等人開口**。
- **改完名要推雲端**（本機改名不會自動同步）：跑 `py -3 <harness>/tools/push_cloud_title.py "同一個標題"`，**推失敗當場講、不准吞**。
- **沒有程式會代勞**：**不叫不是沒名字，是平台塞一個**（你的第一句話，或 `pc-…-sunny-ripple` 代號）——看起來像名字，所以漏命名很難發現。
- **標題格式**（2026-09-06版）：`【分類·短id】名稱｜階段｜進度%`（短id＝session id前8碼；
  階段中文＝查詢/設計/施作/檢查/修復，僅標題用，宣告本文「階段X」仍寫英文），≤48字超過先截名稱。
  收尾用**原名**。
- **標題短 id 只認 `list_sessions`／`get_session` 這套**：跟 `ListAgents`／`SendMessage`
  顯示的短碼是兩套獨立配發、對不上（2026-09-08 查證，理由見檔頭）。要送訊息給哪個對話，
  一律當場查對應工具自己的清單，**不要拿標題上的短 id 手動比對或手抄**。
- **判定順序不能換**：收工／封存／交接／收尾／handoff →【收尾】；ASK／VERIFY 且修改檔案為無或待定 →【討論】；其餘 →【任務】。進度寫得出來才加。
- **【閒置】是 `/clear` 後封存程式寫的**：模型只寫上面三種標記，看到它別蓋掉。
