---
audience: claude
# 2026-08-28 user 決定：Claude 端的對話自動改名**整套退役**——退役的是三個 hook 掛載。
# 2026-09-02 補回 body：退役後沒有任何東西改標題，而 `20-modes.md` 舊句還寫著
#   「對話名稱自己從宣告組（不必手動改名）」⇒ 每一則對話都以為有程式在做、於是都不改。
#   實測 `D:\AI-Projects` 兩則對話從昨晚開到今天都沒有名字。改法不是把 hook 裝回去，
#   是讓模型自己呼叫官方介面（2026-09-01 在本機實測成功）。body 見下。
# 2026-09-02 更正上一句「都沒有名字」：桌面版會自動塞名字（第一句 user 訊息，
#   或 `pc-<host>-<形容詞>-<名詞>` 代號），不是留白。看起來像名字 ⇒ 漏命名更難發現。
#
# 退役理由（三個，全部是同日查證出來的）：
#   1. 官方 Agent SDK 早就有 `rename_session()`，文件明說它就是 append 一筆 `custom-title`
#      —— 與我方手寫的完全同構。我們自幹了一套沒人維護的複本。
#   2. 三天九個版本、最長活 12.9 小時、三次歸因被推翻，從未穩定；
#      而它做的事是「讓側邊欄的名字好看一點」。
#   3. `PreToolUse` 掛載兩次咬到 Cursor CLI（8/26 全滅、8/28 A/B 重現），
#      而 Cursor 是這台機器唯一跨模型族的審查者。
#
# ⚠ 程式碼 `hooks/session_title.py` **沒有刪**：`session_archive.py` 仍 import 它的
#   `compose_idle`／`project_name`／`cloud_request` 等函式做封存時的雲端 idle 名。
#   退役的是**三個 hook 掛載**，不是那支檔案。細節見它自己的檔頭。
# ⚠ Cursor 端（`22-title-cursor.md`）**不退役**：那邊是模型主動呼叫 `rename_chat`，
#   沒有 hook 對抗問題，而且 Cursor 平台端不提供程式化改名，那是唯一的路。
---

- **對話名稱**：任務一確定就呼叫 `set_session_title`（`session_id: "self"`）。轉向再叫一次。
- **誰命名**：user 給了名字就用他的。不必等人開口——**沒有任何 harness 程式會代勞**（自動改名 hook 2026-08-28 已退役、settings 未掛載）。**不叫不是沒名字，是平台自動塞一個**：抓第一句 user 訊息，或給隨機代號 `pc-…-sunny-ripple`。看起來像名字，所以漏命名更難發現。
- **改完名要推雲端**：`set_session_title` **只寫本機**；側邊欄／網頁／手機讀的是雲端那一份。改名後接著跑 `py -3 D:/Patrick-AI/.ai-harness/tools/push_cloud_title.py "同一個標題"`。**推失敗要當場講出來、不准吞**（它 exit 1 並印原因）。憑證過期它會自己叫一次官方 CLI 換發，換不到才報錯。
- **標題格式**（整串 ≤48 字、任務名 ≤12 字、標記在最前）：【收尾】原名｜收尾／【討論】主題／【任務】名稱｜階段｜進度%。
- **判定順序不能換**：收工／封存／交接／收尾／handoff →【收尾】；ASK／VERIFY 且修改檔案為無或待定 →【討論】；其餘 →【任務】。進度寫得出來才加。
- **自我宣告不會改標題**：`任務 舊名→新名` 那條只餵成本歸因看板（`WORKFLOW_5STAGE_PLAN.md` §12.3），2026-08-28 起與側邊欄無關。
- **收尾用原名**：抽不到原名才退回宣告字面，不要寫成「收工封存」。
