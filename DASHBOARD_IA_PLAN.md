# 看板資訊架構重構計畫書（6 大類 ＋ 工作流程）

> 2026-08-06 立案。user 要求：**工作流程 ＋ 6 大頁籤，各自再細分子分頁**。
> **本檔為計畫書，§5 未逐項討論前不動工。**

---

## §0 一個關鍵發現：這 6 類就是 harness 自己的分類

user 列的 6 類與 `dashboard/capability_checks.py` 裡既有的能力分類**一字不差**：

```
① Rule file  ② Tools  ③ Sandbox  ④ Orchestration  ⑤ Hook  ⑥ Observability
```

差別只有一個：**harness 的分類有 8 類**，多出 **⑦ Verification** 與
**⑧ Human-in-the-Loop**。這兩類目前各有 8 項與 6 項檢查、合計 12 項已具備，
**不是空的**，所以它們的去處是本次唯一真正需要決定的事（§5 D-1）。

這件事的意義不只是「剛好一樣」：**頁籤結構若對上能力分類，看板就從「有哪些頁面」
變成「這套 harness 的每個面向做到幾成」** —— 八大類進度條本來就在總覽躺著，
但它跟頁籤各講各的，讀者無法從「④ Orchestration 3/5」跳到那 5 項在哪裡看。

---

## §1 現況盤點（8 頁籤 · 13 區段）

| 頁籤 | 區段 | 產生器 marker |
|---|---|---|
| 總覽 | 兩層對照 | `LAYERS_GLOBAL` |
| | 計畫進度 ／ 八大類能力 ／ 六大類總覽 | `PROGRESS_CHART` |
| | D 槽工作區 | — |
| Hook 現況 | Hook 即時現況 | `HOOK_RULES` |
| Skill 與 Eval | Skill 清冊 ／ Skill Eval（EDD 四層） | — |
| 角色 | 角色編制 | `ROLES_TOPOLOGY` |
| 維運腳本 | 維運腳本 | — |
| 決策與覆蓋 | 技術決策時間軸 ／ 規則覆蓋 & 測試 | — |
| 成本與 mix | 成本與模型 mix ／ 金額量級 ／ 階段成本歸因 ／ 建好了有人用嗎 | `COST_PANEL` |
| 待辦 | 待辦 | — |

**5 個產生器 marker 區間**。重構只是把區間搬到別的 panel 底下，
產生器只認 marker 不認 panel，所以**搬動本身不影響產生器**（低風險）。

### 現況的三個問題

1. **沒有工作流程視圖**——全站 grep「工作流／任務路由／五階段／規劃 → 執行」**零命中**。
   而自我宣告 8/6 起已經要求填五階段的階段名，**規則要求的東西看板上不存在**。
2. **頁籤是「按產出物分」不是「按能力分」**：Skill 與角色分成兩頁，但兩者都屬
   ④ Orchestration；Hook 規則表與「規則覆蓋 & 測試」分在兩頁，但都屬 ⑤/⑦。
3. **「決策與覆蓋」名字會誤讀**——實際是「決策紀錄」＋「測試**覆蓋率**」兩件事，
   跟 override／路由都無關（user 8/6 當場誤讀為路由，這就是證據）。

---

## §2 目標結構（提案）

```
0. 工作流程        ← 全新，目前不存在
1. Rule file       規則檔案：Agent 是誰、在乎什麼、絕不能做什麼
2. Tools           工具：functions／MCP／何時該用什麼
3. Sandbox         沙盒：code 在哪跑、能摸到什麼
4. Orchestration   編排：sub-agent、路由、交接
5. Hook            掛鉤：固定點跑的正確性 code
6. Observability   可觀測性：log／traces／evals／成本
```

### 每一類的子分頁與內容來源

| 頁籤 | 子分頁 | 內容 | 來源 |
|---|---|---|---|
| **0 工作流程** | 五階段 | Research→Design→Execute→Review→Fix，各階段目的／交付物／完成判準／誰做 | **新寫**（`WORKFLOW_5STAGE_PLAN` §1） |
| | 交接契約 | 四個交接點「上一棒給什麼／下一棒能假設什麼」＋三條硬規則 | **新寫**（同上 §2） |
| | 任務模式路由 | 5 模式（ASK/VERIFY/DRY_RUN/DEPLOY/DEV）＋升級安全閥 | **新寫**（全域 §2） |
| **1 Rule file** | 分層對照 | 兩層對照表（全域／專案） | 搬 `LAYERS_GLOBAL` |
| | 規則檔清冊 | always-loaded／path-scoped／on-demand／索引 四層現況 | 八大類①拆出 |
| | 防膨脹 | `check_bloat` 判準與現值 | 八大類①拆出 |
| **2 Tools** | 權限白名單 | allow／deny 收斂現況、Bash↔PowerShell 對稱 | 八大類②拆出 |
| | 維運腳本 | 17 支腳本清冊 | 搬「維運腳本」 |
| | MCP | 連接器授權現況 | 八大類②拆出 |
| **3 Sandbox** | 能力邊界 | 各角色 tools 邊界、agent-scoped hook 收窄 | 角色彈窗「沙箱」層拆出 |
| | 隔離現況 | 主 session／worktree／容器 | 八大類③拆出 |
| **4 Orchestration** | 角色編制 | 部門×人員拓樸、彈窗四層 | 搬 `ROLES_TOPOLOGY` |
| | Skill 清冊 | 14 支 skill、觸發時機 | 搬「Skill 清冊」 |
| | 模型路由 | 四象限（模型強度×推理強度）、各角色落點 | **新寫**（`MODEL_ROUTING_PLAN` §1–2） |
| **5 Hook** | 規則現況 | 8 條規則 applies／would-block | 搬 `HOOK_RULES` |
| | 觸發鏈 | 漏斗圖、心跳、fail-open 不 fail-silent | 搬「Hook 即時現況」其餘 |
| | 驗證紀事 | 13 條判決卡 | 搬（同上） |
| **6 Observability** | 成本與 mix | mix 對照 §7 目標、金額量級 | 搬 `COST_PANEL` |
| | 階段成本 | 階段歸因表 | 搬（同上） |
| | 使用率 | 「建好了有人用嗎」 | 搬（同上） |
| | 新鮮度與 log | event log、看板新鮮度、traces 缺口 | 八大類⑥拆出 |

### 無主內容（§5 D-2 要決定去處）

計畫進度（D1–D18 條）／八大類能力進度條／六大類總覽／D 槽工作區／
技術決策時間軸／規則覆蓋 & 測試／Skill Eval 四層／待辦。

---

## §3 技術做法

### 子分頁怎麼做

現行頁籤是 `role="tab"` ＋ `aria-controls` ＋ `.panel[hidden]` 的標準 tab pattern，
JS 約 40 行。**子分頁沿用同一套機制、不另造**（硬規則 7：先找既有元件）：
外層 `.tabs` 不動，各 panel 內加一列 `.subtabs`，JS 用同一個委派 handler
多帶一個 scope 參數。

**代價要先講**：巢狀 tab 會讓「現在在哪」變模糊，且**深連結會失效**
（目前沒有 URL hash 路由，重整就回到第一頁）。子分頁一多，這個痛會放大。

### 產生器不受影響

5 個 marker 區間搬 panel 不影響產生器——它們 split marker 字串、不解析 DOM 結構。
但 **`test_dashboard_structure.py` 會紅**：它驗「頁籤↔面板配對」，
頁籤數從 8 變 7、panel id 全改，那支要跟著改（**先讓它紅，再改到綠**）。

---

## §4 風險

| 風險 | 說明 |
|---|---|
| **內容搬家搬丟** | 13 區段搬進 7×N 個子分頁，漏一段不會報錯。**對策：搬完逐段點名比對，區段數必須守恆** |
| 巢狀 tab 的可用性 | 見 §3。子分頁超過 4 個就該考慮改別的形式 |
| 一次改太多 | 這是**整份 4400 行 HTML 的重排**。對策見 §5 D-3（分批還是一次） |
| 舊網址／習慣 | 已發布的 artifact 是同一個 URL，重整後讀者的肌肉記憶全失效 |

---

## §5 待決分岔（逐項討論，同意才做）

| # | 分岔 | 選項 | 傾向 |
|---|---|---|---|
| **D-1** | ⑦ Verification 與 ⑧ Human-in-the-Loop 放哪 | (a) 併進 6 類當子分頁（Verification→Observability、HITL→Orchestration）(b) 升成第 7、8 個頂層頁籤（回到 harness 自己的八大類）(c) 合成一個「品質與人機協作」頁籤 | **(b)**：那兩類合計 12 項已具備、不是邊角料；硬塞進別類會讓「⑦ 8/8 全綠」這件事看不見。但這樣就變成 8＋工作流＝9 個頂層頁籤，跟 user 說的「6 大」不符 → **需 user 拍板** |
| **D-2** | 無主內容（進度／決策時間軸／待辦／D 槽工作區）去哪 | (a) 保留「總覽」與「待辦」當兩個工具型頁籤，不算進 6 大類 (b) 全部塞進 6 類（進度→Observability、決策→各類自己的子分頁） (c) 決策時間軸按類別拆散到 6 類底下 | **(a)**：總覽是入口、待辦是行動清單，兩者本來就不屬於任何能力分類；硬塞會讓分類失去意義 |
| **D-3** | 一次改完還是分批 | (a) 分批：先做「0 工作流程」新頁籤（純新增、零風險），其餘維持原狀；下一批再重排 (b) 一次重排到位 | **(a)**：工作流程是**目前完全缺的東西**，價值最高且不動到既有內容；重排是搬家，價值在「找得到」而非「有沒有」 |
| **D-4** | 工作流程頁要不要接真實資料 | (a) 先做靜態（五階段定義＋交接契約，內容來自計畫書） (b) 直接接階段成本歸因與宣告紀律（已經有資料了） | **(b)**：階段歸因今天剛上線、資料已經在跑，靜態頁會馬上顯得脫節 |
| **D-5** | 五階段定義還沒定案，工作流程頁要不要等 | (a) 等 `WORKFLOW_5STAGE_PLAN` W-1～W-5 討論完再畫 (b) 先畫大綱版，標「草案」 | **(a)**：畫面會把草案變成既成事實——8/6 已經發生過一次（階段欄先上線、定義還沒討論） |

---

## §5.1 定案（2026-08-06 逐項討論）

| # | 決定 | 影響 |
|---|---|---|
| **D-1** | ✅ **⑦⑧ 合成第 7 類**「品質與人機協作」 | 頂層分類 7 個（不是 6 也不是 8）。⚠ 兩類性質其實不同（一個是自動驗證、一個是人介入），合併後**子分頁要把這兩半分開**，否則會糊成一團 |
| **D-2** | ✅ **留「總覽」，待辦併進總覽** | 待辦不再是獨立頁籤。總覽＝入口＋進度＋決策時間軸＋D 槽工作區＋待辦 |
| **D-3** | ✅ **一次重排到位** | 13 個既有區段一次搬完。**風險對策見 §4：搬完必須逐段點名比對、區段數守恆** |
| **D-4** | （沿用傾向）接真實資料 | 工作流程頁掛階段成本歸因的實際數字 |
| **D-5** | （未問·我的處理）**五階段標「草案」並把 W-1～W-5 five 個未決分岔畫在頁上** | 定義還沒定案就畫，會把草案變既成事實（8/6 已犯過一次）。折衷：**畫，但讓頁面自己講出「這是草案、還有五項沒決定」**——待決事項可見本來就是進度看板該做的事 |

### 最終結構（9 個頂層頁籤）

```
總覽          入口 · 計畫進度 · 八大類能力 · D 槽工作區 · 待辦
工作流程      五階段(草案) · 交接契約(草案) · 任務模式路由 · 階段成本
1 Rule file   分層對照 · 規則檔清冊 · 防膨脹
2 Tools       權限白名單 · 維運腳本 · MCP
3 Sandbox     能力邊界 · 隔離現況
4 Orchestration  角色編制 · Skill 清冊 · 模型路由
5 Hook        規則現況 · 觸發鏈 · 驗證紀事
6 Observability  成本與 mix · 階段成本 · 使用率 · 新鮮度與 log
7 品質與人機協作  [驗證] Skill Eval · 規則覆蓋 & 測試 ｜ [人機] 選擇題 · 安全閥 · dry-run
```

---

## §6 狀態

- 2026-08-06 立案。§1 現況、§0 分類對照皆為實際盤點（非推測）：
  8 頁籤／13 區段／5 個 marker 區間／八大類 ①–⑧ 逐項清單皆從程式碼與 HTML 取得。
- **§5 五項待決，未逐項討論前不動工。**
